%ifndef NEBOC_RECOVERY_INC
%define NEBOC_RECOVERY_INC 1

; A DiagnosticBag owns no diagnostic payload. Callers retain each immutable
; Diagnostic for the lifetime of the bag and provide fixed entry storage.
%define NEBOC_DIAG_BAG_ENTRIES_OFFSET 0
%define NEBOC_DIAG_BAG_CAPACITY_OFFSET 8
%define NEBOC_DIAG_BAG_COUNT_OFFSET 16
%define NEBOC_DIAG_BAG_MAX_COUNT_OFFSET 24
%define NEBOC_DIAG_BAG_MAX_BYTES_OFFSET 32
%define NEBOC_DIAG_BAG_USED_BYTES_OFFSET 40
%define NEBOC_DIAG_BAG_VISIBLE_COUNT_OFFSET 48
%define NEBOC_DIAG_BAG_ACTIVE_OFFSET 56
%define NEBOC_DIAG_BAG_SIZE 64

%define NEBOC_DIAG_BAG_HARD_MAX 64
%define NEBOC_DIAG_BAG_HARD_MAX_BYTES 1048576

%define NEBOC_DIAG_ENTRY_DIAGNOSTIC_OFFSET 0
%define NEBOC_DIAG_ENTRY_ID_OFFSET 8
%define NEBOC_DIAG_ENTRY_PAYLOAD_BYTES_OFFSET 16
%define NEBOC_DIAG_ENTRY_FLAGS_OFFSET 24
%define NEBOC_DIAG_ENTRY_INSERTION_OFFSET 32
%define NEBOC_RECOVERY_DIAG_ENTRY_SIZE 40

%define NEBOC_DIAG_ENTRY_DUPLICATE 1
%define NEBOC_DIAG_ENTRY_CASCADE 2
%define NEBOC_DIAG_DEDUP_STRUCTURAL 1

%endif
