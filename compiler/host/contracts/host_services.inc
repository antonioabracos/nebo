%ifndef NEBOC_HOST_SERVICES_INC
%define NEBOC_HOST_SERVICES_INC 1

; HostServices v0 is a versioned table. Every service receives the table
; pointer in RDI, so adapters can obtain their private context from the table.

%define NEBOC_HOST_SERVICES_VERSION 0

%define NEBOC_HOST_SERVICES_VERSION_OFFSET 0
%define NEBOC_HOST_SERVICES_SIZE_OFFSET 8
%define NEBOC_HOST_SERVICES_CAPABILITIES_OFFSET 16
%define NEBOC_HOST_SERVICES_CONTEXT_OFFSET 24
%define NEBOC_HOST_SERVICES_MEMORY_RESERVE_OFFSET 32
%define NEBOC_HOST_SERVICES_MEMORY_RELEASE_OFFSET 40
%define NEBOC_HOST_SERVICES_FILE_OPEN_READ_OFFSET 48
%define NEBOC_HOST_SERVICES_FILE_READ_ALL_OFFSET 56
%define NEBOC_HOST_SERVICES_FILE_WRITE_ALL_OFFSET 64
%define NEBOC_HOST_SERVICES_FILE_CLOSE_OFFSET 72
%define NEBOC_HOST_SERVICES_PATH_NORMALIZE_OFFSET 80
%define NEBOC_HOST_SERVICES_TEMP_CREATE_OFFSET 88
%define NEBOC_HOST_SERVICES_TEMP_REMOVE_OFFSET 96
%define NEBOC_HOST_SERVICES_PROCESS_SPAWN_OFFSET 104
%define NEBOC_HOST_SERVICES_PROCESS_WAIT_OFFSET 112
%define NEBOC_HOST_SERVICES_MONOTONIC_TIME_OFFSET 120
%define NEBOC_HOST_SERVICES_DIAGNOSTIC_WRITE_OFFSET 128
%define NEBOC_HOST_SERVICES_PROCESS_EXIT_OFFSET 136
%define NEBOC_HOST_SERVICES_SIZE 144

%define NEBOC_HOST_CAP_MEMORY (1 << 0)
%define NEBOC_HOST_CAP_FILE (1 << 1)
%define NEBOC_HOST_CAP_TEMP (1 << 2)
%define NEBOC_HOST_CAP_PROCESS (1 << 3)
%define NEBOC_HOST_CAP_TIME (1 << 4)
%define NEBOC_HOST_CAP_DIAGNOSTIC (1 << 5)
%define NEBOC_HOST_CAP_EXIT (1 << 6)
%define NEBOC_HOST_CAP_ALL 0x7f
%define NEBOC_HOST_CAP_LINUX_V0 0x77

; HostSlice { pointer, byte_length }
%define NEBOC_HOST_SLICE_POINTER_OFFSET 0
%define NEBOC_HOST_SLICE_LENGTH_OFFSET 8
%define NEBOC_HOST_SLICE_SIZE 16

; HostArg { pointer, byte_length }. Strings used by an operating-system adapter
; may additionally be NUL-terminated, but lengths remain authoritative.
%define NEBOC_HOST_ARG_POINTER_OFFSET 0
%define NEBOC_HOST_ARG_LENGTH_OFFSET 8
%define NEBOC_HOST_ARG_SIZE 16

; ProcessRequest uses argv as an array of HostArg. There is deliberately no
; command_line or shell_string member.
%define NEBOC_PROCESS_REQUEST_ARGV_OFFSET 0
%define NEBOC_PROCESS_REQUEST_ARGC_OFFSET 8
%define NEBOC_PROCESS_REQUEST_ENV_OFFSET 16
%define NEBOC_PROCESS_REQUEST_ENVC_OFFSET 24
%define NEBOC_PROCESS_REQUEST_STDOUT_POINTER_OFFSET 32
%define NEBOC_PROCESS_REQUEST_STDOUT_CAPACITY_OFFSET 40
%define NEBOC_PROCESS_REQUEST_STDERR_POINTER_OFFSET 48
%define NEBOC_PROCESS_REQUEST_STDERR_CAPACITY_OFFSET 56
%define NEBOC_PROCESS_REQUEST_MAX_OUTPUT_OFFSET 64
%define NEBOC_PROCESS_REQUEST_SIZE 72
%define NEBOC_PROCESS_REQUEST_MAX_ARGS 4096
%define NEBOC_PROCESS_MAX_CAPTURE_BYTES 16777216

%define NEBOC_PROCESS_RESULT_EXIT_CODE_OFFSET 0
%define NEBOC_PROCESS_RESULT_TERM_SIGNAL_OFFSET 8
%define NEBOC_PROCESS_RESULT_STDOUT_LENGTH_OFFSET 16
%define NEBOC_PROCESS_RESULT_STDERR_LENGTH_OFFSET 24
%define NEBOC_PROCESS_RESULT_SIZE 32

%endif
