%ifndef NEBOC_FOUNDATION_FLOAT_LOWERING_INC
%define NEBOC_FOUNDATION_FLOAT_LOWERING_INC 1

; TIPOS-PRIMITIVOS-ESCALARES-PF005 public vertical-slice materialization. Source size remains bounded by
; the compiler source buffer; conversion itself does not use an integer digit cap.
%define NEBOC_FLOAT_LOWERING_MAX_TOTAL_DIGITS 65535

%define NEBOC_FLOAT_LOWERING_REQUEST_SOURCE_OFFSET 0
%define NEBOC_FLOAT_LOWERING_REQUEST_LENGTH_OFFSET 8
%define NEBOC_FLOAT_LOWERING_REQUEST_OUT_BITS_OFFSET 16
%define NEBOC_FLOAT_LOWERING_REQUEST_ERROR_CODE_OFFSET 24
%define NEBOC_FLOAT_LOWERING_REQUEST_INTEGER_DIGITS_OFFSET 32
%define NEBOC_FLOAT_LOWERING_REQUEST_FRACTION_DIGITS_OFFSET 40
%define NEBOC_FLOAT_LOWERING_REQUEST_FLAGS_OFFSET 48
%define NEBOC_FLOAT_LOWERING_REQUEST_RESERVED_OFFSET 56
%define NEBOC_FLOAT_LOWERING_REQUEST_SIZE 64
%define NEBOC_FLOAT_LOWERING_REQUEST_QWORDS 8

%define NEBOC_FLOAT_LOWERING_FLAG_MATERIALIZED 1
%define NEBOC_FLOAT_LOWERING_FLAG_STRICT_DECIMAL 2
%define NEBOC_FLOAT_LOWERING_FLAG_PUBLIC_VERTICAL_SLICE 4
%define NEBOC_FLOAT_LOWERING_FLAG_IEEE754_BINARY64 8
%define NEBOC_FLOAT_LOWERING_FLAG_DETERMINISTIC 16
%define NEBOC_FLOAT_LOWERING_REQUIRED_FLAGS 31

%define NEBOC_FLOAT_LOWERING_ERROR_NONE 0
%define NEBOC_FLOAT_LOWERING_ERROR_BAD_REQUEST 1
%define NEBOC_FLOAT_LOWERING_ERROR_INVALID_SHAPE 2
%define NEBOC_FLOAT_LOWERING_ERROR_SOURCE_LIMIT 3
%define NEBOC_FLOAT_LOWERING_ERROR_COUNT 4

%endif
