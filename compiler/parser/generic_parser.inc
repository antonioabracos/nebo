%ifndef NEBOC_GENERIC_PARSER_INC
%define NEBOC_GENERIC_PARSER_INC 1

%define NEBOC_GEN_SOURCE_OFFSET 0
%define NEBOC_GEN_SOURCE_LENGTH_OFFSET 8
%define NEBOC_GEN_TOKENS_OFFSET 16
%define NEBOC_GEN_TOKEN_COUNT_OFFSET 24
%define NEBOC_GEN_RECORDS_OFFSET 32
%define NEBOC_GEN_CAPACITY_OFFSET 40
%define NEBOC_GEN_FOUND_OFFSET 48
%define NEBOC_GEN_DIAGNOSTIC_OFFSET 56
%define NEBOC_GEN_DECLARATION_COUNT_OFFSET 64
%define NEBOC_GEN_USE_COUNT_OFFSET 72
%define NEBOC_GEN_INSTANCE_COUNT_OFFSET 80
%define NEBOC_GEN_RESULT_OFFSET 88
%define NEBOC_GEN_SEMANTIC_HASH_OFFSET 96
%define NEBOC_GEN_FLAGS_OFFSET 104
; Exact token provenance for pure const specializations projected into the
; shared Program. Entries correspond one-to-one with typed use records.
%define NEBOC_GEN_PURE_DECL_END_OFFSET 112
%define NEBOC_GEN_PURE_USE_TOKENS_OFFSET 120
%define NEBOC_GEN_REQUEST_QWORDS 47
%define NEBOC_GEN_REQUEST_SIZE 376

%define NEBOC_GEN_RECORD_DECLARATION_HASH_OFFSET 0
%define NEBOC_GEN_RECORD_TYPE_ID_OFFSET 8
%define NEBOC_GEN_RECORD_CONST_KEY_OFFSET 16
%define NEBOC_GEN_RECORD_CONSTRAINT_OFFSET 24
%define NEBOC_GEN_RECORD_KIND_OFFSET 32
%define NEBOC_GEN_RECORD_INSTANCE_KEY_OFFSET 40
%define NEBOC_GEN_RECORD_SYMBOL_HASH_OFFSET 48
%define NEBOC_GEN_RECORD_CANONICAL_INDEX_OFFSET 56
%define NEBOC_GEN_RECORD_QWORDS 8
%define NEBOC_GEN_RECORD_SIZE 64

%define NEBOC_GEN_MAX_INSTANCES 32
%define NEBOC_GEN_KIND_FUNCTION 1
%define NEBOC_GEN_KIND_TYPE 2
%define NEBOC_GEN_KIND_CONST_FUNCTION 3

%define NEBOC_GEN_TYPE_INT 1
%define NEBOC_GEN_TYPE_BOOL 2
%define NEBOC_GEN_TYPE_CHAR 3
%define NEBOC_GEN_TYPE_FLOAT 4
%define NEBOC_GEN_TYPE_TEXT 5

%define NEBOC_GEN_CONSTRAINT_SCALAR 1
%define NEBOC_GEN_CONSTRAINT_COPY 2
%define NEBOC_GEN_CONSTRAINT_EQ 4
%define NEBOC_GEN_CONSTRAINT_COMPARABLE 8
%define NEBOC_GEN_CONSTRAINT_HASH 16
%define NEBOC_GEN_CONSTRAINT_NONE 0

%define NEBOC_GEN_FLAG_PARSED 1
%define NEBOC_GEN_FLAG_ANALYZED 2

%define NEBOC_GEN_DIAG_CONSTRAINT 8
%define NEBOC_GEN_DIAG_BUDGET 9
%define NEBOC_GEN_DIAG_COLLISION 10
%define NEBOC_GEN_DIAG_RECURSIVE 25
%define NEBOC_GEN_DIAG_SYNTAX 26
%define NEBOC_GEN_DIAG_TYPE 27
%define NEBOC_GEN_DIAG_INTERNAL 28

%endif
