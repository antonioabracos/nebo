%ifndef NEBOC_BUFFER_INC
%define NEBOC_BUFFER_INC 1

; Buffer v0 grows monotonically inside one Arena. Old blocks remain owned by the
; Arena until reset/destroy; no individual deallocation operation exists.

%define NEBOC_BUFFER_ARENA_OFFSET 0
%define NEBOC_BUFFER_DATA_OFFSET 8
%define NEBOC_BUFFER_LENGTH_OFFSET 16
%define NEBOC_BUFFER_CAPACITY_OFFSET 24
%define NEBOC_BUFFER_ELEMENT_SIZE_OFFSET 32
%define NEBOC_BUFFER_ALIGNMENT_OFFSET 40
%define NEBOC_BUFFER_OWNER_OFFSET 48
%define NEBOC_BUFFER_ARENA_GENERATION_OFFSET 56
%define NEBOC_BUFFER_FINALIZED_OFFSET 64
%define NEBOC_BUFFER_GROWTH_COUNT_OFFSET 72
%define NEBOC_BUFFER_SIZE 80

%define NEBOC_BUFFER_MIN_CAPACITY 8
%define NEBOC_BUFFER_HARD_MAX_BYTES 1073741824
%define NEBOC_BUFFER_MAX_ELEMENT_SIZE 4096

%endif
