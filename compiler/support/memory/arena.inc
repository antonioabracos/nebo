%ifndef NEBOC_ARENA_INC
%define NEBOC_ARENA_INC 1

; Arena v0 borrows one active MemoryRegion. The region owns the reservation.

%define NEBOC_ARENA_REGION_OFFSET 0
%define NEBOC_ARENA_BEGIN_OFFSET 8
%define NEBOC_ARENA_CURRENT_OFFSET 16
%define NEBOC_ARENA_END_OFFSET 24
%define NEBOC_ARENA_MAXIMUM_OFFSET 32
%define NEBOC_ARENA_OWNER_OFFSET 40
%define NEBOC_ARENA_GENERATION_OFFSET 48
%define NEBOC_ARENA_REGION_GENERATION_OFFSET 56
%define NEBOC_ARENA_ACTIVE_OFFSET 64
%define NEBOC_ARENA_ALLOCATED_BYTES_OFFSET 72
%define NEBOC_ARENA_HIGH_WATER_OFFSET 80
%define NEBOC_ARENA_ALLOCATION_COUNT_OFFSET 88
%define NEBOC_ARENA_REWIND_COUNT_OFFSET 96
%define NEBOC_ARENA_RESET_COUNT_OFFSET 104
%define NEBOC_ARENA_SIZE 112

%define NEBOC_ARENA_MARK_ARENA_OFFSET 0
%define NEBOC_ARENA_MARK_OFFSET_OFFSET 8
%define NEBOC_ARENA_MARK_GENERATION_OFFSET 16
%define NEBOC_ARENA_MARK_REGION_GENERATION_OFFSET 24
%define NEBOC_ARENA_MARK_OWNER_OFFSET 32
%define NEBOC_ARENA_MARK_SIZE 48

; A minimal checked MemorySpan seam is provided only to prove bounds in MF007.
; The full Slice API remains owned by MF008.
%define NEBOC_MEMORY_SPAN_POINTER_OFFSET 0
%define NEBOC_MEMORY_SPAN_LENGTH_OFFSET 8
%define NEBOC_MEMORY_SPAN_GENERATION_OFFSET 16
%define NEBOC_MEMORY_SPAN_ARENA_OFFSET 24
%define NEBOC_MEMORY_SPAN_SIZE 32

%define NEBOC_ARENA_MAX_ALIGNMENT 4096

%endif
