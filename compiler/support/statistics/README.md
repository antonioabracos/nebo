# Session statistics v0

The statistics block records deterministic counters only. It contains no
pointer, timestamp, absolute path or host-specific value.

Tracked values include phase transitions, invalid transitions, failures,
cleanup calls, limit checks/failures, resource counts and Arena high-water
bytes.
