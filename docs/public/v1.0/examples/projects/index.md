# Executable projects

The typed multiunit application, filesystem/JSON transform, bounded task/channel application and pinned three-package workspace derive from the current RF172 scenarios. Each directory carries main.no and a context with expected results.

project/main.no
data/main.no
concurrency/main.no
packages/workspace.json
