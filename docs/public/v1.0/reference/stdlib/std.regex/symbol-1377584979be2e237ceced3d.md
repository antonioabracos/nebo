# std.regex — Regex

Actual owned typed Regex.compile(pattern) with the current default options, Regex.escape(Text), and isMatch(Text), reusing native pattern_find rather than a Python runtime or fixed report. Initial public native profile is Unicode literal patterns and escaped punctuation only: 256 pattern bytes, 4096 text bytes, 65536 search steps. Regex metacharacter operators/captures and custom options are explicitly rejected in this source profile; the wider SDK model is not counted as native source coverage. Independent Python re search/escape oracles, two live values and explicit return variations are executed. Literal matching and escaped punctuation; 256 pattern bytes; 4096 input bytes; bounded steps; no full regex claim

```text
Identity: claim:ebc047cacea7df56097706f2 (QUALIFIED_INTRINSIC_OR_REGISTRY_ID)
Edition: 1
Target: x86_64-systemv-elf-linux
Availability: STABLE_1_0
```

## Syntax or signature

```text
Regex
Actual owned typed Regex.compile(pattern) with the current default options, Regex.escape(Text), and isMatch(Text), reusing native pattern_find rather than a Python runtime or fixed report. Initial public native profile is Unicode literal patterns and escaped punctuation only: 256 pattern bytes, 4096 text bytes, 65536 search steps. Regex metacharacter operators/captures and custom options are explicitly rejected in this source profile; the wider SDK model is not counted as native source coverage. Independent Python re search/escape oracles, two live values and explicit return variations are executed. Literal matching and escaped punctuation; 256 pattern bytes; 4096 input bytes; bounded steps; no full regex claim
```

## Ownership and complexity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling Actual owned typed Regex.compile(pattern) with the current default options, Regex.escape(Text), and isMatch(Text), reusing native pattern_find rather than a Python runtime or fixed report. Initial public native profile is Unicode literal patterns and escaped punctuation only: 256 pattern bytes, 4096 text bytes, 65536 search steps. Regex metacharacter operators/captures and custom options are explicitly rejected in this source profile; the wider SDK model is not counted as native source coverage. Independent Python re search/escape oracles, two live values and explicit return variations are executed. Literal matching and escaped punctuation; 256 pattern bytes; 4096 input bytes; bounded steps; no full regex claim

## Effects, capabilities and sandbox

Per expression: pure computation plus explicit observed sinks; NO_IMPLICIT_GRANT. Import grants capability: NO.

## Availability and errors

Edition 1; x86_64-systemv-elf-linux; BOUNDED_PUBLIC_EXECUTED_WITHIN_LIMITS. Actual owned typed Regex.compile(pattern) with the current default options, Regex.escape(Text), and isMatch(Text), reusing native pattern_find rather than a Python runtime or fixed report. Initial public native profile is Unicode literal patterns and escaped punctuation only: 256 pattern bytes, 4096 text bytes, 65536 search steps. Regex metacharacter operators/captures and custom options are explicitly rejected in this source profile; the wider SDK model is not counted as native source coverage. Independent Python re search/escape oracles, two live values and explicit return variations are executed. Literal matching and escaped punctuation; 256 pattern bytes; 4096 input bytes; bounded steps; no full regex claim Use the declared operand and receiver domain; source rejection publishes no executable.

## Identity and aliases

STDLIB_DECLARATION_TO_NATIVE_LITERAL_REGEX. Identity kind: QUALIFIED_INTRINSIC_OR_REGISTRY_ID. SymbolId: NOT_SERIALIZED. Alias entries describe the same qualified operation; they do not create another runtime API.

## Executed examples

### regex:match-0-0; expected 23

```nebo
import "std.regex" { Regex; }.patterns;
start(){Regex.compile("").r;r.isMatch("").console();23.return;}
```

Oracle: {"capabilities": {"console": "RETAINED_DOCUMENT", "filesystem": "EXPLICIT_SCRATCH_EFFECT_ORACLE", "network": "DENIED_BY_SECCOMP"}, "console_text_utf8": "true", "filesystem_effects": {}, "independent_builds": 2, "kinds": \[5\], "process_exit": 23, "runtime_determinism": "BYTE_IDENTICAL", "runtime_sha256": "26e99610443e30ab87a629aa5de2392ce6d0117574455307c7c97e44b2fcac90", "text": {"bytes_hex": "74727565"}}

## Rejected examples

### regex:status-type; expected NEBO_ENTRYPOINT_INVALID_SIGNATURE

```nebo
import "std.regex" { Regex; }.patterns;
start(){Regex.compile("a").return;}
```

Oracle: {"artifact_publication": "REJECTED", "compiler_exit": 1, "diagnostic": "NEBO_ENTRYPOINT_INVALID_SIGNATURE"}

## Related entries

[Index](index.md)

- [symbol-2542b87520f89882ffbea266](symbol-2542b87520f89882ffbea266.md)

## Provenance

- sdk/contracts/stdlib/STDLIB-STABLE-CATALOG.tsv — SHA-256 a66eec0cbb7f0a99f20ae489208f1ba1106cd2c327e475daf406d615e3e55bab

- compiler/codegen/textual/x86_64/regex_public_codegen.inc — SHA-256 e9b14e8471c74ac2311e274ae7eacc28999015579c7c937607ad1810c574faa7

- compiler/driver/cli/linux-x86_64/scalar_program.inc — SHA-256 54bad41b6d43ffc48c264adae8a0a776071c6a3917abaf05520344e11ae6afbe

- compiler/codegen/functions/x86_64/function_codegen.asm — SHA-256 944358cec673b2132c51b5c4b5842edaf4cf9ce157b23d83892449fbb12487f7

- runtime/textual/regex_public.asm — SHA-256 2f656cc4140480eebd8285efd442c00ca8b5b1f326cd863f089ee305c6e55881

- tests/rf204/G170/harness.py — SHA-256 6a31e65687ed1199e30e8fc2220bc41e5e5d94429d5bc9800018e338d4fa85b0

- tests/rf204/G170/regex_test.py — SHA-256 8017cdd3461e864dbfd614c251558e78246a5b998668d3df3404fffa1ed9d594

- tests/rf204/G170/text_test.py — SHA-256 2a6be89787726ec8c3e4acbcb2c728b82e1c9fc7acc8a2680f2d830648448f52
