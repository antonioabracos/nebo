# G10 bounded array example

`../g10-array-int4.no` is the stable public example. It constructs the frozen immutable four-`Int` array, performs two constant checked reads, returns their sum and exits 5. General collections, heap allocation, mutation and dynamic indexing remain unavailable.
