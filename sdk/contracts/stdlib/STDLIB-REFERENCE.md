# Stable Standard Library functional reference

Edition 1, Linux x86_64 System V. This current functional freeze supersedes the historical generated placeholder catalogs. Stable means the exact recorded bounded profile, not every possible overload or target. Qualified intrinsic namespaces are not automatically import modules. Imports grant no capabilities. See STDLIB-PROFILE-BOUNDARIES.tsv for experimental, excluded and separately measured profiles.

Source specimens below were compiled and executed twice; their expected values originate in the independent owner tests. All associated positive and negative IDs are required, not merely the selected documentation specimen. Negative source must fail check, emit-asm and build without an artifact. Valid-source domain failures are runtime tests.

<a id="symbol-233cae135785161a6c458f60"></a>

## core / .

Identity: `NSR-CORE-001`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.console()._;if(true){29.console()._;}83.console()._;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:discard-scope`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-2b-0,operator:binary-2b-1,operator:binary-2b-2,operator:binary-2b-3,operator:binary-2b-4,operator:binary-2b-5,operator:binary-2b-6,operator:binary-2b-7,operator:discard-scope`.

Misuse/diagnostic/failure proofs: `operator:discard-mutable`. API fingerprint: `e05cb781cb794d268d8a9d1aa9adfa6ac73a494b502c8d06fdeb2d3102af8206`.

<a id="symbol-c52a671723afc6a2bbc69340"></a>

## core / ()

Identity: `NSR-CORE-002`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.console()._;if(true){29.console()._;}83.console()._;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:discard-scope`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-2b-0,operator:binary-2b-1,operator:binary-2b-2,operator:binary-2b-3,operator:binary-2b-4,operator:binary-2b-5,operator:binary-2b-6,operator:binary-2b-7,operator:discard-scope`.

Misuse/diagnostic/failure proofs: `operator:discard-mutable`. API fingerprint: `07e09d6d162c7a8e4dfce44ec15bd93d088682f10c3130e5f6cfae093e824ba9`.

<a id="symbol-5c7f6c563260bd96de04dbad"></a>

## core / {}

Identity: `NSR-CORE-003`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.console()._;if(true){29.console()._;}83.console()._;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:discard-scope`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-2b-0,operator:binary-2b-1,operator:binary-2b-2,operator:binary-2b-3,operator:binary-2b-4,operator:binary-2b-5,operator:binary-2b-6,operator:binary-2b-7,operator:discard-scope`.

Misuse/diagnostic/failure proofs: `operator:discard-mutable`. API fingerprint: `67d33b9cbd9f713870916d97a42d03e9ce012b997dbbe7dfabfb21e42c6056c0`.

<a id="symbol-a1bcc34c0f607c3bd21d27d5"></a>

## core / []

Identity: `NSR-CORE-004`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].values;(∏values).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:reduce-e2888f-[]`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:reduce-e28891-[],operator:reduce-e2888f-[],operator:reduce-e28891-[17],operator:reduce-e2888f-[17],operator:reduce-e28891-[2_3_5],operator:reduce-e2888f-[2_3_5],operator:reduce-e28891-[2_7_5],operator:reduce-e2888f-[2_7_5],operator:reduce-e28891-[-2_3_5],operator:reduce-e2888f-[-2_3_5],operator:reduce-e28891-[0_7_29],operator:reduce-e2888f-[0_7_29]`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e28891,operator:reduce-overflow-e2888f`. API fingerprint: `17b31618d53564b208110903d2f168406ab8f837af75a6912562a65fd76eb1bd`.

<a id="symbol-76d9529bd10c7510188043fd"></a>

## core / <...>

Identity: `NSR-CORE-005`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].values;(∏values).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:reduce-e2888f-[]`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:reduce-e28891-[],operator:reduce-e2888f-[],operator:reduce-e28891-[17],operator:reduce-e2888f-[17],operator:reduce-e28891-[2_3_5],operator:reduce-e2888f-[2_3_5],operator:reduce-e28891-[2_7_5],operator:reduce-e2888f-[2_7_5],operator:reduce-e28891-[-2_3_5],operator:reduce-e2888f-[-2_3_5],operator:reduce-e28891-[0_7_29],operator:reduce-e2888f-[0_7_29]`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e28891,operator:reduce-overflow-e2888f`. API fingerprint: `a677221ce551f6ce128ea6ebca921209f3c9b9917f6d1c017907b5df60d3fa08`.

<a id="symbol-9342bca221dbd9d560c3f521"></a>

## core / ,

Identity: `NSR-CORE-006`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.v)observe(){v.console();v.return;}start(){Bytes.fromValues(0.observe(),17.observe(),29.observe(),83.observe()).b;b.at(0).console();b.at(3).console();0.return;}
```

Explicit process status: `0`; the full value/effect oracle is `operator:bytes-construct-four-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:bytes-construct-four-0,operator:bytes-construct-four-7,operator:bytes-construct-four-23,operator:bytes-construct-four-29,operator:bytes-construct-four-255`.

Misuse/diagnostic/failure proofs: `operator:bytes-four-arity`. API fingerprint: `ff579c7bcb4b6412f1df124e6cb84ebc5d2ddd0dedae9cc21887e59b4620ed81`.

<a id="symbol-babe1641ef0a800ea0a5956a"></a>

## core / ;

Identity: `NSR-CORE-007`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.console()._;if(true){29.console()._;}83.console()._;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:discard-scope`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-2b-0,operator:binary-2b-1,operator:binary-2b-2,operator:binary-2b-3,operator:binary-2b-4,operator:binary-2b-5,operator:binary-2b-6,operator:binary-2b-7,operator:discard-scope`.

Misuse/diagnostic/failure proofs: `operator:discard-mutable`. API fingerprint: `7cdb77dc821be899e727ca233d88b213353a7ebda35c03a46295dcb0238b307e`.

<a id="symbol-f6c6b384190055f2e6d140c3"></a>

## core / :

Identity: `NSR-CORE-008`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%{nome:s} %{idade:d}".format(idade:29,nome:"Nebo").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:named-29`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:named-29,format:named-83,format:named-once`.

Misuse/diagnostic/failure proofs: `format:named-duplicate,format:named-absent,format:named-type`. API fingerprint: `5d0b5eb256997d27522238fbc71ece91a6eb4683041523762722cf7f684dc977`.

<a id="symbol-174e4e70dd30e5bb580de0d6"></a>

## core / ->

Identity: `NSR-CORE-009`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
enum Number { Empty, Value(Int) }start(){Number.Value(29).v;match(v){Empty -> 0.return;_ -> 7.return;}}
```

Explicit process status: `7`; the full value/effect oracle is `operator:wildcard-_-29`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:wildcard-_-17,operator:wildcard-Value(_)-17,operator:wildcard-_-29,operator:wildcard-Value(_)-29,operator:wildcard-_-83,operator:wildcard-Value(_)-83`.

Misuse/diagnostic/failure proofs: `operator:discard-mutable`. API fingerprint: `499644ad68a2ef84f872268d4f3b89062f7a7a16b68f9132c1c201ada615cce1`.

<a id="symbol-ffa8df221f56f1026f18cefd"></a>

## core / _

Identity: `NSR-CORE-010`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){29.console()._;83.console()._;7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `operator:discard-effects-29-83`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:discard-effects-17-29,operator:wildcard-_-17,operator:wildcard-Value(_)-17,operator:discard-effects-29-83,operator:wildcard-_-29,operator:wildcard-Value(_)-29,operator:discard-effects-83-17,operator:wildcard-_-83,operator:wildcard-Value(_)-83,operator:discard-scope,operator:discard-owned`.

Misuse/diagnostic/failure proofs: `operator:discard-read,operator:discard-mutable`. API fingerprint: `b255d0497243bbca03d3175c938dafd3c80c1d8b0450626cc8f0aa1b15c4b3ca`.

<a id="symbol-1dc475a172d2a889f3a676d6"></a>

## core / //

Identity: `NSR-CORE-011`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
// 99.return; ignored as source comment
start(){"//".console();29.console();7.return;}// comment at EOF
```

Explicit process status: `7`; the full value/effect oracle is `operator:comment-29`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:comment-17,operator:comment-29,operator:comment-83`.

Misuse/diagnostic/failure proofs: `operator:comment-does-not-bind`. API fingerprint: `08f69f087624f3aa7d0dbb457f3963799fa7047d606a3c4fd7f929db5447afc6`.

<a id="symbol-d5542f0adab915dc31d5fd71"></a>

## core.numeric / +

Identity: `NSR-CORE-012`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left + right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-2b-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-2b-0,operator:binary-2b-1,operator:binary-2b-2,operator:binary-2b-3,operator:binary-2b-4,operator:binary-2b-5,operator:binary-2b-6,operator:binary-2b-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-2b`. API fingerprint: `59ae0ac652cf8dd02f8084a9d50115f1f4cfce9fe0ac9bbd5b1208ad6717d5f4`.

<a id="symbol-77d61b140de7af4cea4c66e7"></a>

## core.numeric / -

Identity: `NSR-CORE-013`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left - right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-2d-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-2d-0,operator:binary-2d-1,operator:binary-2d-2,operator:binary-2d-3,operator:binary-2d-4,operator:binary-2d-5,operator:binary-2d-6,operator:binary-2d-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-2d`. API fingerprint: `71651d887e99ee2c251d77046164c2cad4cfe04ac12a0f1b5f6a2e51337232e0`.

<a id="symbol-a1320d1d8768477e1d9f616e"></a>

## core.numeric / *

Identity: `NSR-CORE-014`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left * right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-2a-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-2a-0,operator:binary-2a-1,operator:binary-2a-2,operator:binary-2a-3,operator:binary-2a-4,operator:binary-2a-5,operator:binary-2a-6,operator:binary-2a-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-2a`. API fingerprint: `85e51cd96ce74ac35316a173837afb61525621d76fdaa824681f993a4032a664`.

<a id="symbol-c28d867d69411a276c472112"></a>

## core.numeric / /

Identity: `NSR-CORE-015`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left / right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-2f-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-2f-0,operator:binary-2f-1,operator:binary-2f-2,operator:binary-2f-3,operator:binary-2f-4,operator:binary-2f-5,operator:binary-2f-6,operator:binary-2f-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-2f`. API fingerprint: `f4dabeb6d1916250f189f586e0c0be5248d9f3d59c9c6483fcf25d7d34c45205`.

<a id="symbol-d836ced6bcbc26eeb63ac6d1"></a>

## core.numeric / %

Identity: `NSR-CORE-016`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left % right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-25-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-25-0,operator:binary-25-1,operator:binary-25-2,operator:binary-25-3,operator:binary-25-4,operator:binary-25-5,operator:binary-25-6,operator:binary-25-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-25`. API fingerprint: `5d802e02562c335a2fdfb7d9d7770226894f116cfd276006df2f1d52a19e9f09`.

<a id="symbol-f76dde8b86993e2eb975705f"></a>

## core.numeric / ^

Identity: `NSR-CORE-017`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left ^ right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-5e-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-5e-0,operator:binary-5e-1,operator:binary-5e-2,operator:binary-5e-4,operator:binary-5e-6,operator:binary-5e-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-5e`. API fingerprint: `67e0d317a7b7bc1ee168f334548c4d469e056c93ad4f944433359b444fe8e778`.

<a id="symbol-947dbacc5b8b1d8f4b7adc50"></a>

## core.numeric / +

Identity: `NSR-CORE-018`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.value)calculate(){(+value).return;}start(){(0).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:unary-2b-2`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:unary-2b-0,operator:unary-2b-1,operator:unary-2b-2`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-2b`. API fingerprint: `71b2a4f90fbd46c370c5374e876570e05fae10da075c7dca7d4a1f51e5bd1003`.

<a id="symbol-2ffa8e06869b38e8aa044187"></a>

## core.numeric / -

Identity: `NSR-CORE-019`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.value)calculate(){(-value).return;}start(){(0).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:unary-2d-2`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:unary-2d-0,operator:unary-2d-1,operator:unary-2d-2`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-2d`. API fingerprint: `14c717db6e50d7e9c3bf62abfbbbf8eee0d6da79a0b6e46619dd1b05035c98a5`.

<a id="symbol-8aa3114f59b96a60cde50914"></a>

## core.logic / !

Identity: `NSR-CORE-020`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Bool.value)calculate(){(!value).return;}start(){(true).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:unary-21-1`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:unary-21-0,operator:unary-21-1`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-21`. API fingerprint: `19fbb8161c4f4886467405d3b5d0a3a14441c8262e524aa9df11239df42edc44`.

<a id="symbol-f27b68fb29fb12013c4242b0"></a>

## core.logic / &&

Identity: `NSR-CORE-021`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(false && (1/0==0)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:short-and`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:logic-2626-false-false,operator:logic-2626-false-true,operator:logic-2626-true-false,operator:logic-2626-true-true,operator:short-and`.

Misuse/diagnostic/failure proofs: `operator:wrong-logic-int`. API fingerprint: `b8be927d6bcecf5439794a85c9b0774e0d2d41465a88c2225562b65ecd67484d`.

<a id="symbol-c7b9321bdc78e40ba24167b6"></a>

## core.logic / ||

Identity: `NSR-CORE-022`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(true || (1/0==0)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:short-or`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:logic-7c7c-false-false,operator:logic-7c7c-false-true,operator:logic-7c7c-true-false,operator:logic-7c7c-true-true,operator:short-or`.

Misuse/diagnostic/failure proofs: `operator:wrong-logic-int`. API fingerprint: `ce0f9a50a0e90f7f6106e18636bc7cb39970507cc1637c2892164a207b0169af`.

<a id="symbol-f6af19ba8aada1245ebb5c14"></a>

## core.logic_bits / xor

Identity: `NSR-CORE-023`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){0.v;Bytes.fromByte(v).at(0).console();0.return;}
```

Explicit process status: `0`; the full value/effect oracle is `operator:bytes-construct-binding-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Equal Int/Bool/Bytes operands; immutable Bytes XOR requires equal lengths at most 4096; fromByte takes one Int and fromValues four Ints in 0..255; private initialized storage; ordered effects and two live outputs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-786f72-0,operator:binary-786f72-1,operator:binary-786f72-2,operator:binary-786f72-3,operator:binary-786f72-4,operator:binary-786f72-5,operator:binary-786f72-6,operator:binary-786f72-7,operator:logic-786f72-false-false,operator:logic-786f72-false-true,operator:logic-786f72-true-false,operator:logic-786f72-true-true,operator:bytes-xor-0-786f72-False,operator:bytes-xor-0-786f72-True,operator:bytes-xor-0-e28abb-False,operator:bytes-xor-0-e28abb-True,operator:bytes-xor-1-786f72-False,operator:bytes-xor-1-786f72-True,operator:bytes-xor-1-e28abb-False,operator:bytes-xor-1-e28abb-True,operator:bytes-xor-2-786f72-False,operator:bytes-xor-2-786f72-True,operator:bytes-xor-2-e28abb-False,operator:bytes-xor-2-e28abb-True,operator:bytes-xor-empty,operator:bytes-xor-nested,operator:bytes-xor-two-live,operator:bytes-xor-length-domain,operator:bytes-xor-effects,operator:bytes-construct-binding-0,operator:bytes-construct-call-0,operator:bytes-construct-four-0,operator:bytes-construct-binding-7,operator:bytes-construct-call-7,operator:bytes-construct-four-7,operator:bytes-construct-binding-23,operator:bytes-construct-call-23,operator:bytes-construct-four-23,operator:bytes-construct-binding-29,operator:bytes-construct-call-29,operator:bytes-construct-four-29,operator:bytes-construct-binding-255,operator:bytes-construct-call-255,operator:bytes-construct-four-255,operator:bytes-construct-domain--1,operator:bytes-construct-domain-256,operator:bytes-construct-domain-9223372036854775807`.

Misuse/diagnostic/failure proofs: `operator:wrong-786f72,operator:bytes-xor-mixed,operator:bytes-bool,operator:bytes-arity`. API fingerprint: `5df1eec06032c2c211f638a0ac3a7015c40fedb77fb7db6173f94a44543d5660`.

<a id="symbol-ffafddcec61723507869c0c6"></a>

## core.compare / <

Identity: `NSR-CORE-024`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left < right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-3c-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-3c-0,operator:binary-3c-1,operator:binary-3c-2,operator:binary-3c-3,operator:binary-3c-4,operator:binary-3c-5,operator:binary-3c-6,operator:binary-3c-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-3c`. API fingerprint: `bd8acd649484bcf94edfd746e66d135e901bb6a1b35558d9142c6d3f0f31eb61`.

<a id="symbol-e847ffc4d646d1bd749bfec8"></a>

## core.compare / <=

Identity: `NSR-CORE-025`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left <= right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-3c3d-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-3c3d-0,operator:binary-3c3d-1,operator:binary-3c3d-2,operator:binary-3c3d-3,operator:binary-3c3d-4,operator:binary-3c3d-5,operator:binary-3c3d-6,operator:binary-3c3d-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-3c3d`. API fingerprint: `a1921168dd368d5746795066eccbc149f8535206b3d1b1fb28f040cdeb857dec`.

<a id="symbol-87e29b308c4c72b0e7d3af64"></a>

## core.compare / >

Identity: `NSR-CORE-026`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left > right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-3e-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-3e-0,operator:binary-3e-1,operator:binary-3e-2,operator:binary-3e-3,operator:binary-3e-4,operator:binary-3e-5,operator:binary-3e-6,operator:binary-3e-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-3e`. API fingerprint: `487a597b4a3cabb406c7003f9136f4f683ae7124f38cbe1f9ae6eedca50e0d05`.

<a id="symbol-39608bfd345b671ca27cea42"></a>

## core.compare / >=

Identity: `NSR-CORE-027`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left >= right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-3e3d-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-3e3d-0,operator:binary-3e3d-1,operator:binary-3e3d-2,operator:binary-3e3d-3,operator:binary-3e3d-4,operator:binary-3e3d-5,operator:binary-3e3d-6,operator:binary-3e3d-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-3e3d`. API fingerprint: `6a38ad7983d618ca178a24cb772f41451d0ef84e2873ab1531515fcb745060ce`.

<a id="symbol-15d680f301d90590b417bab6"></a>

## core.eq / ==

Identity: `NSR-CORE-028`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left == right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-3d3d-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-3d3d-0,operator:binary-3d3d-1,operator:binary-3d3d-2,operator:binary-3d3d-3,operator:binary-3d3d-4,operator:binary-3d3d-5,operator:binary-3d3d-6,operator:binary-3d3d-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-3d3d`. API fingerprint: `30b986ee264d75eab45c6f23114261d46117dbc8aa89782f10ab6c24c3d84f87`.

<a id="symbol-4b7f82eb079c0e1ec516e2a3"></a>

## core.eq / !=

Identity: `NSR-CORE-029`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left != right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-213d-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-213d-0,operator:binary-213d-1,operator:binary-213d-2,operator:binary-213d-3,operator:binary-213d-4,operator:binary-213d-5,operator:binary-213d-6,operator:binary-213d-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-213d`. API fingerprint: `d6d682c217e3bc5c088b48025b298705d765fc98b7f7e7945f656bbd70b693eb`.

<a id="symbol-918d3d0d9c4049cea4face3f"></a>

## core.compare / <=>

Identity: `NSR-CORE-030`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(-17<=>0).order;(order==(0<=>0)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:ordering--17-0-0-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:ordering-17-29--1-0,operator:ordering-17-29-0-0,operator:ordering-17-29-1-0,operator:ordering-29-17--1-0,operator:ordering-29-17-0-0,operator:ordering-29-17-1-0,operator:ordering-17-17--1-0,operator:ordering-17-17-0-0,operator:ordering-17-17-1-0,operator:ordering--17-0--1-0,operator:ordering--17-0-0-0,operator:ordering--17-0-1-0,operator:ordering-0--17--1-0,operator:ordering-0--17-0-0,operator:ordering-0--17-1-0`.

Misuse/diagnostic/failure proofs: `operator:ordering-type`. API fingerprint: `45cdf331c494454d76bf9af8f77a25298a2dfa9627a0568eebeecc53d6bf15cf`.

<a id="symbol-46b5aa678a419a2007c44aad"></a>

## core.mutation / =

Identity: `NSR-CORE-031`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.value.mutable;value=71;value.return;}
```

Explicit process status: `71`; the full value/effect oracle is `binding:Int-store`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `binding:Int-store,binding:Bool-store,binding:Bytes-store`.

Misuse/diagnostic/failure proofs: `binding:constant-bool-write,binding:constant-text-write`. API fingerprint: `70756ac07c412b85eccc5bd10f76dbccb6d624053d3be11007d707169606e0c3`.

<a id="symbol-5c8b2b62e02477c2f647e671"></a>

## core.mutation / +=

Identity: `NSR-CORE-032`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.value.mutable;value+=11;value.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `binding:Int-compound-add`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `binding:Int-compound-add`.

Misuse/diagnostic/failure proofs: `binding:constant-bool-write`. API fingerprint: `c05de1d0f9f21f8ea2488fcb274e28e9edae4e98413e39432291e0851e68cefb`.

<a id="symbol-5ee0126d2c1a3273d47b11ed"></a>

## core.mutation / -=

Identity: `NSR-CORE-033`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.value.mutable;value-=3;value.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `binding:Int-compound-subtract`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `binding:Int-compound-subtract`.

Misuse/diagnostic/failure proofs: `binding:constant-bool-write`. API fingerprint: `a81888a119c48db077503b83ed9450613b786416cd5b17f3b07eaee8206b5b56`.

<a id="symbol-7e5ae120970cbfc5d6a62ab1"></a>

## core.mutation / *=

Identity: `NSR-CORE-034`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.value.mutable;value*=7;value.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `binding:Int-compound-multiply`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `binding:Int-compound-multiply`.

Misuse/diagnostic/failure proofs: `binding:constant-bool-write`. API fingerprint: `03c112c71b7dfa48a066007ef3ce005dcdebac73f1cfb728db0db4a0c937d644`.

<a id="symbol-8f2de96b3d11aa00d980fc55"></a>

## core.mutation / /=

Identity: `NSR-CORE-035`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.value.mutable;value/=3;value.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `binding:Int-compound-divide`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `binding:Int-compound-divide`.

Misuse/diagnostic/failure proofs: `binding:constant-bool-write`. API fingerprint: `fb3f6728119cb93cf682c18fd56f2c82546be8468356461ed499e14676848b48`.

<a id="symbol-d1507ef7a50385ff2523509a"></a>

## core.mutation / %=

Identity: `NSR-CORE-036`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.value.mutable;value%=7;value.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `binding:Int-compound-remainder`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `binding:Int-compound-remainder`.

Misuse/diagnostic/failure proofs: `binding:constant-bool-write`. API fingerprint: `47ce0a2acc843bec296c1c117f2a6c69c547f67822f1004d86121bb448acc57a`.

<a id="symbol-6556b3167c669135545295d9"></a>

## core.mutation / ^=

Identity: `NSR-CORE-037`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.value.mutable;value^=2;value.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `binding:Int-compound-power`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `binding:Int-compound-power`.

Misuse/diagnostic/failure proofs: `binding:constant-bool-write`. API fingerprint: `d8bde401e6c18317f110b5206413571d8b4124613eb9ad50cca43108149b998f`.

<a id="symbol-33326957e909dff1b3f74fa2"></a>

## core.result / ?

Identity: `NSR-CORE-038`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Result<Int,Int>.context)propagate(Result<Int,Int>.value){value? .return;}start(){Result<Int,Int>(Ok(0)).propagate(Result<Int,Int>(Ok(0))).get().return;}
```

Explicit process status: `0`; the full value/effect oracle is `operator:propagate-True-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:propagate-False-0,operator:propagate-True-0,operator:propagate-False-7,operator:propagate-True-7,operator:propagate-False-23,operator:propagate-True-23,operator:propagate-False-29,operator:propagate-True-29,operator:propagate-False-255,operator:propagate-True-255`.

Misuse/diagnostic/failure proofs: `operator:result-outside-context`. API fingerprint: `c159ad96aea8d5b05f1bb1244bada48847b11724f8b27f18b051c2d339fe1b18`.

<a id="symbol-48a90c8e53504d330248a0bd"></a>

## core.option / ??

Identity: `NSR-CORE-039`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Option<Int>(None()).v;v ?? 83.return;}
```

Explicit process status: `83`; the full value/effect oracle is `operator:coalesce-False-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:coalesce-False-0,operator:coalesce-True-0,operator:coalesce-False-7,operator:coalesce-True-7,operator:coalesce-False-23,operator:coalesce-True-23,operator:coalesce-False-29,operator:coalesce-True-29,operator:coalesce-False-255,operator:coalesce-True-255`.

Misuse/diagnostic/failure proofs: `operator:coalesce-type-mismatch`. API fingerprint: `c77e3fb054153353da22bf9f05b4c35b1a530e53bbef438d43fd068ee3558bed`.

<a id="symbol-7bfc47d8000ece9903153e8c"></a>

## core.option / ?.

Identity: `NSR-CORE-040`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Option<Int>(None()).v;v?.wrappingAdd(17).mapped;mapped.unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `operator:option-chain-False-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:option-chain-False-0,operator:option-chain-True-0,operator:option-chain-False-7,operator:option-chain-True-7,operator:option-chain-False-23,operator:option-chain-True-23,operator:option-chain-False-29,operator:option-chain-True-29,operator:option-chain-False-255,operator:option-chain-True-255`.

Misuse/diagnostic/failure proofs: `operator:optional-chain-invalid-member`. API fingerprint: `ae9429c05d2e9cacc69cfec9d89eb37706a8a32a3a62b6b9a2b9d908d8ea104c`.

<a id="symbol-ab8abbb9e22114738cd8c4d1"></a>

## core.option_mutation / ??=

Identity: `NSR-CORE-041`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Option<Int>(None()).v.mutable;v ??= 83;v.unwrapOr(0).return;}
```

Explicit process status: `83`; the full value/effect oracle is `operator:coalesce-store-False-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:coalesce-store-False-0,operator:coalesce-store-True-0,operator:coalesce-store-False-7,operator:coalesce-store-True-7,operator:coalesce-store-False-23,operator:coalesce-store-True-23,operator:coalesce-store-False-29,operator:coalesce-store-True-29,operator:coalesce-store-False-255,operator:coalesce-store-True-255`.

Misuse/diagnostic/failure proofs: `operator:option-assign-immutable`. API fingerprint: `e59538c21824426cd4cb897f854c722e0336ff57e327a12d4325e9e756668531`.

<a id="symbol-942b0f0769353f3a8adab4e0"></a>

## core.quantity / %

Identity: `NSR-CORE-042`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0%).q;flow.assert(q == 0‱);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:ratio-25-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:ratio-25-0-False,operator:ratio-25-0-True,operator:ratio-25-7-False,operator:ratio-25-7-True,operator:ratio-25-29-False,operator:ratio-25-29-True`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-25`. API fingerprint: `596d76b2cb7567b75f416749d87c074b5940fa8dd643bb625e4d3e1cdcfccb72`.

<a id="symbol-de46c2fe44efbd7da87be345"></a>

## core.range / …

Identity: `NSR-CORE-043`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0…4).values;values.length().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:range-e280a6-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:range-e280a6-0,operator:range-e280a6-7,operator:range-e280a6-29`.

Misuse/diagnostic/failure proofs: `operator:chained-range,operator:ascii-three-dot-range`. API fingerprint: `2b315728fcab8a017c87a6d0107aa33a3514f2a432a1623d954b65b95b0a91b5`.

<a id="symbol-4abc55d56d9c2692a68ceeec"></a>

## core.range / …<

Identity: `NSR-CORE-044`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0…<4).values;values.length().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:range-e280a63c-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:range-e280a63c-0,operator:range-e280a63c-7,operator:range-e280a63c-29`.

Misuse/diagnostic/failure proofs: `operator:chained-range,operator:ascii-three-dot-range`. API fingerprint: `b93cf3539036b0c70b720e200d1d1e62fe429ebe1349e1676184704b076e6353`.

<a id="symbol-e77f5709cdc7b8ac6d3817f9"></a>

## core.range / <…

Identity: `NSR-CORE-045`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0<…4).values;values.length().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:range-3ce280a6-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:range-3ce280a6-0,operator:range-3ce280a6-7,operator:range-3ce280a6-29`.

Misuse/diagnostic/failure proofs: `operator:chained-range,operator:ascii-three-dot-range`. API fingerprint: `857ba9cf2397e1cbaf3e21cf3dc69bf89ae4d418d3c2189fcf5c9b5207126aa7`.

<a id="symbol-8e65be4e9ece7a6d415e4e9f"></a>

## core.range / <…<

Identity: `NSR-CORE-046`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0<…<4).values;values.length().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:range-3ce280a63c-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:range-3ce280a63c-0,operator:range-3ce280a63c-7,operator:range-3ce280a63c-29`.

Misuse/diagnostic/failure proofs: `operator:chained-range,operator:ascii-three-dot-range`. API fingerprint: `30a125cc47b1817b7b18dbb8e380b044532c454c5968832b90388db8e5a8f123`.

<a id="symbol-26eb1cc3efaf586156013b71"></a>

## core.flow / ..

Identity: `NSR-CORE-047`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)observe(){self..{17.console();}.return;}start(){0.observe().return;}
```

Explicit process status: `0`; the full value/effect oracle is `operator:lateral-effect-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; The linked receiver frame survives native Console and owned Text allocations; body effects occur once before the preserved return. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:lateral-effect-0,operator:lateral-owned-effect-0,operator:lateral-effect-7,operator:lateral-owned-effect-7,operator:lateral-effect-23,operator:lateral-owned-effect-23,operator:lateral-effect-29,operator:lateral-owned-effect-29,operator:lateral-effect-255,operator:lateral-owned-effect-255`.

Misuse/diagnostic/failure proofs: `operator:lateral-mutation`. API fingerprint: `cb37cf2fb3070c6817a81bd1e7cce31f2e974a2c8d1b93f3b0571b19d72f7902`.

<a id="symbol-08c8301bf69c6f35087e43d6"></a>

## std.math / √

Identity: `NSR-DOM-001`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.value)calculate(){(√value).return;}start(){(0).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:root-e2889a-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Native checked exact signed Int roots; nonintegral/invalid real roots trap through the arithmetic domain owner. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:root-e2889a-0,operator:root-e2889a-1,operator:root-e2889a-81,operator:root-e2889a-625`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-e2889a`. API fingerprint: `9a369b8309ec8dbf4823dcff0fe10080053a36af53f34503fa7d482d1ba8a8ed`.

<a id="symbol-fdc13381d00d100fe02deb4d"></a>

## std.math / ∛

Identity: `NSR-DOM-002`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.value)calculate(){(∛value).return;}start(){(0).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:root-e2889b-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Native checked exact signed Int roots; nonintegral/invalid real roots trap through the arithmetic domain owner. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:root-e2889b--125,operator:root-e2889b--8,operator:root-e2889b-0,operator:root-e2889b-27,operator:root-e2889b-343`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-e2889b`. API fingerprint: `ba0d400db8376470178a4fcfbb95273fc31b6854b5ba3170d583cc9f24d0f2b8`.

<a id="symbol-0aaf1c4f534d4c65ba8fd527"></a>

## std.math / ∜

Identity: `NSR-DOM-003`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.value)calculate(){(∜value).return;}start(){(0).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:root-e2889c-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Native checked exact signed Int roots; nonintegral/invalid real roots trap through the arithmetic domain owner. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:root-e2889c-0,operator:root-e2889c-1,operator:root-e2889c-81,operator:root-e2889c-625`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-e2889c`. API fingerprint: `f55be8664664d94a6af0c7a8139153becd14c704d1e00047729935f5ad4afaae`.

<a id="symbol-3f14ea90d8a94f4ae3b8f5b3"></a>

## std.math.combinatorics / !

Identity: `NSR-DOM-004`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(21!).console();23.return;}
```

Explicit process status: `172`; the full value/effect oracle is `operator:domain-factorial-overflow`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:factorial-0,operator:factorial-1,operator:factorial-5,operator:factorial-9,operator:factorial-20,operator:domain-factorial,operator:domain-factorial-overflow`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-666163746f7269616c`. API fingerprint: `ea7cdba62502938cad1fb34585a79271dca51aaa15fb41cdb976bbf27bdebb32`.

<a id="symbol-f7c05bead291b5e6544a6231"></a>

## std.math.extended / ∞

Identity: `NSR-DOM-005`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∞.v;v.isInfinite().console();7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `operator:constant-infinity-7`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:constant-infinity-7,operator:constant-infinity-23,operator:constant-infinity-255`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e2889e`. API fingerprint: `8e3f6dfef2a000e63345e51b0f89f15048808171468b4a7a84c384d26106963d`.

<a id="symbol-cccf8646004e3b1d67c9ca83"></a>

## std.math.constants / π

Identity: `NSR-DOM-006`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){π.v;"%.6f".format(v).console();7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `operator:constant-pi-7`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:constant-pi-7,operator:constant-pi-23,operator:constant-pi-255`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-cf80`. API fingerprint: `761e451b32c5543e96362a049c740a199dbf1ea6741357e344d925f6daeebc19`.

<a id="symbol-1c2ff3dda1c7b3d128eac9a5"></a>

## std.math.constants / τ

Identity: `NSR-DOM-007`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){τ.v;"%.6f".format(v).console();7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `operator:constant-tau-7`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:constant-tau-7,operator:constant-tau-23,operator:constant-tau-255`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-cf84`. API fingerprint: `d38f0e863fa41be7ce63078ca995b184f79de45388f460153b454df9a191337a`.

<a id="symbol-7ccaad58bee301ddcc7eba3e"></a>

## std.math.reduce / ∑

Identity: `NSR-DOM-008`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].values;(∑values).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:reduce-e28891-[]`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite immutable Array<Int,N> domain; canonical checked constant fold in source order; empty identities 0/1; constant overflow is rejected with NEBO_TYPE_CONSTANT_OVERFLOW; later publication and explicit return remain in the shared Program. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:reduce-e28891-[],operator:reduce-e28891-[17],operator:reduce-e28891-[2_3_5],operator:reduce-e28891-[2_7_5],operator:reduce-e28891-[-2_3_5],operator:reduce-e28891-[0_7_29]`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e28891,operator:reduce-overflow-e28891`. API fingerprint: `72b09f2c356408a136c305b436ffe7147f1b9abaf02beefde40561c75be2f109`.

<a id="symbol-247308f8eebbe84e08df67be"></a>

## std.math.reduce / ∏

Identity: `NSR-DOM-009`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].values;(∏values).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:reduce-e2888f-[]`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite immutable Array<Int,N> domain; canonical checked constant fold in source order; empty identities 0/1; constant overflow is rejected with NEBO_TYPE_CONSTANT_OVERFLOW; later publication and explicit return remain in the shared Program. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:reduce-e2888f-[],operator:reduce-e2888f-[17],operator:reduce-e2888f-[2_3_5],operator:reduce-e2888f-[2_7_5],operator:reduce-e2888f-[-2_3_5],operator:reduce-e2888f-[0_7_29]`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e2888f,operator:reduce-overflow-e2888f`. API fingerprint: `b96733932637113c02709f20b5b026afec99d740bc9fa820b63b18fe2f14a94d`.

<a id="symbol-b4a1cc1ca9553d16841c50a3"></a>

## std.measurement / ±

Identity: `NSR-DOM-010`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17 ± 3).m;m.measuredValue().console();m.uncertainty().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:uncertain-value-17-3`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:uncertain-value-17-3,operator:uncertain-value-29-3,operator:uncertain-value-17-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-c2b1`. API fingerprint: `632111b8f2e16272da5ae76895991495000d01a3b7fa358a4ba5cdd70421031f`.

<a id="symbol-aa38abbdf5bce095ce6e4f09"></a>

## std.math.approx / ≈

Identity: `NSR-DOM-011`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){((8 ± 2) ≈ (12 ± 3)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:uncertain-e28988-8-2-12-3`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:uncertain-e28988-17-3-23-3,operator:uncertain-e28988-17-3-24-3,operator:uncertain-e28988-29-3-29-3,operator:uncertain-e28988-8-2-12-3`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e28988`. API fingerprint: `2fc62e683808ed3d0b5c3cc2c6e66c8b7dd8f380500021ef56444b3bd01aea4b`.

<a id="symbol-77ed037837f30181289aebfb"></a>

## std.math.approx / ≉

Identity: `NSR-DOM-012`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){((8 ± 2) ≉ (12 ± 3)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:uncertain-e28989-8-2-12-3`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:uncertain-e28989-17-3-23-3,operator:uncertain-e28989-17-3-24-3,operator:uncertain-e28989-29-3-29-3,operator:uncertain-e28989-8-2-12-3`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e28989`. API fingerprint: `a4f4278dd5fd7af17f6253b3a5f807613938d881eb09d89eba2398daca5f4a6a`.

<a id="symbol-a781a4c2f24d9a2b67a9e288"></a>

## std.symbolic / ≡

Identity: `NSR-DOM-013`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){((8 ± 2) ≡ (12 ± 3)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:uncertain-e289a1-8-2-12-3`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:uncertain-e289a1-17-3-23-3,operator:uncertain-e289a1-17-3-24-3,operator:uncertain-e289a1-29-3-29-3,operator:uncertain-e289a1-8-2-12-3`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e289a1`. API fingerprint: `2bbc3a633631262c0d60c7fabcbbd32fe903431d8dbb19a68bfced3ddc23b564`.

<a id="symbol-5e4e98f9125aa0de261d06e2"></a>

## std.math.number_theory / ∣

Identity: `NSR-DOM-014`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){((0) ∣ (17)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:divisibility-e288a3-0-17`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:divisibility-e288a3-0-17,operator:divisibility-e288a3-7-63,operator:divisibility-e288a3-8-63,operator:divisibility-e288a3--7-63,operator:divisibility-e288a3-7--63`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e288a3`. API fingerprint: `07f053946876d63c28efb523032dc6799f37cf33c37869e16512ad1b1093f4e7`.

<a id="symbol-28a4ddd304f7a7b9e2843017"></a>

## std.math.number_theory / ∤

Identity: `NSR-DOM-015`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){((0) ∤ (17)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:divisibility-e288a4-0-17`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:divisibility-e288a4-0-17,operator:divisibility-e288a4-7-63,operator:divisibility-e288a4-8-63,operator:divisibility-e288a4--7-63,operator:divisibility-e288a4-7--63`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e288a4`. API fingerprint: `1ce7d9eecb8aae348558786d92157c982d8dab8b5d74bbdf12a88134ed1d9353`.

<a id="symbol-b2b6d1ed02b0d6cc041b6bad"></a>

## std.math.rounding / ⌊x⌋

Identity: `NSR-DOM-016`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Float.value)calculate(){(⌊value⌋).return;}start(){(0.5).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:round-e28c8a-0.5`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:round-e28c8a--17.75,operator:round-e28c8a--0.0,operator:round-e28c8a-0.5,operator:round-e28c8a-29.25`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-666c6f6f72`. API fingerprint: `7475d2b8b5d485ca0e8c0f8671aefe67f1fc41bf6da732e1c44e555b6bf65cbc`.

<a id="symbol-9727d4706ea39de68220ef36"></a>

## std.math.rounding / ⌈x⌉

Identity: `NSR-DOM-017`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Float.value)calculate(){(⌈value⌉).return;}start(){(0.5).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:round-e28c88-0.5`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:round-e28c88--17.75,operator:round-e28c88--0.0,operator:round-e28c88-0.5,operator:round-e28c88-29.25`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-6365696c`. API fingerprint: `64891dca47b606ac500b70aa2a1b655a169ebd95552a8424d3d865636b14c399`.

<a id="symbol-e6e87512bb9f6bd0c170d39d"></a>

## std.symbolic / ∝

Identity: `NSR-DOM-018`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){((8 ± 2) ∝ (12 ± 3)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:uncertain-e2889d-8-2-12-3`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:uncertain-e2889d-17-3-23-3,operator:uncertain-e2889d-17-3-24-3,operator:uncertain-e2889d-29-3-29-3,operator:uncertain-e2889d-8-2-12-3`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e2889d`. API fingerprint: `9c6dcc73eb422896ae3b23c22d710497d2c8e751064da6f795070494c6df2d9a`.

<a id="symbol-4b74f4c2b9898a033d4f707f"></a>

## std.units.angle / °

Identity: `NSR-DOM-019`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0°).q;flow.assert((q+7°)==7°);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:angle-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:angle-0-False,operator:angle-0-True,operator:angle-17-False,operator:angle-17-True,operator:angle-29-False,operator:angle-29-True`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-c2b0`. API fingerprint: `7e9748383ae2d945d0c86d5dc8f6306d565a1aa195cacd4036a44d82ba13a91f`.

<a id="symbol-68868e7209c90ef2b637226e"></a>

## std.units.ratio / ‰

Identity: `NSR-DOM-020`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0‰).q;flow.assert(q == 0‱);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:ratio-e280b0-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:ratio-e280b0-0-False,operator:ratio-e280b0-0-True,operator:ratio-e280b0-7-False,operator:ratio-e280b0-7-True,operator:ratio-e280b0-29-False,operator:ratio-e280b0-29-True`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e280b0`. API fingerprint: `a5ce0c2904f154434876b1af2e97086e2063276cd5e45bbaf73c7833b2bd6a65`.

<a id="symbol-cfcb433a8926ee9ffe210ebb"></a>

## std.units.ratio / ‱

Identity: `NSR-DOM-021`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0‱).q;flow.assert(q == 0‱);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:ratio-e280b1-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:ratio-e280b1-0-False,operator:ratio-e280b1-0-True,operator:ratio-e280b1-7-False,operator:ratio-e280b1-7-True,operator:ratio-e280b1-29-False,operator:ratio-e280b1-29-True`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-e280b1`. API fingerprint: `7293bbe4743853b1be70c1ceaae9faeda0033f234d642ac708f04f446f594d8e`.

<a id="symbol-1317c51b6bd0f0c367906b89"></a>

## std.units.temperature / °C

Identity: `NSR-DOM-022`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0°C).c;(32°F).f;flow.assert(c==f);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:temperature-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:temperature-0-False,operator:temperature-0-True,operator:temperature-20-False,operator:temperature-20-True,operator:temperature-100-False,operator:temperature-100-True`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-c2b043`. API fingerprint: `c1d93ab4a682294a7095cc9875e6863e841588bb7313692abc8e2934ea962f52`.

<a id="symbol-89514a0fb4a26a3af3f7e557"></a>

## std.units.temperature / °F

Identity: `NSR-DOM-023`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(0°C).c;(32°F).f;flow.assert(c==f);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:temperature-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:temperature-0-False,operator:temperature-0-True,operator:temperature-20-False,operator:temperature-20-True,operator:temperature-100-False,operator:temperature-100-True`.

Misuse/diagnostic/failure proofs: `operator:wrong-domain-c2b046`. API fingerprint: `fe4388c38f01c0c622fdb06b6cc0c71cf4b01042d49df7b81bd5ef874aeeb664`.

<a id="symbol-6d6b6ab9478a23e5e503c042"></a>

## std.set / ∈

Identity: `NSR-DOM-024`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(2 ∈ left).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:membership-e28888-4-2`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:membership-e28888-0-2,operator:membership-e28888-0-7,operator:membership-e28888-0-62,operator:membership-e28888-1-2,operator:membership-e28888-1-7,operator:membership-e28888-1-62,operator:membership-e28888-2-2,operator:membership-e28888-2-7,operator:membership-e28888-2-62,operator:membership-e28888-3-2,operator:membership-e28888-3-7,operator:membership-e28888-3-62,operator:membership-e28888-4-2,operator:membership-e28888-4-7,operator:membership-e28888-4-62`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e28888`. API fingerprint: `6483bfdef96e741e128ac7a4812bff14c4c1b437c6d5f891cd3d332e649f7db7`.

<a id="symbol-b42706e891f5607a73cb573a"></a>

## std.set / ∉

Identity: `NSR-DOM-025`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(2 ∉ left).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:membership-e28889-4-2`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:membership-e28889-0-2,operator:membership-e28889-0-7,operator:membership-e28889-0-62,operator:membership-e28889-1-2,operator:membership-e28889-1-7,operator:membership-e28889-1-62,operator:membership-e28889-2-2,operator:membership-e28889-2-7,operator:membership-e28889-2-62,operator:membership-e28889-3-2,operator:membership-e28889-3-7,operator:membership-e28889-3-62,operator:membership-e28889-4-2,operator:membership-e28889-4-7,operator:membership-e28889-4-62`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e28889`. API fingerprint: `f487337410084be9885800a1b5072505a3f6c294ceda625b008c96ebea27c73c`.

<a id="symbol-441cd9343bb9792f2a30dd2b"></a>

## std.set / ∪

Identity: `NSR-DOM-026`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left ∪ right).cardinality().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-e288aa-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-e288aa-0,operator:set-e288aa-1,operator:set-e288aa-2,operator:set-e288aa-3,operator:set-e288aa-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e288aa`. API fingerprint: `3f8205ca7c714ba1403bcb822700c3d6653577bc7e5606714631d5900b32be68`.

<a id="symbol-1d25c638f7eae27b1acc7904"></a>

## std.set / ∩

Identity: `NSR-DOM-027`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left ∩ right).cardinality().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-e288a9-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-e288a9-0,operator:set-e288a9-1,operator:set-e288a9-2,operator:set-e288a9-3,operator:set-e288a9-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e288a9`. API fingerprint: `f22ef8e938f039bc6e092deef4dd0b5e99411e078eb5f6f9a885965bbf1ff7f8`.

<a id="symbol-e6aac400d0188a0f5f6fd90d"></a>

## std.set / ∖

Identity: `NSR-DOM-028`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left ∖ right).cardinality().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-e28896-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-e28896-0,operator:set-e28896-1,operator:set-e28896-2,operator:set-e28896-3,operator:set-e28896-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e28896`. API fingerprint: `fb3ebd1da522c8864d24889a8399a3509ab4c6503efa60e3b967af1c465a198a`.

<a id="symbol-2dfeb1de31c2d139532b37c1"></a>

## std.set / △

Identity: `NSR-DOM-029`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left △ right).cardinality().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-e296b3-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-e296b3-0,operator:set-e296b3-1,operator:set-e296b3-2,operator:set-e296b3-3,operator:set-e296b3-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e296b3`. API fingerprint: `c03a4aeb9234c4d599aa6bb0b317e5248e31c7bad124813cf8d37e1b7bca9db5`.

<a id="symbol-ade8cd491b826f12ea982227"></a>

## std.set / ⊆

Identity: `NSR-DOM-030`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left ⊆ right).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-e28a86-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-e28a86-0,operator:set-e28a86-1,operator:set-e28a86-2,operator:set-e28a86-3,operator:set-e28a86-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e28a86`. API fingerprint: `69ca9825a674e24b8623e2a003b076f3c2a860a95017145f3b29beead8d61c1c`.

<a id="symbol-34a695d76228ebdf57e87da4"></a>

## std.set / ⊂

Identity: `NSR-DOM-031`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left ⊂ right).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-e28a82-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-e28a82-0,operator:set-e28a82-1,operator:set-e28a82-2,operator:set-e28a82-3,operator:set-e28a82-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e28a82`. API fingerprint: `0e27cc82e476a3707e972c8413f012a940e9b74fd52f0b76974ad439431ecba7`.

<a id="symbol-69d18df74b95da3e608d10b6"></a>

## std.set / ⊇

Identity: `NSR-DOM-032`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left ⊇ right).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-e28a87-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-e28a87-0,operator:set-e28a87-1,operator:set-e28a87-2,operator:set-e28a87-3,operator:set-e28a87-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e28a87`. API fingerprint: `24df4c191e6e97940d0b6dc2308b37386c845029195d37a0b7e9530eda13da48`.

<a id="symbol-dc47f7040220d4421244293f"></a>

## std.set / ⊃

Identity: `NSR-DOM-033`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left ⊃ right).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-e28a83-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-e28a83-0,operator:set-e28a83-1,operator:set-e28a83-2,operator:set-e28a83-3,operator:set-e28a83-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e28a83`. API fingerprint: `425419bae7c62f482635febf064cbdffaa9f08c688c464c556a91b390a64f19f`.

<a id="symbol-2e3812c0b15903b90b76e4a1"></a>

## std.set / ×

Identity: `NSR-DOM-034`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left × right).cardinality().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-c397-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current G133 finite immutable Array<Int,N> set profile with distinct values in 0..62; canonical mask/cardinality owner; independent Python set operations. This does not substitute for the separately proved native Dict/Set APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-c397-0,operator:set-c397-1,operator:set-c397-2,operator:set-c397-3,operator:set-c397-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-c397`. API fingerprint: `8de97fe3bfcd92feab22327d1b2e09a9f691a2d858dfd0ccb37b9da45bf37f3a`.

<a id="symbol-65bdc89d6580e8b747fefeb2"></a>

## std.set / ∅

Identity: `NSR-DOM-035`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Array<Int,0> [].left;Array<Int,0> [].right;(left ∪ ∅).cardinality().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:set-empty-context-4`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:set-empty-context-0,operator:set-empty-context-1,operator:set-empty-context-2,operator:set-empty-context-3,operator:set-empty-context-4`.

Misuse/diagnostic/failure proofs: `operator:wrong-set-e288aa`. API fingerprint: `863d38878a6aec8b1295381293260ae0d12aa153a42b67b0fac44ff3b771d6e0`.

<a id="symbol-8af8ee591216032cd57525c3"></a>

## std.linalg / ·

Identity: `NSR-DOM-036`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,3> [2,3,4].a;Vector<Int,3> [5,6,8].b;a.dot(b);}
```

Explicit process status: `60`; the full value/effect oracle is `boundedoperator:dom-036-named-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Typed infix statement profile additionally supports Int extents 1..64 (cross/parallel exactly 3), tensor result at most 64 lanes; native checked kernels, distinct owned results, ordered operands and explicit program return. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-036-named-0,boundedoperator:dom-036-named-1,boundedoperator:dom-036-named-2,linear:dot-values-0,linear:dot-values-1,linear:dot-values-2,linear:dot-values-3,linear:dot-values-4,linear:dot-values-5,linear:dot-values-6,linear:dot-return-0,linear:dot-return-7,linear:dot-return-23,linear:dot-return-29,linear:dot-return-255,linear:dot-extent-1,linear:dot-extent-64,linear:dot-overflow,linear:operand-effects-once,linear:dict-owned-argument,linear:loop-temporary-reclamation`.

Misuse/diagnostic/failure proofs: `linear:dot-right-scalar,linear:dot-right-float,linear:dot-right-bool,linear:dot-shape`. API fingerprint: `6d7862641011e6184c82d30d8d96ee03bb26050845497a98ae0361e9bfd5dd51`.

<a id="symbol-254252d7d68d8b29ec9b8bb3"></a>

## std.linalg.geometry / ×

Identity: `NSR-DOM-037`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,3> [2,3,4].a;Vector<Int,3> [5,6,8].b;a.cross(b).sum();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-037-named-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Typed infix statement profile additionally supports Int extents 1..64 (cross/parallel exactly 3), tensor result at most 64 lanes; native checked kernels, distinct owned results, ordered operands and explicit program return. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-037-named-0,boundedoperator:dom-037-named-1,boundedoperator:dom-037-named-2,linear:cross-values-0,linear:cross-values-1,linear:cross-values-2,linear:cross-values-3,linear:cross-values-4,linear:cross-values-5,linear:cross-values-6,linear:cross-return-0,linear:cross-return-7,linear:cross-return-23,linear:cross-return-29,linear:cross-return-255,linear:cross-overflow,linear:operand-effects-once,linear:dict-owned-argument,linear:loop-temporary-reclamation`.

Misuse/diagnostic/failure proofs: `linear:cross-right-scalar,linear:cross-right-float,linear:cross-right-bool,linear:cross-shape,linear:cross-not-three`. API fingerprint: `eeae063fd2123ed1831efe3ed7422eda3ad1df41b41573401097645f351682cf`.

<a id="symbol-b261da33fecc7502297dafe9"></a>

## std.linalg / ⊙

Identity: `NSR-DOM-038`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,3> [2,3,4].a;Vector<Int,3> [5,6,8].b;a.hadamard(b).sum();}
```

Explicit process status: `60`; the full value/effect oracle is `boundedoperator:dom-038-named-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Typed infix statement profile additionally supports Int extents 1..64 (cross/parallel exactly 3), tensor result at most 64 lanes; native checked kernels, distinct owned results, ordered operands and explicit program return. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-038-named-0,boundedoperator:dom-038-named-1,boundedoperator:dom-038-named-2,linear:hadamard-values-0,linear:hadamard-values-1,linear:hadamard-values-2,linear:hadamard-values-3,linear:hadamard-values-4,linear:hadamard-values-5,linear:hadamard-values-6,linear:hadamard-return-0,linear:hadamard-return-7,linear:hadamard-return-23,linear:hadamard-return-29,linear:hadamard-return-255,linear:hadamard-extent-1,linear:hadamard-extent-64,linear:hadamard-overflow,linear:operand-effects-once,linear:dict-owned-argument,linear:loop-temporary-reclamation`.

Misuse/diagnostic/failure proofs: `linear:hadamard-right-scalar,linear:hadamard-right-float,linear:hadamard-right-bool,linear:hadamard-shape`. API fingerprint: `31537d7d45a25c3cc34022d08ceb05b38280ff4b15f6b3cb251238f5dd2abba1`.

<a id="symbol-201fd4d7f46be7faae6a4b25"></a>

## std.linalg / ⊗

Identity: `NSR-DOM-039`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,3> [2,3,4].a;Vector<Int,3> [5,6,8].b;a.tensorProduct(b).sum();}
```

Explicit process status: `171`; the full value/effect oracle is `boundedoperator:dom-039-named-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Typed infix statement profile additionally supports Int extents 1..64 (cross/parallel exactly 3), tensor result at most 64 lanes; native checked kernels, distinct owned results, ordered operands and explicit program return. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-039-named-0,boundedoperator:dom-039-named-1,boundedoperator:dom-039-named-2,linear:tensor-values-0,linear:tensor-values-1,linear:tensor-values-2,linear:tensor-values-3,linear:tensor-values-4,linear:tensor-values-5,linear:tensor-values-6,linear:tensor-return-0,linear:tensor-return-7,linear:tensor-return-23,linear:tensor-return-29,linear:tensor-return-255,linear:tensor-shape-1-64,linear:tensor-shape-8-8,linear:tensor-shape-4-3,linear:tensor-overflow,linear:operand-effects-once,linear:dict-owned-argument,linear:loop-temporary-reclamation`.

Misuse/diagnostic/failure proofs: `linear:tensor-right-scalar,linear:tensor-right-float,linear:tensor-right-bool,linear:tensor-capacity`. API fingerprint: `59168f6f8831d30eb5cd7d3f3fcabae62e4f391af0e7fa2e22fe5c1851137d22`.

<a id="symbol-c7671f7444b6de888fef16e5"></a>

## std.linalg / ⊕

Identity: `NSR-DOM-040`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,3> [2,3,4].a;Vector<Int,3> [5,6,8].b;(a ⊕ b).sum();}
```

Explicit process status: `28`; the full value/effect oracle is `boundedoperator:dom-040-symbol-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-040-named-0,boundedoperator:dom-040-symbol-0,boundedoperator:dom-040-named-1,boundedoperator:dom-040-symbol-1,boundedoperator:dom-040-named-2,boundedoperator:dom-040-symbol-2`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-040-negative`. API fingerprint: `999cfe16a66403be0af2a42ac8ecd4586e5b6d6ac66293a413d23cf888090007`.

<a id="symbol-e5b3f118142127f104034823"></a>

## std.linalg / ᵀ

Identity: `NSR-DOM-041`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
import "std.scientific" { Matrix; }.scientific;
start(){Matrix<Int,2,2> [2,5,7,11].m;mᵀ.at(0,1);}
```

Explicit process status: `7`; the full value/effect oracle is `boundedoperator:dom-041-symbol-0-0-1`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-041-symbol-0-0-1,boundedoperator:dom-041-named-0-0-1,boundedoperator:dom-041-symbol-0-1-0,boundedoperator:dom-041-named-0-1-0,boundedoperator:dom-041-symbol-0-1-1,boundedoperator:dom-041-named-0-1-1,boundedoperator:dom-041-symbol-1-0-1,boundedoperator:dom-041-named-1-0-1,boundedoperator:dom-041-symbol-1-1-0,boundedoperator:dom-041-named-1-1-0,boundedoperator:dom-041-symbol-1-1-1,boundedoperator:dom-041-named-1-1-1,boundedoperator:dom-041-symbol-2-0-1,boundedoperator:dom-041-named-2-0-1,boundedoperator:dom-041-symbol-2-1-0,boundedoperator:dom-041-named-2-1-0,boundedoperator:dom-041-symbol-2-1-1,boundedoperator:dom-041-named-2-1-1`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-041-negative`. API fingerprint: `d13e618e5546946fd28244ff6ef429a5386d4c267239c68f0e3d7b56181da7d8`.

<a id="symbol-1828590c4b38953450fce602"></a>

## std.linalg.complex / †

Identity: `NSR-DOM-042`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
import "std.scientific" { Matrix; }.scientific;
start(){Matrix<Complex,2,1> [(1,2),(3,-4)].m;m†.imagAt(0,0);}
```

Explicit process status: `254`; the full value/effect oracle is `boundedoperator:dom-042-symbol-0-imagAt-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-042-symbol-0-realAt-0,boundedoperator:dom-042-symbol-0-imagAt-0,boundedoperator:dom-042-symbol-0-realAt-1,boundedoperator:dom-042-symbol-0-imagAt-1,boundedoperator:dom-042-named-0-realAt-0,boundedoperator:dom-042-named-0-imagAt-0,boundedoperator:dom-042-named-0-realAt-1,boundedoperator:dom-042-named-0-imagAt-1,boundedoperator:dom-042-symbol-1-realAt-0,boundedoperator:dom-042-symbol-1-imagAt-0,boundedoperator:dom-042-symbol-1-realAt-1,boundedoperator:dom-042-symbol-1-imagAt-1,boundedoperator:dom-042-named-1-realAt-0,boundedoperator:dom-042-named-1-imagAt-0,boundedoperator:dom-042-named-1-realAt-1,boundedoperator:dom-042-named-1-imagAt-1,boundedoperator:dom-042-symbol-2-realAt-0,boundedoperator:dom-042-symbol-2-imagAt-0,boundedoperator:dom-042-symbol-2-realAt-1,boundedoperator:dom-042-symbol-2-imagAt-1,boundedoperator:dom-042-named-2-realAt-0,boundedoperator:dom-042-named-2-imagAt-0,boundedoperator:dom-042-named-2-realAt-1,boundedoperator:dom-042-named-2-imagAt-1`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-042-negative`. API fingerprint: `27c278922966a2dc94c1852891a05d69cafd8440ee1d3f02af11947fbdbdae3d`.

<a id="symbol-80b6f7336fbd2ab320bab736"></a>

## std.linalg / ⁻¹

Identity: `NSR-DOM-043`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
import "std.scientific" { Matrix; }.scientific;
start(){Matrix<Int,2,2> [2,1,1,1].m;m⁻¹.denominator();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-043-symbol-0-denominator`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-043-symbol-0-0-0,boundedoperator:dom-043-symbol-0-0-1,boundedoperator:dom-043-symbol-0-1-0,boundedoperator:dom-043-symbol-0-1-1,boundedoperator:dom-043-symbol-0-denominator,boundedoperator:dom-043-named-0-0-0,boundedoperator:dom-043-named-0-0-1,boundedoperator:dom-043-named-0-1-0,boundedoperator:dom-043-named-0-1-1,boundedoperator:dom-043-named-0-denominator,boundedoperator:dom-043-symbol-1-0-0,boundedoperator:dom-043-symbol-1-0-1,boundedoperator:dom-043-symbol-1-1-0,boundedoperator:dom-043-symbol-1-1-1,boundedoperator:dom-043-symbol-1-denominator,boundedoperator:dom-043-named-1-0-0,boundedoperator:dom-043-named-1-0-1,boundedoperator:dom-043-named-1-1-0,boundedoperator:dom-043-named-1-1-1,boundedoperator:dom-043-named-1-denominator,boundedoperator:dom-043-symbol-2-0-0,boundedoperator:dom-043-symbol-2-0-1,boundedoperator:dom-043-symbol-2-1-0,boundedoperator:dom-043-symbol-2-1-1,boundedoperator:dom-043-symbol-2-denominator,boundedoperator:dom-043-named-2-0-0,boundedoperator:dom-043-named-2-0-1,boundedoperator:dom-043-named-2-1-0,boundedoperator:dom-043-named-2-1-1,boundedoperator:dom-043-named-2-denominator`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-043-negative`. API fingerprint: `7c855a88772c0b1af91416bd0e31f2c6cef863b1335cd85899da8102faeedaa1`.

<a id="symbol-bbc1f54c2ecda50b633e9475"></a>

## std.linalg / ‖x‖

Identity: `NSR-DOM-044`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,2> [3,4].a;Vector<Int,2> [7,11].b;‖a‖;}
```

Explicit process status: `5`; the full value/effect oracle is `boundedoperator:dom-044-symbol-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-044-symbol-0,boundedoperator:dom-044-named-0,boundedoperator:dom-044-symbol-1,boundedoperator:dom-044-named-1,boundedoperator:dom-044-symbol-2,boundedoperator:dom-044-named-2,boundedoperator:dom-044-symbol-3,boundedoperator:dom-044-named-3`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-044-negative`. API fingerprint: `8bdb2cbf6b840b09bae1a831c4cd78b7904a204748ccdf68ed82971276313a6e`.

<a id="symbol-2117900605500db30c488e04"></a>

## std.linalg / ⟨u,v⟩

Identity: `NSR-DOM-045`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,2> [3,4].a;Vector<Int,2> [7,11].b;⟨a,b⟩;}
```

Explicit process status: `65`; the full value/effect oracle is `boundedoperator:dom-045-symbol-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-045-symbol-0,boundedoperator:dom-045-named-0,boundedoperator:dom-045-symbol-1,boundedoperator:dom-045-named-1,boundedoperator:dom-045-symbol-2,boundedoperator:dom-045-named-2,boundedoperator:dom-045-symbol-3,boundedoperator:dom-045-named-3`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-045-negative`. API fingerprint: `d9245c7101616beec5417bf1a53597f32ce67f2bd09309cea237ad2ea9d069aa`.

<a id="symbol-6496e233343964b700bbe8a5"></a>

## std.functional / ∘

Identity: `NSR-DOM-046`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,3> [2,3,4].a;Vector<Int,3> [5,6,8].b;Vector<Int,3> [3,5,7].x;(a ∘ b).apply(x).sum();}
```

Explicit process status: `88`; the full value/effect oracle is `boundedoperator:dom-046-symbol-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-046-named-0,boundedoperator:dom-046-symbol-0,boundedoperator:dom-046-named-1,boundedoperator:dom-046-symbol-1,boundedoperator:dom-046-named-2,boundedoperator:dom-046-symbol-2`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-046-negative`. API fingerprint: `9c939c270111abb94a042fbe46e7a9025a9afc6dd8e2fbf4bdbab875c3bcf79c`.

<a id="symbol-0f708c05a592cea86c9798a2"></a>

## std.linalg.geometry / ⟂

Identity: `NSR-DOM-047`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int> [1].a;Vector<Int> [3].b;(a ⟂ b).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `linear:orthogonal-extent-1`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Typed infix statement profile additionally supports Int extents 1..64 (cross/parallel exactly 3), tensor result at most 64 lanes; native checked kernels, distinct owned results, ordered operands and explicit program return. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-047-named-0,boundedoperator:dom-047-named-1,boundedoperator:dom-047-named-2,linear:orthogonal-values-0,linear:orthogonal-values-1,linear:orthogonal-values-2,linear:orthogonal-values-3,linear:orthogonal-values-4,linear:orthogonal-values-5,linear:orthogonal-values-6,linear:orthogonal-return-0,linear:orthogonal-return-7,linear:orthogonal-return-23,linear:orthogonal-return-29,linear:orthogonal-return-255,linear:orthogonal-extent-1,linear:orthogonal-extent-64,linear:orthogonal-overflow,linear:operand-effects-once,linear:dict-owned-argument,linear:loop-temporary-reclamation`.

Misuse/diagnostic/failure proofs: `linear:orthogonal-right-scalar,linear:orthogonal-right-float,linear:orthogonal-right-bool,linear:orthogonal-shape`. API fingerprint: `3d7a9033766eccf43dd2e73b4225f970b033fd867964fba669ea47b7da3a1235`.

<a id="symbol-5831dd94bae09d6cb655a901"></a>

## std.geometry / ∥

Identity: `NSR-DOM-048`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,3> [2,3,4].a;Vector<Int,3> [5,6,8].b;a.isParallelTo(b);}
```

Explicit process status: `0`; the full value/effect oracle is `boundedoperator:dom-048-named-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Exact immutable Int Vector aggregate grammar, extent 1..4; matrix Int/Complex profile at most four logical cells; vector observers sum, checked rational inverse numerator/denominator, exact integer norm; symbolic/named parity; all source tokens consumed by the canonical current profile, without adjacent statements. Typed infix statement profile additionally supports Int extents 1..64 (cross/parallel exactly 3), tensor result at most 64 lanes; native checked kernels, distinct owned results, ordered operands and explicit program return. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-048-named-0,boundedoperator:dom-048-named-1,boundedoperator:dom-048-named-2,linear:parallel-values-0,linear:parallel-values-1,linear:parallel-values-2,linear:parallel-values-3,linear:parallel-values-4,linear:parallel-values-5,linear:parallel-values-6,linear:parallel-return-0,linear:parallel-return-7,linear:parallel-return-23,linear:parallel-return-29,linear:parallel-return-255,linear:parallel-overflow,linear:operand-effects-once,linear:dict-owned-argument,linear:loop-temporary-reclamation`.

Misuse/diagnostic/failure proofs: `linear:parallel-right-scalar,linear:parallel-right-float,linear:parallel-right-bool,linear:parallel-shape,linear:parallel-not-three`. API fingerprint: `f834650170ccddadef4bbc81e00f27f82b517988f1326d50ff795b2e6382355c`.

<a id="symbol-a0f0c8e43beb532c974c8bbb"></a>

## std.calculus / ∫

Identity: `NSR-DOM-049`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∫(x,0,2,trapezoid,1,16,[0,1,2]).value();}
```

Explicit process status: `2`; the full value/effect oracle is `boundedoperator:dom-049-e288ab-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-049-e288ab-0,boundedoperator:dom-049-696e74656772617465-0,boundedoperator:dom-049-e288ab-1,boundedoperator:dom-049-696e74656772617465-1,boundedoperator:dom-049-e288ab-2,boundedoperator:dom-049-696e74656772617465-2,boundedoperator:dom-049-e288ab-3,boundedoperator:dom-049-696e74656772617465-3`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-049-negative`. API fingerprint: `c0f3934cc5da69dd4b6ef7042aff393af41ed41907a73c3430a9a4c30a572ebf`.

<a id="symbol-57d9b36a2f42c74436b04265"></a>

## std.calculus / ∬

Identity: `NSR-DOM-050`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∬(x,0,2,y,0,2,rectangle,1,16,[1,2,3,4]).value();}
```

Explicit process status: `10`; the full value/effect oracle is `boundedoperator:dom-050-e288ac-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-050-e288ac-0,boundedoperator:dom-050-696e746567726174653244-0,boundedoperator:dom-050-e288ac-1,boundedoperator:dom-050-696e746567726174653244-1,boundedoperator:dom-050-e288ac-2,boundedoperator:dom-050-696e746567726174653244-2`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-050-negative`. API fingerprint: `d03a6bf60d4d398493e048b93eddef21c56cfb981916e464f9836cbb5978bbca`.

<a id="symbol-500eaa399db7165cb2120fdf"></a>

## std.calculus / ∭

Identity: `NSR-DOM-051`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∭(x,0,2,y,0,2,z,0,2,rectangle,1,16,[1,2,3,4]).value();}
```

Explicit process status: `20`; the full value/effect oracle is `boundedoperator:dom-051-e288ad-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-051-e288ad-0,boundedoperator:dom-051-696e746567726174653344-0,boundedoperator:dom-051-e288ad-1,boundedoperator:dom-051-696e746567726174653344-1,boundedoperator:dom-051-e288ad-2,boundedoperator:dom-051-696e746567726174653344-2`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-051-negative`. API fingerprint: `c788bf1f3e14818ba2f7ab0425128eb11a87b7bdfd3ccf005fe65f0041c78f64`.

<a id="symbol-7acf68cdae99cd50c126f130"></a>

## std.calculus / ∮

Identity: `NSR-DOM-052`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∮(t,0,2,line,clockwise,1,16,[1,0,2,0,0,1,0,3]).value();}
```

Explicit process status: `251`; the full value/effect oracle is `boundedoperator:dom-052-e288ae-0-clockwise`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-052-e288ae-0-clockwise,boundedoperator:dom-052-636f6e746f7572496e74656772617465-0-clockwise,boundedoperator:dom-052-e288ae-0-counterclockwise,boundedoperator:dom-052-636f6e746f7572496e74656772617465-0-counterclockwise,boundedoperator:dom-052-e288ae-1-clockwise,boundedoperator:dom-052-636f6e746f7572496e74656772617465-1-clockwise,boundedoperator:dom-052-e288ae-1-counterclockwise,boundedoperator:dom-052-636f6e746f7572496e74656772617465-1-counterclockwise`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-052-negative`. API fingerprint: `1823f2661cf537533daa700a1bae6fb340fb195cd82c4f53fdf90bce0b59a134`.

<a id="symbol-c7b9b9dd2560b3603daa32c0"></a>

## std.calculus / ∂

Identity: `NSR-DOM-053`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∂(x,unitless,symbolic,analytic,4,1,64,[3,2]).value();}
```

Explicit process status: `24`; the full value/effect oracle is `boundedoperator:dom-053-e28882-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-053-e28882-0,boundedoperator:dom-053-7061727469616c-0,boundedoperator:dom-053-e28882-1,boundedoperator:dom-053-7061727469616c-1,boundedoperator:dom-053-e28882-2,boundedoperator:dom-053-7061727469616c-2`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-053-negative`. API fingerprint: `4e9e95c8d461c213251a4f301375f83bdb8e336c7f4bf3d27d9b03bf7a0326d5`.

<a id="symbol-eeca64d3406b6fae70ff9824"></a>

## std.calculus / ∇

Identity: `NSR-DOM-054`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∇(2,unitless,numeric,central,1,64,[0,0,0,0]).component(0);}
```

Explicit process status: `0`; the full value/effect oracle is `boundedoperator:dom-054-e28887-2-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-054-e28887-0-0,boundedoperator:dom-054-e28887-0-1,boundedoperator:dom-054-6772616469656e74-0-0,boundedoperator:dom-054-6772616469656e74-0-1,boundedoperator:dom-054-e28887-1-0,boundedoperator:dom-054-e28887-1-1,boundedoperator:dom-054-6772616469656e74-1-0,boundedoperator:dom-054-6772616469656e74-1-1,boundedoperator:dom-054-e28887-2-0,boundedoperator:dom-054-e28887-2-1,boundedoperator:dom-054-6772616469656e74-2-0,boundedoperator:dom-054-6772616469656e74-2-1`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-054-negative`. API fingerprint: `3777c394c2efe633a54096910b7ae305a9c3ce5114c131300339a65746a0c9ed`.

<a id="symbol-5d55df0a3fb03d6f2746948a"></a>

## std.calculus / ∇·

Identity: `NSR-DOM-055`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∇·(3,unitless,numeric,central,64,[2,3,4]).value();}
```

Explicit process status: `9`; the full value/effect oracle is `boundedoperator:dom-055-e28887c2b7-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-055-e28887c2b7-0,boundedoperator:dom-055-646976657267656e6365-0,boundedoperator:dom-055-e28887c2b7-1,boundedoperator:dom-055-646976657267656e6365-1,boundedoperator:dom-055-e28887c2b7-2,boundedoperator:dom-055-646976657267656e6365-2`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-055-negative`. API fingerprint: `2bbe14740a265d4a74544d5c3612438ecca0522215fd4b4b4af5089ee25952c5`.

<a id="symbol-97f080720c6783e1b4891216"></a>

## std.calculus / ∇×

Identity: `NSR-DOM-056`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∇×(3,unitless,numeric,central,64,[0,1,2,3,4,5,6,7,8]).component(0);}
```

Explicit process status: `2`; the full value/effect oracle is `boundedoperator:dom-056-e28887c397-0-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-056-e28887c397-0-0,boundedoperator:dom-056-e28887c397-0-1,boundedoperator:dom-056-e28887c397-0-2,boundedoperator:dom-056-6375726c-0-0,boundedoperator:dom-056-6375726c-0-1,boundedoperator:dom-056-6375726c-0-2,boundedoperator:dom-056-e28887c397-1-0,boundedoperator:dom-056-e28887c397-1-1,boundedoperator:dom-056-e28887c397-1-2,boundedoperator:dom-056-6375726c-1-0,boundedoperator:dom-056-6375726c-1-1,boundedoperator:dom-056-6375726c-1-2`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-056-negative`. API fingerprint: `d9c111c79641d264a5091ca2dac32cb4174dc5eb748321ce5c2b578f4dbd8eba`.

<a id="symbol-d21fbdab3cf3f5809df60a50"></a>

## std.calculus / Δ

Identity: `NSR-DOM-057`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Δ(2,unitless,numeric,central,dirichlet,4,5,64,[7,9,3,5]).value();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-057-ce94-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit finite samples, dimensions, method, tolerance/step and budget in the current checked calculus grammar; exact rational/integer observations; no implicit integration domain, numeric truncation, live solver or hidden callback. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-057-ce94-0,boundedoperator:dom-057-6c61706c616369616e-0,boundedoperator:dom-057-ce94-1,boundedoperator:dom-057-6c61706c616369616e-1,boundedoperator:dom-057-ce94-2,boundedoperator:dom-057-6c61706c616369616e-2`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-057-negative,boundedoperator:dom-057-nonexact-ce94,boundedoperator:dom-057-nonexact-6c61706c616369616e`. API fingerprint: `488823076d8122fbadf82358fba10bb69af3e93e136e6143df86f2909e92a751`.

<a id="symbol-d74f74d56519b79b0de509a1"></a>

## std.probability / ∼

Identity: `NSR-DOM-058`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){distributedAs(17,3,discrete,1,bernoulli,discrete,1,0,1,64).shape();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-058-discrete-1-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit bounded model/distribution metadata and finite rational conditional probabilities; independence is an explicit model assumption, not an inferred statistical proof. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-058-discrete-1-False,boundedoperator:dom-058-discrete-1-True,boundedoperator:dom-058-discrete-2-False,boundedoperator:dom-058-discrete-2-True,boundedoperator:dom-058-discrete-3-False,boundedoperator:dom-058-discrete-3-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-058-negative`. API fingerprint: `942a5c43f582860f5a6f5cf5a585f06da30bce4003f82081adb2c9e2237ce5bd`.

<a id="symbol-eca42fdaaf7918e2f3b3063d"></a>

## std.probability / ⫫

Identity: `NSR-DOM-059`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){independentOf(23,5,8,dependent,67).value();}
```

Explicit process status: `0`; the full value/effect oracle is `boundedoperator:dom-059-dependent-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit bounded model/distribution metadata and finite rational conditional probabilities; independence is an explicit model assumption, not an inferred statistical proof. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-059-independent-False,boundedoperator:dom-059-independent-True,boundedoperator:dom-059-dependent-False,boundedoperator:dom-059-dependent-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-059-negative`. API fingerprint: `c8ae55a5c4505fb8dd6a29d8c4f551a4a7d2af165849f28d3a10c80c2774e265`.

<a id="symbol-40d84bd329e607b9f5dee6fc"></a>

## std.probability / |

Identity: `NSR-DOM-060`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){P(17,1,4 | 1,2,83).numerator();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-060-P-0-numerator`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Explicit bounded model/distribution metadata and finite rational conditional probabilities; independence is an explicit model assumption, not an inferred statistical proof. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-060-conditionalOn-0-numerator,boundedoperator:dom-060-conditionalOn-0-denominator,boundedoperator:dom-060-P-0-numerator,boundedoperator:dom-060-P-0-denominator,boundedoperator:dom-060-conditionalOn-1-numerator,boundedoperator:dom-060-conditionalOn-1-denominator,boundedoperator:dom-060-P-1-numerator,boundedoperator:dom-060-P-1-denominator,boundedoperator:dom-060-conditionalOn-2-numerator,boundedoperator:dom-060-conditionalOn-2-denominator,boundedoperator:dom-060-P-2-numerator,boundedoperator:dom-060-P-2-denominator`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-060-negative`. API fingerprint: `46ace55bed5aeb3e0db56466eec49231644973262956efcd9bc1c3e0fdbe4832`.

<a id="symbol-1a958319ce7aace9f7b87188"></a>

## std.logic / ∀

Identity: `NSR-DOM-061`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∀(1,0,0,0,31).status();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-061-e28880-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-061-e28880-0,boundedoperator:dom-061-666f72416c6c-0,boundedoperator:dom-061-e28880-1,boundedoperator:dom-061-666f72416c6c-1,boundedoperator:dom-061-e28880-2,boundedoperator:dom-061-666f72416c6c-2,boundedoperator:dom-061-e28880-3,boundedoperator:dom-061-666f72416c6c-3,boundedoperator:dom-061-e28880-4,boundedoperator:dom-061-666f72416c6c-4`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-061-negative`. API fingerprint: `21ff95024c5d8e1c9c2b11fc53443c6e0078b43af695111c240472ca544424ac`.

<a id="symbol-744f23fdd2b2a03592ea03e4"></a>

## std.logic / ∃

Identity: `NSR-DOM-062`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∃(1,0,0,0,31).status();}
```

Explicit process status: `3`; the full value/effect oracle is `boundedoperator:dom-062-e28883-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-062-e28883-0,boundedoperator:dom-062-657869737473-0,boundedoperator:dom-062-e28883-1,boundedoperator:dom-062-657869737473-1,boundedoperator:dom-062-e28883-2,boundedoperator:dom-062-657869737473-2,boundedoperator:dom-062-e28883-3,boundedoperator:dom-062-657869737473-3,boundedoperator:dom-062-e28883-4,boundedoperator:dom-062-657869737473-4`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-062-negative`. API fingerprint: `f2be6f562b0b4e2f85d6cb79d1410e1926ecc8bcfd8d5d13fdc567f062b1eaef`.

<a id="symbol-e4cdd98bb926f37449f20739"></a>

## std.logic / ∄

Identity: `NSR-DOM-063`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∄(1,0,0,0,31).status();}
```

Explicit process status: `0`; the full value/effect oracle is `boundedoperator:dom-063-e28884-0`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-063-e28884-0,boundedoperator:dom-063-6e6f74457869737473-0,boundedoperator:dom-063-e28884-1,boundedoperator:dom-063-6e6f74457869737473-1,boundedoperator:dom-063-e28884-2,boundedoperator:dom-063-6e6f74457869737473-2,boundedoperator:dom-063-e28884-3,boundedoperator:dom-063-6e6f74457869737473-3,boundedoperator:dom-063-e28884-4,boundedoperator:dom-063-6e6f74457869737473-4`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-063-negative`. API fingerprint: `899f677a93bc9c97c13f924674375b054e58b180abba06c23c9ef23ccfe75449`.

<a id="symbol-513ac8c2e5fcf19b40ef5fc0"></a>

## std.logic / ⇒

Identity: `NSR-DOM-064`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){implies(0,0,7).value();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-064-0-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-064-0-0-False,boundedoperator:dom-064-0-0-True,boundedoperator:dom-064-0-1-False,boundedoperator:dom-064-0-1-True,boundedoperator:dom-064-1-0-False,boundedoperator:dom-064-1-0-True,boundedoperator:dom-064-1-1-False,boundedoperator:dom-064-1-1-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-064-negative`. API fingerprint: `91c185d5d46ba560e403bd6aa5696dee2e9bd16685b427fa3deb46b30667c997`.

<a id="symbol-398fc8ee48fa57cc62d2d5a4"></a>

## std.logic / ⇔

Identity: `NSR-DOM-065`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){iff(0,0,7).value();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-065-0-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-065-0-0-False,boundedoperator:dom-065-0-0-True,boundedoperator:dom-065-0-1-False,boundedoperator:dom-065-0-1-True,boundedoperator:dom-065-1-0-False,boundedoperator:dom-065-1-0-True,boundedoperator:dom-065-1-1-False,boundedoperator:dom-065-1-1-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-065-negative`. API fingerprint: `a4d0dfc0df2fca154c22f30b307d0e8a1a53b17883dbc991337c17e448574cad`.

<a id="symbol-74baca4be1fe91110384bf89"></a>

## std.proof / ⊢

Identity: `NSR-DOM-066`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){proves(11,13,proved,17,31).status();}
```

Explicit process status: `0`; the full value/effect oracle is `boundedoperator:dom-066-proved-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-066-proved-False,boundedoperator:dom-066-proved-True,boundedoperator:dom-066-disproved-False,boundedoperator:dom-066-disproved-True,boundedoperator:dom-066-unknown-False,boundedoperator:dom-066-unknown-True,boundedoperator:dom-066-timeout-False,boundedoperator:dom-066-timeout-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-066-negative`. API fingerprint: `28cb3a6642651b31aec552dd93c20d97fdbc75afada567bebea3f4683819b33b`.

<a id="symbol-21f1ee496b3477dacd9105da"></a>

## std.model / ⊨

Identity: `NSR-DOM-067`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){satisfies(11,13,sat,0,31).status();}
```

Explicit process status: `2`; the full value/effect oracle is `boundedoperator:dom-067-sat-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-067-sat-False,boundedoperator:dom-067-sat-True,boundedoperator:dom-067-unsat-False,boundedoperator:dom-067-unsat-True,boundedoperator:dom-067-unknown-False,boundedoperator:dom-067-unknown-True,boundedoperator:dom-067-timeout-False,boundedoperator:dom-067-timeout-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-067-negative`. API fingerprint: `26758dce3b9f0e69948d1b20563560537e65cfd1a1a1c267941f86d41a383d18`.

<a id="symbol-26a32734429a1c429090b803"></a>

## std.logic / ⊼

Identity: `NSR-DOM-068`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){nand(0,0,7).value();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-068-0-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-068-0-0-False,boundedoperator:dom-068-0-0-True,boundedoperator:dom-068-0-1-False,boundedoperator:dom-068-0-1-True,boundedoperator:dom-068-1-0-False,boundedoperator:dom-068-1-0-True,boundedoperator:dom-068-1-1-False,boundedoperator:dom-068-1-1-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-068-negative`. API fingerprint: `cf2262f218b9ff0c6f71ba1c26b8c6a0dabf2e48076e95936087aaef73519410`.

<a id="symbol-76910ae88dfb8eaa99d0b9c7"></a>

## std.logic / ⊽

Identity: `NSR-DOM-069`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){nor(0,0,7).value();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-069-0-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Finite domain partition/result grammar, explicit proof/model status and budget; exhaustive Boolean truth tables; unknown and timeout remain distinct outcomes; this does not claim an automatic theorem prover. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-069-0-0-False,boundedoperator:dom-069-0-0-True,boundedoperator:dom-069-0-1-False,boundedoperator:dom-069-0-1-True,boundedoperator:dom-069-1-0-False,boundedoperator:dom-069-1-0-True,boundedoperator:dom-069-1-1-False,boundedoperator:dom-069-1-1-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-069-negative`. API fingerprint: `12e9d0fae09487d65ac3940e0c6b9ab502678ee598d7039f608fe94ce8e2db8f`.

<a id="symbol-2eacff2619db46e7c6930ef3"></a>

## std.graph / →

Identity: `NSR-DOM-070`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){directedEdgeTo(17,29,3,5,7,7,11,0).replay();}
```

Explicit process status: `29`; the full value/effect oracle is `boundedoperator:dom-070-endpoint-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Bounded typed edge/transition descriptor and deterministic replay projection; explicit endpoints, schema, guard, effect and capability; no asynchronous external execution. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-070-endpoint-0-False,boundedoperator:dom-070-endpoint-0-True,boundedoperator:dom-070-endpoint-1-False,boundedoperator:dom-070-endpoint-1-True,boundedoperator:dom-070-endpoint-2-False,boundedoperator:dom-070-endpoint-2-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-070-negative`. API fingerprint: `dd04d74c6a31412df695b90a2a9a9086bf50cb827243997061870ca0770c8fc3`.

<a id="symbol-b039a78654435a15221af218"></a>

## std.graph / ↔

Identity: `NSR-DOM-071`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){bidirectionalEdge(17,29,3,5,7,7,11,0).replay();}
```

Explicit process status: `29`; the full value/effect oracle is `boundedoperator:dom-071-endpoint-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Bounded typed edge/transition descriptor and deterministic replay projection; explicit endpoints, schema, guard, effect and capability; no asynchronous external execution. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-071-endpoint-0-False,boundedoperator:dom-071-endpoint-0-True,boundedoperator:dom-071-endpoint-1-False,boundedoperator:dom-071-endpoint-1-True,boundedoperator:dom-071-endpoint-2-False,boundedoperator:dom-071-endpoint-2-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-071-negative`. API fingerprint: `98a789d4cc6b0457a2a806c678a25287ff602b98f96c6c7c43f2529d56ff6aa5`.

<a id="symbol-90620273a192a184a35b1231"></a>

## std.workflow / ⇢

Identity: `NSR-DOM-072`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){asyncEdgeTo(17,29,3,5,7,7,11,1,3).replay();}
```

Explicit process status: `29`; the full value/effect oracle is `boundedoperator:dom-072-endpoint-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Bounded typed edge/transition descriptor and deterministic replay projection; explicit endpoints, schema, guard, effect and capability; no asynchronous external execution. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-072-endpoint-0-False,boundedoperator:dom-072-endpoint-0-True,boundedoperator:dom-072-endpoint-1-False,boundedoperator:dom-072-endpoint-1-True,boundedoperator:dom-072-endpoint-2-False,boundedoperator:dom-072-endpoint-2-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-072-negative`. API fingerprint: `45314022c5e05b177fe2d730f6037ed36c8d968a1b84e8fd546c8479d241c73f`.

<a id="symbol-04f5a273d5a3f91ce0debbb0"></a>

## std.statechart / ⟶

Identity: `NSR-DOM-073`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){transitionTo(17,29,3,5,7,7,1,1,3).replay();}
```

Explicit process status: `29`; the full value/effect oracle is `boundedoperator:dom-073-endpoint-0-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Bounded typed edge/transition descriptor and deterministic replay projection; explicit endpoints, schema, guard, effect and capability; no asynchronous external execution. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-073-endpoint-0-False,boundedoperator:dom-073-endpoint-0-True,boundedoperator:dom-073-endpoint-1-False,boundedoperator:dom-073-endpoint-1-True,boundedoperator:dom-073-endpoint-2-False,boundedoperator:dom-073-endpoint-2-True,boundedoperator:dom-073-guard-denied`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-073-negative`. API fingerprint: `8d49af8efe1d4157ab2942d6b3789a7a9d99fe60ea184b554df98d7265b60617`.

<a id="symbol-536a10e214f5f9b85ef37889"></a>

## std.text / ⧺

Identity: `NSR-DOM-074`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){concat("é","🚀").byteLength();}
```

Explicit process status: `7`; the full value/effect oracle is `boundedoperator:dom-074-bytes-2-False`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current Text/Pattern finite profile: UTF-8 concat and literal/dot Pattern matching with explicit mode/work budget; matched/negated values change with content, without a fixed operator report. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-074-bytes-0-False,boundedoperator:dom-074-bytes-0-True,boundedoperator:dom-074-bytes-1-False,boundedoperator:dom-074-bytes-1-True,boundedoperator:dom-074-bytes-2-False,boundedoperator:dom-074-bytes-2-True`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-074-negative`. API fingerprint: `26c2e83b2db4834d50de5b02fb79d72824b2d6edb2cc093e47b8de6545f2a46c`.

<a id="symbol-7862e0aa9608310130b53570"></a>

## std.regex / =~

Identity: `NSR-DOM-075`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){matches("ab" =~ Pattern("abc",1,200)).matched();}
```

Explicit process status: `0`; the full value/effect oracle is `boundedoperator:dom-075-match-3`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current Text/Pattern finite profile: UTF-8 concat and literal/dot Pattern matching with explicit mode/work budget; matched/negated values change with content, without a fixed operator report. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-075-match-0,boundedoperator:dom-075-match-1,boundedoperator:dom-075-match-2,boundedoperator:dom-075-match-3`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-075-negative`. API fingerprint: `a319352ccc5820e583fcd6c94b938be039d8569cc1214c12b6d9bbd43d7fed1f`.

<a id="symbol-a638cf9be5bf0b2f301f6c0b"></a>

## std.regex / !~

Identity: `NSR-DOM-076`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){matches("ab" !~ Pattern("abc",1,200)).matched();}
```

Explicit process status: `1`; the full value/effect oracle is `boundedoperator:dom-076-match-3`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current Text/Pattern finite profile: UTF-8 concat and literal/dot Pattern matching with explicit mode/work budget; matched/negated values change with content, without a fixed operator report. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-076-match-0,boundedoperator:dom-076-match-1,boundedoperator:dom-076-match-2,boundedoperator:dom-076-match-3`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-076-negative`. API fingerprint: `2e9a084752ae32e6a9ec5cf1f45c038565f9dd0b0583f87d78a1fa0250f0ef46`.

<a id="symbol-c973eecc55bc2512680f3162"></a>

## std.text.format / ${...}

Identity: `NSR-DOM-077`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Ω  ${17
 +29} end  ".console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `interpolation:tooling-format-source`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:tooling-format-source,composite:typed-struct-source`.

Misuse/diagnostic/failure proofs: `interpolation:tooling-format-invalid,composite:typed-struct-interpolation-effects`. API fingerprint: `ec76625ee20a106637c16043b477750575ebe13c1f509b494549d07f045910a0`.

<a id="symbol-6ac2d489f76ce86c2f2ee354"></a>

## std.text.format / %s/%d/%f/%b/%%

Identity: `NSR-DOM-078`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"100%%".format().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:literal-percent`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:named-29,format:named-83,format:literal-percent,format:fixed-17.5`.

Misuse/diagnostic/failure proofs: `format:wrong-type,format:unknown`. API fingerprint: `99f56d2c1e3e045026ee31a9e773a9b91467827523623f7ccd91356d2106eb60`.

<a id="symbol-bd5accdc2e3b1927beedc8a0"></a>

## std.render.slash / /name{...}

Identity: `NSR-DOM-079`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){RenderPlan("/bold{Nebo17}").render(.plain()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `slash:directive-bold`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Actual typed RenderPlan(Text).render(contextual target) consumes all source statements. The existing native Slash parser and two-pass renderer own 64 finite directives, 4096 input bytes, 256 aggregate tokens, depth 16 and 32 argument bytes. Target output is an owned Text projection; its separate Console publication is observed. Unknown directives and overflowing plans fail closed. Independent G064 grammar/render oracle, actual contents, two live plans and explicit returns replace the old template metadata/seed profile. Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `slash:directive-bold,slash:directive-title,slash:directive-table,slash:directive-task,slash:target-plain-23,slash:target-ansi-7,slash:target-markdown-29,slash:target-html-255,slash:target-console-0,slash:two-live,slash:two-live-other,slash:depth-16,slash:tokens-boundary`.

Misuse/diagnostic/failure proofs: `slash:constructor-type,slash:target-unknown,slash:status-type,slash:domain-unknown,slash:domain-depth,slash:domain-tokens`. API fingerprint: `b497fdbe70a8e84b7ba16f654258d92381a4567650c15032018b6147be8b6fb2`.

<a id="symbol-d18248265a853fd2be4df7d2"></a>

## std.meta / @

Identity: `NSR-DOM-080`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){@stable(version:1,level:7,enabled:true) function item(seed:17,privacy:0).levelValue();}
```

Explicit process status: `7`; the full value/effect oracle is `boundedoperator:dom-080-metadata-0-levelValue`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Current annotation registry with typed version/level/enabled arguments, targets and bounded seed/privacy; actual argument values and masks are observed; no claim of arbitrary annotation evaluation. Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `boundedoperator:dom-080-metadata-0-levelValue,boundedoperator:dom-080-metadata-0-enabledValue,boundedoperator:dom-080-metadata-0-argumentCount,boundedoperator:dom-080-metadata-0-argumentMask,boundedoperator:dom-080-metadata-1-levelValue,boundedoperator:dom-080-metadata-1-enabledValue,boundedoperator:dom-080-metadata-1-argumentCount,boundedoperator:dom-080-metadata-1-argumentMask,boundedoperator:dom-080-metadata-2-levelValue,boundedoperator:dom-080-metadata-2-enabledValue,boundedoperator:dom-080-metadata-2-argumentCount,boundedoperator:dom-080-metadata-2-argumentMask`.

Misuse/diagnostic/failure proofs: `boundedoperator:dom-080-negative`. API fingerprint: `284c350d84a9ed48708307a217a7ad0dafcb07848e9bde5e9a1a6891bca9bbe0`.

<a id="symbol-e80b9356f92e4378c18ecd2b"></a>

## core.numeric / −

Identity: `NSR-UA-001`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left − right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-e28892-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-e28892-0,operator:binary-e28892-1,operator:binary-e28892-2,operator:binary-e28892-3,operator:binary-e28892-4,operator:binary-e28892-5,operator:binary-e28892-6,operator:binary-e28892-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-e28892`. API fingerprint: `cffed69a4372954427fc9c40a21cfc85cb83f1946d8664b212fa2280d89cbe0c`.

<a id="symbol-5089e2a54adf09edce38f95a"></a>

## core.numeric / ÷

Identity: `NSR-UA-002`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left ÷ right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-c3b7-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-c3b7-0,operator:binary-c3b7-1,operator:binary-c3b7-2,operator:binary-c3b7-3,operator:binary-c3b7-4,operator:binary-c3b7-5,operator:binary-c3b7-6,operator:binary-c3b7-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-c3b7`. API fingerprint: `0d6912f9d3d1e10fb78d689f30cfbdf8f4a198896ec1bb7fb6dae97e591bcf62`.

<a id="symbol-f9a0813cc78a9c23a2eea370"></a>

## core.compare / ≤

Identity: `NSR-UA-003`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left ≤ right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-e289a4-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-e289a4-0,operator:binary-e289a4-1,operator:binary-e289a4-2,operator:binary-e289a4-3,operator:binary-e289a4-4,operator:binary-e289a4-5,operator:binary-e289a4-6,operator:binary-e289a4-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-e289a4`. API fingerprint: `0142da52b0166aa345e4b09ccaa78e961c3b27915d030a0755f409c24f729b37`.

<a id="symbol-84bb1a16d53b495e3c9ebb2e"></a>

## core.compare / ≥

Identity: `NSR-UA-004`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left ≥ right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-e289a5-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-e289a5-0,operator:binary-e289a5-1,operator:binary-e289a5-2,operator:binary-e289a5-3,operator:binary-e289a5-4,operator:binary-e289a5-5,operator:binary-e289a5-6,operator:binary-e289a5-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-e289a5`. API fingerprint: `ec8a5d94371bd5b7371f86cad88a332cb214a09b2b87580bed3496ad8b9ef56a`.

<a id="symbol-5c1c2e9b0038cf22b6c426b7"></a>

## core.eq / ≠

Identity: `NSR-UA-005`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.left)calculate(Int.right){(left ≠ right).return;}start(){(0).calculate(3).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:binary-e289a0-6`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-e289a0-0,operator:binary-e289a0-1,operator:binary-e289a0-2,operator:binary-e289a0-3,operator:binary-e289a0-4,operator:binary-e289a0-5,operator:binary-e289a0-6,operator:binary-e289a0-7`.

Misuse/diagnostic/failure proofs: `operator:wrong-e289a0`. API fingerprint: `8393f6b0c84e67aeb321fd620dc8ccacb52b346479d5dd25d4fd5e87ac1afea8`.

<a id="symbol-d121ee02286a0c279b1e379e"></a>

## core.logic / ∧

Identity: `NSR-UA-006`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(false ∧ (1/0==0)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:short-unicode-and`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:logic-e288a7-false-false,operator:logic-e288a7-false-true,operator:logic-e288a7-true-false,operator:logic-e288a7-true-true,operator:short-unicode-and`.

Misuse/diagnostic/failure proofs: `operator:wrong-logic-int`. API fingerprint: `aaebc44c2e2eace534314509595bce38b68c57f431c319a9efefdf581df6facc`.

<a id="symbol-9e35367d55773c1c7e0069f6"></a>

## core.logic / ∨

Identity: `NSR-UA-007`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(true ∨ (1/0==0)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:short-unicode-or`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:logic-e288a8-false-false,operator:logic-e288a8-false-true,operator:logic-e288a8-true-false,operator:logic-e288a8-true-true,operator:short-unicode-or`.

Misuse/diagnostic/failure proofs: `operator:wrong-logic-int`. API fingerprint: `39ea9ab09191e087be4cabbd31927f76797d31e4b624d4b2219590c338e8cc71`.

<a id="symbol-a551aa33d87dbdba3b113a1b"></a>

## core.logic / ¬

Identity: `NSR-UA-008`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Bool.value)calculate(){(¬value).return;}start(){(true).calculate().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:unary-c2ac-1`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:unary-c2ac-0,operator:unary-c2ac-1`.

Misuse/diagnostic/failure proofs: `operator:wrong-unary-c2ac`. API fingerprint: `e35c434aaf1ac4d7e10631c07922dc9d6fa7bacadbafeae332a936d3db517557`.

<a id="symbol-7919ec492298850d846289fa"></a>

## core.logic_bits / ⊻

Identity: `NSR-UA-009`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `OPERATOR_REGISTRY_TYPED_SOURCE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(Bytes.empty() xor Bytes.empty()).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:bytes-xor-empty`.

### Types, ownership, effects, limits and errors

Exact Registry token and real Program statements; independent value/document/exit oracles, changed operands, associated negatives; Int64/Bool/binary64 or the referenced public typed profile; native checked arithmetic and complete source statement ownership. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:binary-e28abb-0,operator:binary-e28abb-1,operator:binary-e28abb-2,operator:binary-e28abb-3,operator:binary-e28abb-4,operator:binary-e28abb-5,operator:binary-e28abb-6,operator:binary-e28abb-7,operator:logic-e28abb-false-false,operator:logic-e28abb-false-true,operator:logic-e28abb-true-false,operator:logic-e28abb-true-true,operator:bytes-xor-0-786f72-False,operator:bytes-xor-0-786f72-True,operator:bytes-xor-0-e28abb-False,operator:bytes-xor-0-e28abb-True,operator:bytes-xor-1-786f72-False,operator:bytes-xor-1-786f72-True,operator:bytes-xor-1-e28abb-False,operator:bytes-xor-1-e28abb-True,operator:bytes-xor-2-786f72-False,operator:bytes-xor-2-786f72-True,operator:bytes-xor-2-e28abb-False,operator:bytes-xor-2-e28abb-True,operator:bytes-xor-empty,operator:bytes-xor-nested,operator:bytes-xor-two-live,operator:bytes-xor-length-domain,operator:bytes-xor-effects`.

Misuse/diagnostic/failure proofs: `operator:wrong-e28abb,operator:bytes-xor-mixed`. API fingerprint: `a71ed97ef4ec6e0ebe5bb01f10a3fe737e578e757e6fa812047ab09732344fab`.

<a id="symbol-9093ff76f8e4ab8fbe5697cb"></a>

## builtin / Dataset

Identity: `claim:013c10a6e1b8f372919c46ce`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29,71]))]).t;Dataset.fromTables([t]).d;d.repartition(1);7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `data:repartition-explicit-return`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Dataset.repartition,g010-public:Dataset.cache,g010-public:Dataset.fromTables,g010-public:Dataset.schema,g010-public:Dataset.partitionCount,g010-public:Dataset.scan,g010-public:Dataset.scan.sink,g010-public:Dataset.collect,g010-public:Dataset.collect.at,g010-public:Dataset.collect.next; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:cache-collect,data:cache-disable,data:cache-scan,data:cache-two-live,data:collect-block-cleanup,data:collect-break-continue,data:collect-count-0,data:collect-count-1,data:collect-count-255,data:collect-count-256,data:collect-count-3,data:collect-early-return,data:collect-empty-next,data:collect-missing-3,data:collect-option-consumed,data:collect-order,data:collect-result-independent,data:collect-return-0,data:collect-return-23,data:collect-return-7,data:collect-row-independent,data:collect-two-live,data:dataset-return-23,data:dataset-scan-changed,data:dataset-scan-eight,data:dataset-scan-empty,data:dataset-scan-lazy,data:dataset-scan-many,data:dataset-scan-missing,data:dataset-scan-next,data:dataset-scan-row-limit,data:dataset-scan-sink-consumed,data:dataset-scan-sink-remainder,data:dataset-scan-two-live,data:dataset-schema,data:dataset-schema-independent,data:repartition-0-1,data:repartition-17-7,data:repartition-256-32,data:repartition-3-1,data:repartition-explicit-return,data:repartition-missing-3,data:repartition-result-schema,data:repartition-two-live`.

Misuse/diagnostic/failure proofs: `data:cache-arity,data:cache-blocks-repartition,data:cache-invalid-2,data:cache-policy-type,data:collect-arity,data:collect-at-type,data:collect-bound,data:collect-copy,data:collect-count-257,data:collect-negative-index,data:collect-next-arity,data:collect-option-copy,data:collect-option-twice,data:collect-row-not-int,data:dataset-copy,data:dataset-empty-parts,data:dataset-over-parts,data:dataset-part-type,data:dataset-partition-count-arity,data:dataset-scan-callback-trap,data:dataset-scan-copy,data:dataset-scan-option-copy,data:dataset-scan-option-twice,data:dataset-schema-arity,data:dataset-schema-mismatch,data:dataset-schema-order-mismatch,data:dataset-sink-extra-argument,data:dataset-sink-receiver-type,data:dataset-sink-return-type,data:repartition-65-7,data:repartition-arity,data:repartition-invalid-0,data:repartition-invalid-33,data:repartition-size-type`. API fingerprint: `90d0fb7fc8d489b69669c819369ac9c74d9490580061d591c269d56ded21b8df`.

<a id="symbol-c45aa1c38cd649a1f87b2882"></a>

## builtin / isMissing

Identity: `claim:017a7d6c072f8f24e093bae7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).a;Column<Int>.from([a.get(0),a.get(1),a.get(2)]).c;c.countMissing().console();c.sum().console();c.isMissing(0).console();c.isMissing(1).console();c.isMissing(2).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-missing-0`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.isMissing; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-missing-0,data:column-missing-1,data:column-missing-2,data:column-missing-3,data:column-missing-5,data:column-missing-7`.

Misuse/diagnostic/failure proofs: `data:column-isMissing-type`. API fingerprint: `09b0455c5377f033c424a2a80273e3fc5f5f40567e1182660eae695db105b073`.

<a id="symbol-7dec088466e9899389462b76"></a>

## builtin / lowestCommonAncestor

Identity: `claim:018cb7c46b265bfb7263038d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(r,71).b;t.lowestCommonAncestor(a,b).expect("ancestor").value().return;}
```

Explicit process status: `17`; the full value/effect oracle is `relation:tree-lca`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.lowestCommonAncestor; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-lca`.

Misuse/diagnostic/failure proofs: `relation:tree-lca-node-type`. API fingerprint: `aa77eb8f1219323832a00d36ca6055af023da0083141f4c62e55796dc03e0892`.

<a id="symbol-d3e480310f6af2bde5cd29ab"></a>

## builtin / value

Identity: `claim:022f5382cff4eecac77e454b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Flow<Int> 17.x;x.value().return;}
```

Explicit process status: `17`; the full value/effect oracle is `data:Flow-payload-17`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Event<Int>.value,g010-public:Flow<Int>.value; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:Event-payload-13,data:Event-payload-29,data:Event-payload-71,data:Flow-payload-17,data:Flow-payload-29,data:Flow-payload-71`.

Misuse/diagnostic/failure proofs: `data:event-type,data:flow-type`. API fingerprint: `d87c7e178eaf294a67a0dbcd7187077b23d4f5e03e0d8b0e090f9c5c1247d8c5`.

<a id="symbol-91802dd1931a682698d968f8"></a>

## builtin.Vector / variance

Identity: `claim:031af9436a9f7d0dffe33482`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_STATISTICS_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Float> [1.0,(∞-∞)].a;a.variance(population).isNaN().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `statistics:nan-propagation-variance`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 lanes, extent 1..64; checked Int sum, native compensated Float sum, explicit population/sample and linearType7 policies; 1536-byte caller workspace isolates conversion/quantile scratch; source values unchanged; native domain/overflow traps and NaN propagation are observed; no source report or synthetic aggregate Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `statistics:Float-0-variance-population,statistics:Float-0-variance-sample,statistics:Int-1-variance-population,statistics:nan-propagation-variance`.

Misuse/diagnostic/failure proofs: `statistics:policy-variance-17,statistics:shadow-variance,statistics:sample-too-small-variance`. API fingerprint: `f5733f5cf3dd76d7743ea5e78aafd2a296b91eee639dbded1484ee7dc7e03448`.

<a id="symbol-e71c6cf00425f9570c038bdb"></a>

## builtin / Text.parseFloat

Identity: `claim:04d69c4db900cf6feb9e0b5d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"1.2.3".parseFloat().isErr().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `textparse:parse-float-error-2`.

### Types, ownership, effects, limits and errors

parseFloat(); dot decimal f64; Result by value; actual Result value/tag/fallback; native checked output is committed only on Ok; live keyword table; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Explicit parse result; preserve checked numeric domain and Unicode policy

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/numeric_safety_semantic.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_parse.asm`.

Positive/changed-value/boundary proofs: `textparse:parse-float-1,textparse:parse-float-2,textparse:parse-float-error-2,textparse:two-live-float,textparse:float-error-value`.

Misuse/diagnostic/failure proofs: `textparse:arity-parseFloat,textparse:fallback-type-Float`. API fingerprint: `6aba710f42fbcc5135436e675bc0ef8bb3d1897142ced692f6ad7eaae615a0bd`.

<a id="symbol-bbc3cfe8aa8c7ad445cd5efb"></a>

## std.math / hypot

Identity: `claim:090e1636b97a7c2ba7e9dcf7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.hypot(8.0,15.0);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `math:atomic-hypot`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:atomic-hypot`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `cee2576df90c58cfaa475006b2eeebcd043b286a5052572b3744bc5570c4ad36`.

<a id="symbol-3f5bfa772da4c213736beb75"></a>

## builtin / directed

Identity: `claim:0a3ee9f14f4e4ad3d004070d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().a;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph<Int,Int>.directed; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-neighbors,relation:graph-directed-reverse,relation:graph-return-23`.

Misuse/diagnostic/failure proofs: `relation:graph-edge-type,relation:graph-missing-edge-type`. API fingerprint: `d3cd9f131ef052d0aba5de12198b968c0689e56a22787d30f0faa588ffc86e7c`.

<a id="symbol-b51b314758ce64d348eabde3"></a>

## std.core / IntoIterator

Identity: `claim:0a82eaa27482b82b3576e3a9`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)work(){Range.exclusive(0,5).indices;0.total.mutable;for(index in indices){total+=index;}total.return;}start(){7.work().return;}
```

Explicit process status: `10`; the full value/effect oracle is `control:for`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `control:for,control:for-continue,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:range-zero-step`. API fingerprint: `3d5ddb82e08e43cc761cfbd8b0a76b0ef338e3ff187d25c8dcefa882584c720d`.

<a id="symbol-80d71d1f7e15dc296e909116"></a>

## builtin.ScanPlan / noHistory

Identity: `claim:0d203488bdb684e6de9be6f5`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.noHistory(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-8-0-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:noHistory; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-8-0-4,scan:security-8-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-noHistory`. API fingerprint: `24c543cd561dd72cb350a688b4dc3bd587724ae2544897571dc09fc88b60933f`.

<a id="symbol-692ce5ea4a141755ba9984e2"></a>

## std.core / Ordering

Identity: `claim:0dc6e916abc643aea6522c6a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17<=>17).order;(order==(0<=>0)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:ordering-17-17-0-0`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:ordering-17-29--1-0,operator:ordering-17-17-0-0,operator:ordering-29-17-1-0,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `operator:ordering-type`. API fingerprint: `00b9fd00331adf5c2161d14da9e890ed51b81bb3cf0e8694b15b10fe4b149eed`.

<a id="symbol-7de22db8c093d64d52f18b80"></a>

## builtin / fromColumns

Identity: `claim:0ef92073ff665261e0cc7410`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17]))]).t;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.fromColumns; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-constructor-snapshot,data:table-dimensions-empty,data:table-dimensions-limit,data:table-dimensions-many,data:table-dimensions-one,data:table-eight-columns,data:table-return-23`.

Misuse/diagnostic/failure proofs: `data:table-copy,data:table-duplicate-name,data:table-empty-columns,data:table-unequal-lengths,data:table-wrong-column,data:table-wrong-name`. API fingerprint: `3b0334bfb6f273dc734bc7f1957320551f84568f24dd80d1d09864a7e19bbedc`.

<a id="symbol-b2e983448303729487eceb83"></a>

## builtin / TextSplit.join

Identity: `claim:0f2d69ab0b4bafa8eab96ede`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"a,,b,".split(",").join("|").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:split-join-1`.

### Types, ownership, effects, limits and errors

join(Text); 4096 output bytes; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:split-join-1,text:bound-split`.

Misuse/diagnostic/failure proofs: `text:type-join`. API fingerprint: `c39ca07d45611d6c9a0a728bde2649d13e13c3c6bb32e7b3fdc965cb3305eeaf`.

<a id="symbol-ef1a69964d1453a3bd29b70d"></a>

## builtin / Column

Identity: `claim:0ff59811257d56187017f0c2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([17,29]).c;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column<Int>.from,g010-public:Column.sum,g010-public:Column.min,g010-public:Column.max,g010-public:Column.countMissing,g010-public:Column.get,g010-public:Column.cast<U>,g010-public:Column.isMissing,g010-public:Column.fillMissing,g010-public:Column.dropMissing,g010-public:Column.coalesce; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-bounds,data:column-cast-identity,data:column-cast-missing,data:column-cast-negative,data:column-cast-roundtrip,data:column-coalesce,data:column-coalesce-both-missing,data:column-coalesce-length,data:column-drop-0,data:column-drop-1,data:column-drop-2,data:column-drop-3,data:column-drop-5,data:column-drop-7,data:column-fill-0,data:column-fill-1,data:column-fill-2,data:column-fill-3,data:column-fill-5,data:column-fill-7,data:column-get,data:column-list,data:column-list-snapshot,data:column-max-changed,data:column-max-empty,data:column-max-many,data:column-min-changed,data:column-min-empty,data:column-min-many,data:column-missing-0,data:column-missing-1,data:column-missing-2,data:column-missing-3,data:column-missing-5,data:column-missing-7,data:column-missing-extremes,data:column-missing-get,data:column-negative-index,data:column-no-missing,data:column-return-23,data:column-sum-changed,data:column-sum-empty,data:column-sum-limit,data:column-sum-many,data:column-sum-overflow,data:column-two-live,data:column-uint-get-tag`.

Misuse/diagnostic/failure proofs: `data:column-cast-type,data:column-coalesce-type,data:column-copy,data:column-countMissing-arity,data:column-dropMissing-arity,data:column-dtype-coalesce,data:column-element-type,data:column-fillMissing-type,data:column-from-type,data:column-get-type,data:column-isMissing-type,data:column-max-arity,data:column-min-arity,data:column-mixed-type,data:column-sum-arity,data:column-uint-process-status,data:column-value-type`. API fingerprint: `23bfc9c1956e2bb940d2bc5e8fa6e272106cef17dd32ed74e31c375c90cb2b7c`.

<a id="symbol-db7a2eafd66aa29f3b94c384"></a>

## builtin / validate

Identity: `claim:10324390ff13489f56c7db8c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([]))]).t;Schema.new([Tuple.of("age",Int,false)]).rules;t.validate(rules).console();t.invalidRows().bad;bad.rowCount().return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:quality-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.validate; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:quality-empty,data:quality-limit-32,data:quality-mask-0-0-0,data:quality-mask-3-5-0,data:quality-mask-3-5-3,data:quality-repeated,data:quality-source-missing-preserved,data:quality-two-live`.

Misuse/diagnostic/failure proofs: `data:quality-constraints-arity,data:quality-constraints-type,data:quality-schema-count,data:quality-schema-name`. API fingerprint: `383a48c79ae376fa1b01e94283cd1cc3120272a9454de5a836b9bba5dfab9e6d`.

<a id="symbol-7d79513dade113fc721fcb96"></a>

## builtin / hasCycle

Identity: `claim:1069f6270bef9e4c51e0eb71`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.hasCycle().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-acyclic`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.hasCycle; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-acyclic,relation:graph-cycle`.

Misuse/diagnostic/failure proofs: `relation:graph-hasCycle-arity`. API fingerprint: `22cc303cbc9d21c2b3e12adc645a86f503ab6de3cfef00f1e6179b370b369220`.

<a id="symbol-adfccb317eb3b888ea1543ed"></a>

## builtin / cast

Identity: `claim:109821e33f71ae544ad7784f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([-17,71]).c;c.cast<UInt>();23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-cast-negative`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.cast<U>; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-cast-identity,data:column-cast-missing,data:column-cast-negative,data:column-cast-roundtrip,data:column-uint-get-tag`.

Misuse/diagnostic/failure proofs: `data:column-cast-type,data:column-dtype-coalesce,data:column-uint-process-status`. API fingerprint: `af4b1af42b8a4ed063d8c238cd34c31f2eae03001f35cc71e90b0770b3d862ba`.

<a id="symbol-bff899fca19e91371b6f6295"></a>

## builtin / Text.isUtf8

Identity: `claim:1137ab113bef489de4220313`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".isUtf8().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:isUtf8-empty`.

### Types, ownership, effects, limits and errors

isUtf8(); strict UTF-8; O(n); ; borrowed immutable receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:isUtf8-empty,text:isUtf8-4e65626f`.

Misuse/diagnostic/failure proofs: `text:arity-isUtf8`. API fingerprint: `a2cae34925ccaafe50ac074bf5d3ae2158338e73253242790aa767495fb0bc8e`.

<a id="symbol-54f1caf39c04981bc9fcc86f"></a>

## builtin / max

Identity: `claim:122fb8070a74dd176a93a70a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([]).c;c.max().console();23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-max-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.max; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-max-changed,data:column-max-empty,data:column-max-many,data:column-missing-extremes`.

Misuse/diagnostic/failure proofs: `data:column-max-arity`. API fingerprint: `4a37ccb76e61915d30af6001e3d0f3eb35d11e9c0dae2051bb8b06e103c99f2f`.

<a id="symbol-398ae5dd2dee03906de89216"></a>

## builtin / Set

Identity: `claim:123382b0fb71fb06b13e686a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.union(b).c;c.length().return;}

```

Explicit process status: `3`; the full value/effect oracle is `dict:set-union`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Set<Int>.new,g008-public:Set.insert,g008-public:Set.contains,g008-public:Set.remove,g008-public:Set.union,g008-public:Set.intersection,g008-public:Set.difference,g008-public:Set.isSubsetOf; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-difference,dict:set-difference-membership,dict:set-insert-remove,dict:set-intersection,dict:set-intersection-membership,dict:set-subset,dict:set-union,dict:set-union-membership`.

Misuse/diagnostic/failure proofs: `dict:set-contains-type,dict:set-difference-type,dict:set-insert-type,dict:set-intersection-type,dict:set-isSubsetOf-type,dict:set-new-arity,dict:set-remove-type,dict:set-union-type`. API fingerprint: `c0df66205dfd2966a81fb9beb087803d4a8522f1f51b2b6b1c6df4b5b2d74f53`.

<a id="symbol-e2670380ae1972ac71197a3c"></a>

## builtin / Text.trim

Identity: `claim:1454f6e17030ecf4e207a80a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".trim().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:trim-2`.

### Types, ownership, effects, limits and errors

trim(); input bytes; ; borrowed view; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:trim-0,text:trim-2`.

Misuse/diagnostic/failure proofs: `text:arity-trim`. API fingerprint: `324fa196b90c2b281cc072df320456c9134e0251268dcffd977cd1d36007cb1c`.

<a id="symbol-414d0e9c20a5dff758a9270a"></a>

## builtin / Text.sliceGraphemes

Identity: `claim:150a35dd2d599313968e7fce`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"éx".sliceGraphemes(0,1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:slice-graphemes-0`.

### Types, ownership, effects, limits and errors

Text.(Int, Int) -> Text; 1048576 input bytes; 15.1 Latin Extend/CRLF/ZWJ; immutable borrowed view; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:slice-graphemes-0,text:slice-graphemes-3`.

Misuse/diagnostic/failure proofs: `text:slice-count-type`. API fingerprint: `c3b0d07cbe3e14bdcd7d394cc083d68d69831cc7cc7a4b28a7588538afd4222d`.

<a id="symbol-73c44cdda8ea3d1509708eb6"></a>

## builtin / filter

Identity: `claim:15e63ce2f0951bd57d62916a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)step(){(self>20).return;}(Int.self)emit(){self.console();}start(){Stream<Int>.from([17]).s;s.filter(step).out;out.sink(emit).return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:stream-filter-one`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.filter,g010-public:Stream.filter; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:stream-filter-changed,data:stream-filter-many,data:stream-filter-one,data:stream-order-boundary-filter-keep-map-add,data:stream-order-boundary-map-add-filter-keep,data:table-callback-native-local,data:table-filter-changed-20,data:table-filter-empty-20,data:table-filter-limit-20,data:table-filter-many-20,data:table-filter-many-50`.

Misuse/diagnostic/failure proofs: `data:stream-filter-return-type,data:table-filter-arity,data:table-filter-receiver-type,data:table-filter-result-type,data:table-filter-trap`. API fingerprint: `029223601a41146d011e6bb5a4a8457ba9a89f99bb1298daae5f1a0d292f65e4`.

<a id="symbol-e52d30ba892be2606ecabe7c"></a>

## builtin / value

Identity: `claim:168c50f5ccc961a52c2ffdbe`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(71).a;a.value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:Node-payload-71`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Node.value; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:Node-payload-71,relation:Tree-payload-29,relation:graph-stale-node,relation:tree-stale-node`.

Misuse/diagnostic/failure proofs: `relation:node-value-arity`. API fingerprint: `a29dc98260a1687bf7376ef3ff1e2a901854c4cb56cd66a3c12cec43f7fbf484`.

<a id="symbol-a6f49bea034b1067b6d983ff"></a>

## std.core / Iterator

Identity: `claim:17b82eb83aa749dea6ea6d54`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)work(){Range.exclusive(0,5).indices;0.total.mutable;for(index in indices){total+=index;}total.return;}start(){7.work().return;}
```

Explicit process status: `10`; the full value/effect oracle is `control:for`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:G006-04,control:for,control:for-break,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:range-zero-step`. API fingerprint: `9cd95c97346d4c4c5712dd1e971e3d9fa326a5490b934d2c3d8e9390a07824c2`.

<a id="symbol-5058ff1218b3d4ffdddbf29a"></a>

## builtin.visual / close

Identity: `claim:185374eead963ba352652e1c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.close();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:close`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:console.close(); g018-declaration:window.close() Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:close`.

Misuse/diagnostic/failure proofs: `console:close-arity,console:close-publish,console:close-twice`. API fingerprint: `96e15814def6c6968027448b54ca22f9d8d8e0c8335bae2b85f9f8aff46556f9`.

<a id="symbol-30b75e131f9195a344ca772a"></a>

## builtin / collect

Identity: `claim:188aa5c147fc23ce87bf8589`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([]))]).t;Dataset.fromTables([t]).d;d.collect().rows;rows.next().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:collect-empty-next`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Dataset.collect,g010-public:Dataset.collect.at,g010-public:Dataset.collect.next; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:collect-block-cleanup,data:collect-break-continue,data:collect-count-0,data:collect-count-1,data:collect-count-255,data:collect-count-256,data:collect-count-3,data:collect-early-return,data:collect-empty-next,data:collect-missing-3,data:collect-option-consumed,data:collect-order,data:collect-result-independent,data:collect-return-0,data:collect-return-23,data:collect-return-7,data:collect-row-independent,data:collect-two-live`.

Misuse/diagnostic/failure proofs: `data:collect-arity,data:collect-at-type,data:collect-bound,data:collect-copy,data:collect-count-257,data:collect-negative-index,data:collect-next-arity,data:collect-option-copy,data:collect-option-twice,data:collect-row-not-int`. API fingerprint: `8d1d07a056a3dc88d6532c1eb7d35324d48e2af2e9519721fb8e004bd0712952`.

<a id="symbol-794942df0a6f89565f084a3e"></a>

## builtin / Text.parseBool

Identity: `claim:18ffc2806b33703f78e1d402`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"True".parseBool().isOk().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `textparse:parse-bool-tag-2`.

### Types, ownership, effects, limits and errors

parseBool(); exact true or false; Result by value; actual Result value/tag/fallback; native checked output is committed only on Ok; live keyword table; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Explicit parse result; preserve checked numeric domain and Unicode policy

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/numeric_safety_semantic.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_parse.asm`.

Positive/changed-value/boundary proofs: `textparse:parse-bool-0,textparse:parse-bool-1,textparse:parse-bool-2,textparse:parse-bool-tag-2`.

Misuse/diagnostic/failure proofs: `textparse:arity-parseBool,textparse:fallback-type-Bool`. API fingerprint: `72916d001c1a044c89a41ced3bc7294c9ec7a80e7d92eb36fd5aa94f0a6d6e3a`.

<a id="symbol-0eef9a853406035b9fe6cf90"></a>

## builtin.visual / scan

Identity: `claim:1a9373a3a7fa502dc2f48298`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("23")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-23-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:Text.scan(); g018-declaration:Console.scan() Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `metadata:console-live-build,scan:bool-False,scan:char-81,scan:int-23-return-23,scan:text-4e65626f,scan:two-results-live`.

Misuse/diagnostic/failure proofs: `scan:invalid-process-type,scan:unknown`. API fingerprint: `e2208b44218006879c4fc0e39f98957fe3c4adbe25f954b4815b8ce5c437e28f`.

<a id="symbol-e147d48ca5649e0abca10108"></a>

## builtin / topologicalSort

Identity: `claim:1b37363fcba1b2168aabd798`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.addEdge(d,a,13);g.topologicalSort();23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `relation:graph-topological-cycle`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.topologicalSort; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-topological,relation:graph-topological-cycle`.

Misuse/diagnostic/failure proofs: `relation:graph-topologicalSort-arity`. API fingerprint: `32fabdb3beb9823cfde8391adae5f7f527bf3d323cfde4e9b8262c0f432c6202`.

<a id="symbol-7a4ae8a100b1917bcb436394"></a>

## builtin.ScanPlan / noEcho

Identity: `claim:1b39fcd025c453fa8357a32a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.noEcho(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-16-0-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:noEcho; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-16-0-4,scan:security-16-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-noEcho`. API fingerprint: `b2e0c94dea628a6c9ab2ed11f9b6901227beb5ac283f20f316350798def79044`.

<a id="symbol-1ab0c6490e7b6e2ba4c8284a"></a>

## std.math / sqrt

Identity: `claim:1bc7ef48f216aacfd9f9d058`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.sqrt(9.0);}

```

Explicit process status: `0`; the full value/effect oracle is `math:implicit-float`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:implicit-float`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `10581364c243320a7300a4f5c0a2a3c457f4390357f87502d2f168b0a793c191`.

<a id="symbol-6133167d6d487215e809f5ba"></a>

## builtin / Text.codepointCount

Identity: `claim:1dee75379199bd3b461864b6`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".codepointCount().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:codepoints-0`.

### Types, ownership, effects, limits and errors

Text.() -> Int; 1048576 input bytes; UTF-8 scalar boundaries; borrow receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:codepoints-0,text:codepoints-2`.

Misuse/diagnostic/failure proofs: `text:arity-codepointCount`. API fingerprint: `fd61c845fa0153814a13807d7257512c4be3aeb067df6be4522253fd1fa7425e`.

<a id="symbol-682041d3d28e41042a927465"></a>

## builtin.Vector / norm

Identity: `claim:1edb2d6fba6d2069d8ecb1b2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_VECTOR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Float> [3.0,4.0].a;Vector<Float> [1.0,2.0].b;"${a.norm():fixed(6)}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `vector:norm-Float-3.0-4.0`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 vector lanes; extent 1..64; typed filled/literal constructors; native vector algorithms and separate owned frames; exact shape/type checks; constant index bounds and dynamic native checks; linked scalar spills preserve RHS allocations; no whole-source report; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `vector:norm-Float-3.0-4.0,vector:norm-Float-5.0-12.0,vector:norm-Float-8.0-15.0`.

Misuse/diagnostic/failure proofs: `vector:method-norm-empty,vector:float-method-norm`. API fingerprint: `56f7aeb0b4d4662693b4d1b1cec822b12872e9030ac2b50507140f1114200844`.

<a id="symbol-4f0bdc8bdcf65398b4370674"></a>

## builtin.Vector / dot

Identity: `claim:1f646618c206bc4091b49782`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_VECTOR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int> [17,29].a;Vector<Int> [3,5].b;a.dot(b).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `vector:dot-17-29`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 vector lanes; extent 1..64; typed filled/literal constructors; native vector algorithms and separate owned frames; exact shape/type checks; constant index bounds and dynamic native checks; linked scalar spills preserve RHS allocations; no whole-source report; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `vector:dot-17-29,vector:dot-43-29,vector:dot-Float-3.0-4.0,vector:native-storage-failure-atomicity`.

Misuse/diagnostic/failure proofs: `vector:shape-dot-Int-3,vector:shape-dot-Float-4,vector:method-dot-17`. API fingerprint: `eb27f740906f3e01e72472d490474892502e3f68b1ccd57e2554957a73e3d179`.

<a id="symbol-56ef4153c055364c344aa480"></a>

## builtin / Text.byteSlice

Identity: `claim:20202d70d4eddbe581997909`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".byteSlice(0,0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:byte-slice-4`.

### Types, ownership, effects, limits and errors

byteSlice(Int,Int); 0 <= start <= end <= byte length; ; borrowed view; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:byte-slice-1,text:byte-slice-4`.

Misuse/diagnostic/failure proofs: `text:type-byteSlice`. API fingerprint: `f651be9712dbfdd30cc043edd36cb2b14187cc070a0e4a62833bb1f7cd6692d2`.

<a id="symbol-cd9b0571a813bf85e34e53e4"></a>

## builtin / maxProbeLength

Identity: `claim:2164926bff38e7744328017c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.maxProbeLength().return;}

```

Explicit process status: `1`; the full value/effect oracle is `dict:max-probe`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.maxProbeLength; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:max-probe`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `9afeaeac748d79b234fc25a4abf9a20ea4ff5fb64f567745d177c93d6333b89d`.

<a id="symbol-8ceb91caeb4ad17ecf86edf6"></a>

## std.core / IntoIterator

Identity: `claim:21825c3b97051c5e515a0567`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)work(){Range.exclusive(0,5).indices;0.total.mutable;for(index in indices){total+=index;}total.return;}start(){7.work().return;}
```

Explicit process status: `10`; the full value/effect oracle is `control:for`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `control:for,control:for-continue,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:range-zero-step`. API fingerprint: `ea510ebd385ad1b64ae094963c98e4d2d2085c45ef74da1fc620ca6c460973e2`.

<a id="symbol-90c97c5ede9de437f959305a"></a>

## builtin / Schema

Identity: `claim:225c70277ef249bd5b21dbbc`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false)]).s;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:schema-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Schema.new,g010-public:Schema.indexOf,g010-public:Schema.field; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:schema-field-description-length,data:schema-field-snapshot,data:schema-missing-name,data:schema-name-limit,data:schema-ordered-age-score,data:schema-ordered-idade-pontuação,data:schema-ordered-score-age,data:schema-return-23,data:schema-runtime-name,data:schema-two-live`.

Misuse/diagnostic/failure proofs: `data:schema-copy,data:schema-duplicate-name,data:schema-field-missing,data:schema-field-name-type,data:schema-field-projection-bound,data:schema-field-shape,data:schema-index-name-type,data:schema-name-over-limit,data:schema-no-fields,data:schema-over-fields,data:schema-wrong-name,data:schema-wrong-nullability,data:schema-wrong-type`. API fingerprint: `a0e1f6827f27fb822ee5d17077de0bfee6c48e6ca9e6dd3ce330b75c32def0ee`.

<a id="symbol-874a55f26a1231181e62c7a3"></a>

## builtin.ScanPlan / select

Identity: `claim:2415624b2b6b0a01673cac30`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.select(["red","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-variant-select-red`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:select; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-variant-select-red,scan:choice-variant-select-blue`.

Misuse/diagnostic/failure proofs: `scan:choice-variant-select-absent,scan:choice-type-select`. API fingerprint: `209adad11c92a7ba38499c9e3cf9372a361d8905d40751dab31895dee43ee253`.

<a id="symbol-6a015eb4cc50f9f585ea2d70"></a>

## builtin / keys

Identity: `claim:245ff9bfa5103aeef018ffbf`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.keys().v;v.next().unwrapOr(73).x;v.release();x.return;}

```

Explicit process status: `73`; the full value/effect oracle is `dict:view-keys-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.keys; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:view-keys,dict:view-keys-empty,dict:view-scope-cleanup`.

Misuse/diagnostic/failure proofs: `dict:view-copy,dict:view-mutation`. API fingerprint: `e2dc1a8633f0863319bea6ff0e7492bdcfe9cccf9a4da97ac6d7f2c2d9d5ed0c`.

<a id="symbol-6ce9d917f53887fe3c600efc"></a>

## builtin / Text.splitChecked

Identity: `claim:24f727cf6a4134a5df77bbde`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"abc".splitChecked("").isOk().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `textparse:checked-split-tag-2`.

### Types, ownership, effects, limits and errors

splitChecked(Text); 254 generated item slots; borrowed split items; actual Result value/tag/fallback; native checked output is committed only on Ok; live keyword table; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Explicit parse result; preserve checked numeric domain and Unicode policy

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/numeric_safety_semantic.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_parse.asm`.

Positive/changed-value/boundary proofs: `textparse:checked-split-value-0,textparse:checked-split-tag-2,textparse:split-capacity-254,textparse:split-fallback-255`.

Misuse/diagnostic/failure proofs: `textparse:arity-splitChecked,textparse:type-splitChecked,textparse:fallback-type-Split`. API fingerprint: `7bbfc1165260426574b81b2e92431ca5a29a4638525ccfe02a90543f94beb687`.

<a id="symbol-462a1ba45e1da082fcfd2a13"></a>

## builtin / Text.isAlnumAscii

Identity: `claim:261d0a7a79e4a669138f207d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".isAlnumAscii().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:isAlnumAscii-empty`.

### Types, ownership, effects, limits and errors

isAlnumAscii(); nonempty ASCII letters/digits; O(n); ; borrowed immutable receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:isAlnumAscii-empty,text:isAlnumAscii-4e65626f`.

Misuse/diagnostic/failure proofs: `text:arity-isAlnumAscii`. API fingerprint: `731a34ca3736ca26b8369168df405c4e2a999ca311a8bf2c1e89bdefadeeffd8`.

<a id="symbol-1b921d98f6145b31622a3986"></a>

## builtin / removeEdge

Identity: `claim:262485e4560591a88e23d0f6`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(71).b;g.addEdge(a,b,5).e;g.removeEdge(e).console();g.neighbors(a).length().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-directed-remove-edge`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.removeEdge; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-remove-edge,relation:graph-undirected-remove-edge`.

Misuse/diagnostic/failure proofs: `relation:graph-remove-edge-type`. API fingerprint: `6f94b152809041c35db31ba69dda59a1388c3d0d11557bf64a5f6dc897747a10`.

<a id="symbol-e4755d3a45e8a62e4a0eb72d"></a>

## builtin / new

Identity: `claim:26f684ed60b8d7c9c648a2d4`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false)]).s;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:schema-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Schema.new; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:schema-name-limit,data:schema-ordered-age-score,data:schema-ordered-idade-pontuação,data:schema-ordered-score-age,data:schema-return-23,data:schema-runtime-name,data:schema-two-live`.

Misuse/diagnostic/failure proofs: `data:schema-copy,data:schema-duplicate-name,data:schema-field-shape,data:schema-name-over-limit,data:schema-no-fields,data:schema-over-fields,data:schema-wrong-name,data:schema-wrong-nullability,data:schema-wrong-type`. API fingerprint: `552bd02f7042c45c598574799bac13aaf40a11a2339cc85e4eebb0308676b58a`.

<a id="symbol-7f46554c731bfda3e5bc286e"></a>

## builtin / Text.replaceOnce

Identity: `claim:28520f06cc389966c57b1350`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"ação ação".replaceOnce("ação","é").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:replaceOnce-2`.

### Types, ownership, effects, limits and errors

replaceOnce(Text,Text); 4096 output bytes; non-overlapping; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:replaceOnce-0,text:replaceOnce-2`.

Misuse/diagnostic/failure proofs: `text:type-replaceOnce`. API fingerprint: `dd2960bbecf7a9209e7f27e053d612d2d41749b547e65cd76e29f5a42c8b7e9c`.

<a id="symbol-dd5ef6267c63b3b9bf93f5f6"></a>

## std.core / Iterator

Identity: `claim:29096ccbb1c2cfd910ea519a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)work(){Range.exclusive(0,5).indices;0.total.mutable;for(index in indices){total+=index;}total.return;}start(){7.work().return;}
```

Explicit process status: `10`; the full value/effect oracle is `control:for`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:G006-04,control:for,control:for-break,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:range-zero-step`. API fingerprint: `f7247f813d8d60d2c88f375cf31464375c84974755ac424a69c9fadbb47b0cc4`.

<a id="symbol-2a7ee809dde83a4867201560"></a>

## builtin / Text.endsWith

Identity: `claim:2a0587c33f76f50b748c2541`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".endsWith("absent").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:endsWith-2`.

### Types, ownership, effects, limits and errors

endsWith(Text); literal bytes; O(m); ; borrowed immutable receiver and argument; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:endsWith-0,text:endsWith-2`.

Misuse/diagnostic/failure proofs: `text:type-endsWith`. API fingerprint: `f7744927cfa600b776c56485a3602f1e31d0d31e877e668c22867c06efb2e06f`.

<a id="symbol-b0a4e42ee1ed6cca605eab64"></a>

## builtin / invalidRows

Identity: `claim:2a60d49452cf9bfd1fdba334`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().empty;Table.fromColumns([Tuple.of("age",Column<Int>.from([empty.get(0),empty.get(0)]))]).t;Schema.new([Tuple.of("age",Int,false)]).strict;Schema.new([Tuple.of("age",Int,true)]).relaxed;t.invalidRows().bad;bad.rowCount().return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:quality-default`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.invalidRows; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:quality-default,data:quality-limit-32,data:quality-mask-3-5-0,data:quality-mask-7-7-0,data:quality-mask-7-7-3,data:quality-repeated,data:quality-two-live`.

Misuse/diagnostic/failure proofs: `data:quality-invalid-rows-arity`. API fingerprint: `489324df038e31049f11b553ebab830f3d046718cf06945eafa27d54f39d0aca`.

<a id="symbol-2704849dbeadaf3f169cc0dd"></a>

## builtin.ScanPlan / oneOf

Identity: `claim:2a77e90f89d14ab3a1134646`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.oneOf(["red","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-variant-oneOf-red`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:oneOf; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-variant-oneOf-red,scan:choice-variant-oneOf-blue`.

Misuse/diagnostic/failure proofs: `scan:choice-variant-oneOf-absent,scan:choice-type-oneOf`. API fingerprint: `46f193459f1418ae0d1b10063d43a4bc79b7db61a221c2912d555b0698e3f34c`.

<a id="symbol-e192846407f93f1b2af3d43e"></a>

## builtin / getMutable

Identity: `claim:2ba6d1655b5c1271a86e346e`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.getMutable(29).r;r.read().return;}

```

Explicit process status: `177`; the full value/effect oracle is `dict:unique-missing`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.getMutable; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:unique-missing,dict:unique-read,dict:unique-release,dict:unique-scope,dict:unique-temporary,dict:unique-write`.

Misuse/diagnostic/failure proofs: `dict:shared-unique,dict:unique-key-type,dict:unique-owner-read,dict:unique-released,dict:unique-view,dict:unique-write-type`. API fingerprint: `4c0f90e35e57d8339fd1fdfe854e6196643728224397d7ad8fceead326a61046`.

<a id="symbol-de3a7e8e70520dee80f5d2ad"></a>

## builtin / Text.normalizeNfc

Identity: `claim:2bfd213e6c20704403b90651`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".normalizeNfc().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:normalizeNfc-0`.

### Types, ownership, effects, limits and errors

Text.() -> Text; 4096 public workspace; 15.1 Latin acute canonical pair; caller-owned result; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:normalizeNfc-0,text:normalizeNfc-2`.

Misuse/diagnostic/failure proofs: `text:arity-normalizeNfc`. API fingerprint: `defdcdbccd77ef3fe1668e40b12e4867852a34a0ea9b3926e8a8567926f0f198`.

<a id="symbol-a1d95bb5e9ff0e18e0e9b769"></a>

## builtin / Text.caseFold

Identity: `claim:2c46bd41771626d1704b8e86`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".caseFold().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:caseFold-0`.

### Types, ownership, effects, limits and errors

Text.() -> Text; 4096 public workspace; root ASCII/Latin-1 simple fold; caller-owned result; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:caseFold-0,text:caseFold-2`.

Misuse/diagnostic/failure proofs: `text:arity-caseFold`. API fingerprint: `b9b7c795381a3601c32ed196c794404b6dffd748b64405da71af65937dc32784`.

<a id="symbol-ab3eb31f8796d326630e5306"></a>

## builtin.ScanPlan / style

Identity: `claim:2c81c1a8cf531346ba5699b2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.form("profile"),.field("age","Int"),.view("entry"),.panel("main"),.feedback(),.style("blue"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-metadata-profile`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:style; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings`.

Misuse/diagnostic/failure proofs: `scan:option-type-style`. API fingerprint: `5c9fdfcf01d773fd5e741fa678a78c54ae493651d36e772e877553716e978c8c`.

<a id="symbol-ef8ba62f197eee380400aa1e"></a>

## std.core / Ord

Identity: `claim:2d0c75419eb6a8f32bec0892`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
generic<T> (T.value)hold() where T: Ord{value.return;}start(){17.hold().a;29.hold().b;(a+b).console();7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `composite:generic-compose-whereTOrd-7`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:generic-compose-whereTOrd-7,composite:generic-compose-whereTOrd-23,composite:generic-compose-whereTOrd-255,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:generic-ord-bool`. API fingerprint: `39d3449472f0ecce750e75590f0d10e2e0ff464e5e2b1c3ce3d36cd71de613fd`.

<a id="symbol-01cf67b9da68b3920e5578aa"></a>

## builtin / hashStable

Identity: `claim:2d3c798ac72cb131a3a47321`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Int.hashStable().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:int-hash-stable`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Int.hashStable; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:int-hash-stable`.

Misuse/diagnostic/failure proofs: `dict:hash-stable-arity`. API fingerprint: `e18b9e583a216abe844b20dc9db35cf630e6ce4cdac6d8ad108ea9ec6b67c3db`.

<a id="symbol-7d3680fec3e84bb844d1dcad"></a>

## builtin / Text.isAlphaAscii

Identity: `claim:2e196c37538b73525f2527d2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".isAlphaAscii().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:isAlphaAscii-empty`.

### Types, ownership, effects, limits and errors

isAlphaAscii(); nonempty ASCII letters; O(n); ; borrowed immutable receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:isAlphaAscii-empty,text:isAlphaAscii-4e65626f`.

Misuse/diagnostic/failure proofs: `text:arity-isAlphaAscii`. API fingerprint: `4a5ec18da83f749809d869af40b290c4453d856ab16cc1fe6449663b0b022f59`.

<a id="symbol-b12800e15e613cac4c9ff3c0"></a>

## builtin / groupBy

Identity: `claim:2e1c93122fc13d6d34df1b59`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("key",Column<Int>.from([29,17,29])),Tuple.of("value",Column<Int>.from([71,83,97]))]).t;0.i.mutable;while(i<3){t.groupBy(["key"]).g;g.length().console();i+=1;}23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:group-normal-cleanup`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.groupBy,g010-public:Table.groupBy.next,g010-public:Table.groupBy.sink; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:group-changed,data:group-empty,data:group-independent-cursor,data:group-limit,data:group-many,data:group-missing-3,data:group-missing-7,data:group-next,data:group-normal-cleanup,data:group-one,data:group-retained-table,data:group-same,data:group-signed,data:group-sink-cursor,data:group-two-live`.

Misuse/diagnostic/failure proofs: `data:group-copy,data:group-empty-expect,data:group-key-arity,data:group-key-two,data:group-key-type,data:group-method-arity,data:group-missing-name,data:group-sink-type`. API fingerprint: `6e827bd798bc2aaf5acc39970de87408659fd1b1b18ad78e177e068890fad644`.

<a id="symbol-86f101d18b555d46135a7fe8"></a>

## builtin / toSortedEntries

Identity: `claim:2e251f062ef34fad18023122`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.toSortedEntries().s;s.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:sorted-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.toSortedEntries; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:sorted-changed,dict:sorted-empty,dict:sorted-permuted,dict:sorted-reseed,dict:sorted-signed,dict:sorted-snapshot`.

Misuse/diagnostic/failure proofs: `dict:sorted-arity,dict:sorted-index-type`. API fingerprint: `db2419e6ced91a8bb6eb2cf4f0d6e0d5746a4755c49ef79064736e551ecebc78`.

<a id="symbol-e6a7c60dc18c50fa6f5dd7bb"></a>

## builtin / get

Identity: `claim:2e75bab938b02a3dd8167eb9`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([17]).c;c.get(1);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-bounds`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Row.get,g010-public:Column.get; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-bounds,data:column-get,data:column-missing-get,data:column-negative-index,data:row-missing-0,data:row-missing-1,data:row-missing-2,data:row-missing-3,data:row-values-17-71,data:row-values-29-83`.

Misuse/diagnostic/failure proofs: `data:column-get-type,data:row-get-name-type,data:row-unknown-name`. API fingerprint: `bc5a51d5086a9ad6022d0cdfb2eaa3a60451b761b4d672e23ab0d480729e9c4b`.

<a id="symbol-1027d819a3ef06ce23aa1e43"></a>

## builtin.ScanPlan / forgetAfterUse

Identity: `claim:311685a7a02236989d47900a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.console(),.secret(),.forgetAfterUse(),.mock("Synthetic7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:forget-trace-11-S`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:forgetAfterUse; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:forget-trace-11-S,scan:forget-trace-4096-a,scan:forget-confirm-True,scan:forget-confirm-False,scan:forget-two-live-values,scan:forget-retry-preserves-tail`.

Misuse/diagnostic/failure proofs: `scan:forget-needs-secret,scan:security-arity-forgetAfterUse`. API fingerprint: `844758469822a2974b375423ac8b8c1248cc6b29ff49510bf6ba6a39a57aeaba`.

<a id="symbol-5ce37de3e31367e3ef74ac7b"></a>

## builtin / Dict

Identity: `claim:327d71b504a1fea3c8407863`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict<Int,Int>.new,g008-public:Dict<Int,Int>.withCapacity,g008-public:Dict.length,g008-public:Dict.capacity,g008-public:Dict.reserve,g008-public:Dict.loadFactor,g008-public:Dict.insert,g008-public:Dict.get,g008-public:Dict.containsKey,g008-public:Dict.remove,g008-public:Dict.clear,g008-public:Dict.keys,g008-public:Dict.values,g008-public:Dict.entries,g008-public:Dict.reseed,g008-public:Dict.maxProbeLength,g008-public:Dict.validateInvariants,g008-public:Dict.getMutable,g008-public:Dict.entry,g008-public:Dict.iterationOrder,g008-public:Dict.toSortedEntries,g008-public:Dict.retain,g008-public:Dict.collisionCount; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:absent,dict:bound-reference,dict:capacity-16,dict:capacity-32,dict:capacity-64,dict:capacity-8,dict:clear,dict:collision-cluster,dict:collision-empty,dict:collision-single,dict:contains,dict:duplicate-length,dict:duplicate-new-value,dict:duplicate-old-value,dict:empty,dict:empty-reproducer,dict:entry-absent-read,dict:entry-key-once,dict:entry-new,dict:entry-repeated-write,dict:entry-temporary,dict:entry-update,dict:entry-view-empty,dict:entry-view-pair,dict:invariants,dict:iteration-order,dict:length-one,dict:length-two,dict:load-factor-0,dict:load-factor-1,dict:load-factor-2,dict:lookup-29-53-17,dict:max-probe,dict:present,dict:remove,dict:remove-absent,dict:repeated-reseed,dict:reseed-retains,dict:reserve-retains-value,dict:retain-all,dict:retain-changed-key,dict:retain-changed-value,dict:retain-effects-once,dict:retain-empty,dict:retain-key,dict:retain-none,dict:retain-two-live,dict:retain-value,dict:sorted-changed,dict:sorted-empty,dict:sorted-permuted,dict:sorted-reseed,dict:sorted-signed,dict:sorted-snapshot,dict:unique-missing,dict:unique-read,dict:unique-release,dict:unique-scope,dict:unique-temporary,dict:unique-write,dict:view-exhaustion,dict:view-keys,dict:view-keys-empty,dict:view-scope-cleanup,dict:view-values,dict:view-values-empty`.

Misuse/diagnostic/failure proofs: `dict:borrow-mutation,dict:capacity-invalid-9,dict:capacity-type,dict:collision-arity,dict:entry-copy,dict:entry-key-type,dict:entry-read-conflict,dict:entry-write-type,dict:insert-key-type,dict:insert-value-type,dict:iteration-order-arity,dict:key-type,dict:released-reference-read,dict:reserve-type,dict:retain-borrow,dict:retain-capture,dict:retain-key-type,dict:retain-non-callable,dict:retain-return-type,dict:retain-value-type,dict:shared-unique,dict:sorted-arity,dict:sorted-index-type,dict:unique-key-type,dict:unique-owner-read,dict:unique-released,dict:unique-view,dict:unique-write-type,dict:use-after-drop,dict:use-after-move,dict:value-type,dict:view-copy,dict:view-mutation,dict:view-released,dict:view-reseed`. API fingerprint: `16b0dac5f03b75b75ad49217a05dffa08a258f82062e393df1cce20975687df6`.

<a id="symbol-5b4f409c40a8d86a5424f743"></a>

## builtin / Text.isNeboIdentifier

Identity: `claim:33b5bff070ad391908bba2f9`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"a".isNeboIdentifier().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `textparse:identifier-0`.

### Types, ownership, effects, limits and errors

isNeboIdentifier(); ASCII lexer profile plus 27 live keywords; borrowed read only; actual Result value/tag/fallback; native checked output is committed only on Ok; live keyword table; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Explicit parse result; preserve checked numeric domain and Unicode policy

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/numeric_safety_semantic.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_parse.asm`.

Positive/changed-value/boundary proofs: `textparse:identifier-0,textparse:identifier-3,textparse:identifier-6,textparse:keyword-start,textparse:keyword-PI,textparse:keyword-xor`.

Misuse/diagnostic/failure proofs: `textparse:arity-isNeboIdentifier`. API fingerprint: `84e8cd325a0e4fee35298cf987c945065ce4488a67ebc7fba3701d24f1dd3000`.

<a id="symbol-64822f62cf3c66f815766072"></a>

## builtin / difference

Identity: `claim:352f3cee7bab13693d1213fb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.difference(b).c;c.length().return;}

```

Explicit process status: `1`; the full value/effect oracle is `dict:set-difference`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Set.difference; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-difference,dict:set-difference-membership`.

Misuse/diagnostic/failure proofs: `dict:set-difference-type`. API fingerprint: `3a6db5a30f616f2d666d365eae1890aae828303d5e389b4a408871cb7fcb8ab8`.

<a id="symbol-91874561b9c40c46496c2a04"></a>

## builtin / Text.replaceAllChecked

Identity: `claim:35cb3abf9982aba1b63daa35`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"abc".replaceAllChecked("","X").isErr().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `textparse:checked-replace-tag-2`.

### Types, ownership, effects, limits and errors

replaceAllChecked(Text,Text); 4096 generated output bytes; caller-owned Text; actual Result value/tag/fallback; native checked output is committed only on Ok; live keyword table; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Explicit parse result; preserve checked numeric domain and Unicode policy

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/numeric_safety_semantic.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_parse.asm`.

Positive/changed-value/boundary proofs: `textparse:checked-replace-value-0,textparse:checked-replace-tag-2,textparse:replace-capacity-4096,textparse:replace-fallback-1,textparse:two-live-checked,textparse:checked-error-preserves-input`.

Misuse/diagnostic/failure proofs: `textparse:arity-replaceAllChecked,textparse:type-replaceAllChecked,textparse:fallback-type-Text`. API fingerprint: `db039eaa2014e331a8eb406caeb07c5cc5e9a8cdede35168425e6cd4b39ba5e7`.

<a id="symbol-bc8fd6c9543faf67cd135566"></a>

## builtin / addNode

Identity: `claim:3643ddeae3432f2b2f9139f8`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;0.i.mutable;while(i<33){g.addNode(i);i+=1;}23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `relation:graph-capacity-plus-one`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.addNode; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-capacity,relation:graph-capacity-plus-one,relation:graph-two-live`.

Misuse/diagnostic/failure proofs: `relation:graph-add-node-type`. API fingerprint: `9029a4cf7377443cc75739b5124121b1ee5170b0e71e4f9ef1ddb533f141b09e`.

<a id="symbol-fca07f29469cc5ec5b933eeb"></a>

## builtin / retain

Identity: `claim:3a1a5191c43d6c42cae03ceb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.key)keep(Int.value){(key>17).return;}start(){Dict<Int,Int>.new().d;d.retain(keep);d.length().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `dict:retain-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.retain; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:retain-all,dict:retain-changed-key,dict:retain-changed-value,dict:retain-effects-once,dict:retain-empty,dict:retain-key,dict:retain-none,dict:retain-two-live,dict:retain-value`.

Misuse/diagnostic/failure proofs: `dict:retain-borrow,dict:retain-capture,dict:retain-key-type,dict:retain-non-callable,dict:retain-return-type,dict:retain-value-type`. API fingerprint: `b34598d8f61626a283d6dd0d26722f2828bcda559b71fe26bdca2f41c008e889`.

<a id="symbol-6d13a737e25fa02dd191d484"></a>

## builtin.Vector / quantile

Identity: `claim:3af5a07d7b408107b32b19a0`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_STATISTICS_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Float> [1.0,2.0].a;a.quantile(1.25,linearType7);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `statistics:domain-quantile-high`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 lanes, extent 1..64; checked Int sum, native compensated Float sum, explicit population/sample and linearType7 policies; 1536-byte caller workspace isolates conversion/quantile scratch; source values unchanged; native domain/overflow traps and NaN propagation are observed; no source report or synthetic aggregate Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `statistics:Float-0-quantile-0.25,statistics:Int-1-quantile-0.75,statistics:Float-0-quantile-0.0,statistics:Float-0-quantile-1.0,statistics:dynamic-quantile,statistics:quantile-preserves-input,statistics:domain-quantile-low,statistics:domain-quantile-high`.

Misuse/diagnostic/failure proofs: `statistics:quantile-0.25,statistics:shadow-quantile`. API fingerprint: `d0f7f47f7c17e8c0ab71a0b1b8b9da7fdc24361caac866a3ae89989282ed56fc`.

<a id="symbol-14d4696d05d68d4045c8ddcd"></a>

## std.core / Option

Identity: `claim:3b661f2f1f7b1c8dde06f306`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Option<Int>(Some(17)).x;x.map(31).get().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `optionresult:option-map`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Typed active variant and structured errors; bounded combinators; explicit ownership; no arbitrary callback promise

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/option_result_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/scalars/option_result_runtime.asm`.

Positive/changed-value/boundary proofs: `optionresult:option-value-17-return-23,optionresult:option-map,optionresult:option-map-none,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `optionresult:option-zip-array`. API fingerprint: `0f53a333435fd0f67aa44d7e2a9f3d75fb53bd60c8775bef419aa890293c32fc`.

<a id="symbol-aa8e8a8727f77f00dd2b234e"></a>

## builtin / sortBy

Identity: `claim:3b877d773a41ebfce31e2a56`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("age").expect("v").console();self.get("score").expect("v").console();true.return;}start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([71,17,29])),Tuple.of("score",Column<Int>.from([83,7,53]))]).t;t.sortBy(["age"]).out;t.filter(observe);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-sort-source-independent`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.sortBy; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-sort-metamorphic--31-53-11,data:table-sort-metamorphic-29-17-29,data:table-sort-metamorphic-71-7-83,data:table-sort-missing-0,data:table-sort-missing-1,data:table-sort-missing-2,data:table-sort-missing-3,data:table-sort-missing-5,data:table-sort-missing-7,data:table-sort-order,data:table-sort-source-independent`.

Misuse/diagnostic/failure proofs: `data:table-sort-name-type,data:table-sort-two-keys,data:table-sort-unknown-name`. API fingerprint: `286775465abb7f940eced82cd5d18c66d04b3dc716b77cd1993098de99c2b657`.

<a id="symbol-4e9bc0c50f7cfb91367658d3"></a>

## builtin.visual / size

Identity: `claim:3be8074ed4adc596274a600f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"abc".console().view;view.size().s;s.at(0).console();s.at(1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:size-3`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:console.size() Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:size-13,console:size-3,console:size-6`.

Misuse/diagnostic/failure proofs: `console:size-arity`. API fingerprint: `bef649661ef5ee043d1176e7bbd67a7b045f69a2d256f6661f50df85acc7fae5`.

<a id="symbol-e6a69429d0c7cd573f781087"></a>

## builtin.ScanPlan / yesNo

Identity: `claim:3c2eb81e414ed36cc23ec4b2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.yesNo(),.mock("no")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-yesNo-no`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:yesNo; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-yesNo-yes,scan:choice-yesNo-no`.

Misuse/diagnostic/failure proofs: `scan:choice-yesNo-invalid`. API fingerprint: `8bc5833f15f580fd4aca4bd813cb53c7e2cdb44da67f3575cc4f3a728a2b5767`.

<a id="symbol-a1c62d228113d98361a066a7"></a>

## builtin / cache

Identity: `claim:3d361b1b7b6ad0b26d30620c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29,71]))]).t;Dataset.fromTables([t]).d;d.cache(1);d.collect().rows;rows.at(2).get("age").expect("v").return;}
```

Explicit process status: `71`; the full value/effect oracle is `data:cache-collect`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Dataset.cache; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:cache-collect,data:cache-disable,data:cache-scan,data:cache-two-live`.

Misuse/diagnostic/failure proofs: `data:cache-arity,data:cache-blocks-repartition,data:cache-invalid-2,data:cache-policy-type`. API fingerprint: `e9827a1df8042e2319792a52250af445f71158b083dc6eec1fa730545703a94b`.

<a id="symbol-6f8edce072d8a856a31deee5"></a>

## builtin.Random / categorical

Identity: `claim:3d68dc5fd9e828e756c26f2d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_RANDOM_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Random.seed(17).r;Vector<Float> [∞,1.0].w;r.categorical(w);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `random:domain-weights-infinite`.

### Types, ownership, effects, limits and errors

Explicit SplitMix64 v1 state, no entropy capability; native finite-domain distributions and categorical Float Vector weights; nonnegative Int uniform; shuffle of unique local mutable Int Slice at most 64 lanes; lexical creation/release provenance, separate descriptor/payload frames; sample of Int Vector/local Slice returns canonical List with 0..16 entries; independent seed/value/order oracles and rejected effectful interpolation; no whole-source report Independent deterministic seeded states; checked intervals and distribution domains; no cryptographic entropy claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `random:categorical-17-1.0-3,random:categorical-29-9.0-3,random:domain-negative-weight,random:domain-weights-infinite,random:domain-weights-zero,random:native-distribution-controls`.

Misuse/diagnostic/failure proofs: `random:categorical-dtype,random:categorical-type,random:categorical-arity`. API fingerprint: `17debbed8571bfe5c5204d4268aa104694c9820cfc0094c1bcda1a2f9814c250`.

<a id="symbol-2ae552ea6a1f19be2300e2d9"></a>

## builtin / Text.indexOf

Identity: `claim:3d700a5a1897ad69b07b69c2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".indexOf("absent").unwrapOr(-1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:indexOf-2`.

### Types, ownership, effects, limits and errors

indexOf(Text); byte index; O((n-m+1)m); ; borrowed inputs; caller-owned Option slot; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:indexOf-0,text:indexOf-2`.

Misuse/diagnostic/failure proofs: `text:type-indexOf`. API fingerprint: `5203f7860f47582c04d76e5ffedbdb1abf73086773f7be6d015b57ae743da4fc`.

<a id="symbol-81e7897fc284f0f65896c8c4"></a>

## std.math / atan2

Identity: `claim:3e4e1dcddb7062bd03f35941`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.atan2(3.0, 7.0);29.return;}
```

Explicit process status: `29`; the full value/effect oracle is `math:atan2`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:atan2`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `8b00e5f436fdb724bdbf49798a020c8548544a48f3098fb5f2dfc48b7f014fc0`.

<a id="symbol-20e3ab9862a286aeddbb718a"></a>

## builtin / undirected

Identity: `claim:3ed1febf7e80ca9a7ed359a5`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.undirected().g;g.addNode(17).a;g.addNode(71).b;g.addEdge(a,b,5).e;g.neighbors(b).v;v.length().return;}
```

Explicit process status: `1`; the full value/effect oracle is `relation:graph-undirected-reverse`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph<Int,Int>.undirected; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-undirected-neighbors,relation:graph-undirected-reverse`.

Misuse/diagnostic/failure proofs: `relation:graph-edge-type`. API fingerprint: `366075d9b4aaab4f2ad0e4c228bd1933caab57adabfa19bfdbec7986c614ccf8`.

<a id="symbol-f5450de8b36cb338a53e06b7"></a>

## builtin.ScanPlan / maxLines

Identity: `claim:3f36ddd9d2242fe671e83752`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.multiline(),.maxLines(1),.mock("line")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-line-limit-1`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:maxLines; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-line-limit-1,scan:multiline-line-limit-256`.

Misuse/diagnostic/failure proofs: `scan:multiline-maxlines-zero,scan:multiline-maxlines-over`. API fingerprint: `2401fad6633b642213a536e70ac1d8688be93de634e54396c8714f4cda9fe28e`.

<a id="symbol-d47f9fd43222c8c3964c6116"></a>

## builtin / columnCount

Identity: `claim:3faba2f8f65262fab1216bf7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29,71])),Tuple.of("score",Column<Int>.from([83,7,53]))]).t;t.rowCount().console();t.columnCount().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-dimensions-many`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.columnCount; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-dimensions-many,data:table-eight-columns,data:table-select-subset`.

Misuse/diagnostic/failure proofs: `data:table-column-count-arity`. API fingerprint: `6e212f0e5a8c348adac4fc5ea13eb76e379ceb1e4d7f386d67249b9cebf35422`.

<a id="symbol-7553bed3482874196ccb2874"></a>

## builtin.Float / approxEquals

Identity: `claim:40d488fe4490fa05e7e02d26`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_FUNCTION_NATIVE_NUMERIC_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Float> [0.01,0.001].t;17.0.approxEquals(17.005,t).console();255.return;}
```

Explicit process status: `255`; the full value/effect oracle is `precision:approx-return-255`.

### Types, ownership, effects, limits and errors

Binary64 actual receiver and once-evaluated arguments; classifiers include IEEE subnormal/NaN/infinity/signed zero; explicit rounding modes restore caller control word; finite round magnitude <2^63; approxEquals preserves two source arguments with Vector<Float> tolerance [absolute,relative], exactly two lanes (runtime shape checked); nonnegative/non-NaN tolerances, identical infinities equal and distinct infinities unequal; Uncertain methods claim only exact typed operands; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `precision:approx-oracle-0-False,precision:approx-oracle-3-True,precision:approx-oracle-6-False,precision:approx-two-live,precision:approx-effects-once,precision:approx-owned-argument,precision:approx-dynamic-shape-2,precision:approx-loop-temporaries,precision:approx-return-255,precision:approx-uncertain-owner,precision:native-control-word`.

Misuse/diagnostic/failure proofs: `precision:approx-other-type,precision:approx-tolerance-type,precision:approx-element-type,precision:approx-shape-three,precision:approx-arity-4,precision:approx-status-type,precision:approx-dynamic-shape-3,precision:approx-domain-33`. API fingerprint: `46e2189c88e1cb3986c5af14c6a0a8a0f5f8fb795889c2907ecd5f79d27c786a`.

<a id="symbol-39147880c0d05a5086d3765f"></a>

## builtin / postorder

Identity: `claim:415c71c28c43aff4440b2800`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(r,71);t.addChild(a,83);t.postorder().v;v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().expect("node").value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-postorder`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.postorder; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-postorder`.

Misuse/diagnostic/failure proofs: `relation:tree-postorder-arity`. API fingerprint: `5d205f3931bfeb92bc7acdddf061c43b8ee91c0ba415e27c315822474db3b4c5`.

<a id="symbol-7fe5a647a1e542dfbcb9aabd"></a>

## builtin.ScanPlan / jsonBlock

Identity: `claim:424bb83996a3e38a68dcf3e9`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.jsonBlock()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-json-stdin`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:jsonBlock; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-json-mock,scan:multiline-json-stdin,scan:multiline-trace-json`.

Misuse/diagnostic/failure proofs: `scan:multiline-bad-json,scan:multiline-duplicate-json`. API fingerprint: `0113300fa019f87cc07af83ea4b9154b7de40b3dd50910a3761a6fa56949c149`.

<a id="symbol-12b293a4364652b69299a5b8"></a>

## builtin.ScanPlan / rawBlock

Identity: `claim:457f6ba87c85808174445e46`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.rawBlock()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-raw-stdin`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:rawBlock; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-raw-mock,scan:multiline-raw-stdin,scan:multiline-trace-raw`.

Misuse/diagnostic/failure proofs: `scan:multiline-mode-conflict`. API fingerprint: `9f66942e36a593073812797591a92826bb50b674a87542154f5d2a97beca5f54`.

<a id="symbol-84461c36c5e22820bf46b8b8"></a>

## builtin / preorder

Identity: `claim:45ec8d7b2a67419031f67091`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(r,71);t.addChild(a,83);t.preorder().v;v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().expect("node").value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-preorder`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.preorder; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-preorder`.

Misuse/diagnostic/failure proofs: `relation:tree-preorder-arity`. API fingerprint: `eb85f6695ce472ce7612dabe8b686b48970b3a64455c1eb853656ad59d128217`.

<a id="symbol-2016ff72dacddc64a661ae74"></a>

## builtin / Flow

Identity: `claim:4752f69a488b14ba42ab6346`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Flow<Int> 17.x;x.value().return;}
```

Explicit process status: `17`; the full value/effect oracle is `data:Flow-payload-17`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Flow<Int>.value; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:Flow-payload-17,data:Flow-payload-29,data:Flow-payload-71`.

Misuse/diagnostic/failure proofs: `data:flow-type`. API fingerprint: `0a4dbd8b1a556445d03fdcf581203f2c572ffbbfcbe14ff3d4c87495004feea5`.

<a id="symbol-d0b3b94ba0e51f990639f0bc"></a>

## builtin / Text.isEmpty

Identity: `claim:47990c872fdd06e04b656591`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".isEmpty().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:isEmpty-empty`.

### Types, ownership, effects, limits and errors

isEmpty(); O(1); ; borrowed immutable receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:isEmpty-empty,text:isEmpty-4e65626f`.

Misuse/diagnostic/failure proofs: `text:arity-isEmpty`. API fingerprint: `6b3ba1f01570c8350f256cf7e03c8033efe6247d3588a8f22d24f4fca1787b96`.

<a id="symbol-6103709c2fe221841630947f"></a>

## builtin / intersection

Identity: `claim:48e811dda1824a827cb5f5eb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.intersection(b).c;c.length().return;}

```

Explicit process status: `1`; the full value/effect oracle is `dict:set-intersection`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Set.intersection; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-intersection,dict:set-intersection-membership`.

Misuse/diagnostic/failure proofs: `dict:set-intersection-type`. API fingerprint: `3d2ec007d2b6b2143057c686c62786c31e3637be3e5140634d37c55c10b332cc`.

<a id="symbol-0ff6d650342d556ed537edfd"></a>

## builtin.Vector / correlation

Identity: `claim:492add4012b8533e7ead4cb6`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_STATISTICS_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Float> [1.0,1.0].a;a.correlation(a);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `statistics:domain-correlation-constant`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 lanes, extent 1..64; checked Int sum, native compensated Float sum, explicit population/sample and linearType7 policies; 1536-byte caller workspace isolates conversion/quantile scratch; source values unchanged; native domain/overflow traps and NaN propagation are observed; no source report or synthetic aggregate Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `statistics:Float-0-correlation-1,statistics:Float-0-correlation-9,statistics:Float-0-correlation-26,statistics:domain-correlation-constant`.

Misuse/diagnostic/failure proofs: `statistics:correlation-type,statistics:correlation-shape,statistics:correlation-dtype,statistics:correlation-arity`. API fingerprint: `a68a3dd1e69392e5316168eaac3c87ab136501ff586cac19763edc5cac3fa31a`.

<a id="symbol-ece7bc8a1e5c9f72845a0d88"></a>

## builtin.Random / Random

Identity: `claim:49faf7047d69177900e8fe96`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_RANDOM_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.value)draw(){Random.seed(value).r;r.uniform(10,100).return;}start(){29.draw().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `random:function-local-state`.

### Types, ownership, effects, limits and errors

Explicit SplitMix64 v1 state, no entropy capability; native finite-domain distributions and categorical Float Vector weights; nonnegative Int uniform; shuffle of unique local mutable Int Slice at most 64 lanes; lexical creation/release provenance, separate descriptor/payload frames; sample of Int Vector/local Slice returns canonical List with 0..16 entries; independent seed/value/order oracles and rejected effectful interpolation; no whole-source report Independent deterministic seeded states; checked intervals and distribution domains; no cryptographic entropy claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `random:ordered-state-17,random:ordered-state-29,random:two-live-states-17,random:function-local-state,random:seed-evaluated-once`.

Misuse/diagnostic/failure proofs: `random:seed-type,random:seed-arity,random:state-status`. API fingerprint: `c8c9d6ca562556c9c4a4dee070c6ac76402a030a50c5f230a7fd89204fecbfbc`.

<a id="symbol-63a49f2feccaa533a1472bf6"></a>

## builtin.ScanPlan / secret

Identity: `claim:4c4cb795ffcabd4d37c8a850`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.secret(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-93-1-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:secret; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-93-1-4,scan:security-93-1-8`.

Misuse/diagnostic/failure proofs: `scan:weak-secret,scan:secret-arity,scan:option-arity-secret`. API fingerprint: `4a53bfbc6e8bd683958203cf7f604e040d1224683637fb769d4473ef08fdf3b7`.

<a id="symbol-c060cc0a686efb2f0b7c6dcb"></a>

## builtin / reserve

Identity: `claim:4c814abc5cf5237a2bd5d2d2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.reserve(9);(d.capacity()+d.get(17).expect("retained")).return;}

```

Explicit process status: `69`; the full value/effect oracle is `dict:reserve-retains-value`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.reserve; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:reserve-retains-value`.

Misuse/diagnostic/failure proofs: `dict:reserve-type`. API fingerprint: `9a8ad7bb928663be385286f7811cff66d1639b4329481efb30cab9ea6ace4599`.

<a id="symbol-e33b58ea5ae86e0bd30cc862"></a>

## builtin.ScanPlan / feedback

Identity: `claim:4ec899a194e61c8e4122d838`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.feedback("inline"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-feedback-inline`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:feedback; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-retry-events,scan:console-feedback-inline,scan:console-feedback-summary`.

Misuse/diagnostic/failure proofs: `scan:metadata-type,scan:option-arity-feedback`. API fingerprint: `71ef253db4feedfba062c932b741e6d494d9b46588835bafe5583da80472ab6f`.

<a id="symbol-d598a0c4500e4f14f2a03e9e"></a>

## builtin.Float / isNaN

Identity: `claim:4f51bce75a774f5cc3aeb531`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_FUNCTION_NATIVE_NUMERIC_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∞.value;value.isNaN().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `precision:infinite-isNaN`.

### Types, ownership, effects, limits and errors

Binary64 actual receiver and once-evaluated arguments; classifiers include IEEE subnormal/NaN/infinity/signed zero; explicit rounding modes restore caller control word; finite round magnitude <2^63; approxEquals preserves two source arguments with Vector<Float> tolerance [absolute,relative], exactly two lanes (runtime shape checked); nonnegative/non-NaN tolerances, identical infinities equal and distinct infinities unequal; Uncertain methods claim only exact typed operands; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `precision:finite-isNaN,precision:infinite-isNaN,precision:nan-isNaN`.

Misuse/diagnostic/failure proofs: `precision:classifier-extra-argument`. API fingerprint: `723e1587a06a7b7add1f197668a338046ddaf68cac6e1a24a8deb4de89dd13d9`.

<a id="symbol-ac1f8d1c51ef4556e59a2228"></a>

## builtin / Event

Identity: `claim:4fe4393e76342243fa2d88bf`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Event<Int> 13.x;x.value().return;}
```

Explicit process status: `13`; the full value/effect oracle is `data:Event-payload-13`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Event<Int>.value; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:Event-payload-13,data:Event-payload-29,data:Event-payload-71`.

Misuse/diagnostic/failure proofs: `data:event-type`. API fingerprint: `a058ca5d9c61d22c34b58edcfafb24a53ad06f57176f860aed007890ef3ac67d`.

<a id="symbol-f5e85dbfeb8a87ab432a1908"></a>

## builtin.visual / console

Identity: `claim:52a41f4c247955848764e413`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-17`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:Text.console(); g018-declaration:Int.console(); g018-declaration:Bool.console(); g018-declaration:console.title(text); g018-declaration:console.clear(); g018-declaration:console.append(value, style); g018-declaration:console.style(range, style); g018-declaration:console.scrollToEnd(); g018-declaration:console.size(); g018-declaration:console.close() Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:append-29,console:append-False,console:append-next,console:clear,console:clear-append,console:clear-preserves-title,console:clear-removes-color,console:close,console:publish--29,console:publish-17,console:publish-False,console:publish-Olá €,console:publish-True,console:publish-changed,console:publish-seed,console:scroll-13,console:scroll-3,console:scroll-6,console:size-13,console:size-3,console:size-6,console:software-wrap-29,console:style-clear,console:style-dynamic-endpoints,console:style-exclusive-0-1,console:style-exclusive-1-2,console:style-inclusive-0-1,console:styled-append-bool,console:styled-append-int,console:styled-append-text,console:title-13-change,console:title-6-report,console:title-loop`.

Misuse/diagnostic/failure proofs: `console:append-float,console:append-missing,console:bare-color,console:clear-arity,console:close-arity,console:close-clear,console:close-publish,console:close-twice,console:scroll-arity,console:size-arity,console:style-bound--1-1,console:style-bound-0-3,console:style-bound-2-1,console:style-endpoint-type,console:style-role-type,console:style-selection-arity,console:style-selection-type,console:styled-append-arity,console:styled-append-type,console:title-limit,console:title-type`. API fingerprint: `dfd436d490018b53a1dde906ad2e67cf4957121409d069cb336d785a8e6033db`.

<a id="symbol-271b8265c2e7389dbf9fabe8"></a>

## builtin / get

Identity: `claim:5347bdcdb00f42f1c3a42a72`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.get(91).unwrapOr(67).return;}

```

Explicit process status: `67`; the full value/effect oracle is `dict:absent`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.get; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:absent,dict:bound-reference,dict:lookup-29-53-17,dict:present`.

Misuse/diagnostic/failure proofs: `dict:released-reference-read`. API fingerprint: `0e64ffdc53335d7d71b973a100d05b77a53d401f8363cc4f2bc377ec36c0fa6b`.

<a id="symbol-169b1f1c9d76fc8eced344ec"></a>

## builtin / Text or Int or Bool.toText

Identity: `claim:53b7c6e2a6265fa7564fb599`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){true.toText().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `textparse:bool-text-True`.

### Types, ownership, effects, limits and errors

toText(); 4096 generated output bytes; owned for Text/Int static for Bool; actual Result value/tag/fallback; native checked output is committed only on Ok; live keyword table; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Explicit parse result; preserve checked numeric domain and Unicode policy

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/numeric_safety_semantic.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_parse.asm`.

Positive/changed-value/boundary proofs: `textparse:text-copy-2,textparse:int-text--29,textparse:int-text--9223372036854775808,textparse:bool-text-True,textparse:bool-text-False,textparse:bound-conversion,textparse:conversion-capacity-4096,textparse:conversion-stack-budget`.

Misuse/diagnostic/failure proofs: `textparse:arity-toText`. API fingerprint: `e92959ecba6449d9bb21143f1cf8c5939847c9edfb61c61306490e5da64aea49`.

<a id="symbol-98974fddf31cc59237225fe3"></a>

## builtin / partitionCount

Identity: `claim:53cf67a22ac4477f89f4005b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:dataset-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Dataset.partitionCount; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-return-23,data:dataset-scan-eight`.

Misuse/diagnostic/failure proofs: `data:dataset-partition-count-arity`. API fingerprint: `5cb3b259e3073c95f265bf974c6f9556e178e47fb0fdd5cafe283bc37ea5ff43`.

<a id="symbol-b287bb3f31eac4f239678a7b"></a>

## builtin / Text.isAscii

Identity: `claim:547467641629e92203b10bf1`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".isAscii().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:isAscii-empty`.

### Types, ownership, effects, limits and errors

isAscii(); whole input; O(n); ; borrowed immutable receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:isAscii-empty,text:isAscii-4e65626f`.

Misuse/diagnostic/failure proofs: `text:arity-isAscii`. API fingerprint: `123cf04c2076708381c2f77cf4d9c66b30533511dec013892f660cfedfbd67b9`.

<a id="symbol-023d9005404c03ac632aa198"></a>

## builtin.visual / scrollToEnd

Identity: `claim:549e7059025a971fcc365dc9`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"abc".console().view;view.scrollToEnd();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:scroll-3`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:console.scrollToEnd() Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:scroll-13,console:scroll-3,console:scroll-6`.

Misuse/diagnostic/failure proofs: `console:scroll-arity`. API fingerprint: `8fa521fcfaf0d36d71a987b4e7fc7f90047fe109dcebd3ea31cf0438f3310a4e`.

<a id="symbol-1dc1670b5bea2c3c7410aaa9"></a>

## builtin.ScanPlan / autocomplete

Identity: `claim:54ad79f5f8bd26f18fe4bb4d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.autocomplete(["ação","Nebo"]),.mock("a")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:autocomplete-a-ação`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:autocomplete; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:autocomplete-Port-Portugal,scan:autocomplete-Port-Portuguese,scan:autocomplete-a-ação`.

Misuse/diagnostic/failure proofs: `scan:autocomplete-ambiguous,scan:autocomplete-absent,scan:choice-type-autocomplete`. API fingerprint: `368b047dd503c33cf8d5ee118c6f8fb5bbf39a31d5eb0fc66078719288565f72`.

<a id="symbol-33127c562f762ad4fa435362"></a>

## std.math / abs

Identity: `claim:55290ab94bb4dfe62eb3d860`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
// Independent atomic runtime case.
start() { std.math.abs(-37).return; }

```

Explicit process status: `37`; the full value/effect oracle is `math-abs`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math-abs`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `3198071bd01a368dad3cd8731d579cbe47f7295edf7e295fd6b0bfb6344c3de1`.

<a id="symbol-a30347bd0f2bd3936d2c5085"></a>

## builtin / rowCount

Identity: `claim:55dee89308056e33be8c40fb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([])),Tuple.of("score",Column<Int>.from([]))]).t;t.rowCount().console();t.columnCount().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-dimensions-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.rowCount; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-dimensions-empty,data:table-dimensions-limit,data:table-dimensions-one`.

Misuse/diagnostic/failure proofs: `data:table-row-count-arity`. API fingerprint: `2f3ef2d8e40706a6d99673c8a64033ef809999a80c90f591c46abbdd393f67ca`.

<a id="symbol-f3aaf3cb3fa0806bf1d62928"></a>

## builtin / Tree

Identity: `claim:5627492c9ec2cab409b18eaf`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(71).a;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree<Int>.new,g009-public:Tree.root,g009-public:Tree.addChild,g009-public:Tree.removeSubtree,g009-public:Tree.parent,g009-public:Tree.children,g009-public:Tree.height,g009-public:Tree.preorder,g009-public:Tree.postorder,g009-public:Tree.lowestCommonAncestor; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-capacity,relation:tree-capacity-plus-one,relation:tree-child,relation:tree-child-order,relation:tree-foreign-parent,relation:tree-height-empty,relation:tree-height-root,relation:tree-lca,relation:tree-parent,relation:tree-postorder,relation:tree-preorder,relation:tree-removed-count,relation:tree-return-23,relation:tree-root-parent,relation:tree-stale-slot-reuse,relation:tree-two-live`.

Misuse/diagnostic/failure proofs: `relation:tree-children-node-type,relation:tree-copy,relation:tree-height-arity,relation:tree-lca-node-type,relation:tree-parent-node-type,relation:tree-payload-type,relation:tree-postorder-arity,relation:tree-preorder-arity,relation:tree-raw-handle,relation:tree-removeSubtree-node-type,relation:tree-root-arity,relation:tree-type,relation:tree-wrong-child-value`. API fingerprint: `39c424265d3ad95709c9a639baf637798df1a3c1189c54de4c783b6d9d72d3cc`.

<a id="symbol-6d7662eca39c5ce1f30d25cc"></a>

## std.core / Result

Identity: `claim:5650d741e3336cb96ad15195`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Result<Int,Int>(Ok(17)).x;x.get().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `optionresult:result-value-17-return-23`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Typed active variant and structured errors; bounded combinators; explicit ownership; no arbitrary callback promise

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/option_result_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/scalars/option_result_runtime.asm`.

Positive/changed-value/boundary proofs: `optionresult:result-value-17-return-23,optionresult:result-err-map-err,optionresult:result-is-err,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `optionresult:result-or-else-type`. API fingerprint: `17f6d978a39dd7b48fd55cba9f3ff2770d5d68ecbd694395658d454f6d2bd8b8`.

<a id="symbol-5c65d20213197f992d0cbd6e"></a>

## builtin / batch

Identity: `claim:579eeed18af4e5b64384372f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stream<Int>.from([17,29,71]).s;s.batch(2).b;b.next().v;v.expect("v");v.expect("again");23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:batch-option-consumed-twice`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Stream.batch,g010-public:Stream.batch.next,g010-public:Stream.batch.sink; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:batch-changed-7,data:batch-empty-1,data:batch-filter-map-values,data:batch-lazy,data:batch-limit-64,data:batch-many-2,data:batch-option-consumed,data:batch-option-consumed-twice,data:batch-owned-list-iterator,data:batch-owned-list-map,data:batch-owned-list-mutations,data:batch-remaining-cursor,data:batch-retained-independent,data:batch-sink-changed-64,data:batch-sink-consumed,data:batch-sink-empty-2,data:batch-sink-limit-64,data:batch-sink-many-2,data:batch-sink-trap,data:batch-source-independent,data:batch-two-live`.

Misuse/diagnostic/failure proofs: `data:batch-copy,data:batch-option-copy,data:batch-sink-extra-argument,data:batch-sink-return-value,data:batch-sink-scalar-receiver,data:batch-size-arity,data:batch-size-invalid--1,data:batch-size-invalid-0,data:batch-size-invalid-65,data:batch-size-type`. API fingerprint: `e2d1364d9f36f6b3a5a265ce0fdf1d5c2493a4d8c712d0cba4a9681bea902e0f`.

<a id="symbol-82f97a7e4d73431fdee7a4b8"></a>

## builtin / children

Identity: `claim:57b9a3fd10dc01e0aea508b1`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29);t.addChild(r,71);t.children(r).v;v.length().console();v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-child-order`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.children; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-child-order`.

Misuse/diagnostic/failure proofs: `relation:tree-children-node-type`. API fingerprint: `85cecca08ba14506770c5f6457f4ec10f83adacaa5231fde7187d5b5bb77f411`.

<a id="symbol-8ebb9d97077ef50229540b5c"></a>

## builtin / Text.lower

Identity: `claim:587e6f8343e0bd64c09b5952`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".lower().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:lower-2`.

### Types, ownership, effects, limits and errors

lower(); 4096 output bytes; ASCII root profile; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:lower-0,text:lower-2`.

Misuse/diagnostic/failure proofs: `text:arity-lower`. API fingerprint: `a1c105fb1b5b013319ae55f8c769cd683c4fdfa024cf59b09b8c443fcd9446ee`.

<a id="symbol-8ee94d4d42e618380ad4c450"></a>

## builtin.Vector / mean

Identity: `claim:596c8f79dd98c9f8afc299ff`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_STATISTICS_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Float> [1.0,(∞-∞)].a;a.mean().isNaN().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `statistics:nan-propagation-mean`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 lanes, extent 1..64; checked Int sum, native compensated Float sum, explicit population/sample and linearType7 policies; 1536-byte caller workspace isolates conversion/quantile scratch; source values unchanged; native domain/overflow traps and NaN propagation are observed; no source report or synthetic aggregate Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `statistics:Int-0-mean-none,statistics:Float-0-mean-none,statistics:Float-1-mean-none,statistics:two-live-statistics,statistics:return-0,statistics:return-255,statistics:ordinary-function,statistics:nan-propagation-mean`.

Misuse/diagnostic/failure proofs: `statistics:arity-mean,statistics:float-status`. API fingerprint: `9a7329331d7e2028ead18abd3cf893e9cf9726d78780c931275bde95de0bff7b`.

<a id="symbol-963e013d1175f24b88584bbb"></a>

## builtin.visual / summarize

Identity: `claim:5bc5f510dfda3d787fe58416`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){visual.summarize(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:visual-summary-0-23`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:visual.summarize(value) Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:visual-summary-0-23,console:visual-summary-255-255,console:visual-summary-83-23,console:visual-summary-dynamic,console:visual-summary-once,console:visual-summary-two-values`.

Misuse/diagnostic/failure proofs: `console:visual-summary-arity,console:visual-summary-range--1,console:visual-summary-range-256,console:visual-summary-shadow,console:visual-summary-type`. API fingerprint: `74b8e81c7e7d8b60c482129f63908b3edeec02311acb1c705c5abdd7a1e8f365`.

<a id="symbol-3fa273ccb3fdeb1b9b11a23f"></a>

## builtin.ScanPlan / redact

Identity: `claim:5c4067090d3eb3b35c41ae27`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.redact(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-4-0-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:redact; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-4-0-4,scan:security-4-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-redact`. API fingerprint: `704af8218df286351ff0eb2356b500a623881994a36f8ce21c101665d1c629f4`.

<a id="symbol-f1d834ae79a0a8e24706b4f5"></a>

## builtin / degree

Identity: `claim:5c562dd249bf5aac06fc39e8`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(r,71).b;r.degree().console();a.degree().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-degree`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Node.degree; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-degree,relation:tree-degree`.

Misuse/diagnostic/failure proofs: `relation:node-degree-arity`. API fingerprint: `17a82cb71f2b3f0cfec5e4d2ea18692a69e3e5f29349673a312c6f0ef5619eee`.

<a id="symbol-ee34fc1c7cf0f7230c943917"></a>

## std.fs / File

Identity: `claim:5d47e5dcd877ab58dedc8c74`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `STDLIB_DECLARATION_TO_TYPED_NATIVE_FILESYSTEM`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
import "std.fs" { Path; File; }.fs;
start(){Path.parse("first.txt").p;File.readText(p,utf8,64).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `filesystem:read-path-first.txt`.

### Types, ownership, effects, limits and errors

Actual nominal immutable Path, canonical native normalize/join/isAbsolute/fileName owners; 4096-byte paths, 64 components, 255-byte components. File.readText/writeText consume typed Path and Text, explicit utf8 and truncate/append/atomic policies, at most 4096 bytes. Native File capability is restricted to the execution directory through openat2 beneath/no-symlink resolution. Every file and root descriptor closes; private filesystem byte effects and 512 repeated reads are independent observations. This is the material convenience profile, not a claim for arbitrary live handle or directory APIs. Caller-owned beneath directory; no symlinks; UTF-8; 4096-byte paths, 64 components, 255-byte component; atomic write

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: filesystem. Capabilities: PRIVATE_DIRECTORY.

Native chain: `compiler/semantic/system/g011_source_vertical.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `filesystem:read-0,filesystem:read-3,filesystem:read-4,filesystem:read-path-first.txt,filesystem:read-path-second.txt,filesystem:write-truncate-1,filesystem:write-append-1,filesystem:write-atomic-1,filesystem:write-created,filesystem:read-close-loop,filesystem:read-return-255`.

Misuse/diagnostic/failure proofs: `filesystem:read-path-type,filesystem:read-limit-type,filesystem:encoding-policy,filesystem:encoding-shadow,filesystem:write-value-type,filesystem:write-mode,filesystem:file-domain-missing,filesystem:file-domain-encoding,filesystem:file-domain-bound,filesystem:file-domain-absolute`. API fingerprint: `410fc0e43b72019b38a2320bf17c15418c325226dbd9837e1533bf6f3ba08bef`.

<a id="symbol-a993882ea7b0ca9ed43b4710"></a>

## builtin / validateInvariants

Identity: `claim:5d68b86e20730e28aa956938`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.validateInvariants().console();d.insert(17,53);d.validateInvariants().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:invariants`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.validateInvariants; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:invariants`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `e853b025d58749d37af2cbb77b36c33a2e8467969151de499c18d5dfe9abfd6c`.

<a id="symbol-ecb07597fbd1754d0ae1815e"></a>

## builtin / insert

Identity: `claim:5e73e28d4202b9aaa2f0b803`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.insert(17,71);d.length().return;}

```

Explicit process status: `1`; the full value/effect oracle is `dict:duplicate-length`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.insert,g008-public:Set.insert; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:duplicate-length,dict:duplicate-new-value,dict:duplicate-old-value,dict:set-insert-remove`.

Misuse/diagnostic/failure proofs: `dict:insert-key-type,dict:insert-value-type,dict:set-insert-type`. API fingerprint: `5fc07bca8c429716daa6f44830997aeeafd3621464b640d2bbb4613fb2022f09`.

<a id="symbol-3a84598915cd5e6f4f87bdc0"></a>

## builtin / Text.lastIndexOf

Identity: `claim:5eff28a6e44e3a21a5fe4063`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".lastIndexOf("absent").unwrapOr(-1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:lastIndexOf-2`.

### Types, ownership, effects, limits and errors

lastIndexOf(Text); byte index; O((n-m+1)m); ; borrowed inputs; caller-owned Option slot; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:lastIndexOf-0,text:lastIndexOf-2`.

Misuse/diagnostic/failure proofs: `text:type-lastIndexOf`. API fingerprint: `d16fbd2ff0818036981ac91702472cb60a085da8cbf7218deec401d369f9b719`.

<a id="symbol-679e881a761824b35f3603ef"></a>

## builtin / from

Identity: `claim:605839f798b2d5e2acbe2ece`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([17,29]).c;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Row.from,g010-public:Stream<Int>.from,g010-public:Column<Int>.from; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-list,data:column-list-snapshot,data:column-return-23,data:column-sum-empty,data:column-sum-many,data:column-two-live,data:row-eight-fields,data:row-return-23,data:row-two-live,data:row-values--31-53,data:row-values-17-71,data:row-values-evaluated-once,data:stream-from-list,data:stream-list-snapshot,data:stream-next,data:stream-next-empty,data:stream-sink-limit,data:stream-two-live`.

Misuse/diagnostic/failure proofs: `data:column-copy,data:column-element-type,data:column-from-type,data:column-mixed-type,data:column-value-type,data:row-copy,data:row-empty-values,data:row-missing-nonnullable,data:row-schema-type,data:row-too-few,data:row-too-many,data:row-value-float,data:row-value-type,data:stream-copy,data:stream-element-type,data:stream-from-type,data:stream-value-type`. API fingerprint: `bfe9503548bef544ebb0084d89ea6dbb12a371ef7201486b299da53d385f4db0`.

<a id="symbol-3e0796a025fbd47cc6cbfc2e"></a>

## builtin / Text.parseInt

Identity: `claim:60d1fd8b101110462a0f41f7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"7".parseInt().unwrapOr(17).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `textparse:parse-int-7`.

### Types, ownership, effects, limits and errors

parseInt(); decimal signed i64; Result by value; actual Result value/tag/fallback; native checked output is committed only on Ok; live keyword table; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Explicit parse result; preserve checked numeric domain and Unicode policy

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/numeric_safety_semantic.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_parse.asm`.

Positive/changed-value/boundary proofs: `textparse:parse-int-7,textparse:parse-int--29,textparse:parse-int-9223372036854775807,textparse:parse-int--9223372036854775808,textparse:parse-int-error-3,textparse:two-live-results,textparse:bound-input`.

Misuse/diagnostic/failure proofs: `textparse:arity-parseInt,textparse:fallback-type-Int`. API fingerprint: `0af36e703eacbe9c6efa9e741ae854094f0a53c752ad852491d98877cde7fbbc`.

<a id="symbol-66b9d9ac06f0460993548a66"></a>

## builtin.ScanPlan / sensitive

Identity: `claim:6173cc5395595d52edab0b9e`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.sensitive(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-64-0-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:sensitive; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-64-0-4,scan:security-64-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-sensitive`. API fingerprint: `742472f38d811a5ee87f634ea5859352bde124836acc86cae431ee820897068b`.

<a id="symbol-579962318f79a610db4a866e"></a>

## builtin / schema

Identity: `claim:61aa7b5cbbbb8c070ae1ba1e`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;d.schema().s;s.field("age").at<0>().console();s.indexOf("age").expect("v").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:dataset-schema`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Dataset.schema; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-schema,data:dataset-schema-independent`.

Misuse/diagnostic/failure proofs: `data:dataset-schema-arity`. API fingerprint: `bd2cc15c40f0d59b19b512ae03246f593d57a8649d48ba26ad2e3d4e70969e24`.

<a id="symbol-14988a44444079ac4565d2f7"></a>

## builtin.ScanPlan / paragraph

Identity: `claim:620b2733fc66bb9048954ffb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.paragraph()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-paragraph-stdin`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:paragraph; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-paragraph-mock,scan:multiline-paragraph-stdin,scan:multiline-trace-paragraph`.

Misuse/diagnostic/failure proofs: `scan:multiline-mode-conflict`. API fingerprint: `2a076d2fff503779768a41bc41e832a72b17d44e8ac9f4e45bd97daa04a636bd`.

<a id="symbol-e0df3a8038fb75b7e454bf03"></a>

## builtin / union

Identity: `claim:641aec17a589f5dddf4bd6d8`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.union(b).c;c.length().return;}

```

Explicit process status: `3`; the full value/effect oracle is `dict:set-union`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Set.union; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-union,dict:set-union-membership`.

Misuse/diagnostic/failure proofs: `dict:set-union-type`. API fingerprint: `2f37a304fb66da1e739de7932177be7e45f4b7fdc95dc1cdafa8c8543dd3095b`.

<a id="symbol-2876582f7dca766c71d27669"></a>

## builtin / removeNode

Identity: `claim:66f55df88e329e6f7b3845b5`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(71).b;g.addEdge(a,b,5).e;g.removeNode(b);g.neighbors(a).length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `relation:graph-directed-remove-node`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.removeNode; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-remove-node,relation:graph-undirected-remove-node`.

Misuse/diagnostic/failure proofs: `relation:graph-removeNode-node-type`. API fingerprint: `11a9b703b43c6114f8ae0b6950f3d4dbf5b9fe31e8e40bc6e27acedde40c54d2`.

<a id="symbol-d5f1c90194a9dc923b306063"></a>

## builtin.Random / shuffle

Identity: `claim:672cbc44e1fd06f50a7411eb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_RANDOM_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Random.seed(17).r;Array<Int,4> [17,29,43,71].a.mutable;a.asSlice().s;r.shuffle(s);s.at(0).v0;s.at(1).v1;s.at(2).v2;s.at(3).v3;"${v0}/${v1}/${v2}/${v3}".console();0.return;}
```

Explicit process status: `0`; the full value/effect oracle is `random:shuffle-return-0`.

### Types, ownership, effects, limits and errors

Explicit SplitMix64 v1 state, no entropy capability; native finite-domain distributions and categorical Float Vector weights; nonnegative Int uniform; shuffle of unique local mutable Int Slice at most 64 lanes; lexical creation/release provenance, separate descriptor/payload frames; sample of Int Vector/local Slice returns canonical List with 0..16 entries; independent seed/value/order oracles and rejected effectful interpolation; no whole-source report Independent deterministic seeded states; checked intervals and distribution domains; no cryptographic entropy claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `random:shuffle-17-17-4,random:shuffle-29--31-4,random:shuffle-two-live-owners,random:shuffle-before-later-view,random:shuffle-after-alias-release,random:shuffle-before-own-release,random:shuffle-return-0,random:shuffle-return-255`.

Misuse/diagnostic/failure proofs: `random:shuffle-after-release,random:shuffle-two-aliases,random:shuffle-readonly,random:shuffle-arity`. API fingerprint: `54d31ed5a3970ff53b456d8ff3cac67ab85b2dabf1ec6df1fe03731fbe9b45f2`.

<a id="symbol-6843eaebd37589bf5e2560ab"></a>

## builtin / hash

Identity: `claim:67b6d4ca4484c9e25d4b86e1`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Hasher.new(17).h;53.hash(h);h.finish().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:hasher-int`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Int.hash,g008-public:hash.randomized,g008-public:hash.deterministic; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:hasher-int,hashpolicy:deterministic-seed,hashpolicy:deterministic-signed,hashpolicy:deterministic-zero,hashpolicy:randomized,hashpolicy:reset-deterministic`.

Misuse/diagnostic/failure proofs: `dict:int-hash-type,hashpolicy:randomized-arity,hashpolicy:seed-missing,hashpolicy:seed-type`. API fingerprint: `ab6f40574eadd65f138b083d9c3284a969a64d2397afc6cce2b79b50870acfaa`.

<a id="symbol-caf77c8e6e8117844a79d841"></a>

## std.console / scan

Identity: `claim:6937d35c35869eaea1bf0bc4`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("23")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-23-return-23`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:int-23-return-23,scan:text-4e65626f,scan:bool-False,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `scan:mock-type`. API fingerprint: `282e5ac764155f002b23364db0bcca3e52cbb13dd8975726b606f8a9cb9c715d`.

<a id="symbol-2c42a88c8b361773a3874b38"></a>

## builtin.ScanPlan / confirm

Identity: `claim:6a2d6ca2a076aba49e66d2b6`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.confirm(),.mock("no")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-confirm-no`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:confirm; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-confirm-yes,scan:choice-confirm-no`.

Misuse/diagnostic/failure proofs: `scan:choice-confirm-invalid`. API fingerprint: `d9c8d0471adba71a8e7f071f8f534c383ccd891916935731f002540b2957e520`.

<a id="symbol-716e664c3f26d15b80031b9c"></a>

## builtin / shortestPathUnweighted

Identity: `claim:6bb554d0f4e59db5aeb18326`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.shortestPathUnweighted(d,a).v;v.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `relation:graph-unreachable`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.shortestPathUnweighted; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-foreign-path,relation:graph-shortest,relation:graph-unreachable`.

Misuse/diagnostic/failure proofs: `relation:graph-shortestPathUnweighted-nodes-type`. API fingerprint: `bd8c7eceee8cee7718aa14fc6a3022a8a480ae8d75898602a15a53777eb4a0e5`.

<a id="symbol-5bd4b12beb9cc592c26eb575"></a>

## builtin.ScanPlan / menu

Identity: `claim:6c23175d9d55314ba5b93297`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.menu(["red","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-variant-menu-red`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:menu; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-variant-menu-red,scan:choice-variant-menu-blue`.

Misuse/diagnostic/failure proofs: `scan:choice-variant-menu-absent,scan:choice-type-menu`. API fingerprint: `22acb8e00f01552bfa08c28e47e7d66574b7ea944f48710ea0b995e128213d4d`.

<a id="symbol-84157922a137a64815393b36"></a>

## builtin.ScanPlan / replay

Identity: `claim:6d58fe076f0a92fb0f968d5d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.replay("answers.txt")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:replay-no-newline-17`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:replay; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:replay-retry-29-2,scan:replay-relative-subdir,scan:replay-no-newline-17`.

Misuse/diagnostic/failure proofs: `scan:replay-missing,scan:replay-limit,scan:replay-parent,scan:replay-absolute`. API fingerprint: `c766c106bd658b72017b2df9f728ea9ec35390b5f57745eb65b373d490a5257f`.

<a id="symbol-f3bcc765e83cfaa8e60ae4f9"></a>

## builtin / new

Identity: `claim:6ef45fa5e187a095aa4bb988`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(71).a;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:node-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Node<Int>.new,g009-public:Tree<Int>.new; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:Node-payload--17,relation:Node-payload-71,relation:node-return-23,relation:tree-capacity,relation:tree-capacity-plus-one,relation:tree-return-23,relation:tree-two-live`.

Misuse/diagnostic/failure proofs: `relation:node-wrong-replacement,relation:tree-copy,relation:tree-payload-type,relation:tree-type`. API fingerprint: `e7b81d88933717ef466788b21992ee09da78fb8df5671b4ac9f225df8cf67d2e`.

<a id="symbol-718c26bd293965982ab6c1cb"></a>

## builtin / Text.concat

Identity: `claim:6f82e8fa7619ca993905dee9`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Ab9".concat("aB9").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:concat-2`.

### Types, ownership, effects, limits and errors

concat(Text); 4096 output bytes; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:concat-0,text:concat-2`.

Misuse/diagnostic/failure proofs: `text:type-concat`. API fingerprint: `14bb254b8bd8734cfbaf4ae066ceb106dc1fff04751e163982d0046e91ad0b92`.

<a id="symbol-bf252df6a37e3beb55785d24"></a>

## std.math / pow

Identity: `claim:6fb6caef89fc6a429f8ac647`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.pow(3.0,4.0);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `math:atomic-pow`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:atomic-pow`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `0a6f2fd696fda9c5a088c6973ac06a0a5a98c965ce965db2af8c709e83722cb2`.

<a id="symbol-103212de3c1ef619531f3322"></a>

## builtin / Text.graphemeCount

Identity: `claim:70a32b2332904c991acd74a9`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"éx".graphemeCount().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:graphemes-2`.

### Types, ownership, effects, limits and errors

Text.() -> Int; 1048576 input bytes; 15.1 Latin Extend/CRLF/ZWJ; borrow receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:graphemes-2,text:graphemes-4`.

Misuse/diagnostic/failure proofs: `text:grapheme-arity`. API fingerprint: `71f6ceb8e58667fe690c2a9257fed5891d4ceb2a3c7d49ed860e953285d3ce03`.

<a id="symbol-ab531825130b3b0ff88fac45"></a>

## builtin / min

Identity: `claim:73678e7a57b1ccfbeadb9513`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([]).c;c.min().console();23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-min-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.min; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-min-changed,data:column-min-empty,data:column-min-many,data:column-missing-extremes`.

Misuse/diagnostic/failure proofs: `data:column-min-arity`. API fingerprint: `876f3ca76d8c28669bc5de66d4376a6790ccfe6aa1891e99fddbc6b6ac2398c1`.

<a id="symbol-dcd5942664ca728377c426b6"></a>

## builtin / Graph

Identity: `claim:73d2cc57c1e6c24f7d38330d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().a;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph<Int,Int>.directed,g009-public:Graph<Int,Int>.undirected,g009-public:Graph.addNode,g009-public:Graph.addEdge,g009-public:Graph.removeNode,g009-public:Graph.removeEdge,g009-public:Graph.neighbors,g009-public:Graph.breadthFirst,g009-public:Graph.depthFirst,g009-public:Graph.hasCycle,g009-public:Graph.topologicalSort,g009-public:Graph.shortestPathUnweighted,g009-public:Graph.dijkstra,g009-public:Graph.connectedComponents; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-acyclic,relation:graph-breadthFirst,relation:graph-capacity,relation:graph-capacity-plus-one,relation:graph-components,relation:graph-components-directed,relation:graph-components-empty,relation:graph-components-joined,relation:graph-components-snapshot,relation:graph-cycle,relation:graph-depthFirst,relation:graph-dijkstra-19-2,relation:graph-dijkstra-31-3,relation:graph-dijkstra-5-11,relation:graph-dijkstra-overflow,relation:graph-directed-neighbors,relation:graph-directed-remove-edge,relation:graph-directed-remove-node,relation:graph-directed-reverse,relation:graph-duplicate-edge,relation:graph-foreign-node,relation:graph-foreign-path,relation:graph-negative-weight,relation:graph-return-23,relation:graph-self-edge,relation:graph-shortest,relation:graph-topological,relation:graph-topological-cycle,relation:graph-two-live,relation:graph-undirected-neighbors,relation:graph-undirected-remove-edge,relation:graph-undirected-remove-node,relation:graph-undirected-reverse,relation:graph-unreachable,relation:traversal-break,relation:traversal-continue,relation:traversal-return,relation:traversal-scope,relation:traversal-temporary`.

Misuse/diagnostic/failure proofs: `relation:graph-add-node-type,relation:graph-breadthFirst-node-type,relation:graph-connectedComponents-arity,relation:graph-depthFirst-node-type,relation:graph-dijkstra-nodes-type,relation:graph-edge-arity,relation:graph-edge-type,relation:graph-hasCycle-arity,relation:graph-missing-edge-type,relation:graph-neighbors-node-type,relation:graph-raw-handle,relation:graph-remove-edge-type,relation:graph-removeNode-node-type,relation:graph-shortestPathUnweighted-nodes-type,relation:graph-topologicalSort-arity,relation:graph-wrong-weight,relation:traversal-borrow,relation:traversal-copy`. API fingerprint: `a1d9503d50f667044de6f3d7de40098b29f7c61b5801b833d2c84a98861a7bf5`.

<a id="symbol-19aed3ad00c267a613c0084d"></a>

## builtin.Vector / normalize

Identity: `claim:744c77a8fc81f2e310d00d07`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_VECTOR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Float> [3.0,4.0].a;a.normalize().b;"${a.at(0):fixed(2)}/${b.at(0):fixed(2)}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `vector:source-preserved-after-normalize`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 vector lanes; extent 1..64; typed filled/literal constructors; native vector algorithms and separate owned frames; exact shape/type checks; constant index bounds and dynamic native checks; linked scalar spills preserve RHS allocations; no whole-source report; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `vector:normalize-Float-3.0-4.0,vector:normalize-Float-5.0-12.0,vector:source-preserved-after-normalize,vector:native-storage-failure-atomicity`.

Misuse/diagnostic/failure proofs: `vector:method-normalize-empty,vector:float-method-normalize`. API fingerprint: `b6cea56d40370e9d857e64ebc61e3a76a0a041981825114f0dba1f0c6b35d69c`.

<a id="symbol-53efa9737b317a4e1a72c251"></a>

## builtin.ScanPlan / multiSelect

Identity: `claim:7469642aa07e1e30b24b32d0`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.multiSelect(["red","green","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multi-select-3-red`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:multiSelect; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multi-select-3-red,scan:multi-select-3-blue`.

Misuse/diagnostic/failure proofs: `scan:multi-select-duplicate,scan:multi-select-absent,scan:choice-type-multiSelect`. API fingerprint: `b3f9b41e1782a58c5f8109f9f0dea19fd46bb0faf5d42e374efac365ceb4e944`.

<a id="symbol-8c40335b6ff57e6d8923051e"></a>

## builtin / Hasher

Identity: `claim:75a6787eaf379c6879a069c7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Hasher.new(17).h;h.finish().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:hasher-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Hasher.new,g008-public:Hasher.writeBytes,g008-public:Hasher.finish; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:hasher-bytes,dict:hasher-changed-byte,dict:hasher-changed-seed,dict:hasher-empty,dict:hasher-stream`.

Misuse/diagnostic/failure proofs: `dict:hasher-bytes-type,dict:hasher-finish-arity,dict:hasher-seed-type`. API fingerprint: `4fb14444c10e64b898a11de522d51b825e273ae318549cd3e54e2a01189a4585`.

<a id="symbol-cd5c42d82a0490e8ad38e069"></a>

## builtin / containsKey

Identity: `claim:766d1ec0b8bced8b0d79fbb6`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.containsKey(17).console();d.containsKey(29).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:contains`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.containsKey; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:contains`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `4831dea870f939e3612512b803798f89839ba45abdefe60563f5bb3333cdbadd`.

<a id="symbol-06977a2ca4dbc99b60b67322"></a>

## builtin / isSubsetOf

Identity: `claim:76c12b03f3d49b8de281cb2c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.intersection(b).i;i.isSubsetOf(a).console();a.isSubsetOf(b).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:set-subset`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Set.isSubsetOf; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-subset`.

Misuse/diagnostic/failure proofs: `dict:set-isSubsetOf-type`. API fingerprint: `0b683bfe0eeeba71ab9818515ad2ba33eb672ac7f201fd03e3c37ce51bfb2374`.

<a id="symbol-3323568b9e05d5562a515ee9"></a>

## builtin.visual / visual

Identity: `claim:776ba84a7a9af90a944f77da`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){visual.summarize(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:visual-summary-0-23`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:visual.summarize(value) Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:visual-summary-0-23,console:visual-summary-255-255,console:visual-summary-83-23,console:visual-summary-dynamic,console:visual-summary-once,console:visual-summary-two-values`.

Misuse/diagnostic/failure proofs: `console:visual-summary-arity,console:visual-summary-range--1,console:visual-summary-range-256,console:visual-summary-shadow,console:visual-summary-type`. API fingerprint: `68664e1a006b424cee8661368524411a776f83f46845fa869cda5489a7cbdaab`.

<a id="symbol-49d62e8411e01eb78eff031d"></a>

## builtin / removeSubtree

Identity: `claim:792cb04d29a9070ae6b61069`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(a,71);t.removeSubtree(a).return;}
```

Explicit process status: `2`; the full value/effect oracle is `relation:tree-removed-count`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.removeSubtree; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-removed-count,relation:tree-stale-slot-reuse`.

Misuse/diagnostic/failure proofs: `relation:tree-removeSubtree-node-type`. API fingerprint: `a3aba7cc55a193725d731ba6736e5ce3219afa6c4974b4bfa86e6b4501f56ea1`.

<a id="symbol-6e71d864dd306bb8d6d4c2e6"></a>

## builtin / root

Identity: `claim:79eadfe5d0e112c7b8276900`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.addChild(t.root(),71).n;n.value().return;}
```

Explicit process status: `71`; the full value/effect oracle is `relation:tree-child`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.root; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-child,relation:tree-root-parent`.

Misuse/diagnostic/failure proofs: `relation:tree-root-arity`. API fingerprint: `8624d7f49025aa52176aa3f0169d60dfd0c1d411b1ed9b84664604cbf9baec28`.

<a id="symbol-7b004136553b4fd15114fec3"></a>

## builtin / Text.padEnd

Identity: `claim:7af40ce94be0b71e6cf6264b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"A".padEnd(6,"ab").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:pad-end-4`.

### Types, ownership, effects, limits and errors

padEnd(Int,Text); 4096 output bytes; byte width; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:pad-end-0,text:pad-end-4`.

Misuse/diagnostic/failure proofs: `text:type-padEnd`. API fingerprint: `b6fe3bb1cb0ab35e6f6acf8e86be09c311555a2926bca00e929c23290da1cbaf`.

<a id="symbol-353a847643d5ae5132a27ee9"></a>

## builtin.ScanPlan / until

Identity: `claim:7ca242ad78c0757e4c01f045`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.multiline(),.until("STOP")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-until-stdin`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:until; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-until-mock,scan:multiline-until-replay,scan:multiline-until-stdin`.

Misuse/diagnostic/failure proofs: `scan:multiline-empty-delimiter,scan:multiline-type-until`. API fingerprint: `fb57ef28f4124e949d3a4d7edd027c9aff831e88d4753e8d6a979c70506f0279`.

<a id="symbol-c0fd8e11a851f7e48de58044"></a>

## std.core / Error

Identity: `claim:7ca8c7c0b07be6c44e825420`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Error(7101,0,50,2,8,0).root;root.withContext("line\nnext").contextual;contextual.message().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `optionresult:error-message-0-9`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Typed active variant and structured errors; bounded combinators; explicit ownership; no arbitrary callback promise

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/option_result_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/scalars/option_result_runtime.asm`.

Positive/changed-value/boundary proofs: `optionresult:error-message-0-9,optionresult:error-two-messages,optionresult:error-diagnostic-span,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `optionresult:error-invalid-diagnostic-span`. API fingerprint: `c83962369a0fcb0cec31f347cad93b22127879deb692fd41b8a7c5439c1fe78f`.

<a id="symbol-e8d1adf3b3ca4771ac5fba71"></a>

## builtin / Text.startsWith

Identity: `claim:7d801be18c4ca393da223ca0`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".startsWith("absent").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:startsWith-2`.

### Types, ownership, effects, limits and errors

startsWith(Text); literal bytes; O(m); ; borrowed immutable receiver and argument; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:startsWith-0,text:startsWith-2`.

Misuse/diagnostic/failure proofs: `text:type-startsWith`. API fingerprint: `9f168a8c78e81273095f23a66740e0eb632536b1024a4c06022b0ae83cca18fa`.

<a id="symbol-f9e6e3541ec347bc816c587a"></a>

## builtin.ScanPlan / view

Identity: `claim:7f24f3ab7bde1402db6a4ab0`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.form("profile"),.field("age","Int"),.view("entry"),.panel("main"),.feedback(),.style("blue"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-metadata-profile`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:view; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings`.

Misuse/diagnostic/failure proofs: `scan:option-type-view`. API fingerprint: `6cfd1166c5cfe05939c9bb4915baf2924cddcff4bfeeb50a99fd56d6d4d884f8`.

<a id="symbol-308eba156d67e25ef6d347a7"></a>

## builtin.ScanPlan / form

Identity: `claim:7fe154d8b77af9baa35135bb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.form("profile"),.field("age","Int"),.view("entry"),.panel("main"),.feedback(),.style("blue"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-metadata-profile`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:form; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings`.

Misuse/diagnostic/failure proofs: `scan:option-type-form`. API fingerprint: `d78a9591c3067ba1ddda798461da616eb34c822c7072252539b9d6dd97c7134f`.

<a id="symbol-bfc2602b2e5254ccf305f007"></a>

## builtin / Text.normalizeNfd

Identity: `claim:7fe225c46198adab3c4282f7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".normalizeNfd().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:normalizeNfd-0`.

### Types, ownership, effects, limits and errors

Text.() -> Text; 4096 public workspace; 15.1 Latin acute canonical pair; caller-owned result; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:normalizeNfd-0,text:normalizeNfd-2`.

Misuse/diagnostic/failure proofs: `text:arity-normalizeNfd`. API fingerprint: `d37877a83ae7b451978fbe9cad3a92cd9b3a5dc875e5e87c5556adf7f8d67ba0`.

<a id="symbol-f4c958f0f099364764b96169"></a>

## std.core / Ordering

Identity: `claim:8077650a5870b943da633456`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17<=>17).order;(order==(0<=>0)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `operator:ordering-17-17-0-0`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `operator:ordering-17-29--1-0,operator:ordering-17-17-0-0,operator:ordering-29-17-1-0,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `operator:ordering-type`. API fingerprint: `c952f88ead7fcf132db59847506304f3250522c3713ad50386dacaee1e59473f`.

<a id="symbol-0641af1159d81e10ba789d6f"></a>

## builtin / scan

Identity: `claim:814a06fd95b3f3717e0e707b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("age").unwrapOr(89).console();true.return;}(Table.self)consume(){self.rowCount().console();self.filter(observe);}start(){List<Int>.new().a;Table.fromColumns([Tuple.of("age",Column<Int>.from([17,a.get(0)]))]).t;Dataset.fromTables([t]).d;d.scan().s;s.sink(consume).return;}
```

Explicit process status: `1`; the full value/effect oracle is `data:dataset-scan-missing`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Dataset.scan,g010-public:Dataset.scan.sink; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-scan-changed,data:dataset-scan-lazy,data:dataset-scan-many,data:dataset-scan-missing,data:dataset-scan-next,data:dataset-scan-row-limit,data:dataset-scan-sink-consumed,data:dataset-scan-sink-remainder,data:dataset-scan-two-live`.

Misuse/diagnostic/failure proofs: `data:dataset-scan-callback-trap,data:dataset-scan-copy,data:dataset-scan-option-copy,data:dataset-scan-option-twice,data:dataset-sink-extra-argument,data:dataset-sink-receiver-type,data:dataset-sink-return-type`. API fingerprint: `66578e1ed41388ca1fd1791f96c0144e0fc35a93df7b70bcb9d8c53ca8a2918a`.

<a id="symbol-7b6bc71b31bf58265cd3580f"></a>

## builtin.visual / append

Identity: `claim:81f88d5ea73328af2efaa0de`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.append(29);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:append-29`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:console.append(value, style) Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:append-29,console:append-False,console:append-next,console:styled-append-bool,console:styled-append-int,console:styled-append-text`.

Misuse/diagnostic/failure proofs: `console:append-float,console:append-missing,console:styled-append-arity,console:styled-append-type`. API fingerprint: `7252cd295bb49d9e1ada9a1182724e1afd94246dc74947f20fe7942a2c747451`.

<a id="symbol-e5274be2905f0b648b382a6b"></a>

## builtin / sink

Identity: `claim:8425036f3bb061a9469c15d6`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)emit(){self.console();}start(){Stream<Int>.from([]).s;s.sink(emit).return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:stream-sink-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.groupBy.sink,g010-public:Dataset.scan.sink,g010-public:Stream.sink,g010-public:Stream.batch.sink; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:batch-sink-changed-64,data:batch-sink-consumed,data:batch-sink-empty-2,data:batch-sink-limit-64,data:batch-sink-many-2,data:batch-sink-trap,data:dataset-scan-many,data:dataset-scan-row-limit,data:dataset-scan-sink-consumed,data:group-limit,data:group-many,data:group-sink-cursor,data:stream-consume-once,data:stream-sink-changed,data:stream-sink-empty,data:stream-sink-limit,data:stream-sink-many,data:stream-sink-one`.

Misuse/diagnostic/failure proofs: `data:batch-sink-extra-argument,data:batch-sink-return-value,data:batch-sink-scalar-receiver,data:dataset-scan-callback-trap,data:dataset-sink-extra-argument,data:dataset-sink-receiver-type,data:dataset-sink-return-type,data:group-sink-type,data:stream-sink-batch-receiver,data:stream-sink-return-type`. API fingerprint: `c6c1969b72d28be8b849b1312b4c070c1b5a937215f70b7de5af1d8c1cf30cb1`.

<a id="symbol-7c238573baf91d98a6fb21b3"></a>

## builtin / addEdge

Identity: `claim:850b86728cdc67cead69c7dd`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addEdge(a,a,5);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `relation:graph-self-edge`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.addEdge; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-neighbors,relation:graph-duplicate-edge,relation:graph-foreign-node,relation:graph-negative-weight,relation:graph-self-edge`.

Misuse/diagnostic/failure proofs: `relation:graph-edge-arity,relation:graph-raw-handle,relation:graph-wrong-weight`. API fingerprint: `bc99c88686ba59e9c25bcaac54172a5d4a8341350600afda47ced911d8524b50`.

<a id="symbol-a96d657e6c44d1412553774e"></a>

## builtin.Vector / add

Identity: `claim:85b7b475a9f317b026a7b687`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_VECTOR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,4>.filled(17).a;Vector<Int,4>.filled(29).b;a.add(b).c;c.at(2).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `vector:add-17-29`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 vector lanes; extent 1..64; typed filled/literal constructors; native vector algorithms and separate owned frames; exact shape/type checks; constant index bounds and dynamic native checks; linked scalar spills preserve RHS allocations; no whole-source report; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `vector:add-17-29,vector:add-43-29,vector:add-Float-3.0-4.0,vector:source-preserved-after-add,vector:native-storage-failure-atomicity`.

Misuse/diagnostic/failure proofs: `vector:shape-add-Int-3,vector:shape-add-Float-4,vector:method-add-17`. API fingerprint: `d8577ab2da6cd0d851907fac06e955b6ccfbd3423a170b0c1d4cb48e4b86f934`.

<a id="symbol-600b535d381e184c5acedbff"></a>

## builtin / height

Identity: `claim:85e6a241d1d7461f4f73b5d2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.height().return;}
```

Explicit process status: `1`; the full value/effect oracle is `relation:tree-height-root`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.height; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-height-empty,relation:tree-height-root`.

Misuse/diagnostic/failure proofs: `relation:tree-height-arity`. API fingerprint: `d553eb36284dd800125bd36218acba64bfcb9607dab44df02d8588470597199b`.

<a id="symbol-a6ac15acb37a294718fb3f66"></a>

## builtin / Row

Identity: `claim:86e52c9f8f121bb50e591b94`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;Row.from(s,[17,71]).r;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:row-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Row.from,g010-public:Row.get,g010-public:Row.project,g010-public:Row.toTuple; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:row-eight-fields,data:row-missing-0,data:row-missing-1,data:row-missing-2,data:row-missing-3,data:row-project-changed-order,data:row-project-composition,data:row-project-missing,data:row-project-subset,data:row-return-23,data:row-tuple-order,data:row-tuple-two-live,data:row-two-live,data:row-values--31-53,data:row-values-17-71,data:row-values-29-83,data:row-values-evaluated-once`.

Misuse/diagnostic/failure proofs: `data:row-copy,data:row-empty-values,data:row-get-name-type,data:row-missing-nonnullable,data:row-project-argument-type,data:row-project-duplicate-name,data:row-project-empty,data:row-project-name-type,data:row-project-removed-name,data:row-project-unknown-name,data:row-schema-type,data:row-to-tuple-arity,data:row-too-few,data:row-too-many,data:row-tuple-copy,data:row-tuple-index-bound,data:row-tuple-static-bound,data:row-unknown-name,data:row-value-float,data:row-value-type`. API fingerprint: `c073630aed57045df67e13ce115c100da6ad92a650cbd63bd69b7b7d3f912542`.

<a id="symbol-3e97e225acc4e85bd7d053b6"></a>

## builtin.Vector / scale

Identity: `claim:872db359c13358ce35543c68`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_VECTOR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int> [17,17,43].a;a.scale(29).b;b.at(1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `vector:scale-17-29`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 vector lanes; extent 1..64; typed filled/literal constructors; native vector algorithms and separate owned frames; exact shape/type checks; constant index bounds and dynamic native checks; linked scalar spills preserve RHS allocations; no whole-source report; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `vector:scale-17-29,vector:scale-43-29,vector:scale-Float-3.0-4.0,vector:calls-effects-once,vector:native-storage-failure-atomicity`.

Misuse/diagnostic/failure proofs: `vector:method-scale-true,vector:method-scale-empty,vector:float-method-scale`. API fingerprint: `31498c833c75dcf71dc4df66e95f9704a1fb6df857c4295c4c38ec9da396f44f`.

<a id="symbol-ed361329470619374eb75a58"></a>

## builtin / Text.sliceCodepoints

Identity: `claim:873d46ff74862ba8df902efd`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".sliceCodepoints(0,0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:slice-codepoints-4`.

### Types, ownership, effects, limits and errors

Text.(Int, Int) -> Text; 1048576 input bytes; UTF-8 scalar boundaries; immutable borrowed view; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:slice-codepoints-1,text:slice-codepoints-4`.

Misuse/diagnostic/failure proofs: `text:slice-start-type`. API fingerprint: `52e71556fc02a762a2165de53847a122a3164e81a652a14f8e915d5dae017990`.

<a id="symbol-fb5b1228f0b98d91904674a4"></a>

## builtin.ScanPlan / block

Identity: `claim:875916d78ba5e72544abf539`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.block("END"),.replay("answers.txt")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-block-replay`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:block; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-block-replay,scan:block-preserves-next-stdin,scan:multiline-trace-block`.

Misuse/diagnostic/failure proofs: `scan:multiline-missing-terminator,scan:multiline-type-block`. API fingerprint: `116f7de04bc0d894f20bcf46f565bc29a9af51994c9f4e3ba64252f4a2233e25`.

<a id="symbol-665734f8f10c284a8420d45f"></a>

## builtin / Text.upper

Identity: `claim:88f657a205f8ac766d97907b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".upper().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:upper-2`.

### Types, ownership, effects, limits and errors

upper(); 4096 output bytes; ASCII root profile; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:upper-0,text:upper-2`.

Misuse/diagnostic/failure proofs: `text:arity-upper`. API fingerprint: `ea9125a4ee4e83336f25e5dcaa85942e2a0ba50cb5ec92c6296d8ece20b2f2e2`.

<a id="symbol-694424c54b7fcaef6aff98ad"></a>

## builtin / neighbors

Identity: `claim:892c2cb159efe2f607813a51`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(71).b;g.addEdge(a,b,5).e;g.neighbors(a).v;v.next().expect("neighbor").value().return;}
```

Explicit process status: `71`; the full value/effect oracle is `relation:graph-directed-neighbors`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.neighbors; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-neighbors,relation:graph-undirected-neighbors`.

Misuse/diagnostic/failure proofs: `relation:graph-neighbors-node-type`. API fingerprint: `b55a5a1538106bfb8c00960698247e5a576d8628c46b45e6c3b559b1a8ef3460`.

<a id="symbol-d75bdbd0ebdb03120dd93ad2"></a>

## builtin / stopWhen

Identity: `claim:89dfddd5c928ea764b8de89d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)stop(){self.console();(self>20).return;}start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addEdge(a,b,5);g.addEdge(b,c,7);g.breadthFirst(a).v;v.stopWhen(stop);v.next();v.next();v.next().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:traversal-stop-20`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:traversal.stopWhen; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:traversal-stop-20,relation:traversal-stop-50`.

Misuse/diagnostic/failure proofs: `relation:traversal-bad-predicate`. API fingerprint: `75feab6d1cd4cc1832e37a79dcf978c371ec35b66a3406594b33606277fb20a5`.

<a id="symbol-1596b49048777a5e19d70147"></a>

## builtin.ScanPlan / markdown

Identity: `claim:8a391605f4e96a02e9fde741`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.markdown()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-markdown-stdin`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:markdown; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-markdown-mock,scan:multiline-markdown-stdin,scan:multiline-trace-markdown`.

Misuse/diagnostic/failure proofs: `scan:multiline-type-conflict`. API fingerprint: `35fd5d035e140ddd4c4317963bcbbc988534a3f9ba7da7f0ec4dbc35112dd72c`.

<a id="symbol-bc950ecd3384ca0c2b4f9a95"></a>

## builtin / Stream

Identity: `claim:8a75e51d03aef913884b07a4`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stream<Int>.from([]).s;s.next().unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `data:stream-next-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Stream.window,g010-public:Stream<Int>.from,g010-public:Stream.next,g010-public:Stream.map,g010-public:Stream.filter,g010-public:Stream.sink,g010-public:Stream.batch,g010-public:Stream.batch.next,g010-public:Stream.batch.sink; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:batch-changed-7,data:batch-empty-1,data:batch-filter-map-values,data:batch-lazy,data:batch-limit-64,data:batch-many-2,data:batch-option-consumed,data:batch-option-consumed-twice,data:batch-owned-list-iterator,data:batch-owned-list-map,data:batch-owned-list-mutations,data:batch-remaining-cursor,data:batch-retained-independent,data:batch-sink-changed-64,data:batch-sink-consumed,data:batch-sink-empty-2,data:batch-sink-limit-64,data:batch-sink-many-2,data:batch-sink-trap,data:batch-source-independent,data:batch-two-live,data:stream-callback-once,data:stream-consume-once,data:stream-filter-changed,data:stream-filter-many,data:stream-filter-one,data:stream-from-list,data:stream-independent-builders,data:stream-lazy,data:stream-list-snapshot,data:stream-map-changed,data:stream-map-many,data:stream-map-one,data:stream-next,data:stream-next-empty,data:stream-next-exhausted,data:stream-order-boundary-filter-keep-map-add,data:stream-order-boundary-map-add-filter-keep,data:stream-remaining-cursor,data:stream-sink-changed,data:stream-sink-empty,data:stream-sink-limit,data:stream-sink-many,data:stream-sink-one,data:stream-stages-16,data:stream-stages-17,data:stream-two-live,data:window-changed-2,data:window-empty-2,data:window-filter-all-empty,data:window-filter-empty-buckets,data:window-filter-map-2,data:window-lazy-no-lookahead,data:window-limit-64,data:window-many-2,data:window-one-1,data:window-remaining-cursor,data:window-retained,data:window-return-23,data:window-sink-consumed,data:window-two-live`.

Misuse/diagnostic/failure proofs: `data:batch-copy,data:batch-option-copy,data:batch-sink-extra-argument,data:batch-sink-return-value,data:batch-sink-scalar-receiver,data:batch-size-arity,data:batch-size-invalid--1,data:batch-size-invalid-0,data:batch-size-invalid-65,data:batch-size-type,data:stream-copy,data:stream-element-type,data:stream-filter-return-type,data:stream-from-type,data:stream-map-arity,data:stream-map-return-type,data:stream-map-type,data:stream-next-arity,data:stream-sink-batch-receiver,data:stream-sink-return-type,data:stream-value-type,data:window-arity,data:window-callback-trap,data:window-copy,data:window-duration-type,data:window-invalid-duration--1,data:window-invalid-duration-0,data:window-missing-duration,data:window-option-copy,data:window-sink-type`. API fingerprint: `62bd06704d059f10f3f0493cadf84c6864b2d31a2754af0863bc75bf7c1a08e6`.

<a id="symbol-39f7570cce0e37a1fc702db1"></a>

## builtin / finish

Identity: `claim:8d4785a4a08eba9369775a99`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Hasher.new(17).h;53.hash(h);71.hash(h);h.finish().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:hasher-stream`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Hasher.finish; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:hasher-stream`.

Misuse/diagnostic/failure proofs: `dict:hasher-finish-arity`. API fingerprint: `46ab7f066ecbc8dd045855863656e728cce8d3b992a64a77bdd225d31a356bca`.

<a id="symbol-1b332233bc71368ba8a46c19"></a>

## builtin.Float / ceil

Identity: `claim:8e15a8f4a8f5afb584c5a969`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_FUNCTION_NATIVE_NUMERIC_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(7.75).value;value.ceil().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `precision:ceil-7.75`.

### Types, ownership, effects, limits and errors

Binary64 actual receiver and once-evaluated arguments; classifiers include IEEE subnormal/NaN/infinity/signed zero; explicit rounding modes restore caller control word; finite round magnitude <2^63; approxEquals preserves two source arguments with Vector<Float> tolerance [absolute,relative], exactly two lanes (runtime shape checked); nonnegative/non-NaN tolerances, identical infinities equal and distinct infinities unequal; Uncertain methods claim only exact typed operands; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `precision:ceil--7.75,precision:ceil-7.75,precision:float-two-live-floor`.

Misuse/diagnostic/failure proofs: `precision:floor-extra-argument`. API fingerprint: `9428b8c1cf8f97bdb03e9d025b6e35acccd833c253dbe5c1c4ed1029b1c7afbd`.

<a id="symbol-3ad41c8042aa7e9afbab4fd5"></a>

## std.fs / Path

Identity: `claim:906cf79438abd7c0f42d0f42`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `STDLIB_DECLARATION_TO_TYPED_NATIVE_FILESYSTEM`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
import "std.fs" { Path; File; }.fs;
start(){Path.parse("ação/Ω.dat").fileName().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `filesystem:path-name-2`.

### Types, ownership, effects, limits and errors

Actual nominal immutable Path, canonical native normalize/join/isAbsolute/fileName owners; 4096-byte paths, 64 components, 255-byte components. File.readText/writeText consume typed Path and Text, explicit utf8 and truncate/append/atomic policies, at most 4096 bytes. Native File capability is restricted to the execution directory through openat2 beneath/no-symlink resolution. Every file and root descriptor closes; private filesystem byte effects and 512 repeated reads are independent observations. This is the material convenience profile, not a claim for arbitrary live handle or directory APIs. Caller-owned beneath directory; no symlinks; UTF-8; 4096-byte paths, 64 components, 255-byte component; atomic write

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: filesystem. Capabilities: PRIVATE_DIRECTORY.

Native chain: `compiler/semantic/system/g011_source_vertical.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `filesystem:path-name-0,filesystem:path-name-2,filesystem:path-absolute-0,filesystem:path-absolute-1,filesystem:path-independent,filesystem:path-join,filesystem:path-return-255`.

Misuse/diagnostic/failure proofs: `filesystem:path-type,filesystem:path-arity,filesystem:path-join-type,filesystem:path-status,filesystem:path-domain-escape,filesystem:path-domain-nul`. API fingerprint: `928ea6804e2987beee7188ab592f73715b29c52547bdb422ef5f89b507270984`.

<a id="symbol-dc15ba91b1e0a98c288b3fda"></a>

## builtin / id

Identity: `claim:90f616b7d80dfdd6aefa604c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(17).n;n.id().before;n.replaceValue(71);(n.id()==before).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:node-stable-id`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Node.id; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:node-stable-id`.

Misuse/diagnostic/failure proofs: `relation:node-id-arity`. API fingerprint: `79e67b78f652b8ada21a70fc362f8d12fb04aca3fea144ab1e65941a3b6534ef`.

<a id="symbol-89a4ee94929fd2849999dc3c"></a>

## std.core / Range

Identity: `claim:93d1c9445e431ff7c4c14a9f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start() {
    Range.exclusive(2, 10).stepBy(2).progression;
    progression.length().return;
}

```

Explicit process status: `4`; the full value/effect oracle is `composite:range-exclusive-length`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:range-inclusive-length,composite:range-exclusive-length,control:for,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:range-zero-step`. API fingerprint: `a14dd6e163a0f98be48f9acc6ae38a34003947dd192eb54e5a77889141a953b3`.

<a id="symbol-815ed3890b68d9af6428ef05"></a>

## std.core / Eq

Identity: `claim:96cf60e02658947cd1ea3349`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
generic<T> (T.value)hold() where T: Hash + Eq{value.return;}start(){17.hold().a;29.hold().b;(a+b).console();7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `composite:generic-compose-whereTHashEq-7`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:generic-compose-whereTHashEq-7,composite:generic-compose-whereTHashEq-23,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:generic-hash-eq-failure`. API fingerprint: `3d2e60087f5ea54032e857e1fd811ec25cc6d763d917efee4a863dd4d6784082`.

<a id="symbol-002283e611c995a9b8028525"></a>

## builtin / collisionCount

Identity: `claim:97876e7515f5a2663ca12f98`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.collisionCount().console();d.maxProbeLength().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:collision-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.collisionCount; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:collision-cluster,dict:collision-empty,dict:collision-single`.

Misuse/diagnostic/failure proofs: `dict:collision-arity`. API fingerprint: `b73e06ed34db0814e0c879da9acb830815530028bebea8c7f540df23ec2db3c1`.

<a id="symbol-496041f69f3a7eaca92ac26e"></a>

## builtin / clear

Identity: `claim:980d2266a234a171cc0c5407`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.clear();d.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:clear`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.clear; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:clear`.

Misuse/diagnostic/failure proofs: `dict:borrow-mutation`. API fingerprint: `c55585f1e6e41e407c2e21ffa27de12b70b597fd3304e9800ee9ee045dbb3691`.

<a id="symbol-11bcc6f4ff41840c84c0b636"></a>

## builtin / repartition

Identity: `claim:9817044e18a110e36db56434`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29,71]))]).t;Dataset.fromTables([t]).d;d.repartition(1);7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `data:repartition-explicit-return`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Dataset.repartition; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:repartition-0-1,data:repartition-17-7,data:repartition-256-32,data:repartition-3-1,data:repartition-explicit-return,data:repartition-missing-3,data:repartition-result-schema,data:repartition-two-live`.

Misuse/diagnostic/failure proofs: `data:repartition-65-7,data:repartition-arity,data:repartition-invalid-0,data:repartition-invalid-33,data:repartition-size-type`. API fingerprint: `8efaf44737b19c5efcc46005c824a4c890fc2f2038e54f08eb2c1ea3daea9f79`.

<a id="symbol-080b2a9593d9c97f2242538d"></a>

## builtin / Text.dropBytes

Identity: `claim:98c84ae23bad8170c6869b62`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".dropBytes(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:dropBytes-0`.

### Types, ownership, effects, limits and errors

dropBytes(Int); non-negative; saturates at byte length; ; borrowed view; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:dropBytes-0,text:dropBytes-2`.

Misuse/diagnostic/failure proofs: `text:type-dropBytes`. API fingerprint: `4a71bbf09fbc368087d7d9efc1912893a7900e3b476329c03369c62f8148ccc2`.

<a id="symbol-50b0d3897a4a35b3feffd4d9"></a>

## std.console / console

Identity: `claim:992d32bcce649fc5aa92fd76`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-17`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:publish-seed,console:publish-17,console:publish-True,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `console:bare-color`. API fingerprint: `ecf3a1a1129a47a11699fa4b40c482a5cb06ecd448eefc96da6cc940c6765f79`.

<a id="symbol-092e2e8a3fa7fbea5a3a4f81"></a>

## builtin.ScanPlan / code

Identity: `claim:9c7d133565d37ffa1f0d008c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.code("nebo"),.replay("answers.txt")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-code-replay`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:code; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-code-replay,scan:multiline-trace-code-nebo,scan:multiline-trace-code-other`.

Misuse/diagnostic/failure proofs: `scan:multiline-type-code`. API fingerprint: `ba0572c1668b3523fbc65fb0898a21f970921e7f1d5242765f60177ea2386042`.

<a id="symbol-23425fdcbd7a517d6baddaea"></a>

## builtin / Text.padStart

Identity: `claim:9c9df93d2fe2ff17ca8439de`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"A".padStart(6,"ab").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:pad-start-4`.

### Types, ownership, effects, limits and errors

padStart(Int,Text); 4096 output bytes; byte width; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:pad-start-0,text:pad-start-4`.

Misuse/diagnostic/failure proofs: `text:type-padStart`. API fingerprint: `c4c2578a56c3cd2a7ffbf1e0d515f45a19d432ae79c788cf68fe29151ff2c7ef`.

<a id="symbol-e72d27be933ae789137ec624"></a>

## builtin.ScanPlan / multiline

Identity: `claim:9eee2d656736e2dba5df8228`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.multiline()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-multiline-stdin`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:multiline; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-multiline-mock,scan:multiline-multiline-replay,scan:multiline-multiline-stdin,scan:multiline-trace-multiline`.

Misuse/diagnostic/failure proofs: `scan:multiline-type-conflict,scan:multiline-too-many-lines`. API fingerprint: `b40b575c4d487baf65c3ce485fabd18ac4961ca5e1f6c63ec20c1e33bd480b23`.

<a id="symbol-d0be8b7a7db3a24dcafc0886"></a>

## builtin.Random / bernoulli

Identity: `claim:a04f7c67ac62f3dffd7e2d2d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_RANDOM_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Random.seed(17).r;r.bernoulli((∞-∞));23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `random:domain-bernoulli-nan`.

### Types, ownership, effects, limits and errors

Explicit SplitMix64 v1 state, no entropy capability; native finite-domain distributions and categorical Float Vector weights; nonnegative Int uniform; shuffle of unique local mutable Int Slice at most 64 lanes; lexical creation/release provenance, separate descriptor/payload frames; sample of Int Vector/local Slice returns canonical List with 0..16 entries; independent seed/value/order oracles and rejected effectful interpolation; no whole-source report Independent deterministic seeded states; checked intervals and distribution domains; no cryptographic entropy claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `random:bernoulli-17-0.3,random:bernoulli-29-0.75,random:bernoulli-0-0.0,random:bernoulli-0-1.0,random:bool-control-flow,random:domain-bernoulli-nan`.

Misuse/diagnostic/failure proofs: `random:bernoulli-type,random:bernoulli-arity`. API fingerprint: `666820ed282c14d5d8c742b77ee640c8b450b5a87950e454e50411de4c8b12fc`.

<a id="symbol-b79073a00cd11e70f45a043d"></a>

## builtin / depthFirst

Identity: `claim:a123e52af2631ff4b4505176`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.depthFirst(a).v;v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().isNone().console();v.release();g.addNode(7).n;n.value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-depthFirst`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.depthFirst; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-depthFirst`.

Misuse/diagnostic/failure proofs: `relation:graph-depthFirst-node-type,relation:traversal-copy`. API fingerprint: `85f5804392c7a9b0f4b3ee343ab58d6b72fa8c66f14553bf0b412a8a0f4b9c26`.

<a id="symbol-db9a1b4eda984c2e64edc909"></a>

## builtin / Text.isDigits

Identity: `claim:a3bc1e9e18ededf09763bc5a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".isDigits().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:isDigits-empty`.

### Types, ownership, effects, limits and errors

isDigits(); nonempty ASCII digits; O(n); ; borrowed immutable receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:isDigits-empty,text:isDigits-4e65626f`.

Misuse/diagnostic/failure proofs: `text:arity-isDigits`. API fingerprint: `b483b17f4920e348ecbdb5c34e016a848d3593eac7b4508968e09f61783aea29`.

<a id="symbol-33c5b74dc64180a0af674dda"></a>

## std.core / Option

Identity: `claim:a4d646f6ab5654f4270c69e7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Option<Int>(Some(17)).x;x.map(31).get().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `optionresult:option-map`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Typed active variant and structured errors; bounded combinators; explicit ownership; no arbitrary callback promise

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/option_result_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/scalars/option_result_runtime.asm`.

Positive/changed-value/boundary proofs: `optionresult:option-value-17-return-23,optionresult:option-map,optionresult:option-map-none,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `optionresult:option-zip-array`. API fingerprint: `d66e393ab12dbcc373c179efedc73e9cbb8a4655101f66b3cc824ffba931174d`.

<a id="symbol-07616ab959dc592fad9e969c"></a>

## builtin / writeBytes

Identity: `claim:a7d39a6257bb772bb07c730e`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Hasher.new(29).h;h.writeBytes(Bytes.fromValues(17,29,53,71));h.finish().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:hasher-bytes`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Hasher.writeBytes; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:hasher-bytes,dict:hasher-changed-byte`.

Misuse/diagnostic/failure proofs: `dict:hasher-bytes-type`. API fingerprint: `5fd786c34e7e73e541314dc3a284696b19197983add02a42bd9444d5007d9a30`.

<a id="symbol-41860d8ea730c7d07b420b0d"></a>

## std.core / Result

Identity: `claim:aa108d62ea5c02d471c7e874`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Result<Int,Int>(Ok(17)).x;x.get().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `optionresult:result-value-17-return-23`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Typed active variant and structured errors; bounded combinators; explicit ownership; no arbitrary callback promise

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/option_result_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/scalars/option_result_runtime.asm`.

Positive/changed-value/boundary proofs: `optionresult:result-value-17-return-23,optionresult:result-err-map-err,optionresult:result-is-err,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `optionresult:result-or-else-type`. API fingerprint: `ec0d03d737568b465726e3409a9078f71fe7142d9365253b22a689add9c79087`.

<a id="symbol-c3c8fe18ec44ae846168b12e"></a>

## builtin / length

Identity: `claim:aad326d5fef4be450a5f0239`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.length; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:empty,dict:length-one,dict:length-two`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `cfd523dfce808f8705af81b1b123da068434157c43c7bde024b79aba5fac7575`.

<a id="symbol-75947e30b6c1c895874d333f"></a>

## builtin.Float / floor

Identity: `claim:abad9ccaf4dae20d4328fccf`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_FUNCTION_NATIVE_NUMERIC_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(7.75).value;value.floor().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `precision:floor-7.75`.

### Types, ownership, effects, limits and errors

Binary64 actual receiver and once-evaluated arguments; classifiers include IEEE subnormal/NaN/infinity/signed zero; explicit rounding modes restore caller control word; finite round magnitude <2^63; approxEquals preserves two source arguments with Vector<Float> tolerance [absolute,relative], exactly two lanes (runtime shape checked); nonnegative/non-NaN tolerances, identical infinities equal and distinct infinities unequal; Uncertain methods claim only exact typed operands; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `precision:floor--7.75,precision:floor-7.75,precision:float-two-live-floor`.

Misuse/diagnostic/failure proofs: `precision:floor-extra-argument`. API fingerprint: `a627ab1c5c258811e402f345af5013a7b94c1f9b0e9d2c7071cdd21fcef5bf11`.

<a id="symbol-793aa11917ed9fa2e0325f44"></a>

## builtin / reseed

Identity: `claim:abec9cca114540021e52bc8c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.reseed();d.reseed();d.get(17).expect("a").return;}

```

Explicit process status: `53`; the full value/effect oracle is `dict:repeated-reseed`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.reseed; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:repeated-reseed,dict:reseed-retains`.

Misuse/diagnostic/failure proofs: `dict:view-reseed`. API fingerprint: `db0bf88162a4ec66d3430062e2435209d2d665996554de7199727d405bd3284b`.

<a id="symbol-7a1363cb677fd0d000f21a95"></a>

## builtin / path

Identity: `claim:ac60dc5c0711b39f0c57310e`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.breadthFirst(a).v;v.next();v.next();v.next();v.next();v.path().p;p.next().expect("n").value().console();p.next().expect("n").value().console();p.next().expect("n").value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:traversal-bfs-path`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:traversal.path; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:traversal-bfs-path`.

Misuse/diagnostic/failure proofs: `relation:traversal-path-arity`. API fingerprint: `5b3c33a5c07416a040a1690dfa248d3890b4cbcc6b7823758911560ffe0f551b`.

<a id="symbol-a87951f3e8f4409cdd5e086b"></a>

## builtin / withCapacity

Identity: `claim:ae39db657afe152aa210ab2f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.withCapacity(8).d;d.capacity().return;}

```

Explicit process status: `8`; the full value/effect oracle is `dict:capacity-8`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict<Int,Int>.withCapacity; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:capacity-16,dict:capacity-32,dict:capacity-64,dict:capacity-8`.

Misuse/diagnostic/failure proofs: `dict:capacity-invalid-9,dict:capacity-type`. API fingerprint: `45117ee3ae142c674190f75fba1e0e8826db7b1cb9abc55e5ecbc49a719b361c`.

<a id="symbol-f4adfeb8d75014b11a7430b7"></a>

## builtin / entry

Identity: `claim:ae8d22912c1a770129779c49`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.entry(17).e;e.read().return;}

```

Explicit process status: `177`; the full value/effect oracle is `dict:entry-absent-read`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.entry; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:entry-absent-read,dict:entry-key-once,dict:entry-new,dict:entry-repeated-write,dict:entry-temporary,dict:entry-update`.

Misuse/diagnostic/failure proofs: `dict:entry-copy,dict:entry-key-type,dict:entry-read-conflict,dict:entry-write-type`. API fingerprint: `39e79d412d494980184da3fa035457405e0610138443d814d03598c5a033fc29`.

<a id="symbol-2d694b1185ae07672bbedf75"></a>

## builtin / Text.split

Identity: `claim:af1e211ae36a1ecb0c79ed3c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"a,,b,".split(",").join("|").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:split-join-1`.

### Types, ownership, effects, limits and errors

split(Text); 254 items; ; borrowed items in caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:split-join-1,text:bound-split`.

Misuse/diagnostic/failure proofs: `text:type-split`. API fingerprint: `d2cc5fe4aef494c25e1393d72b46dc70a5741c903652a96644657efbaf35189e`.

<a id="symbol-26c8f987ba9838edde5073e6"></a>

## builtin.ScanPlan / untrusted

Identity: `claim:b0a714c04094f35b25d89ad4`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.untrusted(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-256-0-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:untrusted; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-256-0-4,scan:security-256-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-untrusted`. API fingerprint: `dc5bf4e7c763cfa3f63594373af4e8d925c5382c798428146aab6a995d2bf809`.

<a id="symbol-a33f48945e202da73ab6b970"></a>

## builtin.ScanPlan / console

Identity: `claim:b1968febf742b83dac424b01`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.console(),.mock("!accepted")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-adapter-216163636570746564`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:console; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-adapter-216163636570746564,scan:console-retry-events,scan:console-eof-event,scan:console-cancel-event`.

Misuse/diagnostic/failure proofs: `scan:field-arity,scan:option-arity-console`. API fingerprint: `41d3121d0538102c5ff50dba76a10e9f4de7ebca92ea2cdbdcfd27e980d0cf01`.

<a id="symbol-231ef9d1a34523f620a36a89"></a>

## std.math / cos

Identity: `claim:b46bc81bef2288d80c55f06c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.cos(0.5);29.return;}
```

Explicit process status: `29`; the full value/effect oracle is `math:cos`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:cos`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `98f123c5b9f11a84377d7151ef7a7d88c8ee48aadd9582734e829c96419ce122`.

<a id="symbol-16f5a0e1860a5880c5110f54"></a>

## builtin / Text.isBlank

Identity: `claim:b4e6c473be922721f8308fa8`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".isBlank().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:isBlank-empty`.

### Types, ownership, effects, limits and errors

isBlank(); ASCII SP/TAB/LF/CR; O(n); ; borrowed immutable receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:isBlank-empty,text:isBlank-4e65626f`.

Misuse/diagnostic/failure proofs: `text:arity-isBlank`. API fingerprint: `259bfe4c2e4f00bdd3a48ebd69706506221e4c48461192d3d6d2da2cab102f97`.

<a id="symbol-9c7602f695d64398d22e8609"></a>

## builtin.Random / sample

Identity: `claim:b4ef49137abb98c4399ea805`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_RANDOM_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Random.seed(17).r;Array<Int,4> [17,29,43,71].a.mutable;a.asSlice().s;r.sample(s,5);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `random:sample-domain-5`.

### Types, ownership, effects, limits and errors

Explicit SplitMix64 v1 state, no entropy capability; native finite-domain distributions and categorical Float Vector weights; nonnegative Int uniform; shuffle of unique local mutable Int Slice at most 64 lanes; lexical creation/release provenance, separate descriptor/payload frames; sample of Int Vector/local Slice returns canonical List with 0..16 entries; independent seed/value/order oracles and rejected effectful interpolation; no whole-source report Independent deterministic seeded states; checked intervals and distribution domains; no cryptographic entropy claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `random:sample-17-17-4-4,random:sample-29--31-4-1,random:sample-boundary-64-16,random:sample-slice,random:sample-preserves-input,random:sample-two-live-results,random:sample-domain-5`.

Misuse/diagnostic/failure proofs: `random:sample-count-type,random:sample-after-release,random:sample-scalar,random:sample-arity`. API fingerprint: `65462547115f9dc9d2ce1019ce982db62a865187844565593e57815abce9be8c`.

<a id="symbol-43237c4c379ae34367e4d199"></a>

## builtin / contains

Identity: `claim:b527988140b16cb2b195d6ab`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.difference(b).c;c.contains(17).console();c.contains(29).console();c.contains(53).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:set-difference-membership`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Set.contains; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-difference-membership`.

Misuse/diagnostic/failure proofs: `dict:set-contains-type`. API fingerprint: `2d7718394cfcbc78eb0c2a36298923abe5873253d9ac04d2f2bfcb2ab31aae37`.

<a id="symbol-362457379411c48a6cc7c288"></a>

## builtin / map

Identity: `claim:b6afa9cf081896cc3c2a5f0c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)step(){self.console();(self+7).return;}start(){"ready".console();Stream<Int>.from([17,71]).s;s.map(step).out;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:stream-lazy`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Stream.map; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:stream-callback-once,data:stream-independent-builders,data:stream-lazy,data:stream-map-changed,data:stream-map-many,data:stream-map-one,data:stream-stages-16,data:stream-stages-17`.

Misuse/diagnostic/failure proofs: `data:stream-map-arity,data:stream-map-return-type,data:stream-map-type`. API fingerprint: `ed51412ff8d52c17b85f4a8ebe3a36ad0bfd90e0402d9c3b8477f86f695ceda6`.

<a id="symbol-8977ea462583633b6a05719a"></a>

## builtin / remove

Identity: `claim:b7aeab092dbacdd8def161e7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.remove(19).unwrapOr(73).return;}

```

Explicit process status: `73`; the full value/effect oracle is `dict:remove-absent`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.remove,g008-public:Set.remove; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:remove,dict:remove-absent,dict:set-insert-remove`.

Misuse/diagnostic/failure proofs: `dict:set-remove-type,dict:use-after-drop`. API fingerprint: `28e0b4245b6d1d442a995e770d592db00fe59fa296172eca613283e6eb3fa21a`.

<a id="symbol-78387918d9e5f523c5ebfdbf"></a>

## std.math / log2

Identity: `claim:b7d025f424a1282604832494`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.log2(32.0);29.return;}
```

Explicit process status: `29`; the full value/effect oracle is `math:log2`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:log2`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `5a707c68fd45054729e9c74e1c0dcc923207b7010e44b459937a127d4778323b`.

<a id="symbol-bf6c74bf427979b29b81eb35"></a>

## builtin.ScanPlan / panel

Identity: `claim:b7ffcdf5e25b3713c3415c6c`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.form("profile"),.field("age","Int"),.view("entry"),.panel("main"),.feedback(),.style("blue"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-metadata-profile`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:panel; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings`.

Misuse/diagnostic/failure proofs: `scan:option-type-panel`. API fingerprint: `310ffce2d4d97c7d84d9a3267a814775bbdb2e5c76aa151f11cc4f6ff04513e4`.

<a id="symbol-99040e145ca43ea5adf03737"></a>

## builtin / select

Identity: `claim:bc28c557c108e7ae083d19ef`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("score").expect("v").console();true.return;}start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([71,17,29])),Tuple.of("score",Column<Int>.from([83,7,53]))]).t;t.select(["score"]).out;out.filter(observe);out.columnCount().return;}
```

Explicit process status: `1`; the full value/effect oracle is `data:table-select-subset`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.select; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-select-order,data:table-select-sort-compose,data:table-select-subset,data:table-sort-missing-3`.

Misuse/diagnostic/failure proofs: `data:table-select-duplicate-name,data:table-select-empty,data:table-select-name-type,data:table-select-unknown-name`. API fingerprint: `ecc5dba928f8ad742259f63cbf90c7f84031e33957f4df258d20100b7f39a43f`.

<a id="symbol-ed1840d04dc47dfc35455d53"></a>

## builtin / Node

Identity: `claim:bcaffdb822955a437c82c671`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(71).a;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:node-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Node<Int>.new,g009-public:Node.id,g009-public:Node.value,g009-public:Node.replaceValue,g009-public:Node.degree; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:Node-payload--17,relation:Node-payload-71,relation:Tree-payload-29,relation:graph-degree,relation:graph-replace,relation:graph-stale-node,relation:node-replace,relation:node-return-23,relation:node-stable-id,relation:tree-degree,relation:tree-stale-node`.

Misuse/diagnostic/failure proofs: `relation:node-degree-arity,relation:node-id-arity,relation:node-value-arity,relation:node-wrong-replacement`. API fingerprint: `e94626b337a9f5ba5c63da1221d73b1adf1a313ab5ccf7e538363c403c686366`.

<a id="symbol-714d7fd1cc8448ac519d6f3a"></a>

## std.console / scan

Identity: `claim:be1e782a2d56e77dcd88269d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("23")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-23-return-23`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:int-23-return-23,scan:text-4e65626f,scan:bool-False,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `scan:mock-type`. API fingerprint: `ea3e4fd8379fda53ff829594295e89d16e0232e98af9ad71d4c162653f8cea98`.

<a id="symbol-2a8db99bd27a75bedb8c2922"></a>

## builtin / Text.trimStart

Identity: `claim:c01376d8eaedb654340c8593`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".trimStart().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:trimStart-2`.

### Types, ownership, effects, limits and errors

trimStart(); input bytes; ; borrowed view; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:trimStart-0,text:trimStart-2`.

Misuse/diagnostic/failure proofs: `text:arity-trimStart`. API fingerprint: `87d473637990e5d74d5c9ef16dc44c9503b3f664d9c019c6ccf5c2971ecb3b57`.

<a id="symbol-8f49aebc4e16dd787479d81a"></a>

## builtin.Vector / standardDeviation

Identity: `claim:c0739f72efe3896918ce9731`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_STATISTICS_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int> [17,29,43,71].a;"${a.standardDeviation(population):fixed(6)}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `statistics:Int-1-standardDeviation-population`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 lanes, extent 1..64; checked Int sum, native compensated Float sum, explicit population/sample and linearType7 policies; 1536-byte caller workspace isolates conversion/quantile scratch; source values unchanged; native domain/overflow traps and NaN propagation are observed; no source report or synthetic aggregate Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `statistics:Float-0-standardDeviation-population,statistics:Float-0-standardDeviation-sample,statistics:Int-1-standardDeviation-population`.

Misuse/diagnostic/failure proofs: `statistics:policy-standardDeviation-true,statistics:shadow-standardDeviation,statistics:sample-too-small-standardDeviation`. API fingerprint: `6997d2c0f3ddf24902912afee9abb093056acc5cc63137852b3f6030788c506a`.

<a id="symbol-65fa9e940b44ce607f6669b4"></a>

## builtin.Random / uniform

Identity: `claim:c0998ab6b8fbaf1f877a4314`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_RANDOM_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Random.seed(17).r;r.uniform(0.0,∞);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `random:domain-uniform-infinite-high`.

### Types, ownership, effects, limits and errors

Explicit SplitMix64 v1 state, no entropy capability; native finite-domain distributions and categorical Float Vector weights; nonnegative Int uniform; shuffle of unique local mutable Int Slice at most 64 lanes; lexical creation/release provenance, separate descriptor/payload frames; sample of Int Vector/local Slice returns canonical List with 0..16 entries; independent seed/value/order oracles and rejected effectful interpolation; no whole-source report Independent deterministic seeded states; checked intervals and distribution domains; no cryptographic entropy claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `random:uniform-float-17-10.0-20.0,random:uniform-int-29-17-1000,random:return-0,random:return-255,random:domain-uniform-infinite-high,random:domain-uniform-negative,random:native-distribution-controls`.

Misuse/diagnostic/failure proofs: `random:uniform-type,random:uniform-arity,random:uniform-extra`. API fingerprint: `ad82b703cd12811dcaaa210125e9ef1d440b13794d4fc4351c225ea01f792920`.

<a id="symbol-a5f30ff7801115c2db29360d"></a>

## builtin / deterministic

Identity: `claim:c23da4ce0992b047ec8afe30`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){hash.deterministic(0);Dict<Int,Int>.new().d;d.insert(29,71);Hasher.new(17).h;53.hash(h);h.finish();d.get(29).expect("value").return;}
```

Explicit process status: `71`; the full value/effect oracle is `hashpolicy:deterministic-zero`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:hash.deterministic; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `hashpolicy:deterministic-seed,hashpolicy:deterministic-signed,hashpolicy:deterministic-zero`.

Misuse/diagnostic/failure proofs: `hashpolicy:seed-missing,hashpolicy:seed-type`. API fingerprint: `6ae1e6b25146ceeb2adb994275fc0a20ef5817c73c3f5600082b78d82bda2980`.

<a id="symbol-5aa84be47c8efde969c90b33"></a>

## builtin / join

Identity: `claim:c555e83e61df06e38b15a258`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("key",Column<Int>.from([17,29])),Tuple.of("age",Column<Int>.from([71,83]))]).left;Table.fromColumns([Tuple.of("key",Column<Int>.from([17])),Tuple.of("score",Column<Int>.from([103]))]).right;left.join(right,["key"],2).out;left.rowCount().console();right.rowCount().console();out.rowCount().return;}
```

Explicit process status: `2`; the full value/effect oracle is `data:join-source-preserved`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.join; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:join-changed-1,data:join-empty-1,data:join-kind-once,data:join-limit-1,data:join-many-1,data:join-many-2,data:join-missing-1-0-2,data:join-one-1,data:join-signed-1,data:join-source-preserved,data:join-two-live`.

Misuse/diagnostic/failure proofs: `data:join-arity,data:join-duplicate-nonkey,data:join-keys-type,data:join-kind-type,data:join-other-type,data:join-over-limit-1,data:join-over-limit-2`. API fingerprint: `72287d69ab2874017031ca3fddecd4ac97116ef55f9b289e2faa2a8ec6498a13`.

<a id="symbol-169cec7a3384a79124fdc778"></a>

## builtin.ScanPlan / choice

Identity: `claim:c5b1151c2ef4cdd12eca18fe`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.choice(["red","green","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-3-0-726564`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:choice; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-3-0-726564,scan:choice-arguments-once`.

Misuse/diagnostic/failure proofs: `scan:choice-absent,scan:choice-type`. API fingerprint: `98ec09a59c6bf5d8ed2ad7070b32b29cc7b86bb9a2c32f56b85b1c167da7dd07`.

<a id="symbol-9b868e4cbdec09ae8c762432"></a>

## builtin / loadFactor

Identity: `claim:c5f600d8f08de68f0db6e718`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;(d.loadFactor()==0.0).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:load-factor-0`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.loadFactor; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:load-factor-0,dict:load-factor-1,dict:load-factor-2`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `483b2b5f94ad6526e02763f1bbbde76662b6ad1536f72c8ed9aef0b0d235af9f`.

<a id="symbol-462d6b4e054945f771182900"></a>

## builtin / fillMissing

Identity: `claim:c6fcab493c85eccb852feab7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).a;Column<Int>.from([a.get(0),a.get(1),a.get(2)]).c;c.fillMissing(83);c.countMissing().console();c.sum().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-fill-0`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.fillMissing; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-fill-0,data:column-fill-1,data:column-fill-2,data:column-fill-3,data:column-fill-5,data:column-fill-7`.

Misuse/diagnostic/failure proofs: `data:column-fillMissing-type`. API fingerprint: `8b0010b4b6da99ee0a413f6484bd28831dc3b0e8ef0546aea2a34cb704810ca1`.

<a id="symbol-6f9e7800d1752681a405ea0d"></a>

## builtin.Random / normal

Identity: `claim:c76165af03df20e5abaf51e2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_RANDOM_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Random.seed(17).r;r.normal(∞,1.0);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `random:domain-normal-infinite-mean`.

### Types, ownership, effects, limits and errors

Explicit SplitMix64 v1 state, no entropy capability; native finite-domain distributions and categorical Float Vector weights; nonnegative Int uniform; shuffle of unique local mutable Int Slice at most 64 lanes; lexical creation/release provenance, separate descriptor/payload frames; sample of Int Vector/local Slice returns canonical List with 0..16 entries; independent seed/value/order oracles and rejected effectful interpolation; no whole-source report Independent deterministic seeded states; checked intervals and distribution domains; no cryptographic entropy claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `random:normal-17-0.0-1.0,random:normal-29-5.0-2.0,random:domain-normal-zero,random:domain-normal-infinite-mean,random:domain-normal-infinite-sd,random:native-distribution-controls`.

Misuse/diagnostic/failure proofs: `random:normal-type,random:normal-arity`. API fingerprint: `a754f6397383ad0c18748ce95cb326f4a66570c0a3e5fa282b41f67d97033f0d`.

<a id="symbol-2979ce3ee94170040c455274"></a>

## std.core / Error

Identity: `claim:c8b14da9317fa2ae62e9ca17`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Error(7101,0,50,2,8,0).root;root.withContext("line\nnext").contextual;contextual.message().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `optionresult:error-message-0-9`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Typed active variant and structured errors; bounded combinators; explicit ownership; no arbitrary callback promise

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/types/option_result_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/scalars/option_result_runtime.asm`.

Positive/changed-value/boundary proofs: `optionresult:error-message-0-9,optionresult:error-two-messages,optionresult:error-diagnostic-span,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `optionresult:error-invalid-diagnostic-span`. API fingerprint: `61fe5fb281b183c52d81c37f6753c3d9b87b148e2fe3743fee79c30eae4fa625`.

<a id="symbol-f5dcaa5196ad30af4c30b6c1"></a>

## builtin / randomized

Identity: `claim:ca00b7de3e6e77425c82a097`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){hash.randomized();Dict<Int,Int>.new().d;d.insert(29,71);Hasher.new(17).h;53.hash(h);h.finish();d.get(29).expect("value").return;}
```

Explicit process status: `71`; the full value/effect oracle is `hashpolicy:randomized`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:hash.randomized; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `hashpolicy:randomized,hashpolicy:reset-deterministic`.

Misuse/diagnostic/failure proofs: `hashpolicy:randomized-arity`. API fingerprint: `d15368f80aceebed9a4f8206adff58f858940fd38e887b0a6b498c4d12d58e56`.

<a id="symbol-e7cfbd6f9e922f0365b4ede0"></a>

## builtin.Vector / at

Identity: `claim:cb16a44ffa95d8c361d0c0c8`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_VECTOR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int> [17,29,43].a;a.at(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `vector:literal-Int-0-False`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 vector lanes; extent 1..64; typed filled/literal constructors; native vector algorithms and separate owned frames; exact shape/type checks; constant index bounds and dynamic native checks; linked scalar spills preserve RHS allocations; no whole-source report; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `vector:literal-Int-0-False,vector:literal-Int-2-False,vector:literal-Float-1-True,vector:dynamic-index,vector:loop-composition,vector:scalar-binary-owned-rhs,vector:bytes-index-owned-rhs`.

Misuse/diagnostic/failure proofs: `vector:index-4,vector:index--1,vector:method-at-true,vector:method-at-empty`. API fingerprint: `b30338929c6b80d648a630c8f03be47590348eba2c6798ce3775c1471fd704ed`.

<a id="symbol-8353a2449d1aa350d2730694"></a>

## builtin / Text.equals

Identity: `claim:cc1d47dce2d484b84f04478a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".equals("absent").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:equals-2`.

### Types, ownership, effects, limits and errors

equals(Text); byte-exact; O(n); ; borrowed immutable receiver and argument; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:equals-0,text:equals-2`.

Misuse/diagnostic/failure proofs: `text:type-equals`. API fingerprint: `0ff54662f90800f01ff2121d5ef049e5a63cc23e6bf35500a4b7097a12a73463`.

<a id="symbol-d5bb97103e50a88690593e95"></a>

## builtin / field

Identity: `claim:cdf7651cb0c39fe2c1c7041e`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;s.field("age").length().return;}
```

Explicit process status: `3`; the full value/effect oracle is `data:schema-field-description-length`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Schema.field; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:schema-field-description-length,data:schema-field-snapshot,data:schema-ordered-age-score`.

Misuse/diagnostic/failure proofs: `data:schema-field-missing,data:schema-field-name-type,data:schema-field-projection-bound`. API fingerprint: `939e1ac17980d8fa3c69e76c8b25198abc7a8262190b2d7c72c9810cc9b104fd`.

<a id="symbol-59a6da4f63eddad53b340835"></a>

## builtin / iterationOrder

Identity: `claim:ce545f606b61e12a4dfa91de`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.iterationOrder().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:iteration-order`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.iterationOrder; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:iteration-order`.

Misuse/diagnostic/failure proofs: `dict:iteration-order-arity`. API fingerprint: `59582a7661a96777f38a6c1b51ec465324b69ed5411b65ade8a3369746cb22fe`.

<a id="symbol-578ca400f6ab7dd4317b3f7b"></a>

## builtin.visual / title

Identity: `claim:ce58a02fc4cc1d6cdeeaf444`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.title("report");23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:title-6-report`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:console.title(text); g018-declaration:chart.title(text) Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:title-13-change,console:title-6-report,console:title-loop`.

Misuse/diagnostic/failure proofs: `console:title-limit,console:title-type`. API fingerprint: `9a9fd69b75766fcc840788db879f0f585d775f7413d7cec0ba5cab9e83a55739`.

<a id="symbol-7ae7a24b25ad4bac171a9b3a"></a>

## builtin.Vector / sum

Identity: `claim:cf0163f1d9536e519c18869b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_STATISTICS_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int> [1,2,3,4].a;a.sum().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `statistics:Int-0-sum-none`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 lanes, extent 1..64; checked Int sum, native compensated Float sum, explicit population/sample and linearType7 policies; 1536-byte caller workspace isolates conversion/quantile scratch; source values unchanged; native domain/overflow traps and NaN propagation are observed; no source report or synthetic aggregate Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `statistics:Int-0-sum-none,statistics:Float-0-sum-none,statistics:Int-2-sum-none,statistics:checked-int-exact-limit,statistics:checked-int-overflow,statistics:nan-propagation-sum`.

Misuse/diagnostic/failure proofs: `statistics:arity-sum`. API fingerprint: `926be722b091ba2f054864d4642bb44aef220f66bc6e3b822e2f3ebc40351130`.

<a id="symbol-2368d39f043b7711f260ef56"></a>

## builtin.ScanPlan / indexChoice

Identity: `claim:d007c078c2c3e323f5cf3605`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.indexChoice(["red","green","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:index-3-0-726564`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:indexChoice; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:index-3-0-726564,scan:index-3-2-626c7565`.

Misuse/diagnostic/failure proofs: `scan:choice-arity`. API fingerprint: `5870d64240fe0643109beb527e6b6b713e90be640cedd8aca2249718bb08cb57`.

<a id="symbol-dedfdc0c797ca0b1f64d38b8"></a>

## builtin / fromTables

Identity: `claim:d1d386364763385ed98ce935`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:dataset-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Dataset.fromTables; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-return-23,data:dataset-scan-changed,data:dataset-scan-eight,data:dataset-scan-empty,data:dataset-scan-many,data:dataset-scan-row-limit`.

Misuse/diagnostic/failure proofs: `data:dataset-copy,data:dataset-empty-parts,data:dataset-over-parts,data:dataset-part-type,data:dataset-schema-mismatch,data:dataset-schema-order-mismatch`. API fingerprint: `9d2eb2bb8c3499a18dfffda979a9c48edb57facbb438de86f8d219baf597e0b8`.

<a id="symbol-01dbaf023fbcf45cc6e77545"></a>

## builtin.Random / seed

Identity: `claim:d2f39b129b1dd314fc858bd1`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_RANDOM_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Random.seed(17).r;r.uniform(0,1000).a;r.uniform(0,1000).b;r.uniform(0,1000).c;"${a}/${b}/${c}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `random:ordered-state-17`.

### Types, ownership, effects, limits and errors

Explicit SplitMix64 v1 state, no entropy capability; native finite-domain distributions and categorical Float Vector weights; nonnegative Int uniform; shuffle of unique local mutable Int Slice at most 64 lanes; lexical creation/release provenance, separate descriptor/payload frames; sample of Int Vector/local Slice returns canonical List with 0..16 entries; independent seed/value/order oracles and rejected effectful interpolation; no whole-source report Independent deterministic seeded states; checked intervals and distribution domains; no cryptographic entropy claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `random:ordered-state-17,random:ordered-state-29,random:two-live-states-29,random:seed-evaluated-once`.

Misuse/diagnostic/failure proofs: `random:seed-type,random:seed-extra`. API fingerprint: `6108b33c273720a017dc0c26474ab4c73808260ae96e70824814f470d5702c23`.

<a id="symbol-adf753b3bb206b47615cab2f"></a>

## builtin.visual / style

Identity: `claim:d31b675b1c30ee373abc69a1`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().v;"next".console();v.style(Range.exclusive(0,1),Console.fg(17,29,53));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:style-exclusive-0-1`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:console.style(range, style) Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:style-clear,console:style-dynamic-endpoints,console:style-exclusive-0-1,console:style-exclusive-1-2,console:style-inclusive-0-1`.

Misuse/diagnostic/failure proofs: `console:style-bound--1-1,console:style-bound-0-3,console:style-bound-2-1,console:style-endpoint-type,console:style-role-type,console:style-selection-arity,console:style-selection-type`. API fingerprint: `060b4333e686c1ddce389eee59e8de105d15b8f04db5727c96b71f659a53bf55`.

<a id="symbol-ffc869c515073d32e7e42575"></a>

## builtin / window

Identity: `claim:d3d8d6de73194afe74a0a8de`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stream<Int>.from([17,29,71]).s;s.window(2).w;w.next().expect("v").at(1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:window-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Stream.window; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:window-changed-2,data:window-empty-2,data:window-filter-all-empty,data:window-filter-empty-buckets,data:window-filter-map-2,data:window-lazy-no-lookahead,data:window-limit-64,data:window-many-2,data:window-one-1,data:window-remaining-cursor,data:window-retained,data:window-return-23,data:window-sink-consumed,data:window-two-live`.

Misuse/diagnostic/failure proofs: `data:window-arity,data:window-callback-trap,data:window-copy,data:window-duration-type,data:window-invalid-duration--1,data:window-invalid-duration-0,data:window-missing-duration,data:window-option-copy,data:window-sink-type`. API fingerprint: `9dc0b7cb6b07623b00d2030728cb033911ce0e68416d9f358332fc8af7d64470`.

<a id="symbol-7d03e465f5660a0a97738b69"></a>

## builtin / Text.byteLength

Identity: `claim:d41dbfa820ee0843f5ee5bd4`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:bytes-0`.

### Types, ownership, effects, limits and errors

byteLength(); O(1); MAX_TEXT_BYTES; ; borrowed immutable receiver; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:bytes-0,text:bytes-2`.

Misuse/diagnostic/failure proofs: `text:arity-byteLength`. API fingerprint: `4d860ea8bb1a50c1e7be0008462507f12f7b948c86d7282df5c8f3c71e694e8f`.

<a id="symbol-9ac895c95f6ac097ef275144"></a>

## builtin / coalesce

Identity: `claim:d44687ceab8f796e02bc5310`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).a;Column<Int>.from([a.get(0),a.get(9),a.get(2)]).left;Column<Int>.from([7]).right;left.coalesce(right);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-coalesce-length`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.coalesce; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-coalesce,data:column-coalesce-both-missing,data:column-coalesce-length`.

Misuse/diagnostic/failure proofs: `data:column-coalesce-type,data:column-dtype-coalesce`. API fingerprint: `42bd3d770e806e6ce7216df6fda6f4909af6dc0c6b87b701978d90d62b9a5506`.

<a id="symbol-78f145ab1b2f9e0bd35ab69e"></a>

## builtin.ScanPlan / minStrength

Identity: `claim:d57dab5c6bd296989b402a05`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.secret(),.minStrength(4),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-93-4-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:minStrength; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-93-4-4,scan:security-93-4-8`.

Misuse/diagnostic/failure proofs: `scan:option-type-minStrength`. API fingerprint: `90eacd0ad2370ec248e98afe39a2249fbd2f66dddbdfa143f40d06c102021972`.

<a id="symbol-6e3c5b75f2db947efa904d58"></a>

## builtin.Vector / Vector

Identity: `claim:d5944006021423146a51ff73`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_VECTOR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,1>.filled(9).a;a.at(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `vector:filled-Int-1-9`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 vector lanes; extent 1..64; typed filled/literal constructors; native vector algorithms and separate owned frames; exact shape/type checks; constant index bounds and dynamic native checks; linked scalar spills preserve RHS allocations; no whole-source report; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `vector:filled-Int-1-9,vector:filled-Int-64--29,vector:filled-Float-64-29.5,vector:literal-bound-64,vector:literal-Float-2-True,vector:two-live-17-29,vector:ordinary-function-local`.

Misuse/diagnostic/failure proofs: `vector:element-Text,vector:element-Bool,vector:extent-0,vector:extent-65,vector:literal-wrong-type,vector:literal-extent-mismatch`. API fingerprint: `57962fdba1fe7c49add4c310ed1c162f153b9725f67af38f6b06ace4678d77eb`.

<a id="symbol-2c73ebde79aec3e38d822b5c"></a>

## builtin / indexOf

Identity: `claim:d625462cb98de7afd1913ea7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;s.indexOf("absent").isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:schema-missing-name`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Schema.indexOf; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:schema-missing-name,data:schema-ordered-age-score,data:schema-ordered-score-age`.

Misuse/diagnostic/failure proofs: `data:schema-index-name-type`. API fingerprint: `8cf24e417ddcbf932a23fb40da7cc10753967a3ac9764a131918cc53cf29e0c1`.

<a id="symbol-4d4eab46ee0b948a613e430d"></a>

## std.math / max

Identity: `claim:d785dbc264a12187d3df24c4`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
// Independent atomic runtime case.
start() { std.math.max(71, 13).return; }

```

Explicit process status: `71`; the full value/effect oracle is `math-max-changed`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `S09,math-max-changed`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `0d8c137095469ba90dc3a08aa2267bcfe431a1ec92d360d0f664882cb3172511`.

<a id="symbol-53984f432ac8dfe617d349c3"></a>

## std.math / tan

Identity: `claim:d7e3b4d2a0621f233366f3a4`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.tan(0.5);29.return;}
```

Explicit process status: `29`; the full value/effect oracle is `math:tan`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:tan`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `fdd3753c3c68f42e91bd0dd094f0dd26be4cf3a5297086beeebb6dc7e836246f`.

<a id="symbol-58522659b358f29d4f405df6"></a>

## builtin / Text.equalsAsciiIgnoreCase

Identity: `claim:d868cfc43c050cbac67070fb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"É".equalsAsciiIgnoreCase("é").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:equals-ascii-1`.

### Types, ownership, effects, limits and errors

equalsAsciiIgnoreCase(Text); ASCII only; O(n); ; borrowed immutable receiver and argument; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:equals-ascii-0,text:equals-ascii-1`.

Misuse/diagnostic/failure proofs: `text:type-equalsAsciiIgnoreCase`. API fingerprint: `f5b22df55c784a45afd22d7a9101000cf2df16bfb9cccbdd313376a5e407a673`.

<a id="symbol-78440ca81515bc72149ab339"></a>

## builtin.Vector / median

Identity: `claim:d87e5aa71b4e98274d214d47`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_STATISTICS_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int> [17,29,43,71].a;"${a.median():fixed(6)}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `statistics:Int-1-median-none`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 lanes, extent 1..64; checked Int sum, native compensated Float sum, explicit population/sample and linearType7 policies; 1536-byte caller workspace isolates conversion/quantile scratch; source values unchanged; native domain/overflow traps and NaN propagation are observed; no source report or synthetic aggregate Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `statistics:Float-0-median-none,statistics:Int-1-median-none,statistics:median-preserves-input,statistics:extent-Int-64`.

Misuse/diagnostic/failure proofs: `statistics:arity-median`. API fingerprint: `6b84394a45392c2115cbfd13b715c2ca02899c3708edf5d61b0c08edf2836a82`.

<a id="symbol-1a11e4e0ce7103e12a5017ad"></a>

## builtin / capacity

Identity: `claim:d9af5dbe5cd58b17c26c06a6`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.withCapacity(8).d;d.capacity().return;}

```

Explicit process status: `8`; the full value/effect oracle is `dict:capacity-8`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.capacity; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:capacity-8,dict:reserve-retains-value`.

Misuse/diagnostic/failure proofs: `dict:use-after-move`. API fingerprint: `00b2978a277366d62be6989d94d3f6315030ec8f43209e0cc1890fc9de798ade`.

<a id="symbol-f5ba79b0421e9cbf01dbff2a"></a>

## builtin.visual / clear

Identity: `claim:da20bfa83f48811c625e1146`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.clear();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:clear`.

### Types, ownership, effects, limits and errors

Spelling alias only; inspect every qualified declaration: g018-declaration:console.clear(); g018-declaration:canvas.clear(color) Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:clear,console:clear-append,console:clear-preserves-title,console:clear-removes-color,console:style-clear`.

Misuse/diagnostic/failure proofs: `console:clear-arity,console:close-clear`. API fingerprint: `428e1a4a29998f3ac9cbd647af3946c7b5e006900b31f0335198307dafa99b71`.

<a id="symbol-c68afd5b8129ed1d60d05c86"></a>

## builtin.ScanPlan / mock

Identity: `claim:dab9d08c0346a6a395720dae`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("23")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-23-return-23`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:mock; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:int-23-return-23,scan:two-int-values,scan:two-text-values`.

Misuse/diagnostic/failure proofs: `scan:duplicate-mock,scan:mock-type`. API fingerprint: `8a78d39b2c6c428140e1e343f3e8c05015b9fc1233fdd53bdb10289dfa4a3566`.

<a id="symbol-459647e7a64c9eb25c96f6e5"></a>

## builtin / dijkstra

Identity: `claim:dca093f988973a0a07cdd72b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,9223372036854775807);g.addEdge(b,c,9223372036854775807);g.addEdge(c,d,9223372036854775807);g.dijkstra(a,d);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `relation:graph-dijkstra-overflow`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.dijkstra; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-dijkstra-19-2,relation:graph-dijkstra-31-3,relation:graph-dijkstra-5-11,relation:graph-dijkstra-overflow`.

Misuse/diagnostic/failure proofs: `relation:graph-dijkstra-nodes-type`. API fingerprint: `3e32bfc75b6c459b4db380d85a4c7e6f481c40c66e11274af3836500f164600c`.

<a id="symbol-030d65af32c91587755ef389"></a>

## std.math / clamp

Identity: `claim:ddd75188695701aa50db12fa`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
// Atomic pure integer math; expected values come from the Python oracle.
start() { std.math.clamp(73, 17, 41).return; }

```

Explicit process status: `41`; the full value/effect oracle is `math-clamp`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math-clamp,math-clamp-signed`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `3c33c83142d805bf589e551cda30bbf72a4e582a94654dd40d1b2575eef1b5e0`.

<a id="symbol-08bb98b2e6a42bc992dd4ef3"></a>

## builtin / Text.replaceAll

Identity: `claim:de9488198c88a7f95371751a`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"ação ação".replaceAll("ação","é").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:replaceAll-2`.

### Types, ownership, effects, limits and errors

replaceAll(Text,Text); 4096 output bytes; non-overlapping; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:replaceAll-0,text:replaceAll-2`.

Misuse/diagnostic/failure proofs: `text:type-replaceAll`. API fingerprint: `e87caad03edffb860e3a9adb1310cf98bb78544cb82ad3ffbc77954ab1296748`.

<a id="symbol-473713f3e73483e249a1049a"></a>

## builtin / connectedComponents

Identity: `claim:df4f0148b2440ecd656bf01f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.undirected().g;g.connectedComponents().p;p.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `relation:graph-components-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.connectedComponents; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-components,relation:graph-components-directed,relation:graph-components-empty,relation:graph-components-joined,relation:graph-components-snapshot`.

Misuse/diagnostic/failure proofs: `relation:graph-connectedComponents-arity`. API fingerprint: `949276c69db8d887c92743297c5585e0f4c99619b9efcabd2e022230491319ff`.

<a id="symbol-1316009d4034edcfd974d140"></a>

## builtin / equals

Identity: `claim:e1043152c29f090c6eeb1943`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.equals(17).console();17.equals(29).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:int-equals`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Int.equals; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:int-equals`.

Misuse/diagnostic/failure proofs: `dict:equals-type`. API fingerprint: `16db2023fca45bf7d4e9a674e6358d3c3aace78997117a1f5ee80266c369ae1c`.

<a id="symbol-968454974d28c8031c5e693f"></a>

## builtin / addChild

Identity: `claim:e1282f512e9258c2f1e099b7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.addChild(t.root(),71).n;n.value().return;}
```

Explicit process status: `71`; the full value/effect oracle is `relation:tree-child`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.addChild; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-child,relation:tree-foreign-parent`.

Misuse/diagnostic/failure proofs: `relation:tree-raw-handle,relation:tree-wrong-child-value`. API fingerprint: `3ee78a3a9167baa8b55d3660a72376e5c7ac5db18b980f8988fccbeb0b87c758`.

<a id="symbol-1d86b587e2f8948f466bd888"></a>

## std.math / min

Identity: `claim:e15da49ba928c94d5643b708`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
// Atomic pure integer math; expected values come from the Python oracle.
start() { std.math.min(53, 11).return; }

```

Explicit process status: `11`; the full value/effect oracle is `math-min`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math-min`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `051c742a9027cda94cc6aa9f379d6eb94fb198dfc198839b6c8a77ab48dcbd3e`.

<a id="symbol-c2c276f46656f941ebb10145"></a>

## builtin / Table

Identity: `claim:e1694ab28375fdf523789369`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17]))]).t;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-return-23`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Table.join,g010-public:Table.validate,g010-public:Table.invalidRows,g010-public:Table.groupBy,g010-public:Table.groupBy.next,g010-public:Table.groupBy.sink,g010-public:Table.fromColumns,g010-public:Table.rowCount,g010-public:Table.columnCount,g010-public:Table.select,g010-public:Table.filter,g010-public:Table.sortBy; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:group-changed,data:group-empty,data:group-independent-cursor,data:group-limit,data:group-many,data:group-missing-3,data:group-missing-7,data:group-next,data:group-normal-cleanup,data:group-one,data:group-retained-table,data:group-same,data:group-signed,data:group-sink-cursor,data:group-two-live,data:join-changed-1,data:join-empty-1,data:join-kind-once,data:join-limit-1,data:join-many-1,data:join-many-2,data:join-missing-1-0-2,data:join-one-1,data:join-signed-1,data:join-source-preserved,data:join-two-live,data:quality-default,data:quality-empty,data:quality-limit-32,data:quality-mask-0-0-0,data:quality-mask-3-5-0,data:quality-mask-3-5-3,data:quality-mask-7-7-0,data:quality-mask-7-7-3,data:quality-repeated,data:quality-source-missing-preserved,data:quality-two-live,data:table-callback-native-local,data:table-constructor-snapshot,data:table-dimensions-empty,data:table-dimensions-limit,data:table-dimensions-many,data:table-dimensions-one,data:table-eight-columns,data:table-filter-changed-20,data:table-filter-empty-20,data:table-filter-limit-20,data:table-filter-many-20,data:table-filter-many-50,data:table-return-23,data:table-select-order,data:table-select-sort-compose,data:table-select-subset,data:table-sort-metamorphic--31-53-11,data:table-sort-metamorphic-29-17-29,data:table-sort-metamorphic-71-7-83,data:table-sort-missing-0,data:table-sort-missing-1,data:table-sort-missing-2,data:table-sort-missing-3,data:table-sort-missing-5,data:table-sort-missing-7,data:table-sort-order,data:table-sort-source-independent`.

Misuse/diagnostic/failure proofs: `data:group-copy,data:group-empty-expect,data:group-key-arity,data:group-key-two,data:group-key-type,data:group-method-arity,data:group-missing-name,data:group-sink-type,data:join-arity,data:join-duplicate-nonkey,data:join-keys-type,data:join-kind-type,data:join-other-type,data:join-over-limit-1,data:join-over-limit-2,data:quality-constraints-arity,data:quality-constraints-type,data:quality-invalid-rows-arity,data:quality-schema-count,data:quality-schema-name,data:table-column-count-arity,data:table-copy,data:table-duplicate-name,data:table-empty-columns,data:table-filter-arity,data:table-filter-receiver-type,data:table-filter-result-type,data:table-filter-trap,data:table-row-count-arity,data:table-select-duplicate-name,data:table-select-empty,data:table-select-name-type,data:table-select-unknown-name,data:table-sort-name-type,data:table-sort-two-keys,data:table-sort-unknown-name,data:table-unequal-lengths,data:table-wrong-column,data:table-wrong-name`. API fingerprint: `b31d32b2eb2be28c2bddb6547086e061cb9e994099b145d6d5e14705e699d6f5`.

<a id="symbol-5f9b505e82a4a22e2ff794c9"></a>

## builtin / toTuple

Identity: `claim:e1e12033665161272e214426`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;Row.from(s,[17,71]).r;r.toTuple().t;t.length().console();t.at<0>().expect("v").console();t.at<1>().expect("v").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:row-tuple-order`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Row.toTuple; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:row-eight-fields,data:row-missing-3,data:row-tuple-order,data:row-tuple-two-live`.

Misuse/diagnostic/failure proofs: `data:row-to-tuple-arity,data:row-tuple-copy,data:row-tuple-index-bound,data:row-tuple-static-bound`. API fingerprint: `b640c3962e2f6285483c5e0e3d40e8c5abb5ba0cfbb8e03532ac88fcaf762d60`.

<a id="symbol-d5e2955054baf67c3f99c080"></a>

## builtin / Text.normalizeWhitespace

Identity: `claim:e21a03c4a4d252ce269a85bb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"A\r\nB\rC".normalizeWhitespace().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:whitespace-0`.

### Types, ownership, effects, limits and errors

normalizeWhitespace(); 4096 output bytes; ASCII whitespace; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:whitespace-0,text:whitespace-1`.

Misuse/diagnostic/failure proofs: `text:arity-normalizeWhitespace`. API fingerprint: `5607e6182cd485e6f750d76d3c53c06574c28e2e1bf42f7a7c7545c74fc48555`.

<a id="symbol-0648b347872db6e4cc9b3897"></a>

## builtin / Text.normalizeNewlines

Identity: `claim:e28c6ed1f7608e0e538cd4e2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"a\n\nb".normalizeNewlines().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:newlines-3`.

### Types, ownership, effects, limits and errors

normalizeNewlines(); 4096 output bytes; CRLF/CR to LF; ; owned caller workspace; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:newlines-0,text:newlines-3`.

Misuse/diagnostic/failure proofs: `text:arity-normalizeNewlines`. API fingerprint: `6ee2c631f45bfc809744b3cc4dc7f3d404c64249bc92a73eaa6c1fe78b1d9120`.

<a id="symbol-8ac1423d16bf585ff10a9e40"></a>

## builtin / entries

Identity: `claim:e2f8aa80437d021072f2d498`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.entries().v;v.next().isNone().console();v.release();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:entry-view-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.entries; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:entry-view-empty,dict:entry-view-pair`.

Misuse/diagnostic/failure proofs: `dict:view-released`. API fingerprint: `668890c746c882b7e76d8028b6a33be452ae71138a9648d4430d6f0dfc156543`.

<a id="symbol-f13140bbd87f00f9721ee8db"></a>

## builtin.ScanPlan / confirmSecret

Identity: `claim:e373f34ea6d115dc9edf77b3`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.console(),.secret(),.confirmSecret(),.result(),.replay("answers.txt")).isOk().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:confirm-trace-Different9!`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:confirmSecret; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:confirm-trace-Synthetic7!,scan:confirm-trace-Different9!,scan:confirm-retry-pair`.

Misuse/diagnostic/failure proofs: `scan:confirm-eof-result,scan:confirm-binding-failure,scan:confirm-needs-secret,scan:security-arity-confirmSecret`. API fingerprint: `d5683d0faa36ce6f5bacddaf32e65c09fba294ef60bdaf6f5bd873e9a1e5b249`.

<a id="symbol-ebbb5cbaa9128cf282edd620"></a>

## builtin.Float / isInfinite

Identity: `claim:e51b046dbfa30b7291fbad28`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_FUNCTION_NATIVE_NUMERIC_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∞.value;value.isInfinite().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `precision:infinite-isInfinite`.

### Types, ownership, effects, limits and errors

Binary64 actual receiver and once-evaluated arguments; classifiers include IEEE subnormal/NaN/infinity/signed zero; explicit rounding modes restore caller control word; finite round magnitude <2^63; approxEquals preserves two source arguments with Vector<Float> tolerance [absolute,relative], exactly two lanes (runtime shape checked); nonnegative/non-NaN tolerances, identical infinities equal and distinct infinities unequal; Uncertain methods claim only exact typed operands; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `precision:finite-isInfinite,precision:infinite-isInfinite,precision:negative-infinite-isInfinite,precision:nan-isInfinite`.

Misuse/diagnostic/failure proofs: `precision:classifier-extra-argument`. API fingerprint: `8fec4ab0ee8e340cb07dec935f1f4b7aaef67b5ea0950f6417ae14e9667ce77d`.

<a id="symbol-18f0ec86930d0a201dc36535"></a>

## builtin.Float / round

Identity: `claim:e664b92dda5c99ae936f11a7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_FUNCTION_NATIVE_NUMERIC_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){2.5.value;value.round(nearestEven).out;(out==2.0).console();0.return;}
```

Explicit process status: `0`; the full value/effect oracle is `precision:round-return-0`.

### Types, ownership, effects, limits and errors

Binary64 actual receiver and once-evaluated arguments; classifiers include IEEE subnormal/NaN/infinity/signed zero; explicit rounding modes restore caller control word; finite round magnitude <2^63; approxEquals preserves two source arguments with Vector<Float> tolerance [absolute,relative], exactly two lanes (runtime shape checked); nonnegative/non-NaN tolerances, identical infinities equal and distinct infinities unequal; Uncertain methods claim only exact typed operands; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `precision:round-return-0,precision:round-return-7,precision:round-return-23,precision:round-return-29,precision:round-return-255,precision:round-mode-binding,precision:round-mode-effect-once,precision:round-negative-zero,precision:native-control-word`.

Misuse/diagnostic/failure proofs: `precision:round-mode-type,precision:round-missing-mode,precision:round-extra-mode,precision:round-process-return,precision:round-domain-0-positive-bound`. API fingerprint: `9f094adca21fd467ae48532baa11fc93ae79a451c5fed623d618815364683580`.

<a id="symbol-1b3348c920fb0f7fb2c1569e"></a>

## builtin / breadthFirst

Identity: `claim:e7f93db4bceb04c0ad2147b4`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.breadthFirst(a).v;v.next().expect("n").value().return;}
```

Explicit process status: `17`; the full value/effect oracle is `relation:traversal-return`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Graph.breadthFirst; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-breadthFirst,relation:traversal-break,relation:traversal-continue,relation:traversal-return,relation:traversal-scope,relation:traversal-temporary`.

Misuse/diagnostic/failure proofs: `relation:graph-breadthFirst-node-type,relation:traversal-borrow`. API fingerprint: `7da09f7fa14efec0b18cfa26e0f1a6bd5bce2f00f0063034ffa671b08c51c0b4`.

<a id="symbol-b6f9e3ee885433daa0757966"></a>

## builtin.ScanPlan / mask

Identity: `claim:e8149ff28098a7f2e0908db7`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.secret(),.mask("*"),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-605-1-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:mask; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-605-1-4,scan:security-605-1-8`.

Misuse/diagnostic/failure proofs: `scan:option-type-mask`. API fingerprint: `3ce38fed213001809d976fb0c1cc8e3e8cb8f88ccb7df192aedf004414e6c97b`.

<a id="symbol-e6cffe71f40e9581e11cc286"></a>

## std.math / sin

Identity: `claim:ea1c3af532dae0be53acddcd`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.sin(0.5);29.return;}
```

Explicit process status: `29`; the full value/effect oracle is `math:sin`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:sin`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `6960655e66eed24a2704c9bed9bc5a420fae873236b86fe37576bf662184f2e0`.

<a id="symbol-4839382e0102155bb8c0ab91"></a>

## builtin / new

Identity: `claim:eac52e4a95d35b1330df17ef`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict<Int,Int>.new,g008-public:Set<Int>.new,g008-public:Hasher.new; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:empty,dict:empty-reproducer,dict:hasher-changed-seed,dict:hasher-empty,dict:set-insert-remove`.

Misuse/diagnostic/failure proofs: `dict:hasher-seed-type,dict:key-type,dict:set-new-arity,dict:value-type`. API fingerprint: `aa05b2fbe760570a403f89ed1a871986c31e7af8e136b1b7e9707d7392b2acfa`.

<a id="symbol-1377584979be2e237ceced3d"></a>

## std.regex / Regex

Identity: `claim:ebc047cacea7df56097706f2`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `STDLIB_DECLARATION_TO_NATIVE_LITERAL_REGEX`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
import "std.regex" { Regex; }.patterns;
start(){Regex.compile("").r;r.isMatch("").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `regex:match-0-0`.

### Types, ownership, effects, limits and errors

Actual owned typed Regex.compile(pattern) with the current default options, Regex.escape(Text), and isMatch(Text), reusing native pattern_find rather than a Python runtime or fixed report. Initial public native profile is Unicode literal patterns and escaped punctuation only: 256 pattern bytes, 4096 text bytes, 65536 search steps. Regex metacharacter operators/captures and custom options are explicitly rejected in this source profile; the wider SDK model is not counted as native source coverage. Independent Python re search/escape oracles, two live values and explicit return variations are executed. Literal matching and escaped punctuation; 256 pattern bytes; 4096 input bytes; bounded steps; no full regex claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/textual/x86_64/regex_public_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/regex_public.asm`.

Positive/changed-value/boundary proofs: `regex:match-0-0,regex:match-1-2,regex:match-2-3,regex:match-3-4,regex:match-4-5,regex:match-6-7,regex:escape-3,regex:escape-compose-3,regex:two-live,regex:two-live-miss,regex:input-boundary,regex:return-255`.

Misuse/diagnostic/failure proofs: `regex:compile-type,regex:match-type,regex:status-type,regex:pattern-domain-0,regex:text-domain,regex:steps-domain`. API fingerprint: `9280d7820226c7bffc7063b760a7f686e0168b33e5fddef56d9dfc5f408c4b46`.

<a id="symbol-62077b509cce17098e5acb63"></a>

## builtin / project

Identity: `claim:ebc56343af6bbadff156dda5`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;Row.from(s,[17,71]).r;r.project(["score","age"]).p;p.project(["age"]).q;q.get("age").expect("v").return;}
```

Explicit process status: `17`; the full value/effect oracle is `data:row-project-composition`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Row.project; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:row-project-changed-order,data:row-project-composition,data:row-project-missing,data:row-project-subset`.

Misuse/diagnostic/failure proofs: `data:row-project-argument-type,data:row-project-duplicate-name,data:row-project-empty,data:row-project-name-type,data:row-project-removed-name,data:row-project-unknown-name`. API fingerprint: `e88e76835db4bf4b04f87ea36e54e0ec940d45ba0326f43d2e092d818550991d`.

<a id="symbol-6c82b3440ffac59c78afff4d"></a>

## builtin / Text.trimEnd

Identity: `claim:ec6979e772820bedad52b29f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".trimEnd().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:trimEnd-2`.

### Types, ownership, effects, limits and errors

trimEnd(); input bytes; ; borrowed view; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:trimEnd-0,text:trimEnd-2`.

Misuse/diagnostic/failure proofs: `text:arity-trimEnd`. API fingerprint: `cfa6a0c116e6b573bd34d0b87ed11ba352319ca177de332d98d405f17ea01c3b`.

<a id="symbol-bb4ac35bd94c4e151f4e7f32"></a>

## builtin.ScanPlan / password

Identity: `claim:ed6761c74b27ad1b60f145eb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.password(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-95-1-4`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:password; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-95-1-4,scan:security-95-1-8`.

Misuse/diagnostic/failure proofs: `scan:weak-secret,scan:option-arity-password`. API fingerprint: `10f42fdd12f6aed92eb088ece33e387e4b743bbbbfa0d86e656e2e0edcde396f`.

<a id="symbol-6fb9d867fc6efd5c752f9c3b"></a>

## std.core / Range

Identity: `claim:edba8deaca5346c91645f9ed`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start() {
    Range.exclusive(2, 10).stepBy(2).progression;
    progression.length().return;
}

```

Explicit process status: `4`; the full value/effect oracle is `composite:range-exclusive-length`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:range-inclusive-length,composite:range-exclusive-length,control:for,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:range-zero-step`. API fingerprint: `b104f11ed140222f87dd6e4f4fbf97a696a1687cccf4502ff904fce6c6283ec0`.

<a id="symbol-882f4f2141230553560dd599"></a>

## std.math / log

Identity: `claim:ee65c8536fe60039f4a5f212`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.log(7.0);29.return;}
```

Explicit process status: `29`; the full value/effect oracle is `math:log`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:log`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `7b37da854f7f2a20eaec5ae9c8af18f3acdde0257e473816d58ddd7b76eb0eb2`.

<a id="symbol-428618ddadb1f897873f83fa"></a>

## builtin / sum

Identity: `claim:ef117fcc0e9f03791231e0cf`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([]).c;c.sum().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-sum-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.sum; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-sum-changed,data:column-sum-empty,data:column-sum-limit,data:column-sum-many,data:column-sum-overflow`.

Misuse/diagnostic/failure proofs: `data:column-sum-arity`. API fingerprint: `348373dfe3dd8a625f50ffb25699f77fd7d5999305b34912721bcb1b9f2cbd5c`.

<a id="symbol-65250a15ef24fdc8dd0ec4b9"></a>

## std.math / exp

Identity: `claim:efa3beb45cb68277b395a2a8`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TOKEN_TYPED_SCALAR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){std.math.exp(1.25);29.return;}
```

Explicit process status: `29`; the full value/effect oracle is `math:exp`.

### Types, ownership, effects, limits and errors

Literal Int64/binary64 operands; at most 128 complete statements; independent arithmetic trace and return matrix Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `math:exp`.

Misuse/diagnostic/failure proofs: `math:math-text-return,math:record-capacity-plus-one`. API fingerprint: `cb4082444e4622656747b2be3f4eb22954ef0c3617825bb0501621e72dd9d713`.

<a id="symbol-c3a57b408dda9dfac6dfa730"></a>

## builtin / replaceValue

Identity: `claim:f0579b177e3fb61a2d0cdd03`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(17).n;n.replaceValue(71).console();n.value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:node-replace`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Node.replaceValue; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-replace,relation:node-replace`.

Misuse/diagnostic/failure proofs: `relation:node-wrong-replacement`. API fingerprint: `c33d394e56d20bf22adb5186c09dfa8d8bcec985b2b2d1608df7179f47f8dc2e`.

<a id="symbol-51d61e84ef84d142e5ed2f71"></a>

## builtin.Vector / filled

Identity: `claim:f1f3aa4cde215a622900f030`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_AST_NATIVE_VECTOR_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Vector<Int,4>.filled(9).a;a.at(2).console();0.return;}
```

Explicit process status: `0`; the full value/effect oracle is `vector:return-Int-0`.

### Types, ownership, effects, limits and errors

Actual Int64/binary64 vector lanes; extent 1..64; typed filled/literal constructors; native vector algorithms and separate owned frames; exact shape/type checks; constant index bounds and dynamic native checks; linked scalar spills preserve RHS allocations; no whole-source report; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `vector:filled-Int-4-9,vector:filled-Float-4-1.5,vector:return-Int-0,vector:return-Int-255,vector:return-Float-23,vector:calls-effects-once`.

Misuse/diagnostic/failure proofs: `vector:filled-type-Int-true,vector:filled-type-Float-17,vector:empty-arguments,vector:extra-arguments`. API fingerprint: `e4f17ff61f621075b0cf701d411d1998212199caf1194b1b76182c937aff2993`.

<a id="symbol-85019085043659ddc57a58fd"></a>

## builtin.ScanPlan / field

Identity: `claim:f21e8f40ed63519f770b9251`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.field("field0","Int"),.field("field1","Int"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-fields-2`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:field; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings,scan:console-fields-2,scan:console-fields-29`.

Misuse/diagnostic/failure proofs: `scan:field-key-type,scan:field-value-type,scan:field-arity`. API fingerprint: `75cb0e18302f6981b4807b17317894529afabf25eb5d174cc4e98578bbb43387`.

<a id="symbol-452a283c0611d3a49de68df5"></a>

## builtin.Float / isFinite

Identity: `claim:f5e7ec639e82011bf56bb6ab`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_FUNCTION_NATIVE_NUMERIC_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){∞.value;value.isFinite().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `precision:infinite-isFinite`.

### Types, ownership, effects, limits and errors

Binary64 actual receiver and once-evaluated arguments; classifiers include IEEE subnormal/NaN/infinity/signed zero; explicit rounding modes restore caller control word; finite round magnitude <2^63; approxEquals preserves two source arguments with Vector<Float> tolerance [absolute,relative], exactly two lanes (runtime shape checked); nonnegative/non-NaN tolerances, identical infinities equal and distinct infinities unequal; Uncertain methods claim only exact typed operands; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Native checked scalar/vector mathematics; independent numeric oracle; explicit process return separate from intermediate value

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `precision:finite-isFinite,precision:subnormal-isFinite,precision:infinite-isFinite,precision:nan-isFinite`.

Misuse/diagnostic/failure proofs: `precision:classifier-extra-argument`. API fingerprint: `009ce686ad29f84adeb047f1238fbaa4db29dac383ef8cd6ead637a56d5d773a`.

<a id="symbol-48f497839164f3ad1ccb7250"></a>

## std.core / Eq

Identity: `claim:f63e75ad5095ebabf67198ad`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
generic<T> (T.value)hold() where T: Hash + Eq{value.return;}start(){17.hold().a;29.hold().b;(a+b).console();7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `composite:generic-compose-whereTHashEq-7`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:generic-compose-whereTHashEq-7,composite:generic-compose-whereTHashEq-23,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:generic-hash-eq-failure`. API fingerprint: `64d02dc4cc49ba3b2dda2c625e55a16e2e5d6863f751fa2bfd5120dd0e27115c`.

<a id="symbol-468139543624eabcffa6fdee"></a>

## std.core / Ord

Identity: `claim:f6db667ad6eb8d84d0a4c098`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
generic<T> (T.value)hold() where T: Ord{value.return;}start(){17.hold().a;29.hold().b;(a+b).console();7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `composite:generic-compose-whereTOrd-7`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:generic-compose-whereTOrd-7,composite:generic-compose-whereTOrd-23,composite:generic-compose-whereTOrd-255,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `composite:generic-ord-bool`. API fingerprint: `c918c73bdcb4e81c87aa1e4d500e889d4098d7957064ec22007c2a76c625256f`.

<a id="symbol-63534d12481bd6ede534c8e0"></a>

## builtin / parent

Identity: `claim:f6f3bdc4481d28e97276b5ee`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.parent(t.root()).isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-root-parent`.

### Types, ownership, effects, limits and errors

Spelling alias for g009-public:Tree.parent; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-parent,relation:tree-root-parent`.

Misuse/diagnostic/failure proofs: `relation:tree-parent-node-type`. API fingerprint: `2353058f0a5b8222951ab5693f4cf3b2199b6ce7aa28a37974e819c787c98f66`.

<a id="symbol-1f1ac8e1c63e04699fb8a8b1"></a>

## builtin.ScanPlan / scan

Identity: `claim:f7adc0185da65a7014d9fd8e`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("23")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-23-return-23`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:scan; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:int-23-return-23,scan:text-4e65626f,scan:bool-False,scan:char-81,scan:two-results-live`.

Misuse/diagnostic/failure proofs: `scan:invalid-process-type,scan:unknown`. API fingerprint: `8d3f7add4d8445f57ea893a64c950d91fd2f4860c6d50d9b2c8823bd156dc8b0`.

<a id="symbol-d228915ff623266cf4ff328d"></a>

## builtin / dropMissing

Identity: `claim:f964af5f37200ef063a4f559`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).a;Column<Int>.from([a.get(0),a.get(1),a.get(2)]).c;c.dropMissing().d;d.countMissing().console();d.sum().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-drop-0`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.dropMissing; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-drop-0,data:column-drop-1,data:column-drop-2,data:column-drop-3,data:column-drop-5,data:column-drop-7`.

Misuse/diagnostic/failure proofs: `data:column-dropMissing-arity`. API fingerprint: `23806b95e0f96c79322be2d0edb04a4078547c3d1aa67d1871c447a055c767c6`.

<a id="symbol-d62468f7262f939db2caa8b3"></a>

## builtin.ScanPlan / enum

Identity: `claim:f9aa11780a5d6e2053cc2d1f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_CONTEXTUAL_TYPED_SOURCE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.enum(["red","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-variant-enum-red`.

### Types, ownership, effects, limits and errors

Contextual alias for g170-scan:enum; public typed source proof replaces the historical seed/probe evidence. Preserve the qualified row bounds; no separate global option or live capability is claimed. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-variant-enum-red,scan:choice-variant-enum-blue`.

Misuse/diagnostic/failure proofs: `scan:choice-variant-enum-absent,scan:choice-type-enum`. API fingerprint: `a56c805946c91dd3a015811b89b0eaa2777a738d4740dbf55a291ef9d46ee54c`.

<a id="symbol-666132fa5bcc33bf183722ed"></a>

## builtin / countMissing

Identity: `claim:f9d361b4f62271e8a2dd6e6d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([17,71]).c;c.countMissing().return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:column-no-missing`.

### Types, ownership, effects, limits and errors

Spelling alias for g010-public:Column.countMissing; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-missing-0,data:column-missing-1,data:column-missing-2,data:column-missing-3,data:column-missing-5,data:column-missing-7,data:column-no-missing`.

Misuse/diagnostic/failure proofs: `data:column-countMissing-arity`. API fingerprint: `95c0d18f87226328a8d9723cc990f4fc70881ccca5804ab82a97ebf4b6645eff`.

<a id="symbol-6f6fe376d363a8d469dc7541"></a>

## std.console / console

Identity: `claim:fb586a453cf389e6ca5ed7ed`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `AUTHENTICATED_PRELUDE_DECLARATION_TO_RUNTIME_PROFILE`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-17`.

### Types, ownership, effects, limits and errors

Actual edition-owned prelude SymbolId and unique stdlib owner; CLI metadata checked against the local .ni interface and independent FNV identity. Runtime profile is the cited source case family. Iterator/IntoIterator dispatch is bounded Range iteration, not a standalone cursor constructor or arbitrary iterator implementation. Ord reuses the existing comparable capability, with explicit Bool rejection. Native intrinsic profiles are not promoted to arbitrary generic APIs. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:publish-seed,console:publish-17,console:publish-True,metadata:prelude-declarations`.

Misuse/diagnostic/failure proofs: `console:bare-color`. API fingerprint: `675f5d882eda3dc81f0ddd2cc626c07e46defb7cc35b99d28c597a7845280bae`.

<a id="symbol-eed3021c201d93e268042832"></a>

## builtin / values

Identity: `claim:fdc16e93d3b89cd08b34b44b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `REGISTRY_ATOM_TO_QUALIFIED_DECLARATIONS`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.values().v;v.next().unwrapOr(73).x;v.release();x.return;}

```

Explicit process status: `73`; the full value/effect oracle is `dict:view-values-empty`.

### Types, ownership, effects, limits and errors

Spelling alias for g008-public:Dict.values; no additional API counted; preserve qualified profile limits; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:view-exhaustion,dict:view-values,dict:view-values-empty`.

Misuse/diagnostic/failure proofs: `dict:view-reseed`. API fingerprint: `ac4419a995e0d66c9067615d84cdef4302d2fa96819dd09492d8e2da0e668de8`.

<a id="symbol-9483bfcc14982b8f9a280c9c"></a>

## builtin / Text.takeBytes

Identity: `claim:fe608d3195a9d10f99232fc9`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".takeBytes(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:takeBytes-0`.

### Types, ownership, effects, limits and errors

takeBytes(Int); non-negative; saturates at byte length; ; borrowed view; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:takeBytes-0,text:takeBytes-2`.

Misuse/diagnostic/failure proofs: `text:type-takeBytes`. API fingerprint: `ce10dba45cd9f765aac6a9d81edfa1020679234c64c1a8e7391b8b07de9cb30a`.

<a id="symbol-cae12dd8a398fd9388f6c492"></a>

## builtin / Text.contains

Identity: `claim:feb47573c015bf6f3f8c20fb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CURRENT_API_REGISTRY_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".contains("absent").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `text:contains-2`.

### Types, ownership, effects, limits and errors

contains(Text); literal bytes; O((n-m+1)m); ; borrowed immutable receiver and argument; public UTF-8 descriptor bridge and distinct bounded function storage; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite UTF-8 byte/codepoint/grapheme units are distinct; per-member finite profile

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/parser/option_result_vertical_parser.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/textual/text_core.asm`.

Positive/changed-value/boundary proofs: `text:contains-0,text:contains-2`.

Misuse/diagnostic/failure proofs: `text:type-contains`. API fingerprint: `8b1f0db3eab198b78be7f8acb9292798d89b4362d8d5c60a76478d0eae33914c`.

<a id="symbol-3195e1f92eeabe0cb021c8c5"></a>

## builtin / Deque.popBack

Identity: `g007-public:Deque.popBack`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Deque<Int>.new().d;d.pushBack(29);d.pushFront(17);d.pushBack(71);d.popBack().console();d.popFront().console();d.popFront().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:deque-ends`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:deque-ends`.

Misuse/diagnostic/failure proofs: `sequence:deque-list-method`. API fingerprint: `576aaca38b2fbc7fb760554f5ef3229b8053f55c323ab491afda6ec7090e2fee`.

<a id="symbol-78fdfbb807ab24237c3d6a79"></a>

## builtin / Deque.popFront

Identity: `g007-public:Deque.popFront`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Deque<Int>.new().d;d.pushBack(29);d.pushFront(17);d.pushBack(71);d.popBack().console();d.popFront().console();d.popFront().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:deque-ends`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:deque-ends`.

Misuse/diagnostic/failure proofs: `sequence:deque-list-method`. API fingerprint: `4c5f29fbfcd1334c27552873637cd1374fcdfd75caf65b9fe723375cac852e6f`.

<a id="symbol-65c5d253ca0b922744fcfcc8"></a>

## builtin / Deque.pushBack

Identity: `g007-public:Deque.pushBack`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Deque<Int>.new().d;d.pushBack(29);d.pushFront(17);d.pushBack(71);d.popBack().console();d.popFront().console();d.popFront().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:deque-ends`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:deque-ends`.

Misuse/diagnostic/failure proofs: `sequence:Deque-wrong-value`. API fingerprint: `e1ccb65198961e304de8f603b6ffe70ef17dacb43b6b292616a688fbd040b3fb`.

<a id="symbol-beb9c62916e0f4b2d2b42904"></a>

## builtin / Deque.pushFront

Identity: `g007-public:Deque.pushFront`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Deque<Int>.new().d;d.pushBack(29);d.pushFront(17);d.pushBack(71);d.popBack().console();d.popFront().console();d.popFront().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:deque-ends`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:deque-ends`.

Misuse/diagnostic/failure proofs: `sequence:Deque-wrong-value`. API fingerprint: `1d0c3f618394530a0fd35be1d0fea5df277acffcff27b03ba43c12323f6733b6`.

<a id="symbol-8b88ab6ed31ec0042835fdd9"></a>

## builtin / List.at

Identity: `g007-public:List.at`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().a;a.at(0).return;}
```

Explicit process status: `177`; the full value/effect oracle is `sequence:list-runtime-bounds`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:from-many,sequence:list-runtime-bounds`.

Misuse/diagnostic/failure proofs: `sequence:list-at-type`. API fingerprint: `3b30e63171fee52a78831a66c3fc5e19b20f5f8ab17b090230133038fc852923`.

<a id="symbol-33dbc05a44f0ab6d4d45cf5f"></a>

## builtin / List.capacity

Identity: `g007-public:List.capacity`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.withCapacity(5).items;items.capacity().return;}
```

Explicit process status: `5`; the full value/effect oracle is `sequence:list-capacity`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-capacity,sequence:list-reserve`.

Misuse/diagnostic/failure proofs: `sequence:list-capacity-arity`. API fingerprint: `63ab838ff6b8211f2bd52e615944f2598c2e6fcb53f5171cdf1bf273604847ed`.

<a id="symbol-5f611fd51134cf40e6c57846"></a>

## builtin / List.contains

Identity: `g007-public:List.contains`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29]).a;a.contains(29).console();a.contains(71).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:contains-present-absent`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:contains-present-absent`.

Misuse/diagnostic/failure proofs: `sequence:list-contains-type`. API fingerprint: `8a4ca19df7986400327201483c86b318f28754daea45a474fe240f2857fb50eb`.

<a id="symbol-243ffcaabe801f9a1280687e"></a>

## builtin / List.deduplicate

Identity: `g007-public:List.deduplicate`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,17,29,17]).a;a.deduplicate();a.length().console();a.at(2).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:deduplicate-adjacent`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:deduplicate-adjacent`.

Misuse/diagnostic/failure proofs: `sequence:list-deduplicate-arity`. API fingerprint: `1da8b5825be8e334a019bd24ba9102c98fa6c709434666e6086149cbe9be9a9a`.

<a id="symbol-0388e2a11c6f951dd687cf72"></a>

## builtin / List.filter

Identity: `g007-public:List.filter`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)choose(){(self<20).return;}start(){List<Int>.from([29,-17,71]).a;a.filter(choose).b;b.length().console();b.at(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:filter-less`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:filter-greater,sequence:filter-less`.

Misuse/diagnostic/failure proofs: `sequence:filter-return-type,sequence:list-filter-type`. API fingerprint: `ec953169a8ce1eb698260e2adf5ff0a9bb549110306645d53700f7ddb9c5cc6f`.

<a id="symbol-dd91862edc8ffdba537eec22"></a>

## builtin / List.find

Identity: `g007-public:List.find`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)choose(){(self>80).return;}start(){List<Int>.from([17,29,71]).a;a.find(choose).unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `sequence:find-absent`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:find-present,sequence:find-absent`.

Misuse/diagnostic/failure proofs: `sequence:list-find-type`. API fingerprint: `35ccb31ad8eeb4a1579aed70fc750e29c45d893650e143302043d287d8ff1189`.

<a id="symbol-6b2ee2fdbf8cd8a5d5413b71"></a>

## builtin / List.get

Identity: `g007-public:List.get`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29]).items;items.get(1).expect("v").return;}
```

Explicit process status: `29`; the full value/effect oracle is `sequence:list-get-present`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-get-present,sequence:list-get-absent`.

Misuse/diagnostic/failure proofs: `sequence:list-get-type`. API fingerprint: `a597918a46551f51b5f39ca7be15fd216989be1c564701b0e3ea0b0c2ef3d659`.

<a id="symbol-b41f11e0475cee0903cfbb78"></a>

## builtin / List.insert

Identity: `g007-public:List.insert`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29]).items;items.insert(1,71);items.at(1).return;}
```

Explicit process status: `71`; the full value/effect oracle is `sequence:list-insert`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-insert`.

Misuse/diagnostic/failure proofs: `sequence:list-insert-type`. API fingerprint: `96e85adf5cc585684c3fea312d3eed9d4c963571b37083d20f2db99ad8e765cb`.

<a id="symbol-df17821a547b870f8a0f75ad"></a>

## builtin / List.length

Identity: `g007-public:List.length`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().items;items.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `sequence:List-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:List-empty,sequence:list-clear`.

Misuse/diagnostic/failure proofs: `sequence:list-length-arity`. API fingerprint: `8c0f3f10ae7f2723fcb200d338bcc86ac85da4e742f9e246deba33ad9092ff40`.

<a id="symbol-bedc2c9777a4565efc643bd1"></a>

## builtin / List.map

Identity: `g007-public:List.map`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)transform(){self.console();(self+7).return;}start(){List<Int>.new().a;a.map(transform).b;b.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `sequence:map-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:map-add,sequence:map-subtract,sequence:map-multiply,sequence:map-effects-once,sequence:map-empty`.

Misuse/diagnostic/failure proofs: `sequence:map-return-type,sequence:map-arity,sequence:list-map-type`. API fingerprint: `3a8240fda24c26aa8b040293493bd18b96aeb29006b4d86265508292e91c66e7`.

<a id="symbol-b9eb39d9d696126fd64a5aa7"></a>

## builtin / List.pop

Identity: `g007-public:List.pop`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().a;a.pop().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:list-pop-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-pop,sequence:list-pop-empty`.

Misuse/diagnostic/failure proofs: `sequence:list-pop-arity`. API fingerprint: `8b966ef3e04ea7c72f79067df32d3c2f9aaff65fc30a6be6fb523e87bfba6164`.

<a id="symbol-d3ed66cd6cce48b3bcfbc78c"></a>

## builtin / List.push

Identity: `g007-public:List.push`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().a;0.i.mutable;while(i<17){a.push(i);i+=1;}23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `sequence:list-runtime-capacity`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-push,sequence:list-boundary,sequence:list-runtime-capacity`.

Misuse/diagnostic/failure proofs: `sequence:List-wrong-value`. API fingerprint: `97e37442e5219e05cdb329eee4ff763c0a1161cda8cd9e670351436cce5a44d3`.

<a id="symbol-86624278bd1a0a3c49919414"></a>

## builtin / List.remove

Identity: `g007-public:List.remove`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).items;items.remove(1).console();items.at(1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:list-remove`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-remove`.

Misuse/diagnostic/failure proofs: `sequence:list-remove-type`. API fingerprint: `1523fe66a6ec4645e32e2023307d84a4dd95813b3f807e65a15ad3928fd589c7`.

<a id="symbol-916e02627334129511e48c11"></a>

## builtin / List.reserve

Identity: `g007-public:List.reserve`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().a;0.i.mutable;while(i<17){a.push(i);i+=1;}23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `sequence:list-runtime-capacity`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-reserve,sequence:list-runtime-capacity`.

Misuse/diagnostic/failure proofs: `sequence:list-reserve-type`. API fingerprint: `28b3a803b418ec9959f8bb2285d440087c2fbd050e89b4a68e648e7f58670f1b`.

<a id="symbol-f2b90f2005cad2ffa41f6868"></a>

## builtin / List.retain

Identity: `g007-public:List.retain`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)choose(){(self<20).return;}start(){List<Int>.from([29,-17,71]).a;a.retain(choose);a.length().console();a.at(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:retain-less`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:retain-greater,sequence:retain-less`.

Misuse/diagnostic/failure proofs: `sequence:list-retain-type`. API fingerprint: `68fd04d01f93ffa3dc02e95340181adf5c8345243a466e27af5912da514d63ca`.

<a id="symbol-be66105d554eff3c457bb2ca"></a>

## builtin / List.sort

Identity: `g007-public:List.sort`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)order(Int.other){(self<=>other).return;}start(){List<Int>.from([29,-17,71]).a;a.sort(order);a.at(0).console();a.at(1).console();a.at(2).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:sort-False`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:sort-False,sequence:sort-True`.

Misuse/diagnostic/failure proofs: `sequence:sort-return-type,sequence:list-sort-type`. API fingerprint: `fe1625d48a866ca90606157af6b6615b4bd98d71d8aaaa90620a1a3b165eda14`.

<a id="symbol-296c990dd884ab2b1f7a42ba"></a>

## builtin / List.swapRemove

Identity: `g007-public:List.swapRemove`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).items;items.swapRemove(0).console();items.at(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:list-swap-remove`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-swap-remove`.

Misuse/diagnostic/failure proofs: `sequence:list-swapRemove-type`. API fingerprint: `0b30a6fcc47df0e672163f3deeee7149724df5a361dc78b02be9508917506ef4`.

<a id="symbol-7ebb967c64d89b597f57cea0"></a>

## builtin / List<Int>.from

Identity: `g007-public:List<Int>.from`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([]).items;items.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `sequence:from-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:from-empty,sequence:from-one,sequence:from-many,sequence:from-changed,sequence:from-expressions`.

Misuse/diagnostic/failure proofs: `sequence:from-mixed,sequence:from-over-limit,sequence:from-type`. API fingerprint: `cdb5ee29fbca51e9f4bb1966c64ce416aebe66416d30e077a22ca976a6bef181`.

<a id="symbol-0e836a172b1745477386f78f"></a>

## builtin / List<Int>.new

Identity: `g007-public:List<Int>.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().items;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:List-return-23`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:List-empty,sequence:List-return-23`.

Misuse/diagnostic/failure proofs: `sequence:List-element-type,sequence:List-constructor-arity`. API fingerprint: `11f9879687986c2df4ffcf2c3af86d6944d29598d1c89834d64b7e7ea32024e3`.

<a id="symbol-7d07b6ada16f4a03f54684c6"></a>

## builtin / List<Int>.withCapacity

Identity: `g007-public:List<Int>.withCapacity`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.withCapacity(5).items;items.capacity().return;}
```

Explicit process status: `5`; the full value/effect oracle is `sequence:list-capacity`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:list-capacity`.

Misuse/diagnostic/failure proofs: `sequence:List-element-type`. API fingerprint: `b52ea28ff05c62199ae519de8d43a12d278c73ca8dd4630b43970782b4682c46`.

<a id="symbol-efd62834a54e0a858ce5b3c2"></a>

## builtin / Queue.dequeue

Identity: `g007-public:Queue.dequeue`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Queue<Int>.new().a;a.dequeue().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:queue-dequeue-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:queue-fifo,sequence:queue-dequeue-empty`.

Misuse/diagnostic/failure proofs: `sequence:Queue-copy`. API fingerprint: `9b8069dc9113665e2fbc3f90903ebc017bc34a4cd5264a439ea7bbbd14a6ad72`.

<a id="symbol-dc58e72924e36dd2b1738251"></a>

## builtin / Queue.enqueue

Identity: `g007-public:Queue.enqueue`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Queue<Int>.new().q;q.enqueue(17);q.enqueue(29);q.peek().console();q.dequeue().expect("front").console();q.dequeue().expect("front").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:queue-fifo`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:queue-fifo,sequence:queue-wrap`.

Misuse/diagnostic/failure proofs: `sequence:Queue-wrong-value`. API fingerprint: `ee1f4485105ef70cadd4da8f92086e0c1cc4a38d4b1dc69ab106da03e7955799`.

<a id="symbol-e142be7cba630e00f0b9b055"></a>

## builtin / Queue.peek

Identity: `g007-public:Queue.peek`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Queue<Int>.new().q;q.enqueue(17);q.enqueue(29);q.peek().console();q.dequeue().expect("front").console();q.dequeue().expect("front").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:queue-fifo`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:queue-fifo`.

Misuse/diagnostic/failure proofs: `sequence:queue-deque-method`. API fingerprint: `082fc6e986cee10aa0dff6b7680d647e8919230be47128c9c0644100dcbae9a4`.

<a id="symbol-73f9dca55c1ab3d8adfd9bbf"></a>

## builtin / Queue<Int>.new

Identity: `g007-public:Queue<Int>.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Queue<Int>.new().items;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:Queue-return-23`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:Queue-empty,sequence:Queue-return-23`.

Misuse/diagnostic/failure proofs: `sequence:Queue-element-type,sequence:Queue-constructor-arity`. API fingerprint: `eba8d3f5ebbd6713b3da0bfc9bbbdc3fc91f5af6588f3004b64f0e0dc1dd5493`.

<a id="symbol-deec9384021e7826b31f1ac4"></a>

## builtin / Stack.clear

Identity: `g007-public:Stack.clear`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stack<Int>.new().a;a.push(17);a.clear();a.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `sequence:Stack-clear`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:Stack-clear`.

Misuse/diagnostic/failure proofs: `sequence:Stack-copy`. API fingerprint: `b614dc0c743ed77e5b303e997487b366103e2051d6e6bb7fe428c666ea09b11b`.

<a id="symbol-b9941cab3c6941cffb7f29ab"></a>

## builtin / Stack.length

Identity: `g007-public:Stack.length`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stack<Int>.new().items;items.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `sequence:Stack-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:Stack-empty,sequence:Stack-two-live`.

Misuse/diagnostic/failure proofs: `sequence:Stack-copy`. API fingerprint: `a29c2df5df5f884a5db76241f0ed7491a121eeb278afa0675dde641b45e77e7d`.

<a id="symbol-06ada69c06810341e8ba43da"></a>

## builtin / Stack.peek

Identity: `g007-public:Stack.peek`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stack<Int>.new().s;s.push(17);s.push(29);s.peek().console();s.pop().expect("top").console();s.pop().expect("top").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:stack-lifo`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:stack-lifo`.

Misuse/diagnostic/failure proofs: `sequence:stack-list-method`. API fingerprint: `a9637a2e93f27585f3eb0f20ba69bca1ba4092906129ef0c3c63cdcd699a7bae`.

<a id="symbol-f9f5ec3fc590d508d2641a67"></a>

## builtin / Stack.pop

Identity: `g007-public:Stack.pop`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stack<Int>.new().a;a.pop().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:stack-pop-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:stack-lifo,sequence:stack-pop-empty`.

Misuse/diagnostic/failure proofs: `sequence:Stack-copy`. API fingerprint: `7a6d0056dd8a4f14400a221bf403452f0fe81efa4d6ed2895a179e202fbb3d11`.

<a id="symbol-fc1f062fa0b95383b7d09f7a"></a>

## builtin / Stack.push

Identity: `g007-public:Stack.push`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stack<Int>.new().s;s.push(17);s.push(29);s.peek().console();s.pop().expect("top").console();s.pop().expect("top").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:stack-lifo`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:stack-lifo`.

Misuse/diagnostic/failure proofs: `sequence:Stack-wrong-value`. API fingerprint: `43b3c4721173c8512014d1150c69b4cc043e54979e2a02ff6914264de68414f2`.

<a id="symbol-b1d9963ae635e1bb4d1cc112"></a>

## builtin / Stack<Int>.new

Identity: `g007-public:Stack<Int>.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stack<Int>.new().items;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:Stack-return-23`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:Stack-empty,sequence:Stack-return-23`.

Misuse/diagnostic/failure proofs: `sequence:Stack-element-type,sequence:Stack-constructor-arity`. API fingerprint: `956333f2ca774443da92b8c5fb2ced7a2883e32c16ce7f3f6f6419842f3c5942`.

<a id="symbol-446e680a45c939e03c4b7a56"></a>

## builtin / collection.iterator

Identity: `g007-public:collection.iterator`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().a;a.iterator().i;i.next().unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `sequence:iterator-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:iterator-next,sequence:iterator-empty,sequence:iterator-queue,sequence:iterator-stack,sequence:iterator-deque,sequence:iterator-scope,sequence:iterator-loop-cleanup,sequence:iterator-break-cleanup`.

Misuse/diagnostic/failure proofs: `sequence:iterator-borrow,sequence:iterator-second,sequence:iterator-copy`. API fingerprint: `b27aa7b0aa09d49ab2d3bb074cc511ba8a6f90d9e26353d8f6e4cacf007c7701`.

<a id="symbol-3b2deeec00f5f300a96b52fa"></a>

## builtin / collection.iteratorMutable

Identity: `g007-public:collection.iteratorMutable`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17]).a;a.iteratorMutable().i;i.next().expect("v").return;}
```

Explicit process status: `17`; the full value/effect oracle is `sequence:iterator-mutable`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:iterator-mutable`.

Misuse/diagnostic/failure proofs: `sequence:iterator-unique-read`. API fingerprint: `6b1df253b05af332041af107e9c14d6f86c2b7c5e6d5231afa8654c35b8b6a03`.

<a id="symbol-4d14c3cf31d9e9acc959c51c"></a>

## builtin / iterator.collect<List<Int>>

Identity: `g007-public:iterator.collect<List<Int>>`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().a;a.iterator().i;i.collect<List<Int>>().b;b.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `sequence:iterator-collect-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:iterator-collect,sequence:iterator-collect-empty,sequence:iterator-collect-independent`.

Misuse/diagnostic/failure proofs: `sequence:iterator-collect-type,sequence:iterator-collect-pair-as-int`. API fingerprint: `f8a00a2c206542a224edc92239d4f9ece606c199ca43250b04b70e0c20a3b96e`.

<a id="symbol-4aa110c496a4cd76af2bc747"></a>

## builtin / iterator.enumerate

Identity: `g007-public:iterator.enumerate`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().a;a.iterator().i;i.enumerate();i.next().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `sequence:iterator-enumerate-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:iterator-enumerate,sequence:iterator-enumerate-second,sequence:iterator-enumerate-skip,sequence:iterator-enumerate-empty,sequence:iterator-enumerate-transition`.

Misuse/diagnostic/failure proofs: `sequence:iterator-enumerate-conditional,sequence:iterator-collect-pair-as-int`. API fingerprint: `e0f1eb62a5bcf2d68a38159e42de298e77e84e21f7e91e8f9098aa785f8180ba`.

<a id="symbol-7479bff2d658f450d778fa8a"></a>

## builtin / iterator.next

Identity: `g007-public:iterator.next`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().a;a.iterator().i;i.next().unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `sequence:iterator-empty`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:iterator-next,sequence:iterator-empty,sequence:iterator-exhaustion`.

Misuse/diagnostic/failure proofs: `sequence:iterator-released`. API fingerprint: `168026107a31fcd70b23120d6405f905d3fe84bf885bcca034a461b29c673fe0`.

<a id="symbol-b9d8fe4689b5c1355d715d3f"></a>

## builtin / iterator.skip

Identity: `g007-public:iterator.skip`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29]).a;a.iterator().i;i.skip(9);i.next().unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `sequence:iterator-skip-past`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:iterator-skip-take,sequence:iterator-skip-past`.

Misuse/diagnostic/failure proofs: `sequence:iterator-skip-type`. API fingerprint: `4aa216780330134e5462bbd067c0004b24c896d95ffb34b79aa375beebe05caa`.

<a id="symbol-b493eed971b018964c44bc09"></a>

## builtin / iterator.take

Identity: `g007-public:iterator.take`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29]).a;a.iterator().i;i.take(0);i.next().unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `sequence:iterator-take-zero`.

### Types, ownership, effects, limits and errors

Int64 elements; 16 live values; native List/Stack/ring storage and algorithms; typed callback bridges; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed sequential storage, views and checked indices; ownership and capacity follow exact declaration

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/sequential_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/sequential_public.asm`.

Positive/changed-value/boundary proofs: `sequence:iterator-skip-take,sequence:iterator-take-zero`.

Misuse/diagnostic/failure proofs: `sequence:iterator-take-type`. API fingerprint: `aff7dd49f65a35932b8c4a2ee4b4d114afe1bf35f329bf223293e6fafad8f0f5`.

<a id="symbol-0b172cc506be187a988e0398"></a>

## builtin / Dict.capacity

Identity: `g008-public:Dict.capacity`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.withCapacity(8).d;d.capacity().return;}

```

Explicit process status: `8`; the full value/effect oracle is `dict:capacity-8`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:capacity-8,dict:reserve-retains-value`.

Misuse/diagnostic/failure proofs: `dict:use-after-move`. API fingerprint: `ca76634a435460f497a00ab70320f36bf088defb1b2a6f63b549698071a47275`.

<a id="symbol-e4856572206931e1dd14ec98"></a>

## builtin / Dict.clear

Identity: `g008-public:Dict.clear`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.clear();d.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:clear`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:clear`.

Misuse/diagnostic/failure proofs: `dict:borrow-mutation`. API fingerprint: `9959310cb6b79f9df34d6035983cb0ad3ae1b7e1c8f6ff59966eb5e2380c101e`.

<a id="symbol-bc5996943bc2d948ac02c53d"></a>

## builtin / Dict.collisionCount

Identity: `g008-public:Dict.collisionCount`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.collisionCount().console();d.maxProbeLength().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:collision-empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:collision-empty,dict:collision-single,dict:collision-cluster`.

Misuse/diagnostic/failure proofs: `dict:collision-arity`. API fingerprint: `79c3ad410d685a184f86523996b6b414c41a6271906ef7de092359e602ae37ac`.

<a id="symbol-e3f46ad907a7d21ccdec654b"></a>

## builtin / Dict.containsKey

Identity: `g008-public:Dict.containsKey`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.containsKey(17).console();d.containsKey(29).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:contains`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:contains`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `db6d8587809262b5868e1c61712f2cb6380179cdf427912a5e93d23be63f1ea0`.

<a id="symbol-d91ac71dbe85402cdfb3c34d"></a>

## builtin / Dict.entries

Identity: `g008-public:Dict.entries`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.entries().v;v.next().isNone().console();v.release();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:entry-view-empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:entry-view-pair,dict:entry-view-empty`.

Misuse/diagnostic/failure proofs: `dict:view-released`. API fingerprint: `0c882754e16efe64ce2704314b83e8ec8c9c2dcc9c0a66ac607bc01d7e4efb72`.

<a id="symbol-5e39a92105beb70fcefe5669"></a>

## builtin / Dict.entry

Identity: `g008-public:Dict.entry`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.entry(17).e;e.read().return;}

```

Explicit process status: `177`; the full value/effect oracle is `dict:entry-absent-read`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:entry-new,dict:entry-update,dict:entry-repeated-write,dict:entry-absent-read,dict:entry-temporary,dict:entry-key-once`.

Misuse/diagnostic/failure proofs: `dict:entry-key-type,dict:entry-write-type,dict:entry-read-conflict,dict:entry-copy`. API fingerprint: `2b0026d3708c482566a1662ff476aa271ff46eaf478b02c0f49f36db0ff5c903`.

<a id="symbol-c0583e4e12c0f637111c907d"></a>

## builtin / Dict.get

Identity: `g008-public:Dict.get`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.get(91).unwrapOr(67).return;}

```

Explicit process status: `67`; the full value/effect oracle is `dict:absent`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:present,dict:absent,dict:lookup-29-53-17,dict:bound-reference`.

Misuse/diagnostic/failure proofs: `dict:released-reference-read`. API fingerprint: `53efc9a96a9971685c6d05bc6e153e74dda7b5dd3955e855c5845e5b71d31b09`.

<a id="symbol-c1bf792cacecc014391e641f"></a>

## builtin / Dict.getMutable

Identity: `g008-public:Dict.getMutable`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.getMutable(29).r;r.read().return;}

```

Explicit process status: `177`; the full value/effect oracle is `dict:unique-missing`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:unique-read,dict:unique-write,dict:unique-release,dict:unique-scope,dict:unique-temporary,dict:unique-missing`.

Misuse/diagnostic/failure proofs: `dict:unique-owner-read,dict:unique-view,dict:shared-unique,dict:unique-released,dict:unique-write-type,dict:unique-key-type`. API fingerprint: `14f598da6d1c4241a8731c0487ef284ed637955e29d2840497ac9d5166b619c3`.

<a id="symbol-07a2e8c5252334b5ef572282"></a>

## builtin / Dict.insert

Identity: `g008-public:Dict.insert`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.insert(17,71);d.length().return;}

```

Explicit process status: `1`; the full value/effect oracle is `dict:duplicate-length`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:duplicate-old-value,dict:duplicate-new-value,dict:duplicate-length`.

Misuse/diagnostic/failure proofs: `dict:insert-key-type,dict:insert-value-type`. API fingerprint: `c25170b4cc5d7f9413a4397fc1525598a3339fd84a67fde966cfb39dae982027`.

<a id="symbol-df3de84a3a3d23a5cfa7da62"></a>

## builtin / Dict.iterationOrder

Identity: `g008-public:Dict.iterationOrder`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.iterationOrder().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:iteration-order`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:iteration-order`.

Misuse/diagnostic/failure proofs: `dict:iteration-order-arity`. API fingerprint: `398a2178df0be744d8bbbd39c1942906665cf9aa982e68feaf72c9136e3bc84f`.

<a id="symbol-96babd6bf45c82871120132a"></a>

## builtin / Dict.keys

Identity: `g008-public:Dict.keys`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.keys().v;v.next().unwrapOr(73).x;v.release();x.return;}

```

Explicit process status: `73`; the full value/effect oracle is `dict:view-keys-empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:view-keys,dict:view-keys-empty,dict:view-scope-cleanup`.

Misuse/diagnostic/failure proofs: `dict:view-copy,dict:view-mutation`. API fingerprint: `98d2a6c616a33f7419125b27bc3f600ee0facb4017a93c6354a963cd3bea9250`.

<a id="symbol-5396cc85d6d7f3ef73cae875"></a>

## builtin / Dict.length

Identity: `g008-public:Dict.length`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:empty,dict:length-one,dict:length-two`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `ac7371c7b94867169d97e888db48875e8fbe633892441bcbe47fe9d7e5ccf3a3`.

<a id="symbol-e48b0e3cdd2fdeaea6043c54"></a>

## builtin / Dict.loadFactor

Identity: `g008-public:Dict.loadFactor`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;(d.loadFactor()==0.0).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:load-factor-0`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:load-factor-0,dict:load-factor-1,dict:load-factor-2`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `0c6fca8eec4ffb7eb5956604d4237412714b189234dea7eb93dcba9093953239`.

<a id="symbol-6681c04ce38dedc69249550f"></a>

## builtin / Dict.maxProbeLength

Identity: `g008-public:Dict.maxProbeLength`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.maxProbeLength().return;}

```

Explicit process status: `1`; the full value/effect oracle is `dict:max-probe`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:max-probe`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `45c5ab535290a1c2c5600d1676671fd6dfa9cc208cddaaad916bbad8a65ae339`.

<a id="symbol-d04e6e464b427aee7f5065c1"></a>

## builtin / Dict.remove

Identity: `g008-public:Dict.remove`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.remove(19).unwrapOr(73).return;}

```

Explicit process status: `73`; the full value/effect oracle is `dict:remove-absent`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:remove,dict:remove-absent`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `dd959fb7402b0c79e2362b7795f5c1812242d8e8ec036e950c2e426b0783f79b`.

<a id="symbol-6327e55297394153cd76379e"></a>

## builtin / Dict.reseed

Identity: `g008-public:Dict.reseed`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.reseed();d.reseed();d.get(17).expect("a").return;}

```

Explicit process status: `53`; the full value/effect oracle is `dict:repeated-reseed`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:reseed-retains,dict:repeated-reseed`.

Misuse/diagnostic/failure proofs: `dict:view-reseed`. API fingerprint: `bec4a0f480d70fa314925fc096c89e88ad775a9fdd7c69e4b09eddec7e3f96a8`.

<a id="symbol-15b6a63e1461f09463bac2e9"></a>

## builtin / Dict.reserve

Identity: `g008-public:Dict.reserve`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.insert(17,53);d.reserve(9);(d.capacity()+d.get(17).expect("retained")).return;}

```

Explicit process status: `69`; the full value/effect oracle is `dict:reserve-retains-value`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:reserve-retains-value`.

Misuse/diagnostic/failure proofs: `dict:reserve-type`. API fingerprint: `ecc6c5904d61d01758dd9a38b3cc2506f612c3d65a8545b7b2cb538f047ff2b2`.

<a id="symbol-799a2c5ccfdb149433e21c2f"></a>

## builtin / Dict.retain

Identity: `g008-public:Dict.retain`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.key)keep(Int.value){(key>17).return;}start(){Dict<Int,Int>.new().d;d.retain(keep);d.length().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `dict:retain-empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:retain-empty,dict:retain-all,dict:retain-none,dict:retain-key,dict:retain-changed-key,dict:retain-value,dict:retain-changed-value,dict:retain-effects-once,dict:retain-two-live`.

Misuse/diagnostic/failure proofs: `dict:retain-key-type,dict:retain-value-type,dict:retain-return-type,dict:retain-capture,dict:retain-borrow,dict:retain-non-callable`. API fingerprint: `319d531bc76d86bc1f550492da402f4113e62550afbdf4926f7e5e022831ccd5`.

<a id="symbol-7422295afca6ba0c5834065e"></a>

## builtin / Dict.toSortedEntries

Identity: `g008-public:Dict.toSortedEntries`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.toSortedEntries().s;s.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:sorted-empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:sorted-empty,dict:sorted-signed,dict:sorted-permuted,dict:sorted-reseed,dict:sorted-changed,dict:sorted-snapshot`.

Misuse/diagnostic/failure proofs: `dict:sorted-arity,dict:sorted-index-type`. API fingerprint: `e0cc67f466680ea8d6d239c0799df4037ece47f246b952a96678f32e096d36d0`.

<a id="symbol-3b38a310aa3a6d812a3902d5"></a>

## builtin / Dict.validateInvariants

Identity: `g008-public:Dict.validateInvariants`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.validateInvariants().console();d.insert(17,53);d.validateInvariants().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:invariants`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:invariants`.

Misuse/diagnostic/failure proofs: `dict:use-after-drop`. API fingerprint: `82139005def1c94e1eb3e8631e3b7a705e0968aa89daac78383b7bed53814628`.

<a id="symbol-55f27d8a87215540d4225403"></a>

## builtin / Dict.values

Identity: `g008-public:Dict.values`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.values().v;v.next().unwrapOr(73).x;v.release();x.return;}

```

Explicit process status: `73`; the full value/effect oracle is `dict:view-values-empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:view-values,dict:view-values-empty,dict:view-exhaustion`.

Misuse/diagnostic/failure proofs: `dict:view-reseed`. API fingerprint: `a623e2e02c5536bf18b41d7e445671b277643f19a4de89915f0f7a780ae320b0`.

<a id="symbol-603494af5d58473fdc76369d"></a>

## builtin / Dict<Int,Int>.new

Identity: `g008-public:Dict<Int,Int>.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.new().d;d.length().return;}

```

Explicit process status: `0`; the full value/effect oracle is `dict:empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:empty,dict:empty-reproducer`.

Misuse/diagnostic/failure proofs: `dict:key-type,dict:value-type`. API fingerprint: `576f783487eebfcae7acdba889dc26bf09c14c64e999623534d16aef85d4cccf`.

<a id="symbol-8a2a8b49b8eaf77c6a1db689"></a>

## builtin / Dict<Int,Int>.withCapacity

Identity: `g008-public:Dict<Int,Int>.withCapacity`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Dict<Int,Int>.withCapacity(8).d;d.capacity().return;}

```

Explicit process status: `8`; the full value/effect oracle is `dict:capacity-8`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:capacity-8,dict:capacity-16,dict:capacity-32,dict:capacity-64`.

Misuse/diagnostic/failure proofs: `dict:capacity-invalid-9,dict:capacity-type`. API fingerprint: `dd7b70b1e142b2554b22a4dd4cf072182cffef90f194380c0fbdb308c0dbe5e7`.

<a id="symbol-230cc4e71e18a0a6386b1307"></a>

## builtin / Hasher.finish

Identity: `g008-public:Hasher.finish`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Hasher.new(17).h;53.hash(h);71.hash(h);h.finish().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:hasher-stream`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:hasher-stream`.

Misuse/diagnostic/failure proofs: `dict:hasher-finish-arity`. API fingerprint: `2dfe38febbd127d25aa66e845958302d2835e591baa85c20335a6067f113acb6`.

<a id="symbol-cddbd3b08b3ef3f276b6f38c"></a>

## builtin / Hasher.new

Identity: `g008-public:Hasher.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Hasher.new(17).h;h.finish().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:hasher-empty`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:hasher-empty,dict:hasher-changed-seed`.

Misuse/diagnostic/failure proofs: `dict:hasher-seed-type`. API fingerprint: `bd4ceeb766d40ac8b7f1d6e6c677b601e24feccc498dc8de6734426cb5cdd410`.

<a id="symbol-58db520ee7b3924e69488682"></a>

## builtin / Hasher.writeBytes

Identity: `g008-public:Hasher.writeBytes`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Hasher.new(29).h;h.writeBytes(Bytes.fromValues(17,29,53,71));h.finish().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:hasher-bytes`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:hasher-bytes,dict:hasher-changed-byte`.

Misuse/diagnostic/failure proofs: `dict:hasher-bytes-type`. API fingerprint: `496f660d4bd33a088139fb5b511687391629a700c38d9be996d91ecae90fa3d9`.

<a id="symbol-730c15e966731ce109936a30"></a>

## builtin / Int.equals

Identity: `g008-public:Int.equals`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.equals(17).console();17.equals(29).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:int-equals`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:int-equals`.

Misuse/diagnostic/failure proofs: `dict:equals-type`. API fingerprint: `a271caaf82514647aa649727e5720f8c3330ca443d5918d4223a9a85250b2fc0`.

<a id="symbol-b77376294ca182ab200a8ad2"></a>

## builtin / Int.hash

Identity: `g008-public:Int.hash`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Hasher.new(17).h;53.hash(h);h.finish().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:hasher-int`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:hasher-int`.

Misuse/diagnostic/failure proofs: `dict:int-hash-type`. API fingerprint: `40bda87eb3f49542b584950af2e8e9ea098e25aadc50470dd468ea046bd44c6f`.

<a id="symbol-c1c667684c1346c0d6a75eb6"></a>

## builtin / Int.hashStable

Identity: `g008-public:Int.hashStable`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Int.hashStable().console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:int-hash-stable`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:int-hash-stable`.

Misuse/diagnostic/failure proofs: `dict:hash-stable-arity`. API fingerprint: `859297465cbfbb1862af5ac07f7b8ae1c8fc618625251b13b9cd96e0a8235864`.

<a id="symbol-a7e1c23a5af0cc96cc0f4a6a"></a>

## builtin / Set.contains

Identity: `g008-public:Set.contains`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.difference(b).c;c.contains(17).console();c.contains(29).console();c.contains(53).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:set-difference-membership`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-difference-membership`.

Misuse/diagnostic/failure proofs: `dict:set-contains-type`. API fingerprint: `8a29049e46a33bafd7ba09f80c31fbe0bde5f130999f86b2eca4b99daa3e58cc`.

<a id="symbol-3e85e8c7ace0e376e4aab492"></a>

## builtin / Set.difference

Identity: `g008-public:Set.difference`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.difference(b).c;c.length().return;}

```

Explicit process status: `1`; the full value/effect oracle is `dict:set-difference`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-difference,dict:set-difference-membership`.

Misuse/diagnostic/failure proofs: `dict:set-difference-type`. API fingerprint: `3a8f041da0e1a26e1d76d0d91aef634e2a1567685994bff056d4c7e4d9d9ebb0`.

<a id="symbol-47540907dbb79561376cc311"></a>

## builtin / Set.insert

Identity: `g008-public:Set.insert`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().s;s.insert(17).console();s.insert(17).console();s.contains(17).console();s.remove(17).console();s.contains(17).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:set-insert-remove`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-insert-remove`.

Misuse/diagnostic/failure proofs: `dict:set-insert-type`. API fingerprint: `f128aa4ee4fd8997bb35d55444f185b98e95b2d3d31cee4f18e56f569d7a677b`.

<a id="symbol-de767b0d43b0bd3655e3eb0c"></a>

## builtin / Set.intersection

Identity: `g008-public:Set.intersection`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.intersection(b).c;c.length().return;}

```

Explicit process status: `1`; the full value/effect oracle is `dict:set-intersection`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-intersection,dict:set-intersection-membership`.

Misuse/diagnostic/failure proofs: `dict:set-intersection-type`. API fingerprint: `51d3f0cd146227faf4cda72bcbc90a2e968859a614b2230ff660110e18d19d5d`.

<a id="symbol-4c221817cc635be360af71a7"></a>

## builtin / Set.isSubsetOf

Identity: `g008-public:Set.isSubsetOf`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.intersection(b).i;i.isSubsetOf(a).console();a.isSubsetOf(b).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:set-subset`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-subset`.

Misuse/diagnostic/failure proofs: `dict:set-isSubsetOf-type`. API fingerprint: `f09e046df0932ccbe3b32ad7cc2e3951b910ec89a8bce1c8641ec62b24d27575`.

<a id="symbol-e642d14d9a849b1d5c715e83"></a>

## builtin / Set.remove

Identity: `g008-public:Set.remove`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().s;s.insert(17).console();s.insert(17).console();s.contains(17).console();s.remove(17).console();s.contains(17).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:set-insert-remove`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-insert-remove`.

Misuse/diagnostic/failure proofs: `dict:set-remove-type`. API fingerprint: `e8ae2e10c3e49354761c1a57dfcfe9eb02c13df67c0a6203229eb6d8a5675fff`.

<a id="symbol-2c992f2068d51f5af9328d1e"></a>

## builtin / Set.union

Identity: `g008-public:Set.union`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().a;a.insert(17);a.insert(29);Set<Int>.new().b;b.insert(29);b.insert(53);a.union(b).c;c.length().return;}

```

Explicit process status: `3`; the full value/effect oracle is `dict:set-union`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-union,dict:set-union-membership`.

Misuse/diagnostic/failure proofs: `dict:set-union-type`. API fingerprint: `aa4b97e688fde33100edcf7a6c0f92b98ad14a67296aeb19db2af9b6c7c72c6f`.

<a id="symbol-14a8a2fc23f033d87ca9be07"></a>

## builtin / Set<Int>.new

Identity: `g008-public:Set<Int>.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Set<Int>.new().s;s.insert(17).console();s.insert(17).console();s.contains(17).console();s.remove(17).console();s.contains(17).console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `dict:set-insert-remove`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `dict:set-insert-remove`.

Misuse/diagnostic/failure proofs: `dict:set-new-arity`. API fingerprint: `ad975f7dc883e57caa6ed5f1598010047fa54f05f2069e92356cf683e7a569ed`.

<a id="symbol-45f084e323c5a1c62bcfaef3"></a>

## builtin / hash.deterministic

Identity: `g008-public:hash.deterministic`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){hash.deterministic(0);Dict<Int,Int>.new().d;d.insert(29,71);Hasher.new(17).h;53.hash(h);h.finish();d.get(29).expect("value").return;}
```

Explicit process status: `71`; the full value/effect oracle is `hashpolicy:deterministic-zero`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `hashpolicy:deterministic-zero,hashpolicy:deterministic-seed,hashpolicy:deterministic-signed`.

Misuse/diagnostic/failure proofs: `hashpolicy:seed-type,hashpolicy:seed-missing`. API fingerprint: `05d5163ec7bb7aa01480884e0116fa0fd08d1b38f1040feb033b533c4788493b`.

<a id="symbol-2cc86c0d896391dc341c095d"></a>

## builtin / hash.randomized

Identity: `g008-public:hash.randomized`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){hash.randomized();Dict<Int,Int>.new().d;d.insert(29,71);Hasher.new(17).h;53.hash(h);h.finish();d.get(29).expect("value").return;}
```

Explicit process status: `71`; the full value/effect oracle is `hashpolicy:randomized`.

### Types, ownership, effects, limits and errors

Int64 key/value; native capacity 8..64; caller storage; source runtime observations; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Typed key/value and hash/equality policy; two independent live stores; declared capacity and failure atomicity

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/associative_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/collections/associative_public.asm`.

Positive/changed-value/boundary proofs: `hashpolicy:randomized,hashpolicy:reset-deterministic`.

Misuse/diagnostic/failure proofs: `hashpolicy:randomized-arity`. API fingerprint: `53efe450bfd56032c73c1a29bf9ce3e76cc3259d1e161af49443bce62d17a81c`.

<a id="symbol-f1b14455e7e7b0b81097011d"></a>

## builtin / Graph.addEdge

Identity: `g009-public:Graph.addEdge`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addEdge(a,a,5);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `relation:graph-self-edge`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-neighbors,relation:graph-negative-weight,relation:graph-self-edge,relation:graph-duplicate-edge,relation:graph-foreign-node`.

Misuse/diagnostic/failure proofs: `relation:graph-raw-handle,relation:graph-wrong-weight,relation:graph-edge-arity`. API fingerprint: `0a567868e0797eeb3adbd18cbff65c5bc8b05da4111f3a365457fb729c41f0d1`.

<a id="symbol-9cb5b3ab8911d122652a7efa"></a>

## builtin / Graph.addNode

Identity: `g009-public:Graph.addNode`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;0.i.mutable;while(i<33){g.addNode(i);i+=1;}23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `relation:graph-capacity-plus-one`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-two-live,relation:graph-capacity,relation:graph-capacity-plus-one`.

Misuse/diagnostic/failure proofs: `relation:graph-add-node-type`. API fingerprint: `5b7785cd8ce86d9d810610369f3ee8244a01ffd3aee0a48127b97ce6c0bfd76b`.

<a id="symbol-89dc142c4c00532a55701d2d"></a>

## builtin / Graph.breadthFirst

Identity: `g009-public:Graph.breadthFirst`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.breadthFirst(a).v;v.next().expect("n").value().return;}
```

Explicit process status: `17`; the full value/effect oracle is `relation:traversal-return`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-breadthFirst,relation:traversal-scope,relation:traversal-temporary,relation:traversal-break,relation:traversal-continue,relation:traversal-return`.

Misuse/diagnostic/failure proofs: `relation:graph-breadthFirst-node-type,relation:traversal-borrow`. API fingerprint: `e347b28b79646ee36aaa9e76ee8644b0216b7f958ba82f5ffdf03366d467e41f`.

<a id="symbol-f48a7ba32c3fb5984420d535"></a>

## builtin / Graph.connectedComponents

Identity: `g009-public:Graph.connectedComponents`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.undirected().g;g.connectedComponents().p;p.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `relation:graph-components-empty`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-components-empty,relation:graph-components,relation:graph-components-joined,relation:graph-components-directed,relation:graph-components-snapshot`.

Misuse/diagnostic/failure proofs: `relation:graph-connectedComponents-arity`. API fingerprint: `03f9dd6db5d2807ab78b8c57536754f5f1126a490dbbe5f4c0f893deabdad401`.

<a id="symbol-c437dbd146bdcb27472c01b2"></a>

## builtin / Graph.depthFirst

Identity: `g009-public:Graph.depthFirst`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.depthFirst(a).v;v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().isNone().console();v.release();g.addNode(7).n;n.value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-depthFirst`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-depthFirst`.

Misuse/diagnostic/failure proofs: `relation:graph-depthFirst-node-type,relation:traversal-copy`. API fingerprint: `fd8ebbf4feb2c6b226f7bc33b125840873dfc04aefb3949707aed5eaf65d0c02`.

<a id="symbol-c791e88b45f189f27edd4c46"></a>

## builtin / Graph.dijkstra

Identity: `g009-public:Graph.dijkstra`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,9223372036854775807);g.addEdge(b,c,9223372036854775807);g.addEdge(c,d,9223372036854775807);g.dijkstra(a,d);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `relation:graph-dijkstra-overflow`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-dijkstra-5-11,relation:graph-dijkstra-19-2,relation:graph-dijkstra-31-3,relation:graph-dijkstra-overflow`.

Misuse/diagnostic/failure proofs: `relation:graph-dijkstra-nodes-type`. API fingerprint: `63101c47c00e49f464ce024f85bb0d312beb7d1582870991744a970351a3500b`.

<a id="symbol-f681efbf1b9fbc4d891fdd8f"></a>

## builtin / Graph.hasCycle

Identity: `g009-public:Graph.hasCycle`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.hasCycle().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-acyclic`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-acyclic,relation:graph-cycle`.

Misuse/diagnostic/failure proofs: `relation:graph-hasCycle-arity`. API fingerprint: `432f715d3109c4ded215f361b6e410be8fd6ca047b2e4dc66264141b9845e4c3`.

<a id="symbol-bcc2d357799fd3ad191f6292"></a>

## builtin / Graph.neighbors

Identity: `g009-public:Graph.neighbors`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(71).b;g.addEdge(a,b,5).e;g.neighbors(a).v;v.next().expect("neighbor").value().return;}
```

Explicit process status: `71`; the full value/effect oracle is `relation:graph-directed-neighbors`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-neighbors,relation:graph-undirected-neighbors`.

Misuse/diagnostic/failure proofs: `relation:graph-neighbors-node-type`. API fingerprint: `c1502c2f44bf026909522b354232b23fe94103a44009b80dff1e87d812297287`.

<a id="symbol-61af8b242b7fa8bd1cf90863"></a>

## builtin / Graph.removeEdge

Identity: `g009-public:Graph.removeEdge`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(71).b;g.addEdge(a,b,5).e;g.removeEdge(e).console();g.neighbors(a).length().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-directed-remove-edge`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-remove-edge,relation:graph-undirected-remove-edge`.

Misuse/diagnostic/failure proofs: `relation:graph-remove-edge-type`. API fingerprint: `ca4662880212cd201497fe16db5a6c1e49c8482d2dc26cc0bb2d143f06eb9342`.

<a id="symbol-ccac446cf8d79c40de0790df"></a>

## builtin / Graph.removeNode

Identity: `g009-public:Graph.removeNode`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(71).b;g.addEdge(a,b,5).e;g.removeNode(b);g.neighbors(a).length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `relation:graph-directed-remove-node`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-directed-remove-node,relation:graph-undirected-remove-node`.

Misuse/diagnostic/failure proofs: `relation:graph-removeNode-node-type`. API fingerprint: `2ab090f3bdbcec0e25b9475063165bafdd310e3aa57efd7b2065a97d905dfa1b`.

<a id="symbol-4e490510f870d2bf7d42814b"></a>

## builtin / Graph.shortestPathUnweighted

Identity: `g009-public:Graph.shortestPathUnweighted`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.shortestPathUnweighted(d,a).v;v.length().return;}
```

Explicit process status: `0`; the full value/effect oracle is `relation:graph-unreachable`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-shortest,relation:graph-unreachable,relation:graph-foreign-path`.

Misuse/diagnostic/failure proofs: `relation:graph-shortestPathUnweighted-nodes-type`. API fingerprint: `cf0b1317278e1c29de31e0bc1dbfb0d3e775d5bb40b6140718140a6e6f1f9d29`.

<a id="symbol-5a05900d78acb3e524d2d7f8"></a>

## builtin / Graph.topologicalSort

Identity: `g009-public:Graph.topologicalSort`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.addEdge(d,a,13);g.topologicalSort();23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `relation:graph-topological-cycle`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-topological,relation:graph-topological-cycle`.

Misuse/diagnostic/failure proofs: `relation:graph-topologicalSort-arity`. API fingerprint: `0272f88d27ca0b2f9aba578bc0a9fa7be62581be151e28bde52b09db7cd12098`.

<a id="symbol-ae9ebac1b97713a632985c1e"></a>

## builtin / Graph<Int,Int>.directed

Identity: `g009-public:Graph<Int,Int>.directed`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().a;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-return-23`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-return-23,relation:graph-directed-neighbors,relation:graph-directed-reverse`.

Misuse/diagnostic/failure proofs: `relation:graph-missing-edge-type,relation:graph-edge-type`. API fingerprint: `e13465df43d283205e1ca93b4e2c846c7f0ac013250dabae3ab2a667b183d854`.

<a id="symbol-4aa0f06493e86adb56c8079a"></a>

## builtin / Graph<Int,Int>.undirected

Identity: `g009-public:Graph<Int,Int>.undirected`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.undirected().g;g.addNode(17).a;g.addNode(71).b;g.addEdge(a,b,5).e;g.neighbors(b).v;v.length().return;}
```

Explicit process status: `1`; the full value/effect oracle is `relation:graph-undirected-reverse`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-undirected-neighbors,relation:graph-undirected-reverse`.

Misuse/diagnostic/failure proofs: `relation:graph-edge-type`. API fingerprint: `f9150e2a28885afba49ed2830bb4e4e658259ba2d9c1f27f2a41ddff7fad11da`.

<a id="symbol-22c26982cc1432545fc1c1d5"></a>

## builtin / Node.degree

Identity: `g009-public:Node.degree`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(r,71).b;r.degree().console();a.degree().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-degree`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-degree,relation:graph-degree`.

Misuse/diagnostic/failure proofs: `relation:node-degree-arity`. API fingerprint: `c82b14eee4a6d3327f06f9bae85cc52f5c0230f3514c795b9d51525b65e3543c`.

<a id="symbol-bf7dceb1586fe5c114befe86"></a>

## builtin / Node.id

Identity: `g009-public:Node.id`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(17).n;n.id().before;n.replaceValue(71);(n.id()==before).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:node-stable-id`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:node-stable-id`.

Misuse/diagnostic/failure proofs: `relation:node-id-arity`. API fingerprint: `c188a30c905293a066040fd154cb03ffdb272061fdb1f3b0279642b6b147cf44`.

<a id="symbol-615c5b83be5a795a0ded4c64"></a>

## builtin / Node.replaceValue

Identity: `g009-public:Node.replaceValue`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(17).n;n.replaceValue(71).console();n.value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:node-replace`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:node-replace,relation:graph-replace`.

Misuse/diagnostic/failure proofs: `relation:node-wrong-replacement`. API fingerprint: `5663ec0222a1e29e2411c12fa4a05f7e41334fcc456e6b768f9b5fce6e9fed1d`.

<a id="symbol-838dd9ce9c7d4802561be4fb"></a>

## builtin / Node.value

Identity: `g009-public:Node.value`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(71).a;a.value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:Node-payload-71`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:Tree-payload-29,relation:Node-payload-71,relation:tree-stale-node,relation:graph-stale-node`.

Misuse/diagnostic/failure proofs: `relation:node-value-arity`. API fingerprint: `186ce4722d6ab4032e15e6e910df4eed9f69aef85f61bf64c02510ea854024bb`.

<a id="symbol-4c7f47db36995c4f4372eff9"></a>

## builtin / Node<Int> value

Identity: `g009-public:Node<Int> value`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int> 71.node;node.value().return;}
```

Explicit process status: `71`; the full value/effect oracle is `relation:node-prefix-changed`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:node-prefix-existing,relation:node-prefix-changed,relation:node-prefix-expression`.

Misuse/diagnostic/failure proofs: `relation:node-wrong-replacement`. API fingerprint: `99b300c877e863273034af52ef38ac8a0f54e30ae5c7c351dac3a6e35fbef61a`.

<a id="symbol-b488fadf0a52959be7770d95"></a>

## builtin / Node<Int>.new

Identity: `g009-public:Node<Int>.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Node<Int>.new(71).a;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:node-return-23`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:node-return-23,relation:Node-payload--17,relation:Node-payload-71`.

Misuse/diagnostic/failure proofs: `relation:node-wrong-replacement`. API fingerprint: `d83ac0d7f179cc9cfb748d95effea828ebe0507f51ee55200a7a35d439435bfc`.

<a id="symbol-ec717beab3dbca61a1657d5c"></a>

## builtin / Tree.addChild

Identity: `g009-public:Tree.addChild`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.addChild(t.root(),71).n;n.value().return;}
```

Explicit process status: `71`; the full value/effect oracle is `relation:tree-child`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-child,relation:tree-foreign-parent`.

Misuse/diagnostic/failure proofs: `relation:tree-raw-handle,relation:tree-wrong-child-value`. API fingerprint: `3b53495c9498519e74127620f9e2221327a1cc9caeeccf7688493e8d61137d7d`.

<a id="symbol-9588e42be61d58972a70fbba"></a>

## builtin / Tree.children

Identity: `g009-public:Tree.children`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29);t.addChild(r,71);t.children(r).v;v.length().console();v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-child-order`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-child-order`.

Misuse/diagnostic/failure proofs: `relation:tree-children-node-type`. API fingerprint: `fc5a59f33a0acde93c53d2aeaad59266cdf17ba2f11774e08ee9f75f5b567267`.

<a id="symbol-fb201d3d1bd10e90d34b341f"></a>

## builtin / Tree.height

Identity: `g009-public:Tree.height`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.height().return;}
```

Explicit process status: `1`; the full value/effect oracle is `relation:tree-height-root`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-height-root,relation:tree-height-empty`.

Misuse/diagnostic/failure proofs: `relation:tree-height-arity`. API fingerprint: `f5ad386470a23059cb70ddff2eb87ddf6388e69ca93cb7a42171185dbb291d8a`.

<a id="symbol-19582261bb422cee630b767f"></a>

## builtin / Tree.lowestCommonAncestor

Identity: `g009-public:Tree.lowestCommonAncestor`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(r,71).b;t.lowestCommonAncestor(a,b).expect("ancestor").value().return;}
```

Explicit process status: `17`; the full value/effect oracle is `relation:tree-lca`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-lca`.

Misuse/diagnostic/failure proofs: `relation:tree-lca-node-type`. API fingerprint: `34ceaf1a53f1257a6ce5eddf7cb22342d0df5a4d8d912c4fa00ff467058cc59c`.

<a id="symbol-6e2da7382d988f56e9c009c0"></a>

## builtin / Tree.parent

Identity: `g009-public:Tree.parent`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.parent(t.root()).isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-root-parent`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-parent,relation:tree-root-parent`.

Misuse/diagnostic/failure proofs: `relation:tree-parent-node-type`. API fingerprint: `332a2d30c380d511f7caa1585249cb690c36ed0d4375d02bcd877a23b14fee44`.

<a id="symbol-72b7863d5a47b0e47659bc3a"></a>

## builtin / Tree.postorder

Identity: `g009-public:Tree.postorder`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(r,71);t.addChild(a,83);t.postorder().v;v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().expect("node").value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-postorder`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-postorder`.

Misuse/diagnostic/failure proofs: `relation:tree-postorder-arity`. API fingerprint: `3f6e04c34c58e30eafa37e9b3e007d059d1890d554a6b42372f0123ad31406a8`.

<a id="symbol-e5f35dd077e4899a6323b14e"></a>

## builtin / Tree.preorder

Identity: `g009-public:Tree.preorder`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(r,71);t.addChild(a,83);t.preorder().v;v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().expect("node").value().console();v.next().expect("node").value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-preorder`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-preorder`.

Misuse/diagnostic/failure proofs: `relation:tree-preorder-arity`. API fingerprint: `03b6519096e729f210cfed24c65acd3b508df082f973758e806c8d43c8552d06`.

<a id="symbol-d16f6ec474cbbd38f2e40f74"></a>

## builtin / Tree.removeSubtree

Identity: `g009-public:Tree.removeSubtree`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.root().r;t.addChild(r,29).a;t.addChild(a,71);t.removeSubtree(a).return;}
```

Explicit process status: `2`; the full value/effect oracle is `relation:tree-removed-count`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-removed-count,relation:tree-stale-slot-reuse`.

Misuse/diagnostic/failure proofs: `relation:tree-removeSubtree-node-type`. API fingerprint: `df4ca32ed06dc1001043cca66b47cab25f4fd27a55a860633313cd37fe860b50`.

<a id="symbol-9c76bf0717f15420c9a784b9"></a>

## builtin / Tree.root

Identity: `g009-public:Tree.root`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.addChild(t.root(),71).n;n.value().return;}
```

Explicit process status: `71`; the full value/effect oracle is `relation:tree-child`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-child,relation:tree-root-parent`.

Misuse/diagnostic/failure proofs: `relation:tree-root-arity`. API fingerprint: `724e51c82c2d9357c45291dfc2bc4501a27ddf26cbc1b4e32d2b560b125bc3d8`.

<a id="symbol-b1d52d883f3f1206e43aa512"></a>

## builtin / Tree<Int>.new

Identity: `g009-public:Tree<Int>.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(71).a;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:tree-return-23`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:tree-return-23,relation:tree-two-live,relation:tree-capacity,relation:tree-capacity-plus-one`.

Misuse/diagnostic/failure proofs: `relation:tree-type,relation:tree-payload-type,relation:tree-copy`. API fingerprint: `eb5d7da0d9724ca054409e3fc54f4b224df4a350fa222aa879d8ad9be425fd6a`.

<a id="symbol-b2423ad54a32edc62a3009e6"></a>

## builtin / traversal.next

Identity: `g009-public:traversal.next`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.depthFirst(a).v;v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().expect("n").value().console();v.next().isNone().console();v.release();g.addNode(7).n;n.value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:graph-depthFirst`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:graph-breadthFirst,relation:graph-depthFirst`.

Misuse/diagnostic/failure proofs: `relation:traversal-released`. API fingerprint: `0711f9e7185efbfa80b39cf8aa36e040c1508d95621654dd62636a1992589d74`.

<a id="symbol-f47dcba1e8fbf643efaa4c29"></a>

## builtin / traversal.path

Identity: `g009-public:traversal.path`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addNode(83).d;g.addEdge(a,b,5);g.addEdge(a,c,11);g.addEdge(b,d,7);g.addEdge(c,d,3);g.breadthFirst(a).v;v.next();v.next();v.next();v.next();v.path().p;p.next().expect("n").value().console();p.next().expect("n").value().console();p.next().expect("n").value().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:traversal-bfs-path`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:traversal-bfs-path`.

Misuse/diagnostic/failure proofs: `relation:traversal-path-arity`. API fingerprint: `687ebff1a0d927ba6d812bacca93590461cf9877a18701f6afd4f71ad20e947d`.

<a id="symbol-f8c50f21637bd87a9019e55d"></a>

## builtin / traversal.stopWhen

Identity: `g009-public:traversal.stopWhen`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)stop(){self.console();(self>20).return;}start(){Graph<Int,Int>.directed().g;g.addNode(17).a;g.addNode(29).b;g.addNode(71).c;g.addEdge(a,b,5);g.addEdge(b,c,7);g.breadthFirst(a).v;v.stopWhen(stop);v.next();v.next();v.next().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `relation:traversal-stop-20`.

### Types, ownership, effects, limits and errors

Int64 payloads/nonnegative weights; 32 nodes/256 edges; authenticated owner/handle pairs; native paths/partitions; BFS current path; native intrinsic identity is receiver-qualified; no serialized SymbolId or DocRecord is declared by this owner; interface metadata is verified by the separate metadata suite Int64 payloads; 32 nodes/256 edges; authenticated handle/owner pair; no stale or foreign handle

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/codegen/collections/x86_64/relational_codegen.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/graph/relational_public.asm`.

Positive/changed-value/boundary proofs: `relation:traversal-stop-20,relation:traversal-stop-50`.

Misuse/diagnostic/failure proofs: `relation:traversal-bad-predicate`. API fingerprint: `0ac16f3d27c948c7f05f8e581e5d005702f9c56888fb1e5ddb6d60fe689892f7`.

<a id="symbol-17b6b41d3a028625d300847e"></a>

## builtin / Column.cast<U>

Identity: `g010-public:Column.cast<U>`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([-17,71]).c;c.cast<UInt>();23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-cast-negative`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-cast-roundtrip,data:column-cast-identity,data:column-cast-negative,data:column-cast-missing,data:column-uint-get-tag`.

Misuse/diagnostic/failure proofs: `data:column-cast-type,data:column-dtype-coalesce,data:column-uint-process-status`. API fingerprint: `d0ec69cd433856ef20b7134879e9427e073fb92bd59ab2ca04d966d2486aff8d`.

<a id="symbol-130e8b684b988477fe00460a"></a>

## builtin / Column.coalesce

Identity: `g010-public:Column.coalesce`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).a;Column<Int>.from([a.get(0),a.get(9),a.get(2)]).left;Column<Int>.from([7]).right;left.coalesce(right);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-coalesce-length`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-coalesce,data:column-coalesce-both-missing,data:column-coalesce-length`.

Misuse/diagnostic/failure proofs: `data:column-coalesce-type,data:column-dtype-coalesce`. API fingerprint: `9479df875bc83d642ea187c845342a5e84243fdab6bcaf8bb6267220b8316d99`.

<a id="symbol-8249f7a41f335589fbe8963a"></a>

## builtin / Column.countMissing

Identity: `g010-public:Column.countMissing`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([17,71]).c;c.countMissing().return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:column-no-missing`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-no-missing,data:column-missing-0,data:column-missing-1,data:column-missing-2,data:column-missing-3,data:column-missing-5,data:column-missing-7`.

Misuse/diagnostic/failure proofs: `data:column-countMissing-arity`. API fingerprint: `0fb5aa9b641b3963d2451b86dbf4c3de41bf087543008663b26cd0b2f588c584`.

<a id="symbol-2910a8d852bd564dcaeb3353"></a>

## builtin / Column.dropMissing

Identity: `g010-public:Column.dropMissing`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).a;Column<Int>.from([a.get(0),a.get(1),a.get(2)]).c;c.dropMissing().d;d.countMissing().console();d.sum().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-drop-0`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-drop-0,data:column-drop-1,data:column-drop-2,data:column-drop-3,data:column-drop-5,data:column-drop-7`.

Misuse/diagnostic/failure proofs: `data:column-dropMissing-arity`. API fingerprint: `aad4cbb5e2da753def95dbb7ca3a48222ba5a346aa32008bf09dc0204b67d1cd`.

<a id="symbol-f1cea047423691aa0e0c23f3"></a>

## builtin / Column.fillMissing

Identity: `g010-public:Column.fillMissing`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).a;Column<Int>.from([a.get(0),a.get(1),a.get(2)]).c;c.fillMissing(83);c.countMissing().console();c.sum().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-fill-0`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-fill-0,data:column-fill-1,data:column-fill-2,data:column-fill-3,data:column-fill-5,data:column-fill-7`.

Misuse/diagnostic/failure proofs: `data:column-fillMissing-type`. API fingerprint: `9a7c224b1f994cb4f229c8ddb19d6ac9a6614e5dca796dc9a1751e3736f23dd1`.

<a id="symbol-3e3c77fdb6e659bd953855f0"></a>

## builtin / Column.get

Identity: `g010-public:Column.get`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([17]).c;c.get(1);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-bounds`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-get,data:column-missing-get,data:column-bounds,data:column-negative-index`.

Misuse/diagnostic/failure proofs: `data:column-get-type`. API fingerprint: `b29a1976783fe435e89b50f5bfc3f09e53b4b8ea3896221ef0161b921095b9e7`.

<a id="symbol-b113b225bd8dee3643a8c388"></a>

## builtin / Column.isMissing

Identity: `g010-public:Column.isMissing`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.from([17,29,71]).a;Column<Int>.from([a.get(0),a.get(1),a.get(2)]).c;c.countMissing().console();c.sum().console();c.isMissing(0).console();c.isMissing(1).console();c.isMissing(2).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-missing-0`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-missing-0,data:column-missing-1,data:column-missing-2,data:column-missing-3,data:column-missing-5,data:column-missing-7`.

Misuse/diagnostic/failure proofs: `data:column-isMissing-type`. API fingerprint: `4fdd736495e30e350c2f29769dc18a3c334f46d9e665043ed383da2b06ccade9`.

<a id="symbol-199805699ef010d04938cb16"></a>

## builtin / Column.max

Identity: `g010-public:Column.max`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([]).c;c.max().console();23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-max-empty`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-max-empty,data:column-max-many,data:column-max-changed,data:column-missing-extremes`.

Misuse/diagnostic/failure proofs: `data:column-max-arity`. API fingerprint: `3dccd834d6558bbd2cbc5d836c21176fb11a693da95ba75904144e276195335d`.

<a id="symbol-1217dff9ae16a72dddbe44cb"></a>

## builtin / Column.min

Identity: `g010-public:Column.min`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([]).c;c.min().console();23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:column-min-empty`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-min-empty,data:column-min-many,data:column-min-changed,data:column-missing-extremes`.

Misuse/diagnostic/failure proofs: `data:column-min-arity`. API fingerprint: `2de443b44b984ad5e692caed5982c497acd1854dec1d6445bf643d8e80401dc4`.

<a id="symbol-44defff977ea10360fc2eeae"></a>

## builtin / Column.sum

Identity: `g010-public:Column.sum`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([]).c;c.sum().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-sum-empty`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-sum-empty,data:column-sum-many,data:column-sum-changed,data:column-sum-limit,data:column-sum-overflow`.

Misuse/diagnostic/failure proofs: `data:column-sum-arity`. API fingerprint: `44d9a6bbd842cf4f805d9cdd457360932921de18cd8e0327c099fd5856932c4d`.

<a id="symbol-b7f819c8d49f98ed943d6987"></a>

## builtin / Column<Int> [values]

Identity: `g010-public:Column<Int> [values]`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int> [1,2,3,4].c;c.sum().return;}
```

Explicit process status: `10`; the full value/effect oracle is `data:column-prefix`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-prefix,data:column-prefix-changed`.

Misuse/diagnostic/failure proofs: `data:column-element-type`. API fingerprint: `7b2fdc4c208f581e7c5c5f9d77d3a4e08f899344860b3aa748b778f45c58aff2`.

<a id="symbol-62bafe0118796dbd29e8ad97"></a>

## builtin / Column<Int>.from

Identity: `g010-public:Column<Int>.from`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Column<Int>.from([17,29]).c;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:column-return-23`.

### Types, ownership, effects, limits and errors

Up to 32 nullable Int cells; native missing bitmap, checked aggregates and Int/UInt cast storage; UInt is not an implicit process status; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:column-sum-empty,data:column-sum-many,data:column-list,data:column-list-snapshot,data:column-two-live,data:column-return-23`.

Misuse/diagnostic/failure proofs: `data:column-element-type,data:column-value-type,data:column-mixed-type,data:column-copy,data:column-from-type`. API fingerprint: `ce4dbc1ffba0bbe819c221701f8c4d26d0e92d02af8a6fdbcd5fbd562a5edf39`.

<a id="symbol-2961cb8885e4455d56395403"></a>

## builtin / Dataset.cache

Identity: `g010-public:Dataset.cache`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29,71]))]).t;Dataset.fromTables([t]).d;d.cache(1);d.collect().rows;rows.at(2).get("age").expect("v").return;}
```

Explicit process status: `71`; the full value/effect oracle is `data:cache-collect`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:cache-disable,data:cache-collect,data:cache-scan,data:cache-two-live`.

Misuse/diagnostic/failure proofs: `data:cache-policy-type,data:cache-arity,data:cache-invalid-2,data:cache-blocks-repartition`. API fingerprint: `b62b77da6aebb7dcc1463e2ebecff79fdff7af622ba0162725cee823cfe26ffb`.

<a id="symbol-1ad61b1e349c92129d036713"></a>

## builtin / Dataset.collect

Identity: `g010-public:Dataset.collect`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;d.collect().rows;rows.at(2).get("age").expect("v").console();0.return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:collect-return-0`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:collect-return-0,data:collect-return-7,data:collect-return-23,data:collect-count-0,data:collect-count-1,data:collect-count-255,data:collect-count-256,data:collect-order,data:collect-two-live,data:collect-result-independent,data:collect-missing-3,data:collect-block-cleanup,data:collect-early-return,data:collect-break-continue`.

Misuse/diagnostic/failure proofs: `data:collect-arity,data:collect-copy,data:collect-count-257,data:collect-row-not-int`. API fingerprint: `ff37c773712a6cf2cf48fc6448a854140b620a0bfdcfd15772d9c5ca386791d0`.

<a id="symbol-353665876ddbd85db886df20"></a>

## builtin / Dataset.collect.at

Identity: `g010-public:Dataset.collect.at`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;d.collect().rows;rows.at(0).first;rows.at(2).last;last.project(["age"]).p;first.get("age").expect("v").console();p.get("age").expect("v").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:collect-row-independent`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:collect-count-3,data:collect-count-256,data:collect-row-independent`.

Misuse/diagnostic/failure proofs: `data:collect-at-type,data:collect-bound,data:collect-negative-index`. API fingerprint: `3a4460fc3d8d21becd1145a56f3b16ebf1ebf9379cb1ba2dcfd5a6e4bf7121d1`.

<a id="symbol-e2317630166337e22ee832ef"></a>

## builtin / Dataset.collect.next

Identity: `g010-public:Dataset.collect.next`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([]))]).t;Dataset.fromTables([t]).d;d.collect().rows;rows.next().isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:collect-empty-next`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:collect-order,data:collect-empty-next,data:collect-option-consumed`.

Misuse/diagnostic/failure proofs: `data:collect-next-arity,data:collect-option-copy,data:collect-option-twice`. API fingerprint: `59b391d63b46c25d707e54c772e827d28c5596c7b10d0cd40f0242a090ad68f3`.

<a id="symbol-0327dc72eece2fd8aeabb111"></a>

## builtin / Dataset.fromTables

Identity: `g010-public:Dataset.fromTables`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:dataset-return-23`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-return-23,data:dataset-scan-empty,data:dataset-scan-many,data:dataset-scan-changed,data:dataset-scan-eight,data:dataset-scan-row-limit`.

Misuse/diagnostic/failure proofs: `data:dataset-empty-parts,data:dataset-over-parts,data:dataset-part-type,data:dataset-copy,data:dataset-schema-mismatch,data:dataset-schema-order-mismatch`. API fingerprint: `38ac4555bb15236a7f5adac66fb52c46ffdca28937a94c28cbae3b5c4d0bd70e`.

<a id="symbol-15a8375542024766df07f615"></a>

## builtin / Dataset.partitionCount

Identity: `g010-public:Dataset.partitionCount`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:dataset-return-23`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-return-23,data:dataset-scan-eight`.

Misuse/diagnostic/failure proofs: `data:dataset-partition-count-arity`. API fingerprint: `162f6b68650b590976ca476f0862879a2ebd1c4a3f7d9a25acc18b59b1a1fb07`.

<a id="symbol-6565d0aa93f9ccfceb42c261"></a>

## builtin / Dataset.repartition

Identity: `g010-public:Dataset.repartition`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29,71]))]).t;Dataset.fromTables([t]).d;d.repartition(1);7.return;}
```

Explicit process status: `7`; the full value/effect oracle is `data:repartition-explicit-return`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:repartition-0-1,data:repartition-3-1,data:repartition-17-7,data:repartition-256-32,data:repartition-two-live,data:repartition-result-schema,data:repartition-missing-3,data:repartition-explicit-return`.

Misuse/diagnostic/failure proofs: `data:repartition-size-type,data:repartition-arity,data:repartition-invalid-0,data:repartition-invalid-33,data:repartition-65-7`. API fingerprint: `943507f036e7753f5d8c41f2b7b262748681837ee4a0b49bd76e77b39d564a73`.

<a id="symbol-bac5b9d9eb2395cd2a666bf5"></a>

## builtin / Dataset.scan

Identity: `g010-public:Dataset.scan`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("age").unwrapOr(89).console();true.return;}(Table.self)consume(){self.rowCount().console();self.filter(observe);}start(){List<Int>.new().a;Table.fromColumns([Tuple.of("age",Column<Int>.from([17,a.get(0)]))]).t;Dataset.fromTables([t]).d;d.scan().s;s.sink(consume).return;}
```

Explicit process status: `1`; the full value/effect oracle is `data:dataset-scan-missing`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-scan-many,data:dataset-scan-changed,data:dataset-scan-next,data:dataset-scan-two-live,data:dataset-scan-sink-remainder,data:dataset-scan-lazy,data:dataset-scan-missing`.

Misuse/diagnostic/failure proofs: `data:dataset-scan-copy,data:dataset-scan-option-copy,data:dataset-scan-option-twice`. API fingerprint: `8caa2a0b9ead8695f1177f19488d3fbe54a1b1bbb2f9570bccc34b8cf27a0c71`.

<a id="symbol-00aec7292cc08eaf40afc2dc"></a>

## builtin / Dataset.scan.sink

Identity: `g010-public:Dataset.scan.sink`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("age").unwrapOr(89).console();true.return;}(Table.self)consume(){self.rowCount().console();self.filter(observe);}start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;d.scan().s;s.sink(consume);s.sink(consume).return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:dataset-scan-sink-consumed`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-scan-many,data:dataset-scan-row-limit,data:dataset-scan-sink-consumed`.

Misuse/diagnostic/failure proofs: `data:dataset-sink-receiver-type,data:dataset-sink-return-type,data:dataset-sink-extra-argument,data:dataset-scan-callback-trap`. API fingerprint: `f02461d7dd1da71a19e4394ccd8e0b3b3a65d420e1d6876e63b6ee94c8d0f651`.

<a id="symbol-0f333bd6f07e5e8987340e56"></a>

## builtin / Dataset.schema

Identity: `g010-public:Dataset.schema`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29]))]).a;Table.fromColumns([Tuple.of("age",Column<Int>.from([71]))]).b;Dataset.fromTables([a,b]).d;d.schema().s;s.field("age").at<0>().console();s.indexOf("age").expect("v").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:dataset-schema`.

### Types, ownership, effects, limits and errors

Up to 8 owned tables/256 rows/16384 value bytes; by-size repartition accepts 1..32 rows and preserves order; local cache policy 0/1; scan and collect preserve independent values/schema/missing; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:dataset-schema,data:dataset-schema-independent`.

Misuse/diagnostic/failure proofs: `data:dataset-schema-arity`. API fingerprint: `e4ab7f0f1ecabbdac994be159705ad9e79937d3021e25ae20880f1e2211bdfe6`.

<a id="symbol-f47466b0cf162e204b85583c"></a>

## builtin / Event<Int>.value

Identity: `g010-public:Event<Int>.value`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Event<Int> 13.x;x.value().return;}
```

Explicit process status: `13`; the full value/effect oracle is `data:Event-payload-13`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:Event-payload-13,data:Event-payload-29,data:Event-payload-71`.

Misuse/diagnostic/failure proofs: `data:event-type`. API fingerprint: `3bcba59fd71c78f20b45b6ab7229f91fa49718b099e03e32f26502f680d47dc5`.

<a id="symbol-748014ce294985c79c93f196"></a>

## builtin / Flow<Int>.value

Identity: `g010-public:Flow<Int>.value`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Flow<Int> 17.x;x.value().return;}
```

Explicit process status: `17`; the full value/effect oracle is `data:Flow-payload-17`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:Flow-payload-17,data:Flow-payload-29,data:Flow-payload-71`.

Misuse/diagnostic/failure proofs: `data:flow-type`. API fingerprint: `63871ca9bf0c6608e6b0c9fd6a80d4fb4d8d25cafcfb193857f43f31c4b264b3`.

<a id="symbol-f3218a0b1ee3283292adeca8"></a>

## builtin / Row.from

Identity: `g010-public:Row.from`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;Row.from(s,[17,71]).r;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:row-return-23`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:row-return-23,data:row-values-17-71,data:row-values--31-53,data:row-eight-fields,data:row-two-live,data:row-values-evaluated-once`.

Misuse/diagnostic/failure proofs: `data:row-schema-type,data:row-value-type,data:row-value-float,data:row-copy,data:row-too-few,data:row-too-many,data:row-empty-values,data:row-missing-nonnullable`. API fingerprint: `3c1f5b062b5489bd2d5c7f8f540575ac9537d78f1632bece4c64ec10ca11de10`.

<a id="symbol-d8d0066fd58966d55f6a1a7e"></a>

## builtin / Row.get

Identity: `g010-public:Row.get`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;Row.from(s,[17,71]).r;r.get("age").expect("v").console();r.get("score").expect("v").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:row-values-17-71`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:row-values-17-71,data:row-values-29-83,data:row-missing-0,data:row-missing-1,data:row-missing-2,data:row-missing-3`.

Misuse/diagnostic/failure proofs: `data:row-get-name-type,data:row-unknown-name`. API fingerprint: `cdc1f02416d46a11077583bd3c39f72c4b926449198e91ee3c278eeca7a46052`.

<a id="symbol-46aac3b96d85f8ef37ffd9b3"></a>

## builtin / Row.project

Identity: `g010-public:Row.project`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;Row.from(s,[17,71]).r;r.project(["score","age"]).p;p.project(["age"]).q;q.get("age").expect("v").return;}
```

Explicit process status: `17`; the full value/effect oracle is `data:row-project-composition`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:row-project-changed-order,data:row-project-subset,data:row-project-composition,data:row-project-missing`.

Misuse/diagnostic/failure proofs: `data:row-project-name-type,data:row-project-argument-type,data:row-project-empty,data:row-project-duplicate-name,data:row-project-unknown-name,data:row-project-removed-name`. API fingerprint: `354c1cce0d62e677bf9fdffef8dd4da77bfe5ca291e0c8864df33616a3c93695`.

<a id="symbol-06b334b9242e04ed292d4d41"></a>

## builtin / Row.toTuple

Identity: `g010-public:Row.toTuple`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;Row.from(s,[17,71]).r;r.toTuple().t;t.length().console();t.at<0>().expect("v").console();t.at<1>().expect("v").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:row-tuple-order`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:row-tuple-order,data:row-tuple-two-live,data:row-eight-fields,data:row-missing-3`.

Misuse/diagnostic/failure proofs: `data:row-to-tuple-arity,data:row-tuple-copy,data:row-tuple-static-bound,data:row-tuple-index-bound`. API fingerprint: `47354c61433268c6751f0da4d97ace084e19caba055ad39ad4e28fff087c786b`.

<a id="symbol-9b92eb109484fad44542320d"></a>

## builtin / Schema.field

Identity: `g010-public:Schema.field`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;s.field("age").length().return;}
```

Explicit process status: `3`; the full value/effect oracle is `data:schema-field-description-length`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:schema-ordered-age-score,data:schema-field-description-length,data:schema-field-snapshot`.

Misuse/diagnostic/failure proofs: `data:schema-field-name-type,data:schema-field-projection-bound,data:schema-field-missing`. API fingerprint: `53bd7994adcdc2eaa948731af67b0ea6385b594fe97c539288f44f15b5b2b3fe`.

<a id="symbol-b6f38784c9719b74fa30cfe1"></a>

## builtin / Schema.indexOf

Identity: `g010-public:Schema.indexOf`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false),Tuple.of("score",Int,true)]).s;s.indexOf("absent").isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:schema-missing-name`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:schema-ordered-age-score,data:schema-ordered-score-age,data:schema-missing-name`.

Misuse/diagnostic/failure proofs: `data:schema-index-name-type`. API fingerprint: `9d569a8a687ad46aa5778df928261e5e2f810df84bdb8268fbc109396d31fbed`.

<a id="symbol-554a87f51bf1ae5d99dcb4c1"></a>

## builtin / Schema.new

Identity: `g010-public:Schema.new`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Schema.new([Tuple.of("age",Int,false)]).s;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:schema-return-23`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:schema-return-23,data:schema-ordered-age-score,data:schema-ordered-score-age,data:schema-ordered-idade-pontuação,data:schema-runtime-name,data:schema-two-live,data:schema-name-limit`.

Misuse/diagnostic/failure proofs: `data:schema-wrong-type,data:schema-wrong-name,data:schema-wrong-nullability,data:schema-field-shape,data:schema-no-fields,data:schema-over-fields,data:schema-copy,data:schema-duplicate-name,data:schema-name-over-limit`. API fingerprint: `35e0b6202894ef292eed702262facd8c173ed83e7bdfde251591115eb7baed6d`.

<a id="symbol-eed0b3a8b76e4ca7d31fe1f8"></a>

## builtin / Stream.batch

Identity: `g010-public:Stream.batch`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stream<Int>.from([17,29,71]).s;s.next();s.batch(2).b;b.next().expect("v").a;a.at(0).console();a.at(1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:batch-remaining-cursor`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:batch-empty-1,data:batch-many-2,data:batch-changed-7,data:batch-limit-64,data:batch-source-independent,data:batch-two-live,data:batch-retained-independent,data:batch-remaining-cursor,data:batch-lazy,data:batch-filter-map-values,data:batch-owned-list-mutations,data:batch-owned-list-map,data:batch-owned-list-iterator`.

Misuse/diagnostic/failure proofs: `data:batch-size-type,data:batch-size-arity,data:batch-copy,data:batch-size-invalid-0,data:batch-size-invalid--1,data:batch-size-invalid-65`. API fingerprint: `3c6974321041cf9746573975c22aa0133ed87c028039a88cc8f909e8842760a1`.

<a id="symbol-163d5be92b054da37866f4ee"></a>

## builtin / Stream.batch.next

Identity: `g010-public:Stream.batch.next`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stream<Int>.from([17,29,71]).s;s.batch(2).b;b.next().v;v.expect("v");v.expect("again");23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:batch-option-consumed-twice`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:batch-many-2,data:batch-limit-64,data:batch-option-consumed,data:batch-option-consumed-twice`.

Misuse/diagnostic/failure proofs: `data:batch-option-copy`. API fingerprint: `4fbe2103de7a100bb9a8d3965c0ff1ac000fc8bd7f5f61e6e1394259717f2d46`.

<a id="symbol-a96f2f0d0690955a22be02c2"></a>

## builtin / Stream.batch.sink

Identity: `g010-public:Stream.batch.sink`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(List<Int>.self)emit(){self.at(64).console();}start(){Stream<Int>.from([17]).s;s.batch(1).b;b.sink(emit);23.return;}
```

Explicit process status: `177`; the full value/effect oracle is `data:batch-sink-trap`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:batch-sink-empty-2,data:batch-sink-many-2,data:batch-sink-changed-64,data:batch-sink-limit-64,data:batch-sink-consumed,data:batch-sink-trap`.

Misuse/diagnostic/failure proofs: `data:batch-sink-scalar-receiver,data:batch-sink-return-value,data:batch-sink-extra-argument`. API fingerprint: `961701419f722dcfe3f075049af8f40b4af6a87abbf075ee629502c2359b60d0`.

<a id="symbol-94741ec1780be3ffd94dfabe"></a>

## builtin / Stream.filter

Identity: `g010-public:Stream.filter`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)step(){(self>20).return;}(Int.self)emit(){self.console();}start(){Stream<Int>.from([17]).s;s.filter(step).out;out.sink(emit).return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:stream-filter-one`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:stream-filter-one,data:stream-filter-many,data:stream-filter-changed,data:stream-order-boundary-map-add-filter-keep,data:stream-order-boundary-filter-keep-map-add`.

Misuse/diagnostic/failure proofs: `data:stream-filter-return-type`. API fingerprint: `1858fe262b5652f222b7202a70582a6c084069d0ca2c6574a8ef82f51402919b`.

<a id="symbol-996c2a97ce8a54e3a66a3bc5"></a>

## builtin / Stream.map

Identity: `g010-public:Stream.map`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)step(){self.console();(self+7).return;}start(){"ready".console();Stream<Int>.from([17,71]).s;s.map(step).out;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:stream-lazy`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:stream-map-one,data:stream-map-many,data:stream-map-changed,data:stream-lazy,data:stream-callback-once,data:stream-independent-builders,data:stream-stages-16,data:stream-stages-17`.

Misuse/diagnostic/failure proofs: `data:stream-map-return-type,data:stream-map-arity,data:stream-map-type`. API fingerprint: `6b381c6d7efb6fd83b7f6b6cf800204eecec31b9ac185e46439f4f047b59ef6c`.

<a id="symbol-f5b265f53544e5687e731b94"></a>

## builtin / Stream.next

Identity: `g010-public:Stream.next`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stream<Int>.from([]).s;s.next().unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `data:stream-next-empty`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:stream-next,data:stream-next-empty,data:stream-next-exhausted,data:stream-remaining-cursor`.

Misuse/diagnostic/failure proofs: `data:stream-next-arity`. API fingerprint: `80dcc67960f85fa92512db8cbdb169ca510e150a6eb52ef584d85c380eb80d8f`.

<a id="symbol-42d7706bab58be68345d73dd"></a>

## builtin / Stream.sink

Identity: `g010-public:Stream.sink`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.self)emit(){self.console();}start(){Stream<Int>.from([]).s;s.sink(emit).return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:stream-sink-empty`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:stream-sink-empty,data:stream-sink-one,data:stream-sink-many,data:stream-sink-changed,data:stream-sink-limit,data:stream-consume-once`.

Misuse/diagnostic/failure proofs: `data:stream-sink-return-type,data:stream-sink-batch-receiver`. API fingerprint: `c4cf9be389f87e5598e93e4b59213f9674ead047f567cebeb099e6ae9b5a6c69`.

<a id="symbol-addd635b67b412b4fced368a"></a>

## builtin / Stream.window

Identity: `g010-public:Stream.window`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stream<Int>.from([17,29,71]).s;s.window(2).w;w.next().expect("v").at(1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:window-return-23`.

### Types, ownership, effects, limits and errors

Finite 64-event profile; Event.sequence supplies monotonic logical timestamps; positive Int duration; lazy nonempty windows yield independent owned Lists; no next-window callback executes early; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:window-empty-2,data:window-one-1,data:window-many-2,data:window-changed-2,data:window-limit-64,data:window-filter-map-2,data:window-two-live,data:window-retained,data:window-remaining-cursor,data:window-lazy-no-lookahead,data:window-filter-empty-buckets,data:window-filter-all-empty,data:window-sink-consumed,data:window-return-23`.

Misuse/diagnostic/failure proofs: `data:window-duration-type,data:window-arity,data:window-missing-duration,data:window-copy,data:window-option-copy,data:window-sink-type,data:window-invalid-duration-0,data:window-invalid-duration--1,data:window-callback-trap`. API fingerprint: `7f74018f1970fb9cf58485f7edad6f6081a63e10336c7ffc961d052220ffae8e`.

<a id="symbol-84f19c8b4e75c2816a673bcd"></a>

## builtin / Stream<Int>.from

Identity: `g010-public:Stream<Int>.from`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Stream<Int>.from([]).s;s.next().unwrapOr(83).return;}
```

Explicit process status: `83`; the full value/effect oracle is `data:stream-next-empty`.

### Types, ownership, effects, limits and errors

Finite 64-event Int profile; 16 ordered lazy stages; native cursor, typed callbacks and owned List batches; logical-timestamp windows are separately exercised; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:stream-next,data:stream-next-empty,data:stream-from-list,data:stream-list-snapshot,data:stream-two-live,data:stream-sink-limit`.

Misuse/diagnostic/failure proofs: `data:stream-element-type,data:stream-value-type,data:stream-copy,data:stream-from-type`. API fingerprint: `8663db8fe6beff2b2b386cacb1a4d18cfa00ea01247167c8725fe2e073bb0c04`.

<a id="symbol-f999de190f119ad551ce09ce"></a>

## builtin / Table.columnCount

Identity: `g010-public:Table.columnCount`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17,29,71])),Tuple.of("score",Column<Int>.from([83,7,53]))]).t;t.rowCount().console();t.columnCount().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-dimensions-many`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-dimensions-many,data:table-eight-columns,data:table-select-subset`.

Misuse/diagnostic/failure proofs: `data:table-column-count-arity`. API fingerprint: `4a4135efab9f1dec88fda9c2e87ecd0a5152c7b6295570cfebc2e07e0fae35f7`.

<a id="symbol-3187925856b7438493303a89"></a>

## builtin / Table.filter

Identity: `g010-public:Table.filter`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){List<Int>.from([17,29]).a;a.iterator().it;it.next().expect("v").console();true.return;}start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([71,17,29])),Tuple.of("score",Column<Int>.from([83,7,53]))]).t;t.filter(observe);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-callback-native-local`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-filter-empty-20,data:table-filter-many-20,data:table-filter-many-50,data:table-filter-changed-20,data:table-filter-limit-20,data:table-callback-native-local`.

Misuse/diagnostic/failure proofs: `data:table-filter-receiver-type,data:table-filter-result-type,data:table-filter-arity,data:table-filter-trap`. API fingerprint: `8b7fe90a1c325c1554e5bfea0299076910eba738c20df79b7390591e4e138c52`.

<a id="symbol-aba89953474081d837369e4a"></a>

## builtin / Table.fromColumns

Identity: `g010-public:Table.fromColumns`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([17]))]).t;23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-return-23`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-return-23,data:table-dimensions-empty,data:table-dimensions-one,data:table-dimensions-many,data:table-dimensions-limit,data:table-eight-columns,data:table-constructor-snapshot`.

Misuse/diagnostic/failure proofs: `data:table-empty-columns,data:table-wrong-name,data:table-wrong-column,data:table-copy,data:table-unequal-lengths,data:table-duplicate-name`. API fingerprint: `20484faf12b2a1c95278ec80ff0e3449a445ca4a515a06dc892ce60ab123cd6d`.

<a id="symbol-a673c34806fa36f239d35cc2"></a>

## builtin / Table.groupBy

Identity: `g010-public:Table.groupBy`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("key",Column<Int>.from([29,17,29])),Tuple.of("value",Column<Int>.from([71,83,97]))]).t;0.i.mutable;while(i<3){t.groupBy(["key"]).g;g.length().console();i+=1;}23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:group-normal-cleanup`.

### Types, ownership, effects, limits and errors

Up to 32 materialized native Table groups; one Int key; first-seen key and source row order; missing keys excluded by native policy; owned tables and independent cursor; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:group-empty,data:group-one,data:group-many,data:group-changed,data:group-signed,data:group-limit,data:group-same,data:group-missing-3,data:group-missing-7,data:group-two-live,data:group-normal-cleanup`.

Misuse/diagnostic/failure proofs: `data:group-key-type,data:group-key-arity,data:group-key-two,data:group-method-arity,data:group-copy,data:group-missing-name`. API fingerprint: `1eecc239cd00aac3c277e91b77cc29db0437bb2e24ea0a2ac3c2ea98dfbc69f9`.

<a id="symbol-1af79934c567945f5c611419"></a>

## builtin / Table.groupBy.next

Identity: `g010-public:Table.groupBy.next`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("key").unwrapOr(89).console();self.get("value").expect("v").console();true.return;}start(){Table.fromColumns([Tuple.of("key",Column<Int>.from([29,17,29])),Tuple.of("value",Column<Int>.from([71,83,97]))]).t;t.groupBy(["key"]).g;g.next().expect("group").a;g.next().expect("group").b;a.filter(observe);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:group-retained-table`.

### Types, ownership, effects, limits and errors

Up to 32 materialized native Table groups; one Int key; first-seen key and source row order; missing keys excluded by native policy; owned tables and independent cursor; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:group-next,data:group-retained-table,data:group-independent-cursor`.

Misuse/diagnostic/failure proofs: `data:group-empty-expect,data:group-copy`. API fingerprint: `62e0135bbfcf0d8dc58bc1c3ab44349a4a4f8b92449c012636263ef3e74c89e9`.

<a id="symbol-51439e57dc17842263a25118"></a>

## builtin / Table.groupBy.sink

Identity: `g010-public:Table.groupBy.sink`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("key").unwrapOr(89).console();self.get("value").expect("v").console();true.return;}(Table.self)consume(){self.rowCount().console();self.filter(observe);}start(){Table.fromColumns([Tuple.of("key",Column<Int>.from([29,17,29])),Tuple.of("value",Column<Int>.from([71,83,97]))]).t;t.groupBy(["key"]).g;g.next();g.sink(consume).return;}
```

Explicit process status: `1`; the full value/effect oracle is `data:group-sink-cursor`.

### Types, ownership, effects, limits and errors

Up to 32 materialized native Table groups; one Int key; first-seen key and source row order; missing keys excluded by native policy; owned tables and independent cursor; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:group-many,data:group-limit,data:group-sink-cursor`.

Misuse/diagnostic/failure proofs: `data:group-sink-type`. API fingerprint: `a75074f2a1b2fb1f7ba8c24c113bc42eb60834df33e80eb6fb38a27884de6f48`.

<a id="symbol-1cf52dfb680fc62690f0f9a6"></a>

## builtin / Table.invalidRows

Identity: `g010-public:Table.invalidRows`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){List<Int>.new().empty;Table.fromColumns([Tuple.of("age",Column<Int>.from([empty.get(0),empty.get(0)]))]).t;Schema.new([Tuple.of("age",Int,false)]).strict;Schema.new([Tuple.of("age",Int,true)]).relaxed;t.invalidRows().bad;bad.rowCount().return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:quality-default`.

### Types, ownership, effects, limits and errors

Schema constraints preserve field name/type/order; nullability quality produces Bool and an independent invalid-row Table; source cells remain unchanged; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:quality-mask-3-5-0,data:quality-mask-7-7-0,data:quality-mask-7-7-3,data:quality-default,data:quality-repeated,data:quality-two-live,data:quality-limit-32`.

Misuse/diagnostic/failure proofs: `data:quality-invalid-rows-arity`. API fingerprint: `e05950c512a492d0d0ee0060ae9d4f64ec091a9ef8b079ea460f356cb308638c`.

<a id="symbol-f1d9ee7db3ee2fe5bd11fb56"></a>

## builtin / Table.join

Identity: `g010-public:Table.join`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("key",Column<Int>.from([17,29])),Tuple.of("age",Column<Int>.from([71,83]))]).left;Table.fromColumns([Tuple.of("key",Column<Int>.from([17])),Tuple.of("score",Column<Int>.from([103]))]).right;left.join(right,["key"],2).out;left.rowCount().console();right.rowCount().console();out.rowCount().return;}
```

Explicit process status: `2`; the full value/effect oracle is `data:join-source-preserved`.

### Types, ownership, effects, limits and errors

Single shared Int key; native kind 1 inner/2 left; <=32 rows/8 output fields; key emitted once, distinct non-key names; left-then-right order; missing never matches; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:join-empty-1,data:join-one-1,data:join-many-1,data:join-many-2,data:join-changed-1,data:join-signed-1,data:join-limit-1,data:join-kind-once,data:join-two-live,data:join-source-preserved,data:join-missing-1-0-2`.

Misuse/diagnostic/failure proofs: `data:join-other-type,data:join-keys-type,data:join-kind-type,data:join-arity,data:join-over-limit-1,data:join-over-limit-2,data:join-duplicate-nonkey`. API fingerprint: `17e0a74c48c1243175b543f0b5122ece526db7cceef9df36833a4a23f28705a7`.

<a id="symbol-7dba26d3850a189b45fa822a"></a>

## builtin / Table.rowCount

Identity: `g010-public:Table.rowCount`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([])),Tuple.of("score",Column<Int>.from([]))]).t;t.rowCount().console();t.columnCount().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-dimensions-empty`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-dimensions-empty,data:table-dimensions-one,data:table-dimensions-limit`.

Misuse/diagnostic/failure proofs: `data:table-row-count-arity`. API fingerprint: `679d08c2071e926fccfde5af2c5789000a1cf1384d8021d90cebc0fa068316bc`.

<a id="symbol-f4a6193bacbf7e92da44e2e2"></a>

## builtin / Table.select

Identity: `g010-public:Table.select`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("score").expect("v").console();true.return;}start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([71,17,29])),Tuple.of("score",Column<Int>.from([83,7,53]))]).t;t.select(["score"]).out;out.filter(observe);out.columnCount().return;}
```

Explicit process status: `1`; the full value/effect oracle is `data:table-select-subset`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-select-order,data:table-select-subset,data:table-select-sort-compose,data:table-sort-missing-3`.

Misuse/diagnostic/failure proofs: `data:table-select-name-type,data:table-select-empty,data:table-select-unknown-name,data:table-select-duplicate-name`. API fingerprint: `b21299891f0285d2985471367e4a6b0e4d389b86b7fe5dd85798f4e8162d77f2`.

<a id="symbol-c6b4fc20008aa236a26eb3c7"></a>

## builtin / Table.sortBy

Identity: `g010-public:Table.sortBy`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Row.self)observe(){self.get("age").expect("v").console();self.get("score").expect("v").console();true.return;}start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([71,17,29])),Tuple.of("score",Column<Int>.from([83,7,53]))]).t;t.sortBy(["age"]).out;t.filter(observe);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `data:table-sort-source-independent`.

### Types, ownership, effects, limits and errors

Int cells; up to 8 ordered fields and 32 rows; owned UTF-8 names/type/nullability and missing bits; native schema/row/table owners; stable ascending single-key sort with missing last; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:table-sort-order,data:table-sort-source-independent,data:table-sort-metamorphic-29-17-29,data:table-sort-metamorphic-71-7-83,data:table-sort-metamorphic--31-53-11,data:table-sort-missing-0,data:table-sort-missing-1,data:table-sort-missing-2,data:table-sort-missing-3,data:table-sort-missing-5,data:table-sort-missing-7`.

Misuse/diagnostic/failure proofs: `data:table-sort-name-type,data:table-sort-two-keys,data:table-sort-unknown-name`. API fingerprint: `526355c8d1cc16c7bbecdad91b3b5485fcb25620cda9c272ec1372c2ba94b34e`.

<a id="symbol-4a0b552487a7f1cb181d19b0"></a>

## builtin / Table.validate

Identity: `g010-public:Table.validate`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_PUBLIC_CONTRACT_TYPED_NATIVE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("age",Column<Int>.from([]))]).t;Schema.new([Tuple.of("age",Int,false)]).rules;t.validate(rules).console();t.invalidRows().bad;bad.rowCount().return;}
```

Explicit process status: `0`; the full value/effect oracle is `data:quality-empty`.

### Types, ownership, effects, limits and errors

Schema constraints preserve field name/type/order; nullability quality produces Bool and an independent invalid-row Table; source cells remain unchanged; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Typed nullable integer rows/columns/tables/datasets/streams; finite schema, rows and stages; lazy callbacks once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `data:quality-mask-0-0-0,data:quality-mask-3-5-0,data:quality-mask-3-5-3,data:quality-repeated,data:quality-source-missing-preserved,data:quality-two-live,data:quality-empty,data:quality-limit-32`.

Misuse/diagnostic/failure proofs: `data:quality-constraints-type,data:quality-constraints-arity,data:quality-schema-name,data:quality-schema-count`. API fingerprint: `88451007557b14f32ddfc7fe6300c417249f634c1767ee6cd75ac96b08c59812`.

<a id="symbol-324d4e7d6058e9a5f8346fa7"></a>

## builtin.Bool / Bool.console()

Identity: `g018-declaration:Bool.console()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(true).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-True`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Bool.console. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:publish-True,console:publish-False`.

Misuse/diagnostic/failure proofs: `console:bare-color`. API fingerprint: `754d04eca6902aa3ffadfa94ff04afd4d0b4186f60e01cbc24ea803a6ce53aa6`.

<a id="symbol-09ed5264a594d4d19b153c73"></a>

## builtin.Int / Int.console()

Identity: `g018-declaration:Int.console()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-17`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Int.console. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:publish-17,console:publish--29`.

Misuse/diagnostic/failure proofs: `console:bare-color`. API fingerprint: `e737496ea1bd2e35ef5dba0944611411f6575e9d44a08c0ad1b7004c5a56b579`.

<a id="symbol-c6fa99fb80637b9aa5af89e5"></a>

## builtin.Text / Text.console()

Identity: `g018-declaration:Text.console()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){("seed").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-seed`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Text.console. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:publish-seed,console:publish-changed,console:publish-Olá €,console:software-wrap-29`.

Misuse/diagnostic/failure proofs: `console:bare-color`. API fingerprint: `1b9973d2ccdd194ed9d3f9d97527c87992520eb680f0ae63f0e6cfe4f1b6fc84`.

<a id="symbol-491b97a5699c0f3de7bbbffe"></a>

## builtin.Text / Text.scan()

Identity: `g018-declaration:Text.scan()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("23")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-23-return-23`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-scan:scan. Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:int-23-return-23,scan:text-4e65626f,scan:bool-False,scan:char-81,scan:two-results-live`.

Misuse/diagnostic/failure proofs: `scan:invalid-process-type,scan:unknown`. API fingerprint: `e9773ae9e3c9bd2d1c598a40bbf4ba778e41ebd2dabc1dd144a38da1bde2f80b`.

<a id="symbol-7349762ecd8700a5742aef9e"></a>

## builtin.Console / console.append(value, style)

Identity: `g018-declaration:console.append(value, style)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.append(29);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:append-29`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Console.append. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:append-next,console:append-29,console:append-False,console:styled-append-text,console:styled-append-int,console:styled-append-bool`.

Misuse/diagnostic/failure proofs: `console:append-float,console:append-missing,console:styled-append-type,console:styled-append-arity`. API fingerprint: `44546694787de925d85e5abb5e0743e15ad89272a3dfffdbf48a4bfd96fe6b81`.

<a id="symbol-105c36bfe0e4bba4642165f0"></a>

## builtin.Console / console.clear()

Identity: `g018-declaration:console.clear()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.clear();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:clear`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Console.clear. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:clear,console:clear-append,console:clear-preserves-title,console:clear-removes-color,console:style-clear`.

Misuse/diagnostic/failure proofs: `console:clear-arity,console:close-clear`. API fingerprint: `30b785c262a77c65664b8a1c1aab6a869c662fcd0d7159e8110b50e9b9a49b23`.

<a id="symbol-fd9225c0c6ea514a4d39e92f"></a>

## builtin.Console / console.close()

Identity: `g018-declaration:console.close()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.close();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:close`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Console.close. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:close`.

Misuse/diagnostic/failure proofs: `console:close-arity,console:close-twice,console:close-publish`. API fingerprint: `9b95a29d5629a138fdcfcf40dc62d102bee4e2dd1552bbc443dd2657e120b77d`.

<a id="symbol-82a343f1bacc20951d619a86"></a>

## builtin.Console / console.scrollToEnd()

Identity: `g018-declaration:console.scrollToEnd()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"abc".console().view;view.scrollToEnd();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:scroll-3`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Console.scrollToEnd. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:scroll-3,console:scroll-6,console:scroll-13`.

Misuse/diagnostic/failure proofs: `console:scroll-arity`. API fingerprint: `ade329274638b8d80d0aa6e1a1a4944feb56438a99f5361b37ef82854b820d08`.

<a id="symbol-08d0bc5aff1c9ced9bcdaa25"></a>

## builtin.Console / console.size()

Identity: `g018-declaration:console.size()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"abc".console().view;view.size().s;s.at(0).console();s.at(1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:size-3`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Console.size. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:size-3,console:size-6,console:size-13`.

Misuse/diagnostic/failure proofs: `console:size-arity`. API fingerprint: `0625aa41f2abecbd7e8557238b0891d8d995123ad7c8fc9244881f3dc58fc852`.

<a id="symbol-271b34ecaabfb5ca57e362fc"></a>

## builtin.Console / console.style(range, style)

Identity: `g018-declaration:console.style(range, style)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().v;"next".console();v.style(Range.exclusive(0,1),Console.fg(17,29,53));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:style-exclusive-0-1`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Console.style. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:style-exclusive-0-1,console:style-exclusive-1-2,console:style-inclusive-0-1,console:style-dynamic-endpoints,console:style-clear`.

Misuse/diagnostic/failure proofs: `console:style-selection-type,console:style-endpoint-type,console:style-selection-arity,console:style-role-type,console:style-bound--1-1,console:style-bound-0-3,console:style-bound-2-1`. API fingerprint: `daa62cfbd1f21fc5c3bad903ecff930d857fcc158a3eb65d7e8c53efbca5f6f8`.

<a id="symbol-227e39db27e2b1e75d7b357b"></a>

## builtin.Console / console.title(text)

Identity: `g018-declaration:console.title(text)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.title("report");23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:title-6-report`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:Console.title. Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:title-6-report,console:title-13-change,console:title-loop`.

Misuse/diagnostic/failure proofs: `console:title-type,console:title-limit`. API fingerprint: `2bf5f3773e85367f9199c6ec7f89ca86c426d7dc3a3b62d1a97f08b05828fbab`.

<a id="symbol-fefeb82b188e22fb2c2d00fa"></a>

## builtin.visual / visual.summarize(value)

Identity: `g018-declaration:visual.summarize(value)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_VISUAL_DECLARATION`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){visual.summarize(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:visual-summary-0-23`.

### Types, ownership, effects, limits and errors

Current typed source proof g170-console:visual.summarize. Canonical existing pure visual_summary_foundation owner, actual Int value 0..255, decimal provenance, one evaluation and independent explicit return; no document/window/rendering effect is claimed. Corrected G018 SHA256=0944468ee395ee0c83aa6697baf52d111053327405fafd499e3cf08d57031718 Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:visual-summary-0-23,console:visual-summary-83-23,console:visual-summary-255-255,console:visual-summary-dynamic,console:visual-summary-once,console:visual-summary-two-values`.

Misuse/diagnostic/failure proofs: `console:visual-summary-range--1,console:visual-summary-range-256,console:visual-summary-type,console:visual-summary-arity,console:visual-summary-shadow`. API fingerprint: `2214487666ac1131ae6ffbbd2353224a030a4382e41a0c65ea92c653e8b25483`.

<a id="symbol-8b215aa9dba5cf264e8674e2"></a>

## builtin.Text / "%s tem %d anos".format(nome, idade).console();

Identity: `g059-format:"%s tem %d anos".format(nome, idade).console();`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Olá %s %d".format("Nebo",29).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:template-values-0`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:template-values-0,format:template-values-1`.

Misuse/diagnostic/failure proofs: `format:wrong-type`. API fingerprint: `7707fe2218b6ea5be553f575250a3af13299fb0dba7f4a5a3c41e70ca188ea47`.

<a id="symbol-e16f67a1443da8bc078886d5"></a>

## builtin.Text / ConsoleOptions

Identity: `g059-format:ConsoleOptions`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console(Position.at(17,29),Console.fg(17,29,53));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:position-17-29`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:styled-append-text,console:position-17-29,console:fg-bg-17,console:style-exclusive-0-1`.

Misuse/diagnostic/failure proofs: `console:fg-type,console:position-type,console:style-role-type`. API fingerprint: `c3093836a08dfa7d8f31f15cf973bb8eaa4d7bea7b5a51da0877315b7717b070`.

<a id="symbol-7c70511f72da66995c73ad60"></a>

## builtin.Text / FormatError.AllocationFailed

Identity: `g059-format:FormatError.AllocationFailed`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".value;"%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s%1$s".formatWith(0,value).isOk().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:allocation-1048576`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:allocation-1048576,format:allocation-8388608,format:allocation-atomic-fallback`.

Misuse/diagnostic/failure proofs: `format:template-4097`. API fingerprint: `c833b53c3a8c722a2bfdec55f68b6cd02223480915197bcb2167d8eef76a3818`.

<a id="symbol-29836787eadb9b9679c385d2"></a>

## builtin.Text / FormatError.InvalidTemplate

Identity: `g059-format:FormatError.InvalidTemplate`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%z".template;template.formatWith(0 ,17).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-unknown`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-unknown,format:checked-error-arity`.

Misuse/diagnostic/failure proofs: `format:unknown,format:missing`. API fingerprint: `d1fb2bd8001c527dbbd19524dba4de31c8dd20843778c78e87d97b3dea5427b5`.

<a id="symbol-bec158fb6852429a92fb29be"></a>

## builtin.Text / FormatError.SizeOverflow

Identity: `g059-format:FormatError.SizeOverflow`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".template;template.formatWith(0).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-limit`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-limit,format:output-limit-8193`.

Misuse/diagnostic/failure proofs: `format:template-4097`. API fingerprint: `55c936f5ad9eba263573fb26c973de35dd87f1a5fc8540355ff5432bbf2d7e61`.

<a id="symbol-a615c72d20bea4c90a94e56a"></a>

## builtin.Text / FormatString.parseChecked(Text)

Identity: `g059-format:FormatString.parseChecked(Text)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){FormatString.parseChecked("%z").get();23.return;}
```

Explicit process status: `170`; the full value/effect oracle is `format:parsed-failed-get`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:parsed-status-2564,format:parsed-status-257a,format:parsed-unwrap-success,format:parsed-unwrap-fallback,format:parsed-failed-get`.

Misuse/diagnostic/failure proofs: `format:parsed-argument-type,format:parsed-arity,format:parsed-extra`. API fingerprint: `05d816b8dc59c2ad6cee7b2d019c7e0d88767c9d96e531c45780050a37ee40c1`.

<a id="symbol-d3d3d593a8a51473e8218a0d"></a>

## builtin.Text / FormatString.validate(types)

Identity: `g059-format:FormatString.validate(types)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){FormatString.parseChecked("%d").get().t;t.validate(Tuple.of(Int)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:parsed-validate-2564-Int`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:parsed-validate-2564-Int,format:parsed-validate-2564-Text,format:parsed-named-signature-0,format:parsed-named-signature-1,format:parsed-named-signature-2,format:parsed-named-signature-3`.

Misuse/diagnostic/failure proofs: `format:parsed-signature-unknown,format:parsed-signature-limit`. API fingerprint: `f6da782f8c37a38f819717c898db2505e96b4225009663dfc9f2f658a8af8e0d`.

<a id="symbol-127c90b0f9720dc2bed37bf2"></a>

## builtin.Text / FormatString<TArgs>

Identity: `g059-format:FormatString<TArgs>`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"${FormatString.parseChecked("%d").get().validate(Tuple.of(Int))}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:parsed-interpolation`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:parsed-value-17-23,format:parsed-two-live,format:parsed-snapshot,format:parsed-interpolation`.

Misuse/diagnostic/failure proofs: `format:parsed-signature-type,format:parsed-signature-shadow`. API fingerprint: `96d3274ac23d1a01c5903e6c1af45b7dada9868903a35e3ac4ca9f25f0ba7339`.

<a id="symbol-1f484682333393bad4fed684"></a>

## builtin.Text / StaticFormatString

Identity: `g059-format:StaticFormatString`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Olá %s %d".format("Nebo",29).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:template-values-0`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:native-format_plan_native,format:template-values-0`.

Misuse/diagnostic/failure proofs: `format:wrong-type,format:unknown`. API fingerprint: `f7cea031905d4c638b57282949c3b967935970154fd43370c6cfb66ec167f7c1`.

<a id="symbol-cad7a37030139dbffc18bf75"></a>

## builtin.Text / Text.console(options...)

Identity: `g059-format:Text.console(options...)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console(Position.at(17,29),Console.fg(17,29,53));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:position-17-29`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:styled-append-text,console:position-17-29,console:fg-bg-17,console:style-exclusive-0-1`.

Misuse/diagnostic/failure proofs: `console:fg-type,console:position-type,console:style-role-type`. API fingerprint: `e04a9362ea4c661788b9fade6bbfeb4368d4079c375086540af9e4cc1fe6dcae`.

<a id="symbol-ac13cf257e605cfb23fcc2dc"></a>

## builtin.Text / Text.format(args...) -> Text

Identity: `g059-format:Text.format(args...) -> Text`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".format(83).console();0.return;}
```

Explicit process status: `0`; the full value/effect oracle is `format:return-0`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:template-values-0,format:template-values-1,format:evaluated-once,format:two-live,format:format-is-silent,format:return-0,format:return-255`.

Misuse/diagnostic/failure proofs: `format:wrong-type,format:missing,format:extra,format:dynamic-strict`. API fingerprint: `8c033a8dd74d901bf78448a747b1edfe2410b66739372e782b285067c3b4d29e`.

<a id="symbol-526760d08dcb8b45c61362fb"></a>

## builtin.Text / Text.formatWith(policy, args...) -> Result<Text, FormatError>

Identity: `g059-format:Text.formatWith(policy, args...) -> Result<Text, FormatError>`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".formatWith(0,29).isOk().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-isOk-True`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-int-83,format:checked-text-4e65626f,format:checked-two-live,format:checked-once,format:checked-nested,format:checked-isOk-True,format:checked-isErr-True,format:checked-isErr-False,format:output-limit-8192,format:output-limit-8193`.

Misuse/diagnostic/failure proofs: `format:checked-policy-type,format:checked-policy-missing,format:checked-fallback-type,format:checked-predicate-arity`. API fingerprint: `26187e52d017e15f483b35c18c70f581a38f1aba050c16ad6ce0f2548b67ef1f`.

<a id="symbol-17c974c8cbe9b5d2c8608135"></a>

## builtin.Text / format(...).binding

Identity: `g059-format:format(...).binding`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".format(17).one;"%s".format("Nebo").two;one.console();two.console();one.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:two-live`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:two-live`.

Misuse/diagnostic/failure proofs: `format:wrong-type`. API fingerprint: `a41bec14bd952121fec9ecdbe057c91c8aedd8d4539fc7e0dcec1aee6e4cf94d`.

<a id="symbol-76b891ea1442d287d48c9fee"></a>

## builtin.Text / %#

Identity: `g060-format:%#`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%#".format(17).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:structure-#-Int-3137`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:structure-#-Int-3137,format:structure-#-Float-312e35,format:structure-#-Text-224e65626f22`.

Misuse/diagnostic/failure proofs: `format:wrong-value`. API fingerprint: `7effd705b81811c6a388c7f93ea892007080763a30dc253475b5ad3ec2efb698`.

<a id="symbol-43b3367dd529ce4c123fd5e3"></a>

## builtin.Text / %%

Identity: `g060-format:%%`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"100%%".format().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:literal-percent`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:literal-percent`.

Misuse/diagnostic/failure proofs: `format:unknown`. API fingerprint: `3466ab46e7fdee87ef6ab1d61c6cf71a0dc812a32649e406050e622a469d685e`.

<a id="symbol-97def53031acebb75f270920"></a>

## builtin.Text / %-8s

Identity: `g060-format:%-8s`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%-8s".format("Nebo").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:align-left-4e65626f`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:align-left-4e65626f,format:align-left-61c3a7c3a36f`.

Misuse/diagnostic/failure proofs: `format:width-limit`. API fingerprint: `e681226c6cad09769eee9fc20fbfdc7dd80856e8798d16efc673eafa77a4a5c8`.

<a id="symbol-17b98d78b6ab3262fd5dcfce"></a>

## builtin.Text / %.2f

Identity: `g060-format:%.2f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%.2f".format(1.125).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:fixed-nearest-even-1.125`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:fixed-nearest-even-1.125,format:fixed-nearest-even-1.375`.

Misuse/diagnostic/failure proofs: `format:precision-limit`. API fingerprint: `95d355b3b85c34f264430f5b21f3d2b8905f78ae72b97e25343c5c4bacbf230a`.

<a id="symbol-56c1c7457e035844e5994ec9"></a>

## builtin.Text / %04d

Identity: `g060-format:%04d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%04d".format(7).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:zero-width-7`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:zero-width-7,format:zero-width-255,format:signed-padding--29`.

Misuse/diagnostic/failure proofs: `format:width-limit`. API fingerprint: `29ab32fc7109ee4c6e8a77c9f187f247514ced0aa902ba160e7563d7a534fd85`.

<a id="symbol-1bd3c84c056271066e503b33"></a>

## builtin.Text / %1$d

Identity: `g060-format:%1$d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%1$d/%1$d".format(29).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:position-repeat`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:position-reorder,format:position-repeat`.

Misuse/diagnostic/failure proofs: `format:position-zero,format:position-missing`. API fingerprint: `bd543b0b78f624df27e991fcf71ae4ae67f2a77339f2cb9cc0773bd79e85148a`.

<a id="symbol-afbf31d2a3812faa4a3b4288"></a>

## builtin.Text / %2$s

Identity: `g060-format:%2$s`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%2$s %1$d".format(29,"Nebo").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:position-reorder`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:position-reorder`.

Misuse/diagnostic/failure proofs: `format:position-zero,format:position-missing`. API fingerprint: `b6a257950459d4341bb68693fe6a71be7b84976c832c9eba81ba0c036c5ee4b2`.

<a id="symbol-cf3e5395fd86d0794c85a43f"></a>

## builtin.Text / %8s

Identity: `g060-format:%8s`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%8s".format("Nebo").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:align-right-4e65626f`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:align-right-4e65626f,format:align-right-61c3a7c3a36f`.

Misuse/diagnostic/failure proofs: `format:width-limit`. API fingerprint: `0ece3feb206d13398827dde7bea6d0198d1bc6f042c64c81e20e268297de27a3`.

<a id="symbol-8594d503b8ec5ec037e823eb"></a>

## builtin.Text / %?

Identity: `g060-format:%?`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%?".format(-83).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:structure-?-Int-2d3833`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:structure-?-Int-2d3833,format:structure-?-Bool-74727565,format:structure-?-Text-224e65626f22`.

Misuse/diagnostic/failure proofs: `format:wrong-typename`. API fingerprint: `88182568105fd374ac33da47d44e7f41c1993760e46dc12919dfbbc25644784b`.

<a id="symbol-ccf4f0f0b36c3ee911b33d66"></a>

## builtin.Text / %T

Identity: `g060-format:%T`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%T".format(-29).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:typename-Int`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:typename-Text,format:typename-Int,format:typename-Float,format:typename-Bool`.

Misuse/diagnostic/failure proofs: `format:wrong-typename`. API fingerprint: `dbcaece539b2690846fb99ff0e3e9a58e5d4ca8de58d286490d76924b9055a13`.

<a id="symbol-be6c8d01d4223e90ad906017"></a>

## builtin.Text / %b

Identity: `g060-format:%b`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%b".format(true).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:bool-True`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:bool-True,format:bool-False`.

Misuse/diagnostic/failure proofs: `format:wrong-bool`. API fingerprint: `49c89ae4c2b974de4e78b8441658191abd883a8320dc8a14ecc5989757b3fe49`.

<a id="symbol-aff25fac1e9d89264930e697"></a>

## builtin.Text / %d

Identity: `g060-format:%d`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".format(9223372036854775807).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:decimal-9223372036854775807`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:decimal--9223372036854775808,format:decimal-9223372036854775807`.

Misuse/diagnostic/failure proofs: `format:wrong-type`. API fingerprint: `78ec741bd733edcfa711a4b72aa91e9baf8a9e005506ed500ead23860ee0e3f3`.

<a id="symbol-30aca73b458f0feb703063bd"></a>

## builtin.Text / %e

Identity: `g060-format:%e`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%.2e".format(0.0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:scientific-0.0`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:scientific-0.0,format:scientific--0.0,format:scientific-9.999,format:scientific-99.99,format:scientific-precision-0-9.9999999`.

Misuse/diagnostic/failure proofs: `format:wrong-scientific,format:precision-limit`. API fingerprint: `dcf5a592c001756d3fa17637f1f4aa78660c968674445d16e10d3e51fa32aff0`.

<a id="symbol-717d70e5613a3d5d933490a2"></a>

## builtin.Text / %f

Identity: `g060-format:%f`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%.2f".format(17.5).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:fixed-17.5`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:fixed-17.5,format:fixed--29.25`.

Misuse/diagnostic/failure proofs: `format:wrong-float,format:precision-limit`. API fingerprint: `c7d180dcb2405866b7dccff9129715c4a71c5fec6ff3519525347b5e32e79c1b`.

<a id="symbol-492fb8ce1627ebc022b6a1bb"></a>

## builtin.Text / %j

Identity: `g060-format:%j`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%j".format("Nebo").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:json-4e65626f`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:json-4e65626f,format:json-4122425c43`.

Misuse/diagnostic/failure proofs: `format:wrong-json`. API fingerprint: `0f0b0649043243356363cb5165ff4fe0de8f632bd542c291891dfcf55ede0b76`.

<a id="symbol-3f68a6780305081a3a392303"></a>

## builtin.Text / %o

Identity: `g060-format:%o`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%o".format(29).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:octal-29`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:octal-29,format:octal-255`.

Misuse/diagnostic/failure proofs: `format:wrong-octal`. API fingerprint: `1e344770acac8324356e82398ab028e8deb439a3ef323b266ea5025aeda55161`.

<a id="symbol-43ea82f92c6d3ab2b24ba028"></a>

## builtin.Text / %q

Identity: `g060-format:%q`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%q".format("Nebo").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:quote-4e65626f`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:quote-4e65626f,format:quote-4122425c43`.

Misuse/diagnostic/failure proofs: `format:wrong-quote`. API fingerprint: `24a7bbd54bb52f11c7b9ae2b08d87982656fd22e94ca4ab35aa9a7a5200ec66a`.

<a id="symbol-4a990b4414293780dadf811a"></a>

## builtin.Text / %s

Identity: `g060-format:%s`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%s".format("Nebo").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:text-4e65626f`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:text-4e65626f,format:text-61c3a7c3a36f`.

Misuse/diagnostic/failure proofs: `format:coercion-forbidden`. API fingerprint: `c31070e70770ee5a954e1b2a56c6c72977dc247783e4f2679c6d346a08b3202a`.

<a id="symbol-df98703b19385be55d410111"></a>

## builtin.Text / %v

Identity: `g060-format:%v`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%v".format(-29).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:value-Int`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:value-Text,format:value-Int,format:value-Float,format:value-Bool`.

Misuse/diagnostic/failure proofs: `format:wrong-value`. API fingerprint: `fa5e26369bb3b956733dfe66cca4410ed2c03bcb28eb79198645cf29125f6dd8`.

<a id="symbol-62ba26083fdf74e8e679ab19"></a>

## builtin.Text / %x

Identity: `g060-format:%x`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%x".format(29).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:hex-29`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:hex-29,format:hex-255`.

Misuse/diagnostic/failure proofs: `format:wrong-hex`. API fingerprint: `e498bb680b7204306bfa10ada776315fc62005fd47d04ad52879f95257991fc1`.

<a id="symbol-30f782416663db0471c2e10d"></a>

## builtin.Text / %{idade:d}

Identity: `g060-format:%{idade:d}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%{value:d}/%{value:d}".format(value:29).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:named-repeat`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:named-29,format:named-83,format:named-repeat`.

Misuse/diagnostic/failure proofs: `format:named-type,format:named-duplicate`. API fingerprint: `e5665cc5a4d9bb465178984ac4457ab2b085f19f76b059d86bb11485e7ea38c7`.

<a id="symbol-b81db9494f3f42a2c18be081"></a>

## builtin.Text / %{nome:s}

Identity: `g060-format:%{nome:s}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%{nome:s} %{idade:d}".format(idade:29,nome:"Nebo").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:named-29`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:named-29,format:named-83,format:named-once,format:checked-named`.

Misuse/diagnostic/failure proofs: `format:named-duplicate,format:named-absent`. API fingerprint: `79afa911b9fe308a7bb369e56e0686e07629bd59b7a6daf5e9b054cda0f7aea1`.

<a id="symbol-fd5c6357997bd56c0b675ff2"></a>

## builtin.Text / .allowCoerce()

Identity: `g060-format:.allowCoerce()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%s".allowCoerce().format(29).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-coerce-29`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-coerce-29,format:configured-coerce--83`.

Misuse/diagnostic/failure proofs: `format:configured-arity-allowCoerce`. API fingerprint: `2f079f05608db8f127356058a24dc0ff197e4caf8360241459843c0c6dbf53e9`.

<a id="symbol-c380a52f744ca9f142aa8aa9"></a>

## builtin.Text / .extraArgs(policy)

Identity: `g060-format:.extraArgs(policy)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".extraArgs(1).format(17,29).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-extra`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-extra,format:configured-extra-reset,format:configured-effects`.

Misuse/diagnostic/failure proofs: `format:configured-type-extraArgs,format:configured-domain-extraArgs`. API fingerprint: `c1c453902428d77adbe45bd56dca72a05fe3fbc868616b7492206bfebd548d38`.

<a id="symbol-47c3e17a138e9cca4e3b3cb5"></a>

## builtin.Text / .formatOnly()

Identity: `g060-format:.formatOnly()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".validateOnly().formatOnly().format(17).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-format`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-format,format:configured-snapshot`.

Misuse/diagnostic/failure proofs: `format:configured-arity-formatOnly`. API fingerprint: `1c3bc2e86fadde685ea165d47753eb6051339ac6e1d43f51760a79806b99a6c8`.

<a id="symbol-4d1f8bd51ab9262b950a1077"></a>

## builtin.Text / .loose()

Identity: `g060-format:.loose()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%s".loose().format().unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-loose-missing`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-loose-extra,format:configured-loose-missing,format:configured-loose-unknown`.

Misuse/diagnostic/failure proofs: `format:configured-arity-loose`. API fingerprint: `f557c3327218fcc642005bdeacc9b3db55fb3b5ec4a6131791689d3207cb2c55`.

<a id="symbol-49d6601b1566a1d182055ee6"></a>

## builtin.Text / .missingArgs(policy)

Identity: `g060-format:.missingArgs(policy)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%s".missingArgs(1).format().unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-missing`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-missing,format:configured-missing-reset`.

Misuse/diagnostic/failure proofs: `format:configured-type-missingArgs,format:configured-domain-missingArgs`. API fingerprint: `7c9c1aaa8e982ddd3743e0db5487e066537dd1e3042e2e7077b0c919a21cf491`.

<a id="symbol-a88225b08d8188bd36aad029"></a>

## builtin.Text / .noCoerce()

Identity: `g060-format:.noCoerce()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%s".allowCoerce().noCoerce().format(29).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-no-coerce-29`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-no-coerce-29,format:configured-no-coerce--83`.

Misuse/diagnostic/failure proofs: `format:configured-arity-noCoerce`. API fingerprint: `bda3d2e0862111cbdf3974134aca2356058e58fe470f9a847e6dfb4e19df51d5`.

<a id="symbol-6df98269e9838fbc5393d151"></a>

## builtin.Text / .placeholderMode(mode)

Identity: `g060-format:.placeholderMode(mode)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".placeholderMode(1).format(29).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-mode-named-error`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-named,format:configured-position,format:configured-mode-reset,format:configured-mode-named-error`.

Misuse/diagnostic/failure proofs: `format:configured-type-placeholderMode,format:configured-domain-placeholderMode`. API fingerprint: `60471dc36abcee4dedfec6acc98960c3b19a2e937e7ea6803f75e7bc31c2a49e`.

<a id="symbol-5a962b223308bcba02701958"></a>

## builtin.Text / .strict()

Identity: `g060-format:.strict()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".strict().format(29).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-strict-29`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-strict-29,format:configured-strict-reset-29,format:configured-two-live`.

Misuse/diagnostic/failure proofs: `format:configured-arity-strict`. API fingerprint: `146614a698df0e93a55cca2fa7e6f443a012869cf722fdc5c64c96a6808abc64`.

<a id="symbol-4b96602ed31273de4bfd4ad3"></a>

## builtin.Text / .unknownPlaceholder(policy)

Identity: `g060-format:.unknownPlaceholder(policy)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"A%z".unknownPlaceholder(1).format().unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-unknown`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-unknown,format:configured-unknown-reset`.

Misuse/diagnostic/failure proofs: `format:configured-type-unknownPlaceholder,format:configured-domain-unknownPlaceholder`. API fingerprint: `ec5bc412efdfb797042218055620b24ad4092b9fb1db095dae85670c28f8c2ff`.

<a id="symbol-588639d9ed067dd3f280b077"></a>

## builtin.Text / .validateOnly()

Identity: `g060-format:.validateOnly()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".strict().format(17);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:configured-silent`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:configured-validate,format:configured-silent`.

Misuse/diagnostic/failure proofs: `format:configured-arity-validateOnly`. API fingerprint: `cb60155977bd4b82a1e5dfdc76be5e96414ee9be5c0dcda938f743c7fb6e79c4`.

<a id="symbol-da38fbe2f5eb2edb91122465"></a>

## builtin.Text / NEBO-FORMAT-001

Identity: `g060-format:NEBO-FORMAT-001`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%z".template;template.formatWith(0 ,17).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-unknown`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-unknown`.

Misuse/diagnostic/failure proofs: `format:unknown`. API fingerprint: `6ea54b226f483ff4dedc8f1c1474ca30f4590806789721eb69745fccc6eecbb1`.

<a id="symbol-a36906e5cb060716ab7f9c4a"></a>

## builtin.Text / NEBO-FORMAT-002

Identity: `g060-format:NEBO-FORMAT-002`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d %s".template;template.formatWith(0 ,17).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-arity`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-arity`.

Misuse/diagnostic/failure proofs: `format:missing,format:extra`. API fingerprint: `edf96b9a985fb3ae4f70b01dc9092db9e2567bc6ca4b1f76d0a0a82cc2a2a9fc`.

<a id="symbol-5c4778e568eebac49774f5aa"></a>

## builtin.Text / NEBO-FORMAT-003

Identity: `g060-format:NEBO-FORMAT-003`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%d".template;template.formatWith(0 ,"Nebo").unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-type`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-type`.

Misuse/diagnostic/failure proofs: `format:wrong-type`. API fingerprint: `fc02789183592bafeb3bfdea74ce6d62843f5056341a7f9cdf1e2d29b1563951`.

<a id="symbol-45d326da50c8c8288c2dc4ad"></a>

## builtin.Text / NEBO-FORMAT-004

Identity: `g060-format:NEBO-FORMAT-004`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%257d".template;template.formatWith(0 ,17).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-width`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-width`.

Misuse/diagnostic/failure proofs: `format:width-limit,format:precision-limit`. API fingerprint: `583436b68cce09ea225ff69afe96394683c103a14a14953383bd55c3024dbfef`.

<a id="symbol-d30355c2cece43146c07e93c"></a>

## builtin.Text / NEBO-FORMAT-005

Identity: `g060-format:NEBO-FORMAT-005`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%{absent:d}".template;template.formatWith(0 ,17).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-named`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-named`.

Misuse/diagnostic/failure proofs: `format:named-duplicate,format:named-absent`. API fingerprint: `34590f67406ef8288d864310ac86e617dd580c8a08084d971820d1c708037a0c`.

<a id="symbol-86b66c8b359c731c8cd773be"></a>

## builtin.Text / NEBO-FORMAT-006

Identity: `g060-format:NEBO-FORMAT-006`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%0$d".template;template.formatWith(0 ,17).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-position`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-position`.

Misuse/diagnostic/failure proofs: `format:position-zero,format:position-missing`. API fingerprint: `859e77401c15ef1b9c1e03984e918730b515a122a93f8bf8667549940fa5b325`.

<a id="symbol-d2ba4b9eaf9bddc9b87c5574"></a>

## builtin.Text / NEBO-FORMAT-007

Identity: `g060-format:NEBO-FORMAT-007`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"%s".template;template.formatWith(0 ,17).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-coercion`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-coercion`.

Misuse/diagnostic/failure proofs: `format:coercion-forbidden`. API fingerprint: `95b8c7b7f36d5bb03e789c05aaaf96225fa50e16cf7b094f715b115b68f7a2e0`.

<a id="symbol-37b024b85dc0fcb4626a142a"></a>

## builtin.Text / NEBO-FORMAT-008

Identity: `g060-format:NEBO-FORMAT-008`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".template;template.formatWith(0).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `format:checked-error-limit`.

### Types, ownership, effects, limits and errors

Actual Int64/Float/Bool/Text arguments; literal strict template or dynamic checked policy; 4096 template bytes, 128 nodes, 64 args, width 256, precision 6, output 1 MiB; native typed renderer and distinct owned result frames; named lexical values evaluated once; native registry identity and runtime profile are explicit; serialized interface metadata is verified by the separate metadata suite Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `format:checked-error-limit,format:output-limit-8193`.

Misuse/diagnostic/failure proofs: `format:template-4097`. API fingerprint: `6808ff44fc510591257dbf30b158b24d971871a7b70e8f63bf59641cc253c30d`.

<a id="symbol-e1b83788f26f154f766e3067"></a>

## builtin.Text / ${id:hex(width: 8)}

Identity: `g061-format:${id:hex(width: 8)}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(7).value;"${value:hex(width:8)}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:hex-width-7`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:hex-width-7,interpolation:hex-width-29,interpolation:hex-width-255`.

Misuse/diagnostic/failure proofs: `interpolation:width-limit,interpolation:width-type,interpolation:width-name`. API fingerprint: `af48f5f96406633ab0118149e9afd77514849169906097ce27a0deff362a491e`.

<a id="symbol-984a1ab7caa70d80d9478186"></a>

## builtin.Text / ${nome:align(right, 20)}

Identity: `g061-format:${nome:align(right, 20)}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".value;"[${value:align(right,9)}]".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:align-right-4e65626f`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:align-right-4e65626f,interpolation:align-right-61c3a7c3a36f`.

Misuse/diagnostic/failure proofs: `interpolation:align-side,interpolation:align-width`. API fingerprint: `8e220fbbbf257f3d9169be22045c4c0b48b55842d67256881d95560c74de57e1`.

<a id="symbol-f9602aeea50887a02b6acb9f"></a>

## builtin.Text / ${nome}

Identity: `g061-format:${nome}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"ação".text;"[${text}]".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:text-61c3a7c3a36f`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:identifier-23,interpolation:identifier-29,interpolation:text-61c3a7c3a36f`.

Misuse/diagnostic/failure proofs: `interpolation:missing-name`. API fingerprint: `72cd3922774fb6122d73d74290d02f66a0c7a4d407357fcce7a557cccb5c731f`.

<a id="symbol-a7e6c7caaab8f5758093c1bb"></a>

## builtin.Text / ${preco:fixed(2)}

Identity: `g061-format:${preco:fixed(2)}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17.5).value;"${value:fixed(2)}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:fixed-17.5-2`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:fixed-17.5-2,interpolation:fixed--29.25-2`.

Misuse/diagnostic/failure proofs: `interpolation:wrong-type-fixed,interpolation:precision-limit`. API fingerprint: `e77e8c1be483a8d1db9b6fd8794ed155804ce91a9bf5c39daff36794c2b0c1d2`.

<a id="symbol-fe4a9c183b8f80e1cf29a182"></a>

## builtin.Text / ${texto.codepointCount()}

Identity: `g061-format:${texto.codepointCount()}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Nebo".text;"${text.codepointCount()}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:pure-method-4e65626f`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:pure-method-4e65626f,interpolation:pure-method-61c3a7c3a36f,interpolation:pure-text-callee`.

Misuse/diagnostic/failure proofs: `interpolation:effect-console`. API fingerprint: `c7426e57b7debad95a978517a7fba5f382e274eabf046970febf166489f63bad`.

<a id="symbol-c381297cdd85bc882a1eebda"></a>

## builtin.Text / ${utilizador.nome}

Identity: `g061-format:${utilizador.nome}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
struct User { Text.name; Int.age; }start(){"${User {name:"Nebo",age:29}.age}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `composite:typed-struct-inline`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Int64/Bool/binary64/Unicode scalar values; checked arithmetic; lexical evaluation once

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/semantic/bindings/binding_vertical.asm` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `composite:typed-struct-source,composite:typed-struct-two-17-29,composite:typed-struct-nested,composite:typed-struct-text-method,composite:typed-struct-field-length,composite:typed-struct-inline,composite:typed-struct-lexical-effects,composite:typed-struct-profiles`.

Misuse/diagnostic/failure proofs: `composite:typed-struct-wrong-text,composite:typed-struct-wrong-int,composite:typed-struct-unknown-projection,composite:typed-struct-interpolation-effects`. API fingerprint: `9af50d676590ab7939e97a643cb6293921b9486a77c808d8b42033e4566acab4`.

<a id="symbol-738d802bace68632720f7a15"></a>

## builtin.Text / ${valor:fixed(2)}

Identity: `g061-format:${valor:fixed(2)}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17.5).value;"${value:fixed(2)}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:fixed-17.5-2`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:fixed-17.5-2,interpolation:fixed--29.25-2`.

Misuse/diagnostic/failure proofs: `interpolation:wrong-type-fixed,interpolation:precision-limit`. API fingerprint: `2482e9a0809d0ec8fc49003d9b1d28cb4a0e68642e66d9724960a6b26e2b1131`.

<a id="symbol-0c9ff39c358fd2b927713cd2"></a>

## builtin.Text / ${x + y}

Identity: `g061-format:${x + y}`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.first;29.second;"${first+second}|${first-second}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:arithmetic-17-29`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:arithmetic-17-29,interpolation:arithmetic-71-29,interpolation:arithmetic-17-83`.

Misuse/diagnostic/failure proofs: `interpolation:missing-name`. API fingerprint: `5f3d64a5e294d0aea82ce7110c07122ccb2adf5eccdc2dab01d0c3dcdd61eb58`.

<a id="symbol-676f09c660c7e06e95e7a645"></a>

## builtin.Text / A_B_FORMAT_PLAN_EQUIVALENCE

Identity: `g061-format:A_B_FORMAT_PLAN_EQUIVALENCE`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"${29}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:equivalence-decimal`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:equivalence-decimal,interpolation:equivalence-fixed,interpolation:equivalence-hex,interpolation:equivalence-align`.

Misuse/diagnostic/failure proofs: `interpolation:wrong-type-fixed`. API fingerprint: `c6fb7ab5ea9e5e160124a29d114aaf2e21aa86fb153a266a1cd56f5efc292803`.

<a id="symbol-c77f14604299185cae3d3bc2"></a>

## builtin.Text / FormatProfileExpr

Identity: `g061-format:FormatProfileExpr`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(29).value;"${value:hex(width:8)}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:hex-width-29`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:ast-profile,interpolation:ast-named-profile,interpolation:hex-width-29,interpolation:align-center-4e65626f`.

Misuse/diagnostic/failure proofs: `interpolation:unknown-profile,interpolation:width-name`. API fingerprint: `edaea0099dba585ac393e60554a057312c96f71e1e7d222ed02428071386c701`.

<a id="symbol-6e8cbfb0d236fba85b7fac15"></a>

## builtin.Text / INTERPOLATION_EFFECT_POLICY=PURE_ONLY

Identity: `g061-format:INTERPOLATION_EFFECT_POLICY=PURE_ONLY`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.x)plus(){(x+29).return;}start(){"${17.plus()}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:pure-function`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:pure-function,interpolation:pure-transitive,interpolation:pure-text-callee,interpolation:pure-binding-callee`.

Misuse/diagnostic/failure proofs: `interpolation:effect-console,interpolation:effect-function,interpolation:effect-transitive,interpolation:effect-mutable`. API fingerprint: `c331e7f0d5783d24aa3213b7d786c82b0bab9a2d9c1872cf4d771d4ff5f1508d`.

<a id="symbol-2fa33969b0c5aee2d74588af"></a>

## builtin.Text / INTERPOLATION_EVALUATION=EXACTLY_ONCE

Identity: `g061-format:INTERPOLATION_EVALUATION=EXACTLY_ONCE`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"outer=${"inner=${17+29}"}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:nested`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:two-live,interpolation:mutable-snapshot,interpolation:nested`.

Misuse/diagnostic/failure proofs: `interpolation:effect-function`. API fingerprint: `255ed1ec8658fa9793cbf2ddf8e7e944774de39f9462da74d1a5795de363340b`.

<a id="symbol-a4374b03774d28f0d1661362"></a>

## builtin.Text / INTERPOLATION_ORDER=LEFT_TO_RIGHT

Identity: `g061-format:INTERPOLATION_ORDER=LEFT_TO_RIGHT`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){17.first;29.second;"${first+second}|${first-second}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:arithmetic-17-29`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:two-live,interpolation:mutable-snapshot,interpolation:arithmetic-17-29`.

Misuse/diagnostic/failure proofs: `interpolation:effect-transitive`. API fingerprint: `b1702c88760bdafc4ed6ac66cafe48673519a634a5c26e5771639bc3ccffd2e1`.

<a id="symbol-6c4e924ad91c85612cf36c90"></a>

## builtin.Text / InterpolatedTextExpr

Identity: `g061-format:InterpolatedTextExpr`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
// Interpolation must observe the actual bound values and preserve the return.
start() {
    17.first;
    29.second;
    "sum=${first + second}".console();
    23.return;
}

```

Explicit process status: `23`; the full value/effect oracle is `interpolation:source-value`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:source-value,interpolation:ast-arithmetic,interpolation:ast-two,interpolation:ast-nested`.

Misuse/diagnostic/failure proofs: `interpolation:effect-console`. API fingerprint: `bfa9bdaa6fd64a37f4f3ca7f02556d7329101dd5c58be46faedc422e99242e86`.

<a id="symbol-aeb0464e94d9f4992cc3edc5"></a>

## builtin.Text / InterpolatedTextExpr.lowerToFormatPlan()

Identity: `g061-format:InterpolatedTextExpr.lowerToFormatPlan()`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"outer=${"${29:hex(width:8)}"}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:nested-profile`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:source-value,interpolation:fixed-17.5-2,interpolation:nested-profile`.

Misuse/diagnostic/failure proofs: `interpolation:precision-limit`. API fingerprint: `6ca494aa5c657be3990ac250dca2e2a329ae9deb8522ec3a1127e0c9c1bec152`.

<a id="symbol-a136f2fa47db86337d2ef813"></a>

## builtin.Text / InterpolationEndToken

Identity: `g061-format:InterpolationEndToken`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"${"}"}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:literal-brace`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:nested,interpolation:literal-brace,interpolation:comment-brace,interpolation:lexer-comment-brace`.

Misuse/diagnostic/failure proofs: `interpolation:nesting-limit`. API fingerprint: `ff495ce6f52a3f5e7492519720327587e75ea24fd3bc43b9fa7d5994d6c00793`.

<a id="symbol-15b6e96165bb019526eb17ed"></a>

## builtin.Text / InterpolationPurityCheck

Identity: `g061-format:InterpolationPurityCheck`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"${81.sqrt()}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:numeric-method`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:pure-transitive,interpolation:pure-binding-callee,interpolation:numeric-method`.

Misuse/diagnostic/failure proofs: `interpolation:effect-transitive,interpolation:effect-mutable`. API fingerprint: `41b668aadc7945b2c19f9f0f957470e05003bd259457e462182b5d0b07c4b255`.

<a id="symbol-19a4e52e6758b9ded9923207"></a>

## builtin.Text / InterpolationRecovery

Identity: `g061-format:InterpolationRecovery`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
// Interpolation must observe the actual bound values and preserve the return.
start() {
    17.first;
    29.second;
    "sum=${first + second}".console();
    23.return;
}

```

Explicit process status: `23`; the full value/effect oracle is `interpolation:source-value`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:lexer-missing-expression-end,interpolation:lexer-missing-quote,interpolation:ast-empty-expression`.

Misuse/diagnostic/failure proofs: `interpolation:nesting-limit,interpolation:segments-limit`. API fingerprint: `9d7f4a3808cbcfcf08b8a778dd5e0d18f31ef9c1a5899c61808614113de6bb02`.

<a id="symbol-cc84444e00a3c9f3fde58169"></a>

## builtin.Text / InterpolationSegment.expression

Identity: `g061-format:InterpolationSegment.expression`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"ação".text;"${text.codepointCount()}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:pure-method-61c3a7c3a36f`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:arithmetic-17-29,interpolation:pure-method-61c3a7c3a36f,interpolation:ast-arithmetic`.

Misuse/diagnostic/failure proofs: `interpolation:effect-console`. API fingerprint: `4e38fdd7c1d327c12ebd436ccbd5354337854917142cb0d8a44cfe53cf3c977a`.

<a id="symbol-22853da32d0302b76ac61952"></a>

## builtin.Text / InterpolationSegment.literal

Identity: `g061-format:InterpolationSegment.literal`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"${"}"}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:literal-brace`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:escaped,interpolation:literal-brace,interpolation:ast-two`.

Misuse/diagnostic/failure proofs: `interpolation:template-tail-limit`. API fingerprint: `3ec1fbf60481af230a9317461b7bc55dad3ce4d298e6736e4c18608649c02227`.

<a id="symbol-300f283ff0a03712be4e7d3f"></a>

## builtin.Text / InterpolationStartToken

Identity: `g061-format:InterpolationStartToken`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
// Interpolation must observe the actual bound values and preserve the return.
start() {
    17.first;
    29.second;
    "sum=${first + second}".console();
    23.return;
}

```

Explicit process status: `23`; the full value/effect oracle is `interpolation:source-value`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:source-value,interpolation:lexer-one,interpolation:ast-integer`.

Misuse/diagnostic/failure proofs: `interpolation:effect-console`. API fingerprint: `80793b8b792a8ef3ff08da8d4e0c51ed326721daab8f1ad29f5ed3b8267767f7`.

<a id="symbol-8916d4340fc2f4846ad8d4c7"></a>

## builtin.Text / InterpolationTemporary

Identity: `g061-format:InterpolationTemporary`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"outer=${"${29:hex(width:8)}"}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:nested-profile`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:two-live,interpolation:mutable-snapshot,interpolation:nested-profile`.

Misuse/diagnostic/failure proofs: `interpolation:effect-mutable`. API fingerprint: `f223b2b6bcbfa15e6464b4bad8bf504c9a055e124e3a8860886df43fd4098512`.

<a id="symbol-5b7b1b964c6f72143d9a1087"></a>

## builtin.Text / LSP interpolation tokens

Identity: `g061-format:LSP interpolation tokens`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"outer=${"inner=${29}"}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:tooling-nested`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:tooling-highlight-17-utf-8,interpolation:tooling-highlight-29-utf-16,interpolation:tooling-nested`.

Misuse/diagnostic/failure proofs: `interpolation:tooling-literal-and-comment,interpolation:tooling-token-limit`. API fingerprint: `f5900745c1caa5968d0e7926fcd3fb802c30c78be965003de58a93fdc9a33711`.

<a id="symbol-0f299eb500d6ca0b5583c976"></a>

## builtin.Text / RUNTIME_TEMPLATE_PARSE=NO

Identity: `g061-format:RUNTIME_TEMPLATE_PARSE=NO`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"${29}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:equivalence-decimal`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:equivalence-decimal,interpolation:equivalence-fixed,interpolation:equivalence-pure-once`.

Misuse/diagnostic/failure proofs: `interpolation:unknown-profile`. API fingerprint: `cc5038059264b92458e560202fd7e25d0447bd45f3b5db6f690901a8ec60b2ab`.

<a id="symbol-f44e6e09f3fd5df76357b985"></a>

## builtin.Text / \${

Identity: `g061-format:\${`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"\${value}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:escaped`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:escaped,interpolation:lexer-escaped`.

Misuse/diagnostic/failure proofs: `interpolation:effect-console`. API fingerprint: `bb0b2b8dfed9641b5e5c5f950207985d4cbf5cc50faa875c48069ae48819b5b4`.

<a id="symbol-cc63f5b0475b22b837ff14ff"></a>

## builtin.Text / formatter interpolation

Identity: `g061-format:formatter interpolation`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Ω  ${17
 +29} end  ".console();23.return;}

```

Explicit process status: `23`; the full value/effect oracle is `interpolation:tooling-format-source`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:tooling-format-source`.

Misuse/diagnostic/failure proofs: `interpolation:tooling-format-invalid`. API fingerprint: `c7bda8272a8933e64218d5e20bcc2d51d4395785cf6cf736d6423b8a59d3b1f9`.

<a id="symbol-7836359f369b3e30d6c88311"></a>

## builtin.Text / rename inside interpolation

Identity: `g061-format:rename inside interpolation`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CORRECTED_FORMAT_CONTRACT_ROW`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
(Int.value)raised(){(value+7).return;}start(){"${29.raised()} / literal add \${29.add()}".console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `interpolation:tooling-rename-source`.

### Types, ownership, effects, limits and errors

Ordinary token/AST expression children; native pure-only effect policy with resolved callee bodies; static G059 nodes and G062 profiles; 64 segments, 16 nested quotes, 4096 source-content bytes; native named-product fields preserve declared types/offsets and source order (8 declarations/8 fields); native-token tooling preserves literals and exposes expressions; rename uses existing G162 receiver-method SymbolIds; privacy remains a separate obligation Checked format/interpolation profile; 64 segments; finite text/output budgets; pure interpolation

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: Per expression: pure computation plus explicit observed sinks. Capabilities: NO_IMPLICIT_GRANT.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `interpolation:tooling-rename-source`.

Misuse/diagnostic/failure proofs: `interpolation:tooling-rename-literal`. API fingerprint: `f55ab4e0b4db660f244ac9992a536e4b10c3a88d10714517a13f205b288e2a10`.

<a id="symbol-9717365fda11bc569ec5218f"></a>

## builtin.Console / Bool.console

Identity: `g170-console:Bool.console`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(true).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-True`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:publish-True,console:publish-False`.

Misuse/diagnostic/failure proofs: `console:bare-color`. API fingerprint: `7e0275def4edf6c69e95effc1d28a124afd0c5f8412c1add5be302bd48a68800`.

<a id="symbol-e5aef262423aa23b1dbed96b"></a>

## builtin.Console / Color(Int,Int,Int)

Identity: `g170-console:Color(Int,Int,Int)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color(17,29,53);}
```

Explicit process status: `0`; the full value/effect oracle is `console:color-implicit-standalone`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-sugar-rgb-(17_29_53_71),console:color-sugar-rgba-(17_29_53_71),console:color-sugar-hex-(17_29_53_71),console:color-implicit-standalone`.

Misuse/diagnostic/failure proofs: `console:color-sugar-arity`. API fingerprint: `27d834e12927c84e3f1e8c64683e01b4d72f62df6569b30908eac527f3196a13`.

<a id="symbol-1545603bf6b67e0650054d3d"></a>

## builtin.Console / Color(Int,Int,Int,Int)

Identity: `g170-console:Color(Int,Int,Int,Int)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color(17,29,53);}
```

Explicit process status: `0`; the full value/effect oracle is `console:color-implicit-standalone`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-sugar-rgb-(17_29_53_71),console:color-sugar-rgba-(17_29_53_71),console:color-sugar-hex-(17_29_53_71),console:color-implicit-standalone`.

Misuse/diagnostic/failure proofs: `console:color-sugar-arity`. API fingerprint: `be4af1587a9c770ca0f2c014b4323d30c6a970e262d4b3a5dcea3c4ba4150f71`.

<a id="symbol-4fcc39a0e660120cf35d6203"></a>

## builtin.Console / Color(TextLiteral)

Identity: `g170-console:Color(TextLiteral)`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color(17,29,53);}
```

Explicit process status: `0`; the full value/effect oracle is `console:color-implicit-standalone`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-sugar-rgb-(17_29_53_71),console:color-sugar-rgba-(17_29_53_71),console:color-sugar-hex-(17_29_53_71),console:color-implicit-standalone`.

Misuse/diagnostic/failure proofs: `console:color-sugar-arity`. API fingerprint: `8478bc1729818746578a04277f4cd1b64e5b9f29d94493c3a089f75910bbcf7a`.

<a id="symbol-28047b920803d1f18459ea31"></a>

## builtin.Console / Color.alpha

Identity: `g170-console:Color.alpha`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgb(17,29,53).c;c.red().console();c.green().console();c.blue().console();c.alpha().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:channels-17`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:channels-17,console:channels-71,console:alpha-113`.

Misuse/diagnostic/failure proofs: `console:channel-arity`. API fingerprint: `33fb9b1159a2c7cf5c631d2d7645831279c4570f312c20b17184c7352e0f7235`.

<a id="symbol-c71cb2602cb83caa49ca5f40"></a>

## builtin.Console / Color.blue

Identity: `g170-console:Color.blue`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgb(17,29,53).c;c.red().console();c.green().console();c.blue().console();c.alpha().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:channels-17`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:channels-17,console:channels-71,console:alpha-113`.

Misuse/diagnostic/failure proofs: `console:channel-arity`. API fingerprint: `9257330546867235e04be2b198b515707a24b8c668d275e8c4f6b257c01ae44c`.

<a id="symbol-08609bddec5408c08be0046b"></a>

## builtin.Console / Color.copy

Identity: `g170-console:Color.copy`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgba(17,29,53,71).original;original.copy;copy.equals(original).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-copied-(17_29_53_71)`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-copied-(17_29_53_71),console:color-copied-(83_29_53_71)`.

Misuse/diagnostic/failure proofs: `console:equals-type`. API fingerprint: `0be21ea7365c138a5159c6696950750c66af8ec8646421caab1551d4659541e1`.

<a id="symbol-5e96015b74b7fecd127e454d"></a>

## builtin.Console / Color.equals

Identity: `g170-console:Color.equals`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgba(17,29,53,71).original;original.copy;copy.equals(original).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-copied-(17_29_53_71)`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-copied-(17_29_53_71),console:color-copied-(83_29_53_71)`.

Misuse/diagnostic/failure proofs: `console:equals-type`. API fingerprint: `87e362ab2cc043bc19bb24f152d8488aca1513b0c405fccccba6e10b7835c8e1`.

<a id="symbol-dfd159c85046568c624558c7"></a>

## builtin.Console / Color.green

Identity: `g170-console:Color.green`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgb(17,29,53).c;c.red().console();c.green().console();c.blue().console();c.alpha().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:channels-17`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:channels-17,console:channels-71,console:alpha-113`.

Misuse/diagnostic/failure proofs: `console:channel-arity`. API fingerprint: `9beaa98e3e659eb75b53317b30261174ae46291fd13ee3ee0ef9a5b6c8a9d361`.

<a id="symbol-e08a99c842c6d0762bf60681"></a>

## builtin.Console / Color.hash

Identity: `g170-console:Color.hash`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgba(17,29,53,71).hash().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-hash-(17_29_53_71)`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-hash-(17_29_53_71),console:color-hash-(83_29_53_71)`.

Misuse/diagnostic/failure proofs: `console:color-hash-arity`. API fingerprint: `3be999b5e8547b82e4a2e9e241c47341b6ffd97c1746e5817da82f382080eb82`.

<a id="symbol-51c143323d02459e64410f51"></a>

## builtin.Console / Color.hex

Identity: `g170-console:Color.hex`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.hex("#111D3547").c;c.toHex().console();c.toHexWithAlpha().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-sugar-literal-(17_29_53_71)`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-sugar-literal-(17_29_53_71),console:color-sugar-literal-(83_29_53_71)`.

Misuse/diagnostic/failure proofs: `console:color-invalid-hex,console:color-hex-arity,console:color-dynamic-hex`. API fingerprint: `c89a37475009863aad2b551347b904f8734f51c737331359d45f2aeb3972c6b0`.

<a id="symbol-fd34f906aec051e567e5a474"></a>

## builtin.Console / Color.isOpaque

Identity: `g170-console:Color.isOpaque`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgba(71,83,97,0).c;c.alpha().console();c.withAlpha(29).alpha().console();c.isOpaque().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:alpha-0`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:alpha-0,console:alpha-255`.

Misuse/diagnostic/failure proofs: `console:color-opaque-arity`. API fingerprint: `35897b7fecd3c65387a6af3a99a299613877fbd39c093cf7d5cbca02a52c23cd`.

<a id="symbol-cb3768b59cca5d57f891afdc"></a>

## builtin.Console / Color.parseHex

Identity: `g170-console:Color.parseHex`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.parseHex("").p;p.isOk().console();p.isErr().console();p.unwrapOr(Color.rgb(71,83,97)).toHex().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-parse-error-`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-parsed-(17_29_53_71),console:color-parse-error-#12G456,console:color-parse-error-`.

Misuse/diagnostic/failure proofs: `console:color-result-fallback-type,console:color-expect-error-#12G456`. API fingerprint: `78d7f24a77c4b0523706a954b1af5128adf6954341142d05dbb0ef473687e981`.

<a id="symbol-4392e835a939fa998c614474"></a>

## builtin.Console / Color.red

Identity: `g170-console:Color.red`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgb(17,29,53).c;c.red().console();c.green().console();c.blue().console();c.alpha().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:channels-17`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:channels-17,console:channels-71,console:alpha-113`.

Misuse/diagnostic/failure proofs: `console:channel-arity`. API fingerprint: `0f2505e8d17a49460ef6e909de42e7f27a6cfabee571e0ccb3da9981c4b5ad84`.

<a id="symbol-6331d9978654a910c2bf7489"></a>

## builtin.Console / Color.rgb

Identity: `g170-console:Color.rgb`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgb(17,29,53).red().console();}
```

Explicit process status: `0`; the full value/effect oracle is `console:color-implicit-status`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:channels-17,console:channels-71,console:color-implicit-status`.

Misuse/diagnostic/failure proofs: `console:color-static-channel-high,console:rgb-type,console:rgb-arity,console:rgb-high`. API fingerprint: `423a978a91a8e6aa914a74a1e86e37c159a3203d06271991dbbc097da2b17b70`.

<a id="symbol-240814ee5817b1274e835647"></a>

## builtin.Console / Color.rgba

Identity: `g170-console:Color.rgba`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console(Console.fg(Color.rgba(71,83,97,113)));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:rgba-pixels-113`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:alpha-113,console:rgba-pixels-113`.

Misuse/diagnostic/failure proofs: `console:color-static-channel-low,console:rgba-type,console:rgba-high`. API fingerprint: `f35b2bc7e76c116f7ccb26163e2d41caf35d24dbf7f14dd690ad7aceecdbcc2f`.

<a id="symbol-9949081c2bacb048483a7421"></a>

## builtin.Console / Color.toHex

Identity: `g170-console:Color.toHex`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color(17,29,53,71).c;c.toHex().console();c.toHexWithAlpha().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-sugar-rgba-(17_29_53_71)`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-sugar-rgba-(17_29_53_71),console:color-sugar-rgba-(83_29_53_71),console:color-G087-S08`.

Misuse/diagnostic/failure proofs: `console:color-serialize-arity`. API fingerprint: `29a34e278f0a9c2b7f84e4d7f1139fb3f1db38b741c7edbf3f134439c3158100`.

<a id="symbol-619bd964d8a50be7e2f3aec8"></a>

## builtin.Console / Color.toHexWithAlpha

Identity: `g170-console:Color.toHexWithAlpha`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color(17,29,53,71).c;c.toHex().console();c.toHexWithAlpha().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-sugar-rgba-(17_29_53_71)`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-sugar-rgba-(17_29_53_71),console:color-sugar-rgba-(83_29_53_71),console:color-G087-S08`.

Misuse/diagnostic/failure proofs: `console:color-serialize-arity`. API fingerprint: `e4e974da7e04d8aa8f166c52f085d06712b2e60b653154279485ec361bc52d72`.

<a id="symbol-6608fe8b549fd9baea7103b9"></a>

## builtin.Console / Color.withAlpha

Identity: `g170-console:Color.withAlpha`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.rgba(71,83,97,113).c;c.alpha().console();c.withAlpha(29).alpha().console();c.isOpaque().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:alpha-113`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:alpha-113`.

Misuse/diagnostic/failure proofs: `console:alpha-type,console:color-static-alpha,console:alpha-low`. API fingerprint: `0df7ee3a1ec7bfd82261ca4e8c56dd83bbf2f89b179c20e369e7ec346147981f`.

<a id="symbol-1881a7d0afb7d9abd42300b4"></a>

## builtin.Console / Console.append

Identity: `g170-console:Console.append`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.append(29);23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:append-29`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:append-next,console:append-29,console:append-False,console:styled-append-text,console:styled-append-int,console:styled-append-bool`.

Misuse/diagnostic/failure proofs: `console:append-float,console:append-missing,console:styled-append-type,console:styled-append-arity`. API fingerprint: `b4558583c474bed506a5dfa906ad88e5c3d01c9e134b91490a569a104cd37908`.

<a id="symbol-293e7a2703e4ece6f741c0f2"></a>

## builtin.Console / Console.bg

Identity: `g170-console:Console.bg`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console(Console.bg(Color.rgb(17,29,53)));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:bg-17`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:bg-17,console:bg-71,console:fg-bg-17`.

Misuse/diagnostic/failure proofs: `console:bg-arity,console:duplicate-role`. API fingerprint: `36111fd103b91139bed79c4a67444e06fd7886ac20a00bcbe5858f2bae604ff0`.

<a id="symbol-3658b6079ebba17bea7aad3a"></a>

## builtin.Console / Console.clear

Identity: `g170-console:Console.clear`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.clear();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:clear`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:clear,console:clear-append,console:clear-preserves-title,console:clear-removes-color,console:style-clear`.

Misuse/diagnostic/failure proofs: `console:clear-arity,console:close-clear`. API fingerprint: `cd7f86a5e9b194cd8cf2fe4cb6e46dad9d1e647d6838c8bdbaac562a0447b305`.

<a id="symbol-6005e22d19843a549adb202b"></a>

## builtin.Console / Console.close

Identity: `g170-console:Console.close`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.close();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:close`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:close`.

Misuse/diagnostic/failure proofs: `console:close-arity,console:close-twice,console:close-publish`. API fingerprint: `982837ceea129906844e4bdfdcc42c976da069b6d1d385c914190f35fa721683`.

<a id="symbol-b3964b050df69a147b1daf37"></a>

## builtin.Console / Console.fg

Identity: `g170-console:Console.fg`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console(Console.fg(Color.rgb(17,29,53)));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:fg-17`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:fg-17,console:fg-71,console:fg-bg-17,console:rgba-pixels-113`.

Misuse/diagnostic/failure proofs: `console:fg-type,console:duplicate-role`. API fingerprint: `3e7b8e30526d6b21abdbe40a56d286bcc6ce19a02d93dff4a6ba8ba4796ca88a`.

<a id="symbol-a3ba536c09a0d07e70d2b264"></a>

## builtin.Console / Console.scrollToEnd

Identity: `g170-console:Console.scrollToEnd`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"abc".console().view;view.scrollToEnd();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:scroll-3`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:scroll-3,console:scroll-6,console:scroll-13`.

Misuse/diagnostic/failure proofs: `console:scroll-arity`. API fingerprint: `478f81a1e8d0f2f3fddd752e90d09c81833673359ee83f33d23750ead748decd`.

<a id="symbol-1628c3d621964492d9683890"></a>

## builtin.Console / Console.size

Identity: `g170-console:Console.size`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"abc".console().view;view.size().s;s.at(0).console();s.at(1).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:size-3`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:size-3,console:size-6,console:size-13`.

Misuse/diagnostic/failure proofs: `console:size-arity`. API fingerprint: `24b292fbb6f04b7bd895bff4254ba52fc4f1b14865613d1b097a98f44624a0a7`.

<a id="symbol-e2bb5667cddd587ebda7c1bf"></a>

## builtin.Console / Console.style

Identity: `g170-console:Console.style`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().v;"next".console();v.style(Range.exclusive(0,1),Console.fg(17,29,53));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:style-exclusive-0-1`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:style-exclusive-0-1,console:style-exclusive-1-2,console:style-inclusive-0-1,console:style-dynamic-endpoints,console:style-clear`.

Misuse/diagnostic/failure proofs: `console:style-selection-type,console:style-endpoint-type,console:style-selection-arity,console:style-role-type,console:style-bound--1-1,console:style-bound-0-3,console:style-bound-2-1`. API fingerprint: `5160c494e22917df31df7814cbe2d04f413b7e710c4131f23191a45ea1aafeff`.

<a id="symbol-c4dc35b003e7b6cdf66de395"></a>

## builtin.Console / Console.title

Identity: `g170-console:Console.title`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console().view;view.title("report");23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:title-6-report`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:title-6-report,console:title-13-change,console:title-loop`.

Misuse/diagnostic/failure proofs: `console:title-type,console:title-limit`. API fingerprint: `580c69366882726af49f0ed14b66af0a20f9e6e8c1e9c76d5d039ff3e73e65f3`.

<a id="symbol-3481deba26433a842c786b25"></a>

## builtin.Console / Int.console

Identity: `g170-console:Int.console`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){(17).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-17`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:publish-17,console:publish--29`.

Misuse/diagnostic/failure proofs: `console:bare-color`. API fingerprint: `ea5bae7fda3356d760ee8c2a77839eecfa9564b96188c714fc369d62fcc02d7c`.

<a id="symbol-8750610cff4ad0274ff48823"></a>

## builtin.Console / Object.console

Identity: `g170-console:Object.console`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
struct User {Text.name;Int.age;}start(){User {age:29,name:"Nebo"}.x;x.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:structured-object-Nebo-29`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:structured-object-Nebo-29,console:structured-object-Nebo-71,console:structured-object-two-live,console:structured-object-nested,console:structured-object-padding-Ω,console:structured-object-effects-once`.

Misuse/diagnostic/failure proofs: `console:structured-object-status,console:structured-object-field,console:structured-object-type`. API fingerprint: `61f98432a44dff67c8d86e6beaef1767fbffcf59074cd5d1458ade0839c86d9d`.

<a id="symbol-e95aa1f3100135d76cdb4f9d"></a>

## builtin.Console / Position.at

Identity: `g170-console:Position.at`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"seed".console(Position.at(0,0),Console.fg(17,29,53));23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:position-0-0`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:position-17-29,console:position-71-53,console:position-0-0`.

Misuse/diagnostic/failure proofs: `console:position-arity,console:position-type,console:position-limit--1-29,console:position-limit-256-53`. API fingerprint: `8aa2f1509f3c8b442a39901c2ef2244fcf01fb72670933925a23dd0527beeb39`.

<a id="symbol-fe090def5166e382dcd2b679"></a>

## builtin.Console / Result<Color,ColorParseError>.expect

Identity: `g170-console:Result<Color,ColorParseError>.expect`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.parseHex("#12G456").p;p.isOk().console();p.isErr().console();p.unwrapOr(Color.rgb(71,83,97)).toHex().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-parse-error-#12G456`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-parsed-(17_29_53_71),console:color-parse-error-#12G456`.

Misuse/diagnostic/failure proofs: `console:color-result-fallback-type,console:color-result-expect-type,console:color-expect-error-#12G456`. API fingerprint: `12d7063b61950ed310225e06263e541b4ca82d6896abd02d8010334a72a01998`.

<a id="symbol-29dd808963854f5e041d9282"></a>

## builtin.Console / Result<Color,ColorParseError>.isErr

Identity: `g170-console:Result<Color,ColorParseError>.isErr`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.parseHex("#12G456").p;p.isOk().console();p.isErr().console();p.unwrapOr(Color.rgb(71,83,97)).toHex().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-parse-error-#12G456`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-parsed-(17_29_53_71),console:color-parse-error-#12G456`.

Misuse/diagnostic/failure proofs: `console:color-result-fallback-type,console:color-result-expect-type,console:color-expect-error-#12G456`. API fingerprint: `4eb46fb07a02d019ac766976ad2459c11fbd8af2c0eb8e209db17607af12651f`.

<a id="symbol-d58a433b1f28ed1c4d1fd692"></a>

## builtin.Console / Result<Color,ColorParseError>.isOk

Identity: `g170-console:Result<Color,ColorParseError>.isOk`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.parseHex("#12G456").p;p.isOk().console();p.isErr().console();p.unwrapOr(Color.rgb(71,83,97)).toHex().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-parse-error-#12G456`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-parsed-(17_29_53_71),console:color-parse-error-#12G456`.

Misuse/diagnostic/failure proofs: `console:color-result-fallback-type,console:color-result-expect-type,console:color-expect-error-#12G456`. API fingerprint: `f50c782914efdafd43909712942bc250b390fc7a2b253ca6dcd74874c8ff297b`.

<a id="symbol-36da89477b44b38d7ff9ee52"></a>

## builtin.Console / Result<Color,ColorParseError>.unwrapOr

Identity: `g170-console:Result<Color,ColorParseError>.unwrapOr`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Color.parseHex("#12G456").p;p.isOk().console();p.isErr().console();p.unwrapOr(Color.rgb(71,83,97)).toHex().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-parse-error-#12G456`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-parsed-(17_29_53_71),console:color-parse-error-#12G456`.

Misuse/diagnostic/failure proofs: `console:color-result-fallback-type,console:color-result-expect-type,console:color-expect-error-#12G456`. API fingerprint: `31f685656994d332d132f1419df37247131cbca3ec412252b347e4ede88b9c58`.

<a id="symbol-655a02bd12fe6af1e3e3c31e"></a>

## builtin.Console / Table.console

Identity: `g170-console:Table.console`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Table.fromColumns([Tuple.of("index",Column<Int>.from([]))]).t;t.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:structured-table-rows-0`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:structured-table-rows-0,console:structured-table-rows-32,console:structured-table-eight-columns,console:structured-table-two-live,console:structured-table-missing,console:structured-table-sorted`.

Misuse/diagnostic/failure proofs: `console:structured-table-column,console:structured-view-count-limit`. API fingerprint: `b5ecc625c68f859e4ee10ef16c84185ad007ac687fb7a7c286889a81a9ef6bdd`.

<a id="symbol-5f5fc6d461fddf2436f09c17"></a>

## builtin.Console / Text.console

Identity: `g170-console:Text.console`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){("seed").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:publish-seed`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:publish-seed,console:publish-changed,console:publish-Olá €,console:software-wrap-29`.

Misuse/diagnostic/failure proofs: `console:bare-color`. API fingerprint: `4946cc3d33bbec11e4059b8b76a02322b631d8c33fdc003c356f234f18f3c263`.

<a id="symbol-0a4b05e4865f886b5f9d5ec0"></a>

## builtin.Console / Text.hash

Identity: `g170-console:Text.hash`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"".hash().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:color-text-hash-0`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:color-text-hash-0,console:color-text-hash-9#6D2A,console:color-text-hash-2Ω雪`.

Misuse/diagnostic/failure proofs: `console:color-text-hash-limit`. API fingerprint: `f634cfd0be526df5123f7bd1a2634c694c351785ba5cd70a79abdcfa8f9e9c43`.

<a id="symbol-edd70a69897f39d9045601f8"></a>

## builtin.Console / Tree.console

Identity: `g170-console:Tree.console`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){Tree<Int>.new(17).t;t.removeSubtree(t.root());t.console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:structured-tree-empty`.

### Types, ownership, effects, limits and errors

Retained source values plus opt-in pointer-free document/RenderTree/software observation; 256x128 BGRA8 synthetic glyphs; linear document 4096 bytes and title 256 bytes; size is UTF-8 bytes/lines; style selects zero-based payload nodes; Position is logical software pixels; typed Color RGBA8 and native Result; structured Table<Int> 32x8, Tree<Int> 32 nodes/depth16, named products 8 fields/declarations; 4096 rendered bytes and 64 structured publications. Every effect executes once; no window/backend capability inferred. See G170 checklist for narrower method bounds; metadata joins remain in F01. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:structured-tree-empty,console:structured-tree-two-live,console:structured-tree-removed,console:structured-tree-depth-16,console:structured-tree-32-nodes`.

Misuse/diagnostic/failure proofs: `console:structured-tree-value,console:structured-tree-depth-limit`. API fingerprint: `8d749b968d25ced48f8fdd6fd807be71db0fa02006660531707aa1006ec2c991`.

<a id="symbol-a522d475eb1b0290542ef136"></a>

## builtin.Console / visual.summarize

Identity: `g170-console:visual.summarize`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `TYPED_NATIVE_VISUAL_VALUE_AND_SOFTWARE_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){visual.summarize(0).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `console:visual-summary-0-23`.

### Types, ownership, effects, limits and errors

Canonical existing pure visual_summary_foundation owner, actual Int value 0..255, decimal provenance, one evaluation and independent explicit return; no document/window/rendering effect is claimed. Retained document/headless software profile; exact nodes and bytes; no live desktop claim

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `console:visual-summary-0-23,console:visual-summary-83-23,console:visual-summary-255-255,console:visual-summary-dynamic,console:visual-summary-once,console:visual-summary-two-values`.

Misuse/diagnostic/failure proofs: `console:visual-summary-range--1,console:visual-summary-range-256,console:visual-summary-type,console:visual-summary-arity,console:visual-summary-shadow`. API fingerprint: `59d5c35560bb932deafd4d7754f2ce1ed201462b00a1b06fdb79b86b00e3df21`.

<a id="symbol-d280a7afaf138a03c91196b5"></a>

## builtin.ScanPlan / ScanPlan.allowEmpty

Identity: `g170-scan:allowEmpty`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.allowEmpty(),.mock("")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:capacity-0`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:capacity-0,scan:capacity-4096`.

Misuse/diagnostic/failure proofs: `scan:too-long,scan:option-arity-allowEmpty`. API fingerprint: `276d5794e7defd6791c29abb4754c7a393e10df292aac68c04aaad35a25c7b3e`.

<a id="symbol-4eed5f7b6e2e3f40dbbecd12"></a>

## builtin.ScanPlan / ScanPlan.autocomplete

Identity: `g170-scan:autocomplete`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.autocomplete(["ação","Nebo"]),.mock("a")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:autocomplete-a-ação`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:autocomplete-Port-Portugal,scan:autocomplete-Port-Portuguese,scan:autocomplete-a-ação`.

Misuse/diagnostic/failure proofs: `scan:autocomplete-ambiguous,scan:autocomplete-absent,scan:choice-type-autocomplete`. API fingerprint: `0d1608a2d654babce69dc5302486c46ae9af35e70ceb3a739ab4b416f4463fe5`.

<a id="symbol-a59b9b781698924a97c2b63d"></a>

## builtin.ScanPlan / ScanPlan.block

Identity: `g170-scan:block`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.block("END"),.replay("answers.txt")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-block-replay`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-block-replay,scan:block-preserves-next-stdin,scan:multiline-trace-block`.

Misuse/diagnostic/failure proofs: `scan:multiline-missing-terminator,scan:multiline-type-block`. API fingerprint: `f4f740b754e05b96133334544b5811f97a544585195d369f84aa73dc186f1386`.

<a id="symbol-661156a5c3f607ff8479067f"></a>

## builtin.ScanPlan / ScanPlan.bool

Identity: `g170-scan:bool`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Ready: ".scan(.bool(),.mock("true")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:bool-True`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:bool-True,scan:bool-False`.

Misuse/diagnostic/failure proofs: `scan:bool-invalid,scan:option-arity-bool`. API fingerprint: `6a1a82bf5f197cb21858d9e7aefaf14ccc0e4b0b833e2274a014676f604cb4b0`.

<a id="symbol-18cf1afc6b2430617b19fa6e"></a>

## builtin.ScanPlan / ScanPlan.cancelOn

Identity: `g170-scan:cancelOn`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.int(),.cancelOn("cancel"),.result(),.mock("cancel")).isErr().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:cancel-result-cancel`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:cancel-result-cancel,scan:console-cancel-event`.

Misuse/diagnostic/failure proofs: `scan:option-type-cancelOn`. API fingerprint: `00ca8bf2298cfb987e6962507902791ce5e354ea3467d0fcc3ef66ccbd898495`.

<a id="symbol-baa4c7e795571f7dc06da886"></a>

## builtin.ScanPlan / ScanPlan.char

Identity: `g170-scan:char`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Key: ".scan(.char(),.mock("x")).codepoint().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:char-120`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:char-81,scan:char-120`.

Misuse/diagnostic/failure proofs: `scan:char-utf8,scan:char-empty,scan:option-arity-char`. API fingerprint: `b830dc408923f236f061a281d3a5cbc7357d39b5afd755d2cd0c084b6a557287`.

<a id="symbol-f46e5c699e113f52a726c8bf"></a>

## builtin.ScanPlan / ScanPlan.choice

Identity: `g170-scan:choice`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.choice(["red","green","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-3-0-726564`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-3-0-726564,scan:choice-arguments-once`.

Misuse/diagnostic/failure proofs: `scan:choice-absent,scan:choice-type`. API fingerprint: `f81a1c923703638b40df2a26a402d9d24fe7e4422ec99b850152776cd804db8a`.

<a id="symbol-6458f0a11163acce02e5ebb4"></a>

## builtin.ScanPlan / ScanPlan.chomp

Identity: `g170-scan:chomp`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.chomp(),.mock("Nebo\r\n")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:explicit-chomp`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:explicit-chomp`.

Misuse/diagnostic/failure proofs: `scan:option-arity-chomp`. API fingerprint: `85a5ad692c0c52a8f167ed6bab550a832361582dbc3164b312bc1fde435390bc`.

<a id="symbol-5fe63297e9b9f31dac4ec7ca"></a>

## builtin.ScanPlan / ScanPlan.code

Identity: `g170-scan:code`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.code("nebo"),.replay("answers.txt")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-code-replay`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-code-replay,scan:multiline-trace-code-nebo,scan:multiline-trace-code-other`.

Misuse/diagnostic/failure proofs: `scan:multiline-type-code`. API fingerprint: `d39b72f0bf502043b6f0099b8ab228decdf3a5fa6ea0f7e8f13c9f2a583de6b1`.

<a id="symbol-ba17dbb89c91e0268d1c32c3"></a>

## builtin.ScanPlan / ScanPlan.confirm

Identity: `g170-scan:confirm`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.confirm(),.mock("no")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-confirm-no`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-confirm-yes,scan:choice-confirm-no`.

Misuse/diagnostic/failure proofs: `scan:choice-confirm-invalid`. API fingerprint: `bfcfe656560b7bc292466b3208fd04c6ba92c3cd0da7525a5393652365ef9d4b`.

<a id="symbol-bf3cbb036ad14be75a9267cc"></a>

## builtin.ScanPlan / ScanPlan.confirmSecret

Identity: `g170-scan:confirmSecret`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.console(),.secret(),.confirmSecret(),.result(),.replay("answers.txt")).isOk().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:confirm-trace-Different9!`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:confirm-trace-Synthetic7!,scan:confirm-trace-Different9!,scan:confirm-retry-pair`.

Misuse/diagnostic/failure proofs: `scan:confirm-eof-result,scan:confirm-binding-failure,scan:confirm-needs-secret,scan:security-arity-confirmSecret`. API fingerprint: `862c6f7f3a2799d94503e42dc54dbf877d83ea34a29c2c087fb6e84f0a1f55a5`.

<a id="symbol-67ee345c0dbb50298fa298a1"></a>

## builtin.ScanPlan / ScanPlan.console

Identity: `g170-scan:console`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.console(),.mock("!accepted")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-adapter-216163636570746564`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-adapter-216163636570746564,scan:console-retry-events,scan:console-eof-event,scan:console-cancel-event`.

Misuse/diagnostic/failure proofs: `scan:field-arity,scan:option-arity-console`. API fingerprint: `89282cdf8e2b65971da05812ea006f75ba6aff9f961d290037c078847155551a`.

<a id="symbol-26359e25e58903e29480db1f"></a>

## builtin.ScanPlan / ScanPlan.enum

Identity: `g170-scan:enum`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.enum(["red","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-variant-enum-red`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-variant-enum-red,scan:choice-variant-enum-blue`.

Misuse/diagnostic/failure proofs: `scan:choice-variant-enum-absent,scan:choice-type-enum`. API fingerprint: `a8b3c6c4a2a7e2639117cbe70b34192a9b0fa36e985b3c23dedf3378e4983e82`.

<a id="symbol-38ec76fb539b885063554914"></a>

## builtin.ScanPlan / ScanPlan.eofAs

Identity: `g170-scan:eofAs`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.int(),.eofAs(71)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:eof-fallback-int`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:eof-fallback-int,scan:eof-fallback-text,scan:eof-fallback-char,scan:eof-fallback-bool`.

Misuse/diagnostic/failure proofs: `scan:eof-fallback-type`. API fingerprint: `206eb4351f8f3e07eb3da56379999a69cd5b76d06adf8bd63b26bfaf1d32ba4a`.

<a id="symbol-bbe29e14b9f4bce9c1d99141"></a>

## builtin.ScanPlan / ScanPlan.eofAsNone

Identity: `g170-scan:eofAsNone`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.int(),.eofAsNone()).isSome().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:eof-option-eof`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:eof-option-eof,scan:eof-option-value-value`.

Misuse/diagnostic/failure proofs: `scan:eof-option-empty-is-invalid,scan:eof-policy-conflict,scan:option-arity-eofAsNone`. API fingerprint: `2f9b7bb604783a9e0637374a313a3685f2895f6ead0f1556d637af97b279350b`.

<a id="symbol-b3d4d0c5717bcba03862b9d0"></a>

## builtin.ScanPlan / ScanPlan.feedback

Identity: `g170-scan:feedback`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.feedback("inline"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-feedback-inline`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-retry-events,scan:console-feedback-inline,scan:console-feedback-summary`.

Misuse/diagnostic/failure proofs: `scan:metadata-type,scan:option-arity-feedback`. API fingerprint: `8202e423d9f34735df287b1f888b67c3452004ef1db199987bb10bbe9dd3a138`.

<a id="symbol-49c1da140cb91a2150219324"></a>

## builtin.ScanPlan / ScanPlan.field

Identity: `g170-scan:field`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.field("field0","Int"),.field("field1","Int"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-fields-2`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings,scan:console-fields-2,scan:console-fields-29`.

Misuse/diagnostic/failure proofs: `scan:field-key-type,scan:field-value-type,scan:field-arity`. API fingerprint: `20c71b8b29595c6ab80adaeb84918056362894652086e76b8a9e2cb2a5d3488f`.

<a id="symbol-fcfb6580743cbc9de40dde48"></a>

## builtin.ScanPlan / ScanPlan.forgetAfterUse

Identity: `g170-scan:forgetAfterUse`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.console(),.secret(),.forgetAfterUse(),.mock("Synthetic7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:forget-trace-11-S`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:forget-trace-11-S,scan:forget-trace-4096-a,scan:forget-confirm-True,scan:forget-confirm-False,scan:forget-two-live-values,scan:forget-retry-preserves-tail`.

Misuse/diagnostic/failure proofs: `scan:forget-needs-secret,scan:security-arity-forgetAfterUse`. API fingerprint: `cdaa0606c977097e412d96f7ff0b2a38d6eaea75810aea1772f9ac3308684927`.

<a id="symbol-fc6a1eae11c6deae1278b277"></a>

## builtin.ScanPlan / ScanPlan.form

Identity: `g170-scan:form`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.form("profile"),.field("age","Int"),.view("entry"),.panel("main"),.feedback(),.style("blue"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-metadata-profile`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings`.

Misuse/diagnostic/failure proofs: `scan:option-type-form`. API fingerprint: `62333af7820fa646377e780c4d8a42966a628aca89513222ad0e16d380438e9e`.

<a id="symbol-b77957b906ae1b4d62eb740d"></a>

## builtin.ScanPlan / ScanPlan.indexChoice

Identity: `g170-scan:indexChoice`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.indexChoice(["red","green","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:index-3-0-726564`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:index-3-0-726564,scan:index-3-2-626c7565`.

Misuse/diagnostic/failure proofs: `scan:choice-arity`. API fingerprint: `eb075aad74a6efb2d409e49a14c30f54d3c3bcd2dfed02946fcd67506e47c950`.

<a id="symbol-34ac76f13e52fd72d0dbcafd"></a>

## builtin.ScanPlan / ScanPlan.int

Identity: `g170-scan:int`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("7")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-7-return-23`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:int-7-return-23,scan:int--9223372036854775808-return-23,scan:int-9223372036854775807-return-23`.

Misuse/diagnostic/failure proofs: `scan:int-overflow,scan:int-invalid`. API fingerprint: `2e4afa2a3f2a32285f678d39e6eba8230f0c10f1ee28ceb74cb3b3e1ee4ae1f7`.

<a id="symbol-0438d14b7f41bc36a893417e"></a>

## builtin.ScanPlan / ScanPlan.jsonBlock

Identity: `g170-scan:jsonBlock`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.jsonBlock()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-json-stdin`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-json-mock,scan:multiline-json-stdin,scan:multiline-trace-json`.

Misuse/diagnostic/failure proofs: `scan:multiline-bad-json,scan:multiline-duplicate-json`. API fingerprint: `ab4959c1f558bfe3b403ea88cb67743255487f09eed508d351effa56a787a272`.

<a id="symbol-8933c17ec4ad13e937f395f3"></a>

## builtin.ScanPlan / ScanPlan.lower

Identity: `g170-scan:lower`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.lower(),.mock("HeLLo")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:lower-48654c4c6f`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:lower-48654c4c6f`.

Misuse/diagnostic/failure proofs: `scan:case-conflict,scan:option-arity-lower`. API fingerprint: `6b8c0868cf8da12fc1fe2b68b30980e45a0c9bd20cf9946a6aa88fb7c1880549`.

<a id="symbol-d241e8045d64984fefc5ba0d"></a>

## builtin.ScanPlan / ScanPlan.markdown

Identity: `g170-scan:markdown`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.markdown()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-markdown-stdin`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-markdown-mock,scan:multiline-markdown-stdin,scan:multiline-trace-markdown`.

Misuse/diagnostic/failure proofs: `scan:multiline-type-conflict`. API fingerprint: `b10e9e8c21206ab5c4c3c7b8151d32331b77c9e3298437fc47dbb2fdb82518ff`.

<a id="symbol-0eb9b543cf7f05fd003162e3"></a>

## builtin.ScanPlan / ScanPlan.mask

Identity: `g170-scan:mask`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.secret(),.mask("*"),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-605-1-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-605-1-4,scan:security-605-1-8`.

Misuse/diagnostic/failure proofs: `scan:option-type-mask`. API fingerprint: `ded875209a635be35a103275eea1e25943a4a203dd15839952958e56e8d948fa`.

<a id="symbol-f67cc22c07e59891b8aa72ca"></a>

## builtin.ScanPlan / ScanPlan.max

Identity: `g170-scan:max`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.min(17),.max(83),.mock("17")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:range-17`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:range-17,scan:range-83`.

Misuse/diagnostic/failure proofs: `scan:upper-bound`. API fingerprint: `cd64aaf320973190c2f130db7a10aae3b861cdce3ad58a6452127381408bda8b`.

<a id="symbol-e0c3c38fdf7f72cb2a7c3e3e"></a>

## builtin.ScanPlan / ScanPlan.maxAttempts

Identity: `g170-scan:maxAttempts`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.replay("answers.txt"),.retry(),.maxAttempts(1)).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:replay-retry-29-1`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:replay-retry-29-1,scan:replay-retry-29-8`.

Misuse/diagnostic/failure proofs: `scan:attempts-zero,scan:attempts-nine`. API fingerprint: `daa66ea2bc31aaa58def038ef4cb9035e8ffe50bc81e4738d519778b4e00f530`.

<a id="symbol-2986e2863b1495d689869149"></a>

## builtin.ScanPlan / ScanPlan.maxLen

Identity: `g170-scan:maxLen`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.maxLen(2),.mock("ab")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:maxlen-exact`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:maxlen-exact`.

Misuse/diagnostic/failure proofs: `scan:maximum-length,scan:option-type-maxLen`. API fingerprint: `8f54ea0696817dd3318f6b6aea93a3d770dca7cd2678e23d0c1204d9db05bd13`.

<a id="symbol-8eeca3eaf21b9b50f5f19332"></a>

## builtin.ScanPlan / ScanPlan.maxLines

Identity: `g170-scan:maxLines`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.multiline(),.maxLines(1),.mock("line")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-line-limit-1`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-line-limit-1,scan:multiline-line-limit-256`.

Misuse/diagnostic/failure proofs: `scan:multiline-maxlines-zero,scan:multiline-maxlines-over`. API fingerprint: `91238684132415161cae98e5a0c4608584a1f0b66b90d0ecd0c155366e680f9e`.

<a id="symbol-ce50bfe174380d915f79bbd1"></a>

## builtin.ScanPlan / ScanPlan.menu

Identity: `g170-scan:menu`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.menu(["red","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-variant-menu-red`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-variant-menu-red,scan:choice-variant-menu-blue`.

Misuse/diagnostic/failure proofs: `scan:choice-variant-menu-absent,scan:choice-type-menu`. API fingerprint: `94b090ddfff8a4b9d74eb87b6486b90f68c4f62ec643330f4fa6809d72806a4e`.

<a id="symbol-13f6ccc8f95395711472a8ac"></a>

## builtin.ScanPlan / ScanPlan.min

Identity: `g170-scan:min`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.min(17),.max(83),.mock("17")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:range-17`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:range-17,scan:range-83`.

Misuse/diagnostic/failure proofs: `scan:lower-bound`. API fingerprint: `700105553496cfdd716cc09f5aba00ae5eac8dc1c2080195b63da57df1f8f55f`.

<a id="symbol-1306f9455e3785bc45727df3"></a>

## builtin.ScanPlan / ScanPlan.minLen

Identity: `g170-scan:minLen`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.minLen(2),.mock("ab")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:minlen-exact`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:minlen-exact`.

Misuse/diagnostic/failure proofs: `scan:minimum-length,scan:option-type-minLen`. API fingerprint: `01639140a07605ec7997c43ef74f9c125eaf3e8bfddaef31518b02d8aea2613c`.

<a id="symbol-56d13cf12b46bb5100c9b006"></a>

## builtin.ScanPlan / ScanPlan.minStrength

Identity: `g170-scan:minStrength`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.secret(),.minStrength(4),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-93-4-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-93-4-4,scan:security-93-4-8`.

Misuse/diagnostic/failure proofs: `scan:option-type-minStrength`. API fingerprint: `daf0134b2ca6624b9eb5271f407f20fd57608791e3d78af7d6087a604a45cef0`.

<a id="symbol-ebf1713b5b1d7d5dd48ab14f"></a>

## builtin.ScanPlan / ScanPlan.mock

Identity: `g170-scan:mock`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("23")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-23-return-23`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:int-23-return-23,scan:two-int-values,scan:two-text-values`.

Misuse/diagnostic/failure proofs: `scan:duplicate-mock,scan:mock-type`. API fingerprint: `0c34f16af6bab6075e8314c10c471bef0b8961c6855d0b4dbf95048bf1bb18aa`.

<a id="symbol-e8837ff73fccf630942ae24c"></a>

## builtin.ScanPlan / ScanPlan.multiSelect

Identity: `g170-scan:multiSelect`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.multiSelect(["red","green","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multi-select-3-red`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multi-select-3-red,scan:multi-select-3-blue`.

Misuse/diagnostic/failure proofs: `scan:multi-select-duplicate,scan:multi-select-absent,scan:choice-type-multiSelect`. API fingerprint: `a0449026d5b5e0565911d393aaf5d15419a0fb7c88fd3470426e8282933ae1a0`.

<a id="symbol-196f060009b30f0ecbdad71a"></a>

## builtin.ScanPlan / ScanPlan.multiline

Identity: `g170-scan:multiline`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.multiline()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-multiline-stdin`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-multiline-mock,scan:multiline-multiline-replay,scan:multiline-multiline-stdin,scan:multiline-trace-multiline`.

Misuse/diagnostic/failure proofs: `scan:multiline-type-conflict,scan:multiline-too-many-lines`. API fingerprint: `339515ff46117e8bfa44204c173fa271a6d7e86daacd67d6c69536246cdd804e`.

<a id="symbol-0ff2e1ff410ac27808cca427"></a>

## builtin.ScanPlan / ScanPlan.noEcho

Identity: `g170-scan:noEcho`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.noEcho(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-16-0-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-16-0-4,scan:security-16-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-noEcho`. API fingerprint: `d07e966420fe36a0dc35110e7c289c804ee2d2089000137779ba05f71f246df5`.

<a id="symbol-2f2c95f2e35db1300a209fe5"></a>

## builtin.ScanPlan / ScanPlan.noHistory

Identity: `g170-scan:noHistory`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.noHistory(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-8-0-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-8-0-4,scan:security-8-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-noHistory`. API fingerprint: `24c6cb48fe8e68c0e5b3dcbeb711fce9dd107f27fb190c16b06b70f5c0296da9`.

<a id="symbol-2c761ad0c956c49a68b49f52"></a>

## builtin.ScanPlan / ScanPlan.onCancel

Identity: `g170-scan:onCancel`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.int(),.cancelOn("17"),.onCancel(83),.mock("17")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:cancel-fallback-17`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:cancel-fallback-cancel,scan:cancel-fallback-17`.

Misuse/diagnostic/failure proofs: `scan:cancel-fallback-type`. API fingerprint: `0ac8cde05c95872436b70bbef6e70dbc1bee90856dfe1f6ed87f6188ce865860`.

<a id="symbol-c17cc55b9d90e5858a37d01d"></a>

## builtin.ScanPlan / ScanPlan.oneOf

Identity: `g170-scan:oneOf`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.oneOf(["red","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-variant-oneOf-red`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-variant-oneOf-red,scan:choice-variant-oneOf-blue`.

Misuse/diagnostic/failure proofs: `scan:choice-variant-oneOf-absent,scan:choice-type-oneOf`. API fingerprint: `d801b8213b30dfc1188d515a45aa8447b2f3a7935c2454f1cd77633f3930e6db`.

<a id="symbol-b7ed416f3664b7024cebe3a7"></a>

## builtin.ScanPlan / ScanPlan.optional

Identity: `g170-scan:optional`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.optional(),.mock("")).isNone().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:none-empty`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:optional-empty,scan:optional-17,scan:some-17,scan:none-empty`.

Misuse/diagnostic/failure proofs: `scan:optional-type,scan:option-arity-optional`. API fingerprint: `aa2b3d38e11173ebe93a112cb9b13ec66e3e047f769fc36e32552668938db38d`.

<a id="symbol-413208289d7b69120a9ccee6"></a>

## builtin.ScanPlan / ScanPlan.panel

Identity: `g170-scan:panel`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.form("profile"),.field("age","Int"),.view("entry"),.panel("main"),.feedback(),.style("blue"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-metadata-profile`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings`.

Misuse/diagnostic/failure proofs: `scan:option-type-panel`. API fingerprint: `0a771562a306c68426dfbfc18e16effe5a9bd4f0c837fb06e1efee9e876adc7d`.

<a id="symbol-587bf53150af0870e5e70f87"></a>

## builtin.ScanPlan / ScanPlan.paragraph

Identity: `g170-scan:paragraph`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.paragraph()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-paragraph-stdin`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-paragraph-mock,scan:multiline-paragraph-stdin,scan:multiline-trace-paragraph`.

Misuse/diagnostic/failure proofs: `scan:multiline-mode-conflict`. API fingerprint: `8dc9d6f3f239273632f21544a65ea6856a2a3fca802b2c10ab55240322f14726`.

<a id="symbol-dedb259d2078ecef47e2960a"></a>

## builtin.ScanPlan / ScanPlan.password

Identity: `g170-scan:password`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.password(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-95-1-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-95-1-4,scan:security-95-1-8`.

Misuse/diagnostic/failure proofs: `scan:weak-secret,scan:option-arity-password`. API fingerprint: `6508f11abf6aed57274ef71417805f088013b3defef8781454cc9570283a5998`.

<a id="symbol-f4f519f39f6dda1ed23187cf"></a>

## builtin.ScanPlan / ScanPlan.rawBlock

Identity: `g170-scan:rawBlock`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.rawBlock()).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-raw-stdin`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-raw-mock,scan:multiline-raw-stdin,scan:multiline-trace-raw`.

Misuse/diagnostic/failure proofs: `scan:multiline-mode-conflict`. API fingerprint: `bed4b454b5d63789b3a0dc4a0f58dddf05f8034280ff3e79af2d2e21f2e2a7aa`.

<a id="symbol-e4c76481540868cd3f68a13e"></a>

## builtin.ScanPlan / ScanPlan.redact

Identity: `g170-scan:redact`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.redact(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-4-0-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-4-0-4,scan:security-4-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-redact`. API fingerprint: `6c1e5da79d7b3edd91d554881af3fc10adca86ebaefa548ae5a7418964c4ebf3`.

<a id="symbol-f3430da48791727b211312c4"></a>

## builtin.ScanPlan / ScanPlan.replay

Identity: `g170-scan:replay`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.replay("answers.txt")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:replay-no-newline-17`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:replay-retry-29-2,scan:replay-relative-subdir,scan:replay-no-newline-17`.

Misuse/diagnostic/failure proofs: `scan:replay-missing,scan:replay-limit,scan:replay-parent,scan:replay-absolute`. API fingerprint: `cd869ee8c3d6d9ac81550cdf17f2d624f1914e9c1b6b8d03e7ad32a9af0391f2`.

<a id="symbol-d7837459f16a50943d90090f"></a>

## builtin.ScanPlan / ScanPlan.required

Identity: `g170-scan:required`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.required(),.result(),.mock("Nebo")).unwrapOr("fallback").console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:result-value-text-True`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:result-value-text-True`.

Misuse/diagnostic/failure proofs: `scan:required-empty,scan:option-arity-required`. API fingerprint: `d4979dc52820fec44498287076f3691b904e046ce7cb0c67aec642a54f7c061d`.

<a id="symbol-8b7c741c34b57e3a5aa98e58"></a>

## builtin.ScanPlan / ScanPlan.result

Identity: `g170-scan:result`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.int(),.result(),.mock("17")).unwrapOr(71).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:result-value-int-True`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:result-value-int-True,scan:result-value-int-False,scan:result-value-text-True,scan:two-results-live,scan:result-retry-success,scan:result-retry-failure`.

Misuse/diagnostic/failure proofs: `scan:result-optional-conflict,scan:option-arity-result`. API fingerprint: `fd1a7dbb94c85f64146dc6f247bbeab330cfef303134884d8b9c276cdcd1672f`.

<a id="symbol-48a275d59fa0eb21dd269411"></a>

## builtin.ScanPlan / ScanPlan.retry

Identity: `g170-scan:retry`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.int(),.retry(),.replay("answers.txt")).return;}
```

Explicit process status: `29`; the full value/effect oracle is `scan:retry-default-eighth`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:replay-retry-29-2,scan:retry-default-eighth,scan:stdin-retry-83-8,scan:retry-message-416761696e3a20,scan:retry-message-526574727920cea93a20`.

Misuse/diagnostic/failure proofs: `scan:retry-exhausted,scan:retry-default-exhausted,scan:option-arity-retry`. API fingerprint: `f0e3d6ea523393bcc53913ced90a4c627cc52ba2e1cc9b6aab2180ff32a7156b`.

<a id="symbol-3feee0daa1b011f41ff0457c"></a>

## builtin.ScanPlan / ScanPlan.scan

Identity: `g170-scan:scan`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age: ".scan(.int(),.mock("23")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:int-23-return-23`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:int-23-return-23,scan:text-4e65626f,scan:bool-False,scan:char-81,scan:two-results-live`.

Misuse/diagnostic/failure proofs: `scan:invalid-process-type,scan:unknown`. API fingerprint: `7386b406bfc94f6b523c032951cb5604d788fd338553e09c72016051b599c522`.

<a id="symbol-f2ef32623c1bdd59d4cc6b6b"></a>

## builtin.ScanPlan / ScanPlan.secret

Identity: `g170-scan:secret`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.secret(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-93-1-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-93-1-4,scan:security-93-1-8`.

Misuse/diagnostic/failure proofs: `scan:weak-secret,scan:secret-arity,scan:option-arity-secret`. API fingerprint: `4decd72158b0c206ccd1b2ed158afa755b2d56e55ff532aed33c20f1b18196c8`.

<a id="symbol-07dac9fd8a9c2ed823fda59c"></a>

## builtin.ScanPlan / ScanPlan.select

Identity: `g170-scan:select`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.console(),.select(["red","blue"]),.mock("red")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-variant-select-red`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-variant-select-red,scan:choice-variant-select-blue`.

Misuse/diagnostic/failure proofs: `scan:choice-variant-select-absent,scan:choice-type-select`. API fingerprint: `3c8e9effc6d88669acb25b81612b1962c590c43a3b6a89dafa719737b2aef41a`.

<a id="symbol-a6a26f6da1ae56f703191257"></a>

## builtin.ScanPlan / ScanPlan.sensitive

Identity: `g170-scan:sensitive`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.sensitive(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-64-0-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-64-0-4,scan:security-64-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-sensitive`. API fingerprint: `a6bd45634cb812470b2b6d58ecec5b9dfaaff78a336aab8e3351d84fdefac1ec`.

<a id="symbol-d528208992d9ee835bf600b2"></a>

## builtin.ScanPlan / ScanPlan.style

Identity: `g170-scan:style`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.form("profile"),.field("age","Int"),.view("entry"),.panel("main"),.feedback(),.style("blue"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-metadata-profile`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings`.

Misuse/diagnostic/failure proofs: `scan:option-type-style`. API fingerprint: `545800cb2750d164ab07ad76702d52b19de9a1cf9985a1e0c146e2d448eebd1a`.

<a id="symbol-92a414058eb8de86aadcdb0c"></a>

## builtin.ScanPlan / ScanPlan.text

Identity: `g170-scan:text`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Name: ".scan(.text(),.mock("Nebo")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:text-4e65626f`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:text-4e65626f,scan:text-61c3a7c3a36f20cea9`.

Misuse/diagnostic/failure proofs: `scan:mock-type,scan:option-arity-text`. API fingerprint: `5913e26549654f026a85f288d5840e2238b6a379f1d7c0a8285180e4e0947645`.

<a id="symbol-f11ee74df293ec330cbcfe51"></a>

## builtin.ScanPlan / ScanPlan.trim

Identity: `g170-scan:trim`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.trim(),.mock(" Nebo ")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:trim-204e65626f20`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:trim-204e65626f20,scan:trim-0948654c4c6f09`.

Misuse/diagnostic/failure proofs: `scan:mock-type,scan:option-arity-trim`. API fingerprint: `7a27a4194d43ea2eebb46d056d6f8fedd3d704de3108f4f7c6be44394c706b6f`.

<a id="symbol-f78507e58cfa3370ce7595e7"></a>

## builtin.ScanPlan / ScanPlan.until

Identity: `g170-scan:until`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.multiline(),.until("STOP")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:multiline-until-stdin`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:multiline-until-mock,scan:multiline-until-replay,scan:multiline-until-stdin`.

Misuse/diagnostic/failure proofs: `scan:multiline-empty-delimiter,scan:multiline-type-until`. API fingerprint: `d984cd32e106d5be30a37125cc156deacca4e543d079d02ccc7ad067759280ef`.

<a id="symbol-1a512e62b3ee18ff2f65167a"></a>

## builtin.ScanPlan / ScanPlan.untrusted

Identity: `g170-scan:untrusted`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Synthetic input".scan(.text(),.console(),.untrusted(),.mock("Aa7!")).byteLength().console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:security-256-0-4`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:security-256-0-4,scan:security-256-0-8`.

Misuse/diagnostic/failure proofs: `scan:secret-arity,scan:option-arity-untrusted`. API fingerprint: `c322572ff399531e637fa80dbc1b5ed2cfd836df05e6ca5f424636f3b647bf7d`.

<a id="symbol-f0681f4bdffe68b3c40c0069"></a>

## builtin.ScanPlan / ScanPlan.upper

Identity: `g170-scan:upper`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Input".scan(.text(),.upper(),.mock("HeLLo")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:upper-48654c4c6f`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:upper-48654c4c6f`.

Misuse/diagnostic/failure proofs: `scan:case-conflict,scan:option-arity-upper`. API fingerprint: `b603ba51323f6914f7b4f31d2b57b440408dae059d50b41758f1cb6e70fed48e`.

<a id="symbol-e29018ff5393bb2bc83b965c"></a>

## builtin.ScanPlan / ScanPlan.view

Identity: `g170-scan:view`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Age".scan(.int(),.console(),.form("profile"),.field("age","Int"),.view("entry"),.panel("main"),.feedback(),.style("blue"),.mock("29")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:console-metadata-profile`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:console-metadata-profile,scan:console-metadata-settings`.

Misuse/diagnostic/failure proofs: `scan:option-type-view`. API fingerprint: `1efe784d02a16e12eeb2022357afb9ee34788d7078aa531ca834ee5607a9c1ff`.

<a id="symbol-6c95ad57ed04726111deb244"></a>

## builtin.ScanPlan / ScanPlan.yesNo

Identity: `g170-scan:yesNo`. Tier: `STABLE_1_0`. Target: `x86_64-systemv-elf-linux`. Declaration kind: `CONTEXTUAL_TYPED_SOURCE_AND_NATIVE_SCAN_OWNER`. Registry and contract row names do not declare additional callable constructors.

### Source form and behavior

```nebo
start(){"Choice".scan(.yesNo(),.mock("no")).console();23.return;}
```

Explicit process status: `23`; the full value/effect oracle is `scan:choice-yesNo-no`.

### Types, ownership, effects, limits and errors

Real contextual option AST children, lexical operands and native ScanPlan owners. Text UTF-8 <=4096 bytes; Int64; Bool true/false; Char one-byte profile; 1..32 choices; finite retries <=8; readonly relative replay file <=4096 bytes/path<=256; typed Option<Int>/Result<T,ScanError>; headless Console metadata and pointer-free native events. Public secret events hide input bytes/length/digest. No live backend claim. Multiline/paragraph/block/Markdown/code/JSON/raw modes use native G078/G080 owners with at most 256 lines; finite Console enum choices are not nominal enum reflection; multiSelect exposes the native unique 32-bit mask; confirmSecret consumes a second input; forgetAfterUse zeroizes consumed private bytes and preserves the owned result. At most 32 total options. SymbolId/DocRecord reconciliation stays in F01. Synthetic mock/stdin replay; typed bounded ScanPlan; redaction; no real secret or live device

Exact typed owner contract; no copy/borrow/clone capability inferred from spelling. Effects: input,console. Capabilities: SYNTHETIC_INPUT_AND_RETAINED_CONSOLE.

Native chain: `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/driver/cli/linux-x86_64/scalar_program.inc` → `compiler/codegen/functions/x86_64/function_codegen.asm` → `runtime/core/runtime_core.asm`.

Positive/changed-value/boundary proofs: `scan:choice-yesNo-yes,scan:choice-yesNo-no`.

Misuse/diagnostic/failure proofs: `scan:choice-yesNo-invalid`. API fingerprint: `b42c876d8fc1b07b872ddb64dbdc9d59827f7bbb008e6c2ec07aad1f913dcdaf`.

## Separately measured material profiles

These later source profiles retain their bounded contracts without an implicit stability promotion. They supplement the stable set and have complete current positive, negative and ownership observations.

### Process/Environment

Current ID; synthetic NEBO_TEST_* values and bounded arguments only; no arbitrary process spawn. Owner: `runtime/system/public.asm`.

Executed cases: `system:channel-1-17,system:channel-1-29,system:channel-3-17,system:channel-3-29,system:channel-8-17,system:channel-8-29,system:channel-64-17,system:channel-64-29,system:channel-two-live,system:channel-bounded-waits,system:channel-rendezvous-no-peer,system:cancel-repeat-0,system:cancel-repeat-1,system:cancel-repeat-3,system:duration-0,system:duration-1,system:duration-17,system:duration-1000,system:monotonic-elapsed,system:pid-observed,system:channel-capacity-over,system:channel-element-type,system:channel-wrong-value,system:cancel-wrong-arity,system:duration-wrong-type,system:environment-3137,system:environment-3731,system:environment-61c3a7c3a36f,system:environment-,system:environment-missing,system:arguments-seed-17,system:arguments-seed-71,system:arguments-boundary,system:option-text-owned-4e65626f,system:option-text-owned-61c3a7c3a36f20cea9`.

### Duration/Instant/time.sleep

Integer nanoseconds; bounded monotonic/sleep observations; no exact wall-clock value promise. Owner: `runtime/system/public.asm`.

Executed cases: `system:channel-1-17,system:channel-1-29,system:channel-3-17,system:channel-3-29,system:channel-8-17,system:channel-8-29,system:channel-64-17,system:channel-64-29,system:channel-two-live,system:channel-bounded-waits,system:channel-rendezvous-no-peer,system:cancel-repeat-0,system:cancel-repeat-1,system:cancel-repeat-3,system:duration-0,system:duration-1,system:duration-17,system:duration-1000,system:monotonic-elapsed,system:pid-observed,system:channel-capacity-over,system:channel-element-type,system:channel-wrong-value,system:cancel-wrong-arity,system:duration-wrong-type,system:environment-3137,system:environment-3731,system:environment-61c3a7c3a36f,system:environment-,system:environment-missing,system:arguments-seed-17,system:arguments-seed-71,system:arguments-boundary,system:option-text-owned-4e65626f,system:option-text-owned-61c3a7c3a36f20cea9`.

### Task/TaskGroup/Future

Noncapturing Int callback; cooperative deterministic admission; single-consumer Future; callback once; cancellation. Owner: `runtime/concurrency/task.asm`.

Executed cases: `tasks:task-map-17,tasks:task-single-17,tasks:task-map-29,tasks:task-single-29,tasks:task-map-71,tasks:task-single-71,tasks:task-effects-once,tasks:task-cancel-admission,tasks:task-select-False,tasks:task-select-True,tasks:task-file-scan,tasks:task-negative-callback-type,tasks:task-negative-callback-result,tasks:task-negative-callback-arity,tasks:task-negative-missing-callback,tasks:task-negative-join-arity`.

### Channel/CancellationToken

Int64 FIFO capacity 1..64; unbuffered no-peer; closed/empty/full/wait error codes; independent token state. Owner: `runtime/concurrency/channel.asm`.

Executed cases: `system:channel-1-17,system:channel-1-29,system:channel-3-17,system:channel-3-29,system:channel-8-17,system:channel-8-29,system:channel-64-17,system:channel-64-29,system:channel-two-live,system:channel-bounded-waits,system:channel-rendezvous-no-peer,system:cancel-repeat-0,system:cancel-repeat-1,system:cancel-repeat-3,system:duration-0,system:duration-1,system:duration-17,system:duration-1000,system:monotonic-elapsed,system:pid-observed,system:channel-capacity-over,system:channel-element-type,system:channel-wrong-value,system:cancel-wrong-arity,system:duration-wrong-type,system:environment-3137,system:environment-3731,system:environment-61c3a7c3a36f,system:environment-,system:environment-missing,system:arguments-seed-17,system:arguments-seed-71,system:arguments-boundary,system:option-text-owned-4e65626f,system:option-text-owned-61c3a7c3a36f20cea9`.

### Mutex/RwLock/AtomicInt/guards

Typed Int payloads; guard release across scope/return/break/continue; compareExchange; unique ownership and no escape. Owner: `runtime/concurrency/sync_public.asm`.

Executed cases: `sync:mutex-17,sync:rwlock-17,sync:atomic-17,sync:mutex--29,sync:rwlock--29,sync:atomic--29,sync:mutex-97,sync:rwlock-97,sync:atomic-97,sync:sync-lifetime-scope,sync:sync-lifetime-temporary,sync:sync-lifetime-continue,sync:sync-lifetime-break,sync:sync-lifetime-untaken,sync:sync-negative-mutex-type,sync:sync-negative-rwlock-value,sync:sync-negative-atomic-type,sync:sync-negative-atomic-arity,sync:sync-negative-guard-argument,sync:sync-negative-guard-copy,sync:sync-negative-atomic-copy,sync:sync-negative-guard-escape,sync:sync-lifetime-return`.

### Json.parse/toJson

Canonical key order; duplicate/trailing/malformed/depth rejection; 4096-byte input; private atomic sink. Owner: `runtime/serialization/json.asm`.

Executed cases: `codec:json-file-17,codec:json-file-53,codec:json-file-71,codec:json-failure-trailing,codec:json-failure-duplicate,codec:json-failure-unsorted,codec:json-failure-malformed,codec:json-failure-depth,codec:json-negative-wrong-type,codec:json-negative-wrong-option,codec:json-negative-shadowed-option,codec:csv-dataset-17,codec:csv-dataset--29,codec:csv-dataset-83,codec:csv-nullable-False,codec:csv-nullable-True,codec:csv-failure-bad-cell,codec:csv-failure-width,codec:csv-failure-overflow,codec:csv-failure-rows,codec:csv-negative-path-type,codec:csv-negative-schema-type,codec:csv-negative-arity`.

### Csv.read/write

Typed nullable Int schema; 32 rows; exact independent output bytes; malformed width/cell/overflow failure before publication. Owner: `runtime/serialization/csv.asm`.

Executed cases: `codec:json-file-17,codec:json-file-53,codec:json-file-71,codec:json-failure-trailing,codec:json-failure-duplicate,codec:json-failure-unsorted,codec:json-failure-malformed,codec:json-failure-depth,codec:json-negative-wrong-type,codec:json-negative-wrong-option,codec:json-negative-shadowed-option,codec:csv-dataset-17,codec:csv-dataset--29,codec:csv-dataset-83,codec:csv-nullable-False,codec:csv-nullable-True,codec:csv-failure-bad-cell,codec:csv-failure-width,codec:csv-failure-overflow,codec:csv-failure-rows,codec:csv-negative-path-type,codec:csv-negative-schema-type,codec:csv-negative-arity`.
