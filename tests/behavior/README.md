# MF024 semantic test

`mf024_semantic_test.asm` covers the sixteen primary BEHAVIOR, CONTROL and
EFFECT contracts. Scenarios 1–9 use numeric arguments; scenarios 10–16 use
`A` through `G` to keep the native harness single-character and deterministic.
