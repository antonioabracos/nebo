# MF013 diagnostic infrastructure suite

The suite has no primary Test ID because MF013 is an infrastructure front. It
proves the catalog, structured object, secondary span, arguments, bounded store,
sorting, no-color human rendering, limit handling and address-independent output.
