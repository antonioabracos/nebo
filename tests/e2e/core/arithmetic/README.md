# MF037 arithmetic E2E

The fixtures cover checked signed 64-bit arithmetic, canonical comparison Bool, overflow exit `172`, and division-by-zero exit `173`. All programs use `start()` and expression statements only; `.return`, functions and `if/else` remain later fronts.
