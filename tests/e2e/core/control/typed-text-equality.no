start() {
    Text.left;
    "alpha".left;
    Text.same;
    "alpha".same;
    Text.other;
    "beta".other;
    if (left == same) { 0; } else { 1 / 0; }
    if (left != other) { 0; } else { 1 / 0; }
    if (left == other) { 1 / 0; } else { 0; }
    if (left != same) { 1 / 0; } else { 0; }
}
