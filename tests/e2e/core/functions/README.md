# MF038 Receiver-First Function E2E

`2.soma(3)` must compile through the public CLI, produce deterministic NASM/ELF64, execute without output and terminate with exit code `5`. `recursive.no` proves the finite non-recursive graph contract.
