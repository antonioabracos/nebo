# FakeHost v0

The FakeHost exposes every HostServices capability without invoking the OS.
Its context supports programmed OOM, file-open and process failures, bounded
partial reads, structured argv capture, output-limit failures, temporary
handle accounting, diagnostics and deterministic time.
