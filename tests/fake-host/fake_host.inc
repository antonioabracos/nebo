%ifndef NEBOC_FAKE_HOST_INC
%define NEBOC_FAKE_HOST_INC 1

%define NEBOC_FAKE_FAIL_MEMORY_RESERVE (1 << 0)
%define NEBOC_FAKE_FAIL_FILE_OPEN (1 << 1)
%define NEBOC_FAKE_FAIL_PROCESS_SPAWN (1 << 2)

%define NEBOC_FAKE_FAIL_MASK_OFFSET 0
%define NEBOC_FAKE_PARTIAL_CHUNK_OFFSET 8
%define NEBOC_FAKE_FILE_DATA_POINTER_OFFSET 16
%define NEBOC_FAKE_FILE_DATA_LENGTH_OFFSET 24
%define NEBOC_FAKE_FILE_CURSOR_OFFSET 32
%define NEBOC_FAKE_FILE_OPEN_OFFSET 40
%define NEBOC_FAKE_DIAGNOSTIC_COUNT_OFFSET 48
%define NEBOC_FAKE_LAST_DIAGNOSTIC_OFFSET 56
%define NEBOC_FAKE_MEMORY_POINTER_OFFSET 64
%define NEBOC_FAKE_MEMORY_CAPACITY_OFFSET 72
%define NEBOC_FAKE_MEMORY_ACTIVE_OFFSET 80
%define NEBOC_FAKE_TEMP_ACTIVE_OFFSET 88
%define NEBOC_FAKE_NEXT_TEMP_HANDLE_OFFSET 96
%define NEBOC_FAKE_CLOCK_NOW_OFFSET 104
%define NEBOC_FAKE_CLOCK_STEP_OFFSET 112
%define NEBOC_FAKE_LAST_ARGV_POINTER_OFFSET 120
%define NEBOC_FAKE_LAST_ARGC_OFFSET 128
%define NEBOC_FAKE_PROCESS_REQUEST_OFFSET 136
%define NEBOC_FAKE_PROCESS_STDOUT_LENGTH_OFFSET 144
%define NEBOC_FAKE_PROCESS_STDERR_LENGTH_OFFSET 152
%define NEBOC_FAKE_PROCESS_EXIT_CODE_OFFSET 160
%define NEBOC_FAKE_PROCESS_SPAWNED_OFFSET 168
%define NEBOC_FAKE_PROCESS_WAITED_OFFSET 176
%define NEBOC_FAKE_READ_CALLS_OFFSET 184
%define NEBOC_FAKE_EXIT_STATUS_OFFSET 192
%define NEBOC_FAKE_CONTEXT_SIZE 200

%endif
