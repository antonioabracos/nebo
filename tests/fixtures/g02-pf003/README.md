# G02-PF003 fixtures

These sources define semantic/type/IR expectations for the frozen G02 literal
profile. Only the decimal v0.1 regression is executable through the public CLI
in PF003. Based literals and separators remain `SEMANTIC_GOLDEN` or
`EXPECTED_DIAGNOSTIC` inputs exercised by the isolated native semantic test;
public lexer, target lowering and runtime integration are deferred.
