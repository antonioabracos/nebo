# G02-PF004 fixtures

These sources distinguish the certified decimal `Int` runtime path from the isolated native prototype for based/separated spellings. Only `001` is publicly executable in PF004; `002`–`004` remain proposed and `005` remains an expected rejection.
