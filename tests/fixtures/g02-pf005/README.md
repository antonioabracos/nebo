# G02-PF005 fixtures

Five positive programs exercise public based/separated `Int` literals through check, emission, build and native execution. Nine negative programs assert the stable G02 diagnostic surface and the unary-only `INT64_MIN` invariant.
