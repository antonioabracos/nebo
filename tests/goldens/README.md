# Golden files

Goldens are deterministic normative outputs.

They must not contain:

- absolute paths;
- timestamps;
- pointers;
- random IDs;
- unordered hash output.

Bulk golden acceptance is forbidden. Every change requires a reviewed semantic reason.
