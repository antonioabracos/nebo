# MF015 literal lexer tests

The suite covers signed Int boundaries, overflow, decoded Text escapes,
invalid/unterminated escapes, nested block-comment trivia, ASCII-only
identifiers, token determinism and a bounded malformed-byte corpus.
