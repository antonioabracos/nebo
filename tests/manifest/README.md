# Public validation

Run `bash scripts/ci-public.sh` from the repository root. The entry point runs bounded executable checks and independent negative controls. Test output belongs in temporary directories, outside the source inventory.
