# MF017 expression parser tests

The three normative goldens cover terminal binding, `.return` and arithmetic
precedence. Internal scenarios additionally cover unary/grouping, receiver calls
with arguments, multiline chain equivalence and the nesting limit.
