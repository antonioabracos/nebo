// Time-series and aggregate views share one target-neutral numeric pipeline.
start() {
    RenderPlan;
    3204;
    output.stdout().stderr().target("chart-table-service").plain().visual().headlessEvent();
    lifecycle.event("chart-table").payload("timeseries-aggregate").headless().jsonl().targetConformance().validate().deterministic().failureAtomic().bounded();
}
