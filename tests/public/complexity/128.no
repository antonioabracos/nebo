// Dashboard creation, validated specs, and layout stay in the logical service layer.
start() {
    RenderPlan;
    3301;
    output.stdout().stderr().target("domain-visual-service").plain().visual().headlessEvent();
    lifecycle.event("domain-visual").payload("dashboard-create-from-spec-layout").headless().jsonl().targetConformance().validate().deterministic().failureAtomic().bounded();
}
