# MF010 session tests

The native suite proves:

- approved DG-009 defaults;
- exact phase ordering and invalid-transition rejection;
- controlled limit status and statistics;
- blocking failure with automatic central cleanup;
- idempotent cleanup;
- programmed HostServices allocation failure;
- deterministic limits/statistics independent of session address;
- default Linux HostServices initialization and release.

MF010 has no primary Test ID in the approved roadmap. These tests are
infrastructure evidence for the TR02 exit gate.
