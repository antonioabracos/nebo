# MF011 source tests

The native suite uses FakeHost for the four primary cases and the Linux adapter
for a real read smoke. Goldens contain only logical relative paths. Physical
fixture paths and absolute host paths are never emitted.
