# Console deterministic stress — MF050

The runner repeats existing native scenario boundaries with fixed counts. It uses no OS threads and no timing oracle: equal inputs must produce equal states and hashes. Mode `all` closes the nine MF050 primary contracts.
