# MF022 type tests

The parameterized Assembly test covers the ten roadmap IDs: fundamental `Int`,
`Bool`, UTF-8 `Text`, first-class `Console`, `Pending<Text>`, zero implicit
coercions, rejected Bool arithmetic, rejected `Text + Text`, checked constant
overflow and signed division truncating toward zero.
